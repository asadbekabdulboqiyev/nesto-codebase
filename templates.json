[
  {
    "id": "starter",
    "n": "Flutter Starter",
    "i": "🚀",
    "diff": "easy",
    "c": "starter",
    "t": [
      "flutter",
      "material",
      "theme",
      "setup"
    ],
    "d": "Asosiy loyiha strukturasi. Material 3, dark/light mode.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\n\nvoid main() {\n  runApp(const MyApp());\n}\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'APP_NAME',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        colorSchemeSeed: Colors.blue,\n        useMaterial3: true,\n        brightness: Brightness.light,\n      ),\n      darkTheme: ThemeData(\n        colorSchemeSeed: Colors.blue,\n        useMaterial3: true,\n        brightness: Brightness.dark,\n      ),\n      themeMode: ThemeMode.system,\n      home: const HomePage(),\n    );\n  }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('APP_NAME'), centerTitle: true),\n      body: const Center(\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            Icon(Icons.flutter_dash, size: 80, color: Colors.blue),\n            SizedBox(height: 16),\n            Text('Xush kelibsiz!', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n          ],\n        ),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Flutter Starter\n\n**Kategoriya:** Starter\n**Qiyinlik:** Easy\n**Teglar:** material, theme, setup\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n",
    "preview": "code_info/starter.png"
  },
  {
    "id": "bottom-nav",
    "n": "Bottom Navigation",
    "i": "🧭",
    "diff": "medium",
    "c": "navigation",
    "t": [
      "flutter",
      "bottomnav",
      "indexedstack",
      "navigation"
    ],
    "d": "3-4 sahifali bottom nav. IndexedStack bilan state saqlash.",
    "f": [
      {
        "n": "main_navigation.dart",
        "p": "lib/pages/main_navigation.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass MainNavigation extends StatefulWidget {\n  const MainNavigation({super.key});\n  @override\n  State<MainNavigation> createState() => _MainNavigationState();\n}\n\nclass _MainNavigationState extends State<MainNavigation> {\n  int _currentIndex = 0;\n  final List<Widget> _pages = const [HomePage(), SearchPage(), ProfilePage()];\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: IndexedStack(index: _currentIndex, children: _pages),\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (i) => setState(() => _currentIndex = i),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home), label: 'Bosh sahifa'),\n          NavigationDestination(icon: Icon(Icons.search_outlined), selectedIcon: Icon(Icons.search), label: 'Qidirish'),\n          NavigationDestination(icon: Icon(Icons.person_outline), selectedIcon: Icon(Icons.person), label: 'Profil'),\n        ],\n      ),\n    );\n  }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext c) => Scaffold(appBar: AppBar(title: const Text('Bosh sahifa')), body: const Center(child: Text('Bosh sahifa')));\n}\n\nclass SearchPage extends StatelessWidget {\n  const SearchPage({super.key});\n  @override\n  Widget build(BuildContext c) => Scaffold(appBar: AppBar(title: const Text('Qidirish')), body: const Center(child: Text('Qidirish')));\n}\n\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n  @override\n  Widget build(BuildContext c) => Scaffold(appBar: AppBar(title: const Text('Profil')), body: const Center(child: Text('Profil')));\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## Bottom Navigation\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Medium\n**Teglar:** bottomnav, indexedstack, navigation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/main_navigation.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n",
    "preview": "code_info/bottom-nav.png"
  },
  {
    "id": "profile",
    "n": "Profile Page",
    "i": "👤",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "profile",
      "settings",
      "avatar"
    ],
    "d": "Avatar, stats, settings, logout bilan to'liq profil.",
    "f": [
      {
        "n": "profile_page.dart",
        "p": "lib/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Profil'), actions: [IconButton(icon: const Icon(Icons.settings), onPressed: () {})]),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        const Center(child: CircleAvatar(radius: 50, backgroundColor: Colors.blue, child: Icon(Icons.person, size: 50, color: Colors.white))),\n        const SizedBox(height: 16),\n        const Center(child: Text('Foydalanuvchi', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold))),\n        const Center(child: Text('user@email.com', style: TextStyle(color: Colors.grey))),\n        const SizedBox(height: 24),\n        Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: [\n          _stat('125', 'Postlar'), _stat('1.2K', 'Obunachilar'), _stat('340', 'Obuna'),\n        ]),\n        const SizedBox(height: 24), const Divider(),\n        _item(Icons.edit, 'Tahrirlash'), _item(Icons.lock, 'Maxfiylik'),\n        _item(Icons.notifications, 'Bildirishnomalar'), const Divider(),\n        _item(Icons.logout, 'Chiqish', color: Colors.red),\n      ]),\n    );\n  }\n  static Widget _stat(String c, String l) => Column(children: [\n    Text(c, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n    Text(l, style: const TextStyle(color: Colors.grey)),\n  ]);\n  static Widget _item(IconData i, String t, {Color? c}) => ListTile(leading: Icon(i, color: c), title: Text(t, style: TextStyle(color: c)), trailing: const Icon(Icons.chevron_right), onTap: () {});\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Profile Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** profile, settings, avatar\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/profile_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "login",
    "n": "Login / Register",
    "i": "🔐",
    "diff": "medium",
    "c": "auth",
    "t": [
      "flutter",
      "login",
      "register",
      "form",
      "validation"
    ],
    "d": "Form validation bilan chiroyli login sahifasi.",
    "f": [
      {
        "n": "login_page.dart",
        "p": "lib/pages/auth/login_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass LoginPage extends StatefulWidget {\n  const LoginPage({super.key});\n  @override\n  State<LoginPage> createState() => _LoginPageState();\n}\n\nclass _LoginPageState extends State<LoginPage> {\n  final _formKey = GlobalKey<FormState>();\n  final _email = TextEditingController();\n  final _pass = TextEditingController();\n  bool _hide = true;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: SafeArea(child: Center(child: SingleChildScrollView(\n        padding: const EdgeInsets.all(24),\n        child: Form(key: _formKey, child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n          const Icon(Icons.flutter_dash, size: 80, color: Colors.blue),\n          const SizedBox(height: 16),\n          const Text('Xush kelibsiz!', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold)),\n          const SizedBox(height: 8),\n          const Text('Hisobingizga kiring', style: TextStyle(color: Colors.grey)),\n          const SizedBox(height: 32),\n          TextFormField(controller: _email, decoration: const InputDecoration(labelText: 'Email', prefixIcon: Icon(Icons.email_outlined), border: OutlineInputBorder()),\n            validator: (v) => v == null || v.isEmpty ? 'Email kiriting' : null),\n          const SizedBox(height: 16),\n          TextFormField(controller: _pass, obscureText: _hide,\n            decoration: InputDecoration(labelText: 'Parol', prefixIcon: const Icon(Icons.lock_outlined), border: const OutlineInputBorder(),\n              suffixIcon: IconButton(icon: Icon(_hide ? Icons.visibility_off : Icons.visibility), onPressed: () => setState(() => _hide = !_hide))),\n            validator: (v) => v == null || v.length < 6 ? 'Kamida 6 ta belgi' : null),\n          const SizedBox(height: 24),\n          SizedBox(width: double.infinity, height: 50, child: FilledButton(\n            onPressed: () { if (_formKey.currentState!.validate()) {} },\n            child: const Text('Kirish', style: TextStyle(fontSize: 16)))),\n          const SizedBox(height: 16),\n          Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n            const Text('Hisobingiz yo'qmi? '),\n            TextButton(onPressed: () {}, child: const Text('Ro'yxatdan o'ting')),\n          ]),\n        ]))),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "lock",
    "instructions": "## Login / Register\n\n**Kategoriya:** Auth\n**Qiyinlik:** Medium\n**Teglar:** login, register, form, validation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/login_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "api",
    "n": "HTTP Service (Dio)",
    "i": "🌐",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "dio",
      "api",
      "http",
      "rest"
    ],
    "d": "Dio bilan API xizmati. GET, POST, interceptors, error handling.",
    "f": [
      {
        "n": "api_service.dart",
        "p": "lib/services/api_service.dart",
        "c": "import 'package:dio/dio.dart';\n\nclass ApiService {\n  static final ApiService _instance = ApiService._internal();\n  factory ApiService() => _instance;\n  late final Dio _dio;\n\n  ApiService._internal() {\n    _dio = Dio(BaseOptions(\n      baseUrl: 'https://api.example.com/v1',\n      connectTimeout: const Duration(seconds: 10),\n      receiveTimeout: const Duration(seconds: 10),\n      headers: {'Content-Type': 'application/json'},\n    ));\n    _dio.interceptors.add(LogInterceptor(requestBody: true, responseBody: true));\n    _dio.interceptors.add(InterceptorsWrapper(\n      onRequest: (o, h) { h.next(o); },\n      onError: (e, h) { h.next(e); },\n    ));\n  }\n\n  Future<dynamic> get(String path, {Map<String, dynamic>? params}) async {\n    try { return (await _dio.get(path, queryParameters: params)).data; }\n    on DioException catch (e) { throw _err(e); }\n  }\n\n  Future<dynamic> post(String path, {dynamic data}) async {\n    try { return (await _dio.post(path, data: data)).data; }\n    on DioException catch (e) { throw _err(e); }\n  }\n\n  Future<dynamic> put(String path, {dynamic data}) async {\n    try { return (await _dio.put(path, data: data)).data; }\n    on DioException catch (e) { throw _err(e); }\n  }\n\n  Future<dynamic> delete(String path) async {\n    try { return (await _dio.delete(path)).data; }\n    on DioException catch (e) { throw _err(e); }\n  }\n\n  Exception _err(DioException e) {\n    switch (e.type) {\n      case DioExceptionType.connectionTimeout: return Exception('Ulanish vaqti tugadi');\n      case DioExceptionType.receiveTimeout: return Exception('Javob vaqti tugadi');\n      default: return Exception('Xatolik');\n    }\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## HTTP Service (Dio)\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** dio, api, http, rest\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/api_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add dio\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-list",
    "n": "Animated List",
    "i": "✨",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "animatedlist",
      "animation",
      "list"
    ],
    "d": "Elementlarni qo'shish/o'chirish bilan animatsiyali ro'yxat.",
    "f": [
      {
        "n": "animated_list_page.dart",
        "p": "lib/pages/animated_list_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AnimatedListPage extends StatefulWidget {\n  const AnimatedListPage({super.key});\n  @override\n  State<AnimatedListPage> createState() => _State();\n}\n\nclass _State extends State<AnimatedListPage> {\n  final _key = GlobalKey<AnimatedListState>();\n  final List<String> _items = [];\n  int _c = 0;\n\n  void _add() {\n    _items.add('Element ' + _c.toString());\n    _key.currentState?.insertItem(_items.length - 1);\n  }\n\n  void _remove(int i) {\n    final item = _items.removeAt(i);\n    _key.currentState?.removeItem(i, (_, a) => SizeTransition(sizeFactor: a,\n      child: Card(child: ListTile(leading: const Icon(Icons.delete, color: Colors.red), title: Text(item)))));\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Animatsiyali Ro'yxat')),\n      floatingActionButton: FloatingActionButton(onPressed: _add, child: const Icon(Icons.add)),\n      body: AnimatedList(\n        key: _key, initialItemCount: _items.length, padding: const EdgeInsets.all(8),\n        itemBuilder: (_, i, a) => SizeTransition(sizeFactor: a,\n          child: Card(margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 4),\n            child: ListTile(\n              leading: CircleAvatar(child: Text((i + 1).toString())),\n              title: Text(_items[i]),\n              trailing: IconButton(icon: const Icon(Icons.delete_outline, color: Colors.red), onPressed: () => _remove(i)),\n            ))),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animated List\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** animatedlist, animation, list\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/animated_list_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "settings",
    "n": "Settings Page",
    "i": "⚙️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "settings",
      "switch",
      "slider"
    ],
    "d": "Switch, slider, dropdown bilan sozlamalar sahifasi.",
    "f": [
      {
        "n": "settings_page.dart",
        "p": "lib/pages/settings_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SettingsPage extends StatefulWidget {\n  const SettingsPage({super.key});\n  @override\n  State<SettingsPage> createState() => _State();\n}\n\nclass _State extends State<SettingsPage> {\n  bool _notif = true, _dark = false, _bio = false;\n  double _size = 16; String _lang = 'uz';\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Sozlamalar')),\n      body: ListView(children: [\n        SwitchListTile(title: const Text('Bildirishnomalar'), value: _notif, onChanged: (v) => setState(() => _notif = v)),\n        SwitchListTile(title: const Text('Qorong'i rejim'), value: _dark, onChanged: (v) => setState(() => _dark = v)),\n        SwitchListTile(title: const Text('Biometrik kirish'), value: _bio, onChanged: (v) => setState(() => _bio = v)),\n        const Divider(),\n        ListTile(title: const Text('Shrift o'lchami'), subtitle: Text(_size.round().toString() + ' px')),\n        Slider(value: _size, min: 12, max: 24, divisions: 12, onChanged: (v) => setState(() => _size = v)),\n        const Divider(),\n        ListTile(title: const Text('Til'), trailing: DropdownButton<String>(value: _lang,\n          items: const [DropdownMenuItem(value: 'uz', child: Text('O'zbek')), DropdownMenuItem(value: 'ru', child: Text('Русский')), DropdownMenuItem(value: 'en', child: Text('English'))],\n          onChanged: (v) => setState(() => _lang = v!))),\n      ]),);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Settings Page\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** settings, switch, slider\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/settings_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "provider",
    "n": "Provider Setup",
    "i": "⚡",
    "diff": "medium",
    "c": "state",
    "t": [
      "flutter",
      "provider",
      "statemanagement",
      "change-notifier"
    ],
    "d": "Provider + ChangeNotifier bilan state management.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:provider/provider.dart';\n\nvoid main() => runApp(MultiProvider(\n  providers: [\n    ChangeNotifierProvider(create: (_) => CounterProvider()),\n    ChangeNotifierProvider(create: (_) => ThemeProvider()),\n  ], child: const MyApp()));\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Consumer<ThemeProvider>(builder: (_, tp, __) => MaterialApp(\n      title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      themeMode: tp.dark ? ThemeMode.dark : ThemeMode.light,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true, brightness: Brightness.light),\n      darkTheme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true, brightness: Brightness.dark),\n      home: const HomePage()));\n  }\n}\n\nclass CounterProvider extends ChangeNotifier {\n  int _c = 0; int get count => _c;\n  void inc() { _c++; notifyListeners(); }\n  void dec() { if (_c > 0) { _c--; notifyListeners(); } }\n  void reset() { _c = 0; notifyListeners(); }\n}\n\nclass ThemeProvider extends ChangeNotifier {\n  bool _d = false; bool get dark => _d;\n  void toggle() { _d = !_d; notifyListeners(); }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Provider Demo'),\n      actions: [IconButton(icon: Icon(context.watch<ThemeProvider>().dark ? Icons.light_mode : Icons.dark_mode),\n        onPressed: () => context.read<ThemeProvider>().toggle())]),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        const Text('Hisoblagich:', style: TextStyle(fontSize: 20)),\n        const SizedBox(height: 16),\n        Text(context.watch<CounterProvider>().count.toString(), style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 24),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton.tonal(onPressed: () => context.read<CounterProvider>().dec(), child: const Icon(Icons.remove)),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: () => context.read<CounterProvider>().inc(), child: const Icon(Icons.add)),\n          const SizedBox(width: 16),\n          OutlinedButton(onPressed: () => context.read<CounterProvider>().reset(), child: const Text('Reset')),\n        ]),\n      ])),);\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## Provider Setup\n\n**Kategoriya:** State\n**Qiyinlik:** Medium\n**Teglar:** provider, statemanagement, change-notifier\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add provider\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n",
    "preview": "code_info/provider.png"
  },
  {
    "id": "drawer",
    "n": "Drawer Navigation",
    "i": "📋",
    "diff": "easy",
    "c": "navigation",
    "t": [
      "flutter",
      "drawer",
      "navigation",
      "menu"
    ],
    "d": "Drawer menu bilan navigatsiya.",
    "f": [
      {
        "n": "drawer_navigation.dart",
        "p": "lib/pages/drawer_navigation.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DrawerNavigation extends StatelessWidget {\n  const DrawerNavigation({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      drawer: Drawer(child: ListView(padding: EdgeInsets.zero, children: [\n        const DrawerHeader(decoration: BoxDecoration(gradient: LinearGradient(colors: [Colors.blue, Colors.purple])),\n          child: Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisAlignment: MainAxisAlignment.end, children: [\n            CircleAvatar(radius: 30, child: Icon(Icons.person, size: 30)),\n            SizedBox(height: 8), Text('Ism Familiya', style: TextStyle(color: Colors.white, fontSize: 16)),\n          ])),\n        _item(Icons.home, 'Bosh sahifa'),\n        _item(Icons.person, 'Profil'),\n        _item(Icons.settings, 'Sozlamalar'),\n        const Divider(),\n        _item(Icons.logout, 'Chiqish'),\n      ])),\n      appBar: AppBar(title: const Text('Drawer')),\n      body: const Center(child: Text('Asosiy kontent')),\n    );\n  }\n  static Widget _item(IconData i, String t) => ListTile(leading: Icon(i), title: Text(t));\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## Drawer Navigation\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Easy\n**Teglar:** drawer, navigation, menu\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/drawer_navigation.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "onboarding",
    "n": "Onboarding Screens",
    "i": "👋",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "onboarding",
      "pageview",
      "intro"
    ],
    "d": "3 sahifali onboarding. PageView, dots indicator.",
    "f": [
      {
        "n": "onboarding_page.dart",
        "p": "lib/pages/onboarding_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass OnboardingPage extends StatefulWidget {\n  const OnboardingPage({super.key});\n  @override\n  State<OnboardingPage> createState() => _State();\n}\n\nclass _State extends State<OnboardingPage> {\n  final _ctrl = PageController(); int _i = 0;\n  final _pages = const [\n    {'icon': Icons.rocket_launch, 'title': 'Xush kelibsiz!', 'desc': 'Eng yaxshi tajriba uchun.', 'color': Colors.blue},\n    {'icon': Icons.speed, 'title': 'Tezkor', 'desc': 'Sekundlar ichida natija.', 'color': Colors.purple},\n    {'icon': Icons.security, 'title': 'Xavfsiz', 'desc': 'Ma'lumotlar himoyada.', 'color': Colors.green},\n  ];\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: Stack(children: [\n      PageView.builder(controller: _ctrl, itemCount: _pages.length, onPageChanged: (i) => setState(() => _i = i),\n        itemBuilder: (_, i) { final p = _pages[i]; return Padding(padding: const EdgeInsets.all(40),\n          child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n            Container(padding: const EdgeInsets.all(32), decoration: BoxDecoration(color: (p['color'] as Color).withOpacity(0.1), shape: BoxShape.circle),\n              child: Icon(p['icon'] as IconData, size: 100, color: p['color'] as Color)),\n            const SizedBox(height: 40),\n            Text(p['title'] as String, style: const TextStyle(fontSize: 28, fontWeight: FontWeight.bold)),\n            const SizedBox(height: 16),\n            Text(p['desc'] as String, textAlign: TextAlign.center, style: const TextStyle(fontSize: 16, color: Colors.grey)),\n          ]));\n      }),\n      Positioned(bottom: 48, left: 0, right: 0, child: Column(children: [\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: List.generate(_pages.length, (i) => AnimatedContainer(\n          duration: const Duration(milliseconds: 300), margin: const EdgeInsets.symmetric(horizontal: 4),\n          width: _i == i ? 24 : 8, height: 8,\n          decoration: BoxDecoration(color: _i == i ? (_pages[i]['color'] as Color) : Colors.grey.withOpacity(0.3), borderRadius: BorderRadius.circular(4))))),\n        const SizedBox(height: 24),\n        Padding(padding: const EdgeInsets.symmetric(horizontal: 24), child: SizedBox(width: double.infinity, height: 50,\n          child: FilledButton(onPressed: () { if (_i < _pages.length - 1) _ctrl.nextPage(duration: const Duration(milliseconds: 300), curve: Curves.easeInOut); },\n            child: Text(_i < _pages.length - 1 ? 'Keyingi' : 'Boshlash')))),\n      ])),\n    ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Onboarding Screens\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** onboarding, pageview, intro\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/onboarding_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "splash",
    "n": "Splash Screen",
    "i": "💫",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "splash",
      "loading",
      "animation"
    ],
    "d": "Animatsiyali splash screen.",
    "f": [
      {
        "n": "splash_page.dart",
        "p": "lib/pages/splash_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SplashPage extends StatefulWidget {\n  const SplashPage({super.key});\n  @override\n  State<SplashPage> createState() => _SplashPageState();\n}\n\nclass _SplashPageState extends State<SplashPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<double> _fade;\n\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 2));\n    _fade = Tween<double>(begin: 0, end: 1).animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeIn));\n    _ctrl.forward();\n    Future.delayed(const Duration(seconds: 3), () {\n      if (mounted) Navigator.pushReplacementNamed(context, '/home');\n    });\n  }\n\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: Center(child: FadeTransition(opacity: _fade,\n        child: const Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n          Icon(Icons.flutter_dash, size: 100, color: Colors.blue),\n          SizedBox(height: 20),\n          Text('APP_NAME', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold)),\n        ]))),\n    );\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Splash Screen\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** splash, loading, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/splash_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "dashboard",
    "n": "Dashboard",
    "i": "📊",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "dashboard",
      "stats",
      "chart"
    ],
    "d": "Statistikasi bor dashboard sahifasi.",
    "f": [
      {
        "n": "dashboard_page.dart",
        "p": "lib/pages/dashboard_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DashboardPage extends StatelessWidget {\n  const DashboardPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Dashboard')),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        Row(children: const [_Stat('Foydalanuvchilar', '12,345', Icons.people, Colors.blue), SizedBox(width: 12), _Stat('Sotuvlar', '$45,678', Icons.shopping_cart, Colors.green)]),\n        const SizedBox(height: 12),\n        Row(children: const [_Stat('Buyurtmalar', '892', Icons.receipt, Colors.orange), SizedBox(width: 12), _Stat('Daromad', '$12,345', Icons.attach_money, Colors.purple)]),\n        const SizedBox(height: 20),\n        const Text('So'nggi faoliyat', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        ...List.generate(5, (i) => ListTile(leading: CircleAvatar(child: Text((i + 1).toString())), title: Text('Faoliyat ' + (i + 1).toString()), subtitle: const Text('2 soat oldin'), trailing: const Icon(Icons.chevron_right))),\n      ]));\n  }\n}\n\nclass _Stat extends StatelessWidget {\n  final String title, value; final IconData icon; final Color color;\n  const _Stat(this.title, this.value, this.icon, this.color);\n  @override\n  Widget build(BuildContext context) {\n    return Expanded(child: Card(child: Padding(padding: const EdgeInsets.all(16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Icon(icon, color: color, size: 28), const SizedBox(height: 8),\n      Text(value, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),\n      Text(title, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n    ]))));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Dashboard\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** dashboard, stats, chart\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/dashboard_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "chat",
    "n": "Chat Page",
    "i": "💬",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "chat",
      "message",
      "conversation"
    ],
    "d": "Chat sahifasi.",
    "f": [
      {
        "n": "chat_page.dart",
        "p": "lib/pages/chat_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ChatPage extends StatelessWidget {\n  const ChatPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Chat')),\n      body: Column(children: [\n        Expanded(child: ListView(padding: const EdgeInsets.all(16), children: [\n          _bubble('Salom!', true), _bubble('Salom, qalaysan?', false), _bubble('Yaxshi!', true),\n        ])),\n        Container(padding: const EdgeInsets.all(8), decoration: BoxDecoration(color: Colors.grey[100]),\n          child: Row(children: [\n            const Expanded(child: TextField(decoration: InputDecoration(hintText: 'Xabar...', border: OutlineInputBorder(), contentPadding: EdgeInsets.symmetric(horizontal: 16)))),\n            const SizedBox(width: 8),\n            IconButton(onPressed: () {}, icon: const Icon(Icons.send, color: Colors.blue)),\n          ])),\n      ]));\n  }\n  static Widget _bubble(String t, bool me) => Align(\n    alignment: me ? Alignment.centerRight : Alignment.centerLeft,\n    child: Container(margin: const EdgeInsets.only(bottom: 8), padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 10),\n      decoration: BoxDecoration(color: me ? Colors.blue : Colors.grey[200], borderRadius: BorderRadius.circular(16)),\n      child: Text(t, style: TextStyle(color: me ? Colors.white : Colors.black))));\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Chat Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** chat, message, conversation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/chat_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "cart",
    "n": "Cart Page",
    "i": "🛒",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "cart",
      "shopping",
      "checkout"
    ],
    "d": "Savat sahifasi.",
    "f": [
      {
        "n": "cart_page.dart",
        "p": "lib/pages/cart_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CartPage extends StatelessWidget {\n  const CartPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Savat')),\n      body: ListView.separated(itemCount: 3, separatorBuilder: (_, __) => const Divider(),\n        itemBuilder: (_, i) => ListTile(\n          leading: CircleAvatar(child: Text((i + 1).toString())),\n          title: Text('Mahsulot ' + (i + 1).toString()),\n          subtitle: Text('$' + ((i + 1) * 29).toString()),\n          trailing: Row(mainAxisSize: MainAxisSize.min, children: [\n            IconButton(icon: const Icon(Icons.remove_circle_outline), onPressed: () {}),\n            Text((i + 1).toString()),\n            IconButton(icon: const Icon(Icons.add_circle_outline), onPressed: () {}),\n          ]))),\n      bottomNavigationBar: Padding(padding: const EdgeInsets.all(16),\n        child: FilledButton(onPressed: () {}, child: const Text('To'lovga o'tish'))));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Cart Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** cart, shopping, checkout\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/cart_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "feed",
    "n": "Feed Page",
    "i": "📰",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "feed",
      "social",
      "post"
    ],
    "d": "Ijtimoiy tarmoq lenti.",
    "f": [
      {
        "n": "feed_page.dart",
        "p": "lib/pages/feed_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FeedPage extends StatelessWidget {\n  const FeedPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Lent')),\n      body: ListView.builder(itemCount: 8,\n        itemBuilder: (_, i) => Card(margin: const EdgeInsets.all(8), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          ListTile(leading: CircleAvatar(child: Text('U' + (i + 1).toString())), title: Text('Foydalanuvchi ' + (i + 1).toString()), subtitle: Text((i + 1).toString() + ' soat oldin')),\n          Container(height: 200, color: Colors.grey[200], child: const Center(child: Icon(Icons.image, size: 48, color: Colors.grey))),\n          Padding(padding: const EdgeInsets.all(12), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n            const Row(children: [Icon(Icons.favorite_border), SizedBox(width: 16), Icon(Icons.comment_outlined), SizedBox(width: 16), Icon(Icons.share)]),\n            const SizedBox(height: 8),\n            const Text('Bu juda qiziqarli post!', style: TextStyle(fontWeight: FontWeight.bold)),\n          ])),\n        ])));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Feed Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** feed, social, post\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/feed_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "gallery",
    "n": "Gallery Page",
    "i": "🖼️",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "gallery",
      "image",
      "photo"
    ],
    "d": "Galereya sahifasi.",
    "f": [
      {
        "n": "gallery_page.dart",
        "p": "lib/pages/gallery_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass GalleryPage extends StatelessWidget {\n  const GalleryPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Galereya')),\n      body: GridView.builder(padding: const EdgeInsets.all(4),\n        gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3, crossAxisSpacing: 4, mainAxisSpacing: 4),\n        itemCount: 30,\n        itemBuilder: (_, i) => Container(\n          color: Colors.primaries[i % Colors.primaries.length].withOpacity(0.3),\n          child: const Center(child: Icon(Icons.image, color: Colors.grey))),\n      ));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Gallery Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** gallery, image, photo\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/gallery_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "otp",
    "n": "OTP Verify",
    "i": "📱",
    "diff": "easy",
    "c": "auth",
    "t": [
      "flutter",
      "otp",
      "verify",
      "code"
    ],
    "d": "SMS kod tekshirish.",
    "f": [
      {
        "n": "otp_page.dart",
        "p": "lib/pages/auth/otp_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass OtpPage extends StatelessWidget {\n  const OtpPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: SafeArea(child: Padding(padding: const EdgeInsets.all(24), child: Column(children: [\n      const SizedBox(height: 40),\n      const Icon(Icons.lock_open, size: 64, color: Colors.blue),\n      const SizedBox(height: 20),\n      const Text('Tasdiqlash kodi', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n      const SizedBox(height: 8),\n      const Text('Raqamingizga yuborilgan kodni kiriting', style: TextStyle(color: Colors.grey)),\n      const SizedBox(height: 32),\n      Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: List.generate(4, (i) =>\n        SizedBox(width: 50, child: TextField(textAlign: TextAlign.center, style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold), decoration: const InputDecoration(border: OutlineInputBorder()))))),\n      const SizedBox(height: 24),\n      SizedBox(width: double.infinity, child: FilledButton(onPressed: () {}, child: const Text('Tasdiqlash'))),\n      const SizedBox(height: 16),\n      TextButton(onPressed: () {}, child: const Text('Qayta yuborish (59s)')),\n    ]))));\n  }\n}"
      }
    ],
    "icon": "lock",
    "instructions": "## OTP Verify\n\n**Kategoriya:** Auth\n**Qiyinlik:** Easy\n**Teglar:** otp, verify, code\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/otp_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "forgot-pw",
    "n": "Forgot Password",
    "i": "🔑",
    "diff": "easy",
    "c": "auth",
    "t": [
      "flutter",
      "forgot",
      "password",
      "reset"
    ],
    "d": "Parolni tiklash.",
    "f": [
      {
        "n": "forgot_password.dart",
        "p": "lib/pages/auth/forgot_password.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ForgotPasswordPage extends StatelessWidget {\n  const ForgotPasswordPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Parolni tiklash')),\n      body: Padding(padding: const EdgeInsets.all(24), child: Column(children: [\n        const SizedBox(height: 40),\n        const Icon(Icons.lock_reset, size: 80, color: Colors.orange),\n        const SizedBox(height: 20),\n        const Text('Parolni unutdingizmi?', style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 8),\n        const Text('Email manzilingizni kiriting', style: TextStyle(color: Colors.grey)),\n        const SizedBox(height: 32),\n        const TextField(decoration: InputDecoration(labelText: 'Email', prefixIcon: Icon(Icons.email_outlined), border: OutlineInputBorder())),\n        const SizedBox(height: 16),\n        SizedBox(width: double.infinity, child: FilledButton(onPressed: () {}, child: const Text('Kod yuborish'))),\n      ])));\n  }\n}"
      }
    ],
    "icon": "lock",
    "instructions": "## Forgot Password\n\n**Kategoriya:** Auth\n**Qiyinlik:** Easy\n**Teglar:** forgot, password, reset\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/forgot_password.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "social-login",
    "n": "Social Login",
    "i": "🌐",
    "diff": "easy",
    "c": "auth",
    "t": [
      "flutter",
      "social",
      "google",
      "apple"
    ],
    "d": "Ijtimoiy tarmoq orqali kirish.",
    "f": [
      {
        "n": "social_login.dart",
        "p": "lib/pages/auth/social_login.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SocialLoginPage extends StatelessWidget {\n  const SocialLoginPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: SafeArea(child: Center(child: Padding(padding: const EdgeInsets.all(32),\n      child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        const Icon(Icons.flutter_dash, size: 80, color: Colors.blue),\n        const SizedBox(height: 20),\n        const Text('Kirish usulini tanlang', style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 40),\n        SizedBox(width: double.infinity, child: ElevatedButton.icon(onPressed: () {}, icon: const Icon(Icons.g_mobiledata, color: Colors.red), label: const Text('Google'), style: ElevatedButton.styleFrom(padding: const EdgeInsets.all(16)))),\n        const SizedBox(height: 12),\n        SizedBox(width: double.infinity, child: ElevatedButton.icon(onPressed: () {}, icon: const Icon(Icons.apple), label: const Text('Apple'), style: ElevatedButton.styleFrom(padding: const EdgeInsets.all(16)))),\n        const SizedBox(height: 12),\n        SizedBox(width: double.infinity, child: ElevatedButton.icon(onPressed: () {}, icon: const Icon(Icons.facebook, color: Colors.blue), label: const Text('Facebook'), style: ElevatedButton.styleFrom(padding: const EdgeInsets.all(16)))),\n      ])))));\n  }\n}"
      }
    ],
    "icon": "lock",
    "instructions": "## Social Login\n\n**Kategoriya:** Auth\n**Qiyinlik:** Easy\n**Teglar:** social, google, apple\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/social_login.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "tab-bar",
    "n": "Tab Bar Navigation",
    "i": "📑",
    "diff": "easy",
    "c": "navigation",
    "t": [
      "flutter",
      "tabbar",
      "tabs"
    ],
    "d": "Tab bar bilan navigatsiya.",
    "f": [
      {
        "n": "tab_bar_page.dart",
        "p": "lib/pages/tab_bar_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TabBarPage extends StatelessWidget {\n  const TabBarPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return DefaultTabController(length: 3, child: Scaffold(\n      appBar: AppBar(title: const Text('Tab Bar'), bottom: const TabBar(tabs: [\n        Tab(icon: Icon(Icons.home), text: 'Bosh sahifa'),\n        Tab(icon: Icon(Icons.search), text: 'Qidirish'),\n        Tab(icon: Icon(Icons.person), text: 'Profil'),\n      ])),\n      body: const TabBarView(children: [\n        Center(child: Text('Bosh sahifa')), Center(child: Text('Qidirish')), Center(child: Text('Profil')),\n      ]),\n    ));\n  }\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## Tab Bar Navigation\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Easy\n**Teglar:** tabbar, tabs\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/tab_bar_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "pageview",
    "n": "PageView Nav",
    "i": "↔️",
    "diff": "medium",
    "c": "navigation",
    "t": [
      "flutter",
      "pageview",
      "swipe",
      "gesture"
    ],
    "d": "Surgan bilan o'tish.",
    "f": [
      {
        "n": "pageview_page.dart",
        "p": "lib/pages/pageview_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PageViewPage extends StatelessWidget {\n  const PageViewPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('PageView')),\n      body: PageView(children: [\n        Container(color: Colors.blue[100], child: const Center(child: Text('Sahifa 1', style: TextStyle(fontSize: 24)))),\n        Container(color: Colors.green[100], child: const Center(child: Text('Sahifa 2', style: TextStyle(fontSize: 24)))),\n        Container(color: Colors.orange[100], child: const Center(child: Text('Sahifa 3', style: TextStyle(fontSize: 24)))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## PageView Nav\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Medium\n**Teglar:** pageview, swipe, gesture\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/pageview_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "rail",
    "n": "Navigation Rail",
    "i": "🚀",
    "diff": "medium",
    "c": "navigation",
    "t": [
      "flutter",
      "rail",
      "side",
      "responsive"
    ],
    "d": "Yon tomondagi navigatsiya.",
    "f": [
      {
        "n": "rail_page.dart",
        "p": "lib/pages/rail_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass RailPage extends StatefulWidget {\n  const RailPage({super.key});\n  @override\n  State<RailPage> createState() => _RailPageState();\n}\n\nclass _RailPageState extends State<RailPage> {\n  int _i = 0;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: Row(children: [\n      NavigationRail(selectedIndex: _i, onDestinationSelected: (i) => setState(() => _i = i),\n        destinations: const [\n          NavigationRailDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home), label: Text('Bosh sahifa')),\n          NavigationRailDestination(icon: Icon(Icons.search_outlined), selectedIcon: Icon(Icons.search), label: Text('Qidirish')),\n          NavigationRailDestination(icon: Icon(Icons.person_outlined), selectedIcon: Icon(Icons.person), label: Text('Profil')),\n        ]),\n      const VerticalDivider(),\n      Expanded(child: Center(child: Text('Sahifa ' + _i.toString()))),\n    ]));\n  }\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## Navigation Rail\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Medium\n**Teglar:** rail, side, responsive\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/rail_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "sidebar",
    "n": "Sidebar Navigation",
    "i": "📌",
    "diff": "medium",
    "c": "navigation",
    "t": [
      "flutter",
      "sidebar",
      "navigation",
      "panel"
    ],
    "d": "Yon panel navigatsiya.",
    "f": [
      {
        "n": "sidebar_page.dart",
        "p": "lib/pages/sidebar_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SidebarPage extends StatelessWidget {\n  const SidebarPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Sidebar')),\n      body: Row(children: [\n        Container(width: 240, color: Colors.grey[900], child: ListView(children: const [\n          UserAccountsDrawerHeader(accountName: Text('User'), accountEmail: Text('user@email.com'), currentAccountPicture: CircleAvatar(child: Icon(Icons.person))),\n          ListTile(leading: Icon(Icons.home), title: Text('Bosh sahifa')),\n          ListTile(leading: Icon(Icons.person), title: Text('Profil')),\n          ListTile(leading: Icon(Icons.settings), title: Text('Sozlamalar')),\n        ])),\n        const Expanded(child: Center(child: Text('Asosiy kontent'))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "compass",
    "instructions": "## Sidebar Navigation\n\n**Kategoriya:** Navigation\n**Qiyinlik:** Medium\n**Teglar:** sidebar, navigation, panel\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/navigation\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/sidebar_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)bar\n# (kerakli paketni o'rnating)\nflutter pub add panel\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "riverpod",
    "n": "Riverpod Setup",
    "i": "🔄",
    "diff": "medium",
    "c": "state",
    "t": [
      "flutter",
      "riverpod",
      "provider",
      "state"
    ],
    "d": "Riverpod state management.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\n\nfinal counterProvider = StateNotifierProvider<CounterNotifier, int>((ref) => CounterNotifier());\n\nclass CounterNotifier extends StateNotifier<int> {\n  CounterNotifier() : super(0);\n  void increment() => state++;\n  void decrement() { if (state > 0) state--; }\n}\n\nvoid main() => runApp(const ProviderScope(child: MyApp()));\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true), home: const HomePage());\n  }\n}\n\nclass HomePage extends ConsumerWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final count = ref.watch(counterProvider);\n    return Scaffold(appBar: AppBar(title: const Text('Riverpod')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Text(count.toString(), style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 24),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton.tonal(onPressed: () => ref.read(counterProvider.notifier).decrement(), child: const Icon(Icons.remove)),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: () => ref.read(counterProvider.notifier).increment(), child: const Icon(Icons.add)),\n        ]),\n      ])));\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## Riverpod Setup\n\n**Kategoriya:** State\n**Qiyinlik:** Medium\n**Teglar:** riverpod, provider, state\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add riverpod\nflutter pub add provider\nflutter pub add state\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n",
    "preview": "code_info/riverpod.png"
  },
  {
    "id": "bloc",
    "n": "Bloc Setup",
    "i": "📊",
    "diff": "hard",
    "c": "state",
    "t": [
      "flutter",
      "bloc",
      "cubit",
      "event"
    ],
    "d": "BLoC state management.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_bloc/flutter_bloc.dart';\n\nclass CounterCubit extends Cubit<int> {\n  CounterCubit() : super(0);\n  void increment() => emit(state + 1);\n  void decrement() { if (state > 0) emit(state - 1); }\n}\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return BlocProvider(create: (_) => CounterCubit(), child: MaterialApp(\n      title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true), home: const HomePage()));\n  }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('BLoC')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        BlocBuilder<CounterCubit, int>(builder: (_, c) => Text(c.toString(), style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold))),\n        const SizedBox(height: 24),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton.tonal(onPressed: () => context.read<CounterCubit>().decrement(), child: const Icon(Icons.remove)),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: () => context.read<CounterCubit>().increment(), child: const Icon(Icons.add)),\n        ]),\n      ])));\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## Bloc Setup\n\n**Kategoriya:** State\n**Qiyinlik:** Hard\n**Teglar:** bloc, cubit, event\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add bloc\nflutter pub add cubit\nflutter pub add event\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n",
    "preview": "code_info/bloc.png"
  },
  {
    "id": "getx",
    "n": "GetX Setup",
    "i": "💾",
    "diff": "medium",
    "c": "state",
    "t": [
      "flutter",
      "getx",
      "reactive",
      "simple"
    ],
    "d": "GetX state management.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:get/get.dart';\n\nclass CounterController extends GetxController {\n  var count = 0.obs;\n  void increment() => count++;\n  void decrement() { if (count > 0) count--; }\n}\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return GetMaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true), home: const HomePage());\n  }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    final c = Get.put(CounterController());\n    return Scaffold(appBar: AppBar(title: const Text('GetX')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Obx(() => Text(c.count.toString(), style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold))),\n        const SizedBox(height: 24),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton.tonal(onPressed: c.decrement, child: const Icon(Icons.remove)),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: c.increment, child: const Icon(Icons.add)),\n        ]),\n      ])));\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## GetX Setup\n\n**Kategoriya:** State\n**Qiyinlik:** Medium\n**Teglar:** getx, reactive, simple\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add getx\nflutter pub add reactive\nflutter pub add simple\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "form-validation",
    "n": "Form Validation",
    "i": "📝",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "form",
      "validation",
      "input"
    ],
    "d": "To'liq form validation.",
    "f": [
      {
        "n": "form_page.dart",
        "p": "lib/pages/form_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FormPage extends StatefulWidget {\n  const FormPage({super.key});\n  @override\n  State<FormPage> createState() => _FormPageState();\n}\n\nclass _FormPageState extends State<FormPage> {\n  final _key = GlobalKey<FormState>();\n  String? _gender;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Form')),\n      body: SingleChildScrollView(padding: const EdgeInsets.all(24), child: Form(key: _key, child: Column(children: [\n        const TextFormField(decoration: InputDecoration(labelText: 'Ism', border: OutlineInputBorder()), validator: (v) => v == null || v.isEmpty ? 'Kiritilishi shart' : null),\n        const SizedBox(height: 16),\n        const TextFormField(decoration: InputDecoration(labelText: 'Email', border: OutlineInputBorder()), validator: (v) => v != null && v.contains('@') ? null : 'Email noto'g'ri'),\n        const SizedBox(height: 16),\n        DropdownButtonFormField<String>(value: _gender, decoration: const InputDecoration(labelText: 'Jins', border: OutlineInputBorder()),\n          items: const [DropdownMenuItem(value: 'erkak', child: Text('Erkak')), DropdownMenuItem(value: 'ayol', child: Text('Ayol'))],\n          onChanged: (v) => setState(() => _gender = v), validator: (v) => v == null ? 'Tanlang' : null),\n        const SizedBox(height: 24),\n        SizedBox(width: double.infinity, child: FilledButton(onPressed: () {\n          if (_key.currentState!.validate()) ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Yuborildi!')));},\n          child: const Text('Yuborish'))),\n      ]))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Form Validation\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** form, validation, input\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/form_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add validation\nflutter pub add input\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "search-filter",
    "n": "Search & Filter",
    "i": "🔍",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "search",
      "filter",
      "realtime"
    ],
    "d": "Qidiruv va filter bilan ro'yxat.",
    "f": [
      {
        "n": "search_page.dart",
        "p": "lib/pages/search_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SearchPage extends StatefulWidget {\n  const SearchPage({super.key});\n  @override\n  State<SearchPage> createState() => _SearchPageState();\n}\n\nclass _SearchPageState extends State<SearchPage> {\n  final _ctrl = TextEditingController();\n  List<String> _items = [];\n  final _all = ['Flutter', 'Dart', 'React', 'Vue', 'Angular', 'Next.js', 'Swift', 'Kotlin', 'Python', 'Go'];\n  @override\n  void initState() { super.initState(); _items = _all; }\n  void _filter() => setState(() => _items = _all.where((e) => e.toLowerCase().contains(_ctrl.text.toLowerCase())).toList());\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Qidirish')),\n      body: Column(children: [\n        Padding(padding: const EdgeInsets.all(16), child: TextField(controller: _ctrl,\n          decoration: InputDecoration(hintText: 'Qidirish...', prefixIcon: const Icon(Icons.search), border: OutlineInputBorder(borderRadius: BorderRadius.circular(12))),\n          onChanged: (_) => _filter())),\n        Expanded(child: ListView.builder(itemCount: _items.length,\n          itemBuilder: (_, i) => ListTile(leading: CircleAvatar(child: Text((i + 1).toString())), title: Text(_items[i])))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Search & Filter\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** search, filter, realtime\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/search_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add search\nflutter pub add filter\nflutter pub add realtime\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "product-card",
    "n": "Product Card",
    "i": "🛍️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "product",
      "card",
      "shop"
    ],
    "d": "Mahsulot kartochkasi.",
    "f": [
      {
        "n": "product_card.dart",
        "p": "lib/widgets/product_card.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ProductCard extends StatelessWidget {\n  final String name, price;\n  final double rating;\n  const ProductCard({super.key, required this.name, required this.price, this.rating = 4.5});\n  @override\n  Widget build(BuildContext context) {\n    return Card(clipBehavior: Clip.antiAlias, child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      AspectRatio(aspectRatio: 1, child: Container(color: Colors.blue[100], child: const Center(child: Icon(Icons.image, size: 48, color: Colors.blue)))),\n      Padding(padding: const EdgeInsets.all(12), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(name, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 14)),\n        const SizedBox(height: 4),\n        Text(price, style: const TextStyle(color: Colors.blue, fontWeight: FontWeight.w600)),\n        const SizedBox(height: 4),\n        Row(children: [const Icon(Icons.star, size: 14, color: Colors.amber), Text(rating.toString(), style: const TextStyle(fontSize: 12))]),\n      ])),\n    ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Product Card\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** product, card, shop\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/product_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add product\nflutter pub add card\nflutter pub add shop\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "implicit-anim",
    "n": "Implicit Animations",
    "i": "🔄",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "implicit",
      "animatedcontainer"
    ],
    "d": "Avtomatik animatsiyalar.",
    "f": [
      {
        "n": "implicit_anim.dart",
        "p": "lib/pages/implicit_anim.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ImplicitAnimPage extends StatefulWidget {\n  const ImplicitAnimPage({super.key});\n  @override\n  State<ImplicitAnimPage> createState() => _ImplicitAnimPageState();\n}\n\nclass _ImplicitAnimPageState extends State<ImplicitAnimPage> {\n  bool _big = false;\n  Color _color = Colors.blue;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Implicit Animations')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        AnimatedContainer(duration: const Duration(milliseconds: 500), curve: Curves.easeInOut,\n          width: _big ? 200 : 100, height: _big ? 200 : 100,\n          decoration: BoxDecoration(color: _color, borderRadius: BorderRadius.circular(_big ? 100 : 16))),\n        const SizedBox(height: 32),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton(onPressed: () => setState(() => _big = !_big), child: const Text('O'lcham')),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: () => setState(() => _color = _color == Colors.blue ? Colors.red : Colors.blue), child: const Text('Rang')),\n        ]),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Implicit Animations\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** implicit, animatedcontainer\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/implicit_anim.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add implicit\nflutter pub add animatedcontainer\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "hero-anim",
    "n": "Hero Animation",
    "i": "🦸",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "hero",
      "animation",
      "transition"
    ],
    "d": "Hero transition animatsiyasi.",
    "f": [
      {
        "n": "hero_page.dart",
        "p": "lib/pages/hero_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass HeroPage extends StatelessWidget {\n  const HeroPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Hero')),\n      body: ListView.builder(padding: const EdgeInsets.all(8), itemCount: 10,\n        itemBuilder: (_, i) => Card(child: ListTile(\n          leading: Hero(tag: 'hero-' + i.toString(), child: CircleAvatar(backgroundColor: Colors.primaries[i], child: const Icon(Icons.star, color: Colors.white))),\n          title: Text('Element ' + (i + 1).toString()),\n          onTap: () => Navigator.push(context, MaterialPageRoute(\n            builder: (_) => Scaffold(appBar: AppBar(title: Text('Element ' + (i + 1).toString())),\n              body: Center(child: Hero(tag: 'hero-' + i.toString(),\n                child: Container(width: 200, height: 200,\n                  decoration: BoxDecoration(color: Colors.primaries[i], shape: BoxShape.circle),\n                  child: const Icon(Icons.star, size: 80, color: Colors.white))))))),\n        )));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Hero Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** hero, animation, transition\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/hero_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add hero\n# (kerakli paketni o'rnating)\nflutter pub add transition\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "scale-anim",
    "n": "Scale Animation",
    "i": "📏",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "scale",
      "zoom",
      "transform"
    ],
    "d": "Masshtab animatsiyasi.",
    "f": [
      {
        "n": "scale_anim.dart",
        "p": "lib/pages/scale_anim.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ScaleAnimPage extends StatefulWidget {\n  const ScaleAnimPage({super.key});\n  @override\n  State<ScaleAnimPage> createState() => _ScaleAnimPageState();\n}\n\nclass _ScaleAnimPageState extends State<ScaleAnimPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<double> _scale;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 600));\n    _scale = Tween<double>(begin: 1, end: 1.5).animate(CurvedAnimation(parent: _ctrl, curve: Curves.elasticOut));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Scale')),\n      body: Center(child: ScaleTransition(scale: _scale, child: GestureDetector(\n        onTap: () { _ctrl.isCompleted ? _ctrl.reverse() : _ctrl.forward(); },\n        child: Container(width: 100, height: 100, decoration: BoxDecoration(color: Colors.blue, borderRadius: BorderRadius.circular(16)),\n          child: const Icon(Icons.touch_app, color: Colors.white, size: 40)),\n      ))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Scale Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** scale, zoom, transform\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/scale_anim.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add scale\nflutter pub add zoom\nflutter pub add transform\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "firebase-auth",
    "n": "Firebase Auth",
    "i": "🔥",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "firebase",
      "auth"
    ],
    "d": "Firebase autentifikatsiya.",
    "f": [
      {
        "n": "auth_service.dart",
        "p": "lib/services/auth_service.dart",
        "c": "import 'package:firebase_auth/firebase_auth.dart';\n\nclass AuthService {\n  final _auth = FirebaseAuth.instance;\n  User? get user => _auth.currentUser;\n  Stream<User?> get authState => _auth.authStateChanges();\n\n  Future<UserCredential?> signIn(String email, String password) async {\n    return await _auth.signInWithEmailAndPassword(email: email, password: password);\n  }\n\n  Future<UserCredential?> signUp(String email, String password) async {\n    return await _auth.createUserWithEmailAndPassword(email: email, password: password);\n  }\n\n  Future<void> signOut() async => await _auth.signOut();\n\n  Future<void> resetPassword(String email) async => await _auth.sendPasswordResetEmail(email: email);\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## Firebase Auth\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** firebase, auth\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/auth_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add firebase\nflutter pub add auth\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "rest-client",
    "n": "REST API Client",
    "i": "📡",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "rest",
      "api",
      "client"
    ],
    "d": "REST API mijoz.",
    "f": [
      {
        "n": "rest_client.dart",
        "p": "lib/services/rest_client.dart",
        "c": "import 'dart:convert';\nimport 'package:http/http.dart' as http;\n\nclass RestClient {\n  final String baseUrl;\n  RestClient(this.baseUrl);\n\n  Future<dynamic> get(String path, {Map<String, String>? headers}) async {\n    final resp = await http.get(Uri.parse(baseUrl + path), headers: headers);\n    return _handle(resp);\n  }\n\n  Future<dynamic> post(String path, {dynamic body, Map<String, String>? headers}) async {\n    final resp = await http.post(Uri.parse(baseUrl + path), body: jsonEncode(body),\n      headers: {'Content-Type': 'application/json', ...?headers});\n    return _handle(resp);\n  }\n\n  Future<dynamic> delete(String path, {Map<String, String>? headers}) async {\n    final resp = await http.delete(Uri.parse(baseUrl + path), headers: headers);\n    return _handle(resp);\n  }\n\n  dynamic _handle(http.Response resp) {\n    if (resp.statusCode >= 200 && resp.statusCode < 300) return jsonDecode(resp.body);\n    throw Exception('Xatolik: ' + resp.statusCode.toString());\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## REST API Client\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** rest, api, client\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/rest_client.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add rest\n# (kerakli paketni o'rnating)\nflutter pub add client\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "skeleton",
    "n": "Skeleton Loading",
    "i": "💀",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "skeleton",
      "loading",
      "placeholder"
    ],
    "d": "Skelet yuklanish effekti.",
    "f": [
      {
        "n": "skeleton.dart",
        "p": "lib/widgets/skeleton.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass Skeleton extends StatelessWidget {\n  final double width, height, borderRadius;\n  const Skeleton({super.key, this.width = double.infinity, this.height = 20, this.borderRadius = 8});\n  @override\n  Widget build(BuildContext context) {\n    return Container(width: width, height: height,\n      decoration: BoxDecoration(color: Colors.grey[300], borderRadius: BorderRadius.circular(borderRadius)));\n  }\n}\n\nclass SkeletonListTile extends StatelessWidget {\n  const SkeletonListTile({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(leading: const CircleAvatar(backgroundColor: Colors.grey),\n      title: Skeleton(width: 150), subtitle: Skeleton(width: 100, height: 14));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Skeleton Loading\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** skeleton, loading, placeholder\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/skeleton.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add skeleton\n# (kerakli paketni o'rnating)\nflutter pub add placeholder\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "shimmer",
    "n": "Shimmer Effect",
    "i": "✨",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "shimmer",
      "glow",
      "loading"
    ],
    "d": "Yaltirash effekti.",
    "f": [
      {
        "n": "shimmer_widget.dart",
        "p": "lib/widgets/shimmer_widget.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:shimmer/shimmer.dart';\n\nclass ShimmerWidget extends StatelessWidget {\n  final Widget child;\n  const ShimmerWidget({super.key, required this.child});\n  @override\n  Widget build(BuildContext context) {\n    return Shimmer.fromColors(baseColor: Colors.grey[300]!, highlightColor: Colors.grey[100]!, child: child);\n  }\n}\n\nclass ShimmerListTile extends StatelessWidget {\n  const ShimmerListTile({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return ShimmerWidget(child: ListTile(\n      leading: const CircleAvatar(backgroundColor: Colors.white),\n      title: Container(height: 14, width: 150, color: Colors.white),\n      subtitle: Container(height: 12, width: 100, color: Colors.white)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Shimmer Effect\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** shimmer, glow, loading\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/shimmer_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add shimmer\nflutter pub add glow\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "timeline",
    "n": "Timeline Widget",
    "i": "📅",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "timeline",
      "history",
      "steps"
    ],
    "d": "Timeline komponenti.",
    "f": [
      {
        "n": "timeline.dart",
        "p": "lib/widgets/timeline.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TimelineItem {\n  final String title, subtitle;\n  final IconData icon;\n  const TimelineItem({required this.title, required this.subtitle, required this.icon});\n}\n\nclass TimelineWidget extends StatelessWidget {\n  final List<TimelineItem> items;\n  const TimelineWidget({super.key, required this.items});\n  @override\n  Widget build(BuildContext context) {\n    return Column(children: items.map((item) => Row(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Column(children: [\n        CircleAvatar(radius: 16, child: Icon(item.icon, size: 16)),\n        Container(width: 2, height: 40, color: Colors.grey[300]),\n      ]),\n      const SizedBox(width: 12),\n      Expanded(child: Padding(padding: const EdgeInsets.only(bottom: 16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(item.title, style: const TextStyle(fontWeight: FontWeight.bold)),\n        Text(item.subtitle, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n      ]))),\n    ])).toList());\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Timeline Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** timeline, history, steps\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/timeline.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add timeline\nflutter pub add history\nflutter pub add steps\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "chat-bubble",
    "n": "Chat Bubble",
    "i": "💬",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "chat",
      "bubble",
      "message"
    ],
    "d": "Chat pufakchasi widget.",
    "f": [
      {
        "n": "chat_bubble.dart",
        "p": "lib/widgets/chat_bubble.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ChatBubble extends StatelessWidget {\n  final String text, time;\n  final bool isMe;\n  const ChatBubble({super.key, required this.text, required this.time, this.isMe = false});\n  @override\n  Widget build(BuildContext context) {\n    return Align(alignment: isMe ? Alignment.centerRight : Alignment.centerLeft,\n      child: Container(margin: const EdgeInsets.only(bottom: 8, left: 48, right: 48),\n        padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 10),\n        decoration: BoxDecoration(color: isMe ? Colors.blue : Colors.grey[200],\n          borderRadius: BorderRadius.only(topLeft: const Radius.circular(16), topRight: const Radius.circular(16),\n            bottomLeft: Radius.circular(isMe ? 16 : 0), bottomRight: Radius.circular(isMe ? 0 : 16))),\n        child: Column(crossAxisAlignment: isMe ? CrossAxisAlignment.end : CrossAxisAlignment.start, children: [\n          Text(text, style: TextStyle(color: isMe ? Colors.white : Colors.black)),\n          const SizedBox(height: 4),\n          Text(time, style: TextStyle(fontSize: 10, color: isMe ? Colors.white70 : Colors.grey)),\n        ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Chat Bubble\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** chat, bubble, message\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/chat_bubble.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add bubble\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "rating",
    "n": "Rating Widget",
    "i": "⭐",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "rating",
      "star",
      "score"
    ],
    "d": "Baholash widget.",
    "f": [
      {
        "n": "rating_widget.dart",
        "p": "lib/widgets/rating_widget.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass RatingWidget extends StatelessWidget {\n  final double rating;\n  final int maxRating;\n  const RatingWidget({super.key, required this.rating, this.maxRating = 5});\n  @override\n  Widget build(BuildContext context) {\n    return Row(mainAxisSize: MainAxisSize.min, children: List.generate(maxRating, (i) =>\n      Icon(i < rating.floor() ? Icons.star : (i == rating.floor() && rating % 1 > 0 ? Icons.star_half : Icons.star_border),\n        color: Colors.amber, size: 20)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Rating Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** rating, star, score\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/rating_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add rating\nflutter pub add star\nflutter pub add score\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "pricing",
    "n": "Pricing Card",
    "i": "💳",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "pricing",
      "plan",
      "subscription"
    ],
    "d": "Narx kartochkasi.",
    "f": [
      {
        "n": "pricing_card.dart",
        "p": "lib/widgets/pricing_card.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PricingCard extends StatelessWidget {\n  final String title, price, period;\n  final List<String> features;\n  final bool isPopular;\n  const PricingCard({super.key, required this.title, required this.price, required this.period, required this.features, this.isPopular = false});\n  @override\n  Widget build(BuildContext context) {\n    return Card(elevation: isPopular ? 8 : 2, child: Padding(padding: const EdgeInsets.all(24), child: Column(children: [\n      if (isPopular) Container(padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4), decoration: BoxDecoration(color: Colors.blue, borderRadius: BorderRadius.circular(20)), child: const Text('Mashhur', style: TextStyle(color: Colors.white, fontSize: 12))),\n      const SizedBox(height: 12),\n      Text(title, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n      Text(price + '/' + period, style: const TextStyle(fontSize: 32, fontWeight: FontWeight.bold)),\n      const Divider(),\n      ...features.map((f) => Padding(padding: const EdgeInsets.symmetric(vertical: 4), child: Row(children: [const Icon(Icons.check_circle, color: Colors.green, size: 18), const SizedBox(width: 8), Text(f)]))),\n      const SizedBox(height: 16),\n      SizedBox(width: double.infinity, child: FilledButton(onPressed: () {}, child: const Text('Tanlash'))),\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Pricing Card\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** pricing, plan, subscription\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/pricing_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add pricing\nflutter pub add plan\nflutter pub add subscription\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "tooltip",
    "n": "Tooltip Widget",
    "i": "🏷️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "tooltip",
      "hint",
      "info"
    ],
    "d": "Maslahat tooltip.",
    "f": [
      {
        "n": "tooltip_page.dart",
        "p": "lib/pages/tooltip_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TooltipPage extends StatelessWidget {\n  const TooltipPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Tooltip')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Tooltip(message: 'Bu Home tugmasi', child: IconButton(icon: const Icon(Icons.home, size: 48), onPressed: () {})),\n        const SizedBox(height: 16),\n        Tooltip(message: 'Qidiruv', triggerMode: TooltipTriggerMode.tap, child: ElevatedButton.icon(onPressed: () {}, icon: const Icon(Icons.search), label: const Text('Qidirish'))),\n      ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Tooltip Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** tooltip, hint, info\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/tooltip_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add tooltip\nflutter pub add hint\nflutter pub add info\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "chip",
    "n": "Chip Widget",
    "i": "🏷️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "chip",
      "tag",
      "filter"
    ],
    "d": "Chip widget.",
    "f": [
      {
        "n": "chip_page.dart",
        "p": "lib/pages/chip_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ChipPage extends StatefulWidget {\n  const ChipPage({super.key});\n  @override\n  State<ChipPage> createState() => _ChipPageState();\n}\n\nclass _ChipPageState extends State<ChipPage> {\n  bool _selected = false;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Chips')),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        Wrap(spacing: 8, children: [\n          const Chip(label: Text('Flutter')),\n          const Chip(label: Text('Dart'), avatar: Icon(Icons.code)),\n          FilterChip(label: const Text('Filter'), selected: _selected, onSelected: (v) => setState(() => _selected = v)),\n          ChoiceChip(label: const Text('Tanlangan'), selected: true, onSelected: (_) {}),\n        ]),\n        const SizedBox(height: 16),\n        Wrap(spacing: 8, children: List.generate(10, (i) => ActionChip(label: Text('Tag ' + (i + 1).toString()), onPressed: () {}))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Chip Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** chip, tag, filter\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/chip_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add chip\nflutter pub add tag\nflutter pub add filter\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "swipe",
    "n": "Swipe Dismiss",
    "i": "👆",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "swipe",
      "dismiss",
      "dismissible"
    ],
    "d": "Surgan bilan o'chirish.",
    "f": [
      {
        "n": "swipe_page.dart",
        "p": "lib/pages/swipe_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SwipePage extends StatefulWidget {\n  const SwipePage({super.key});\n  @override\n  State<SwipePage> createState() => _SwipePageState();\n}\n\nclass _SwipePageState extends State<SwipePage> {\n  final List<String> _items = List.generate(20, (i) => 'Element ' + (i + 1).toString());\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Swipe')),\n      body: ListView.builder(itemCount: _items.length,\n        itemBuilder: (_, i) => Dismissible(key: Key(_items[i]),\n          background: Container(color: Colors.red, alignment: Alignment.centerRight, padding: const EdgeInsets.only(right: 20),\n            child: const Icon(Icons.delete, color: Colors.white)),\n          onDismissed: (_) => setState(() => _items.removeAt(i)),\n          child: ListTile(title: Text(_items[i]), leading: const Icon(Icons.drag_handle)))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Swipe Dismiss\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** swipe, dismiss, dismissible\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/swipe_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add dismiss\nflutter pub add dismissible\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "pull-refresh",
    "n": "Pull to Refresh",
    "i": "🔄",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "refresh",
      "pull",
      "reload"
    ],
    "d": "Surgan bilan yangilash.",
    "f": [
      {
        "n": "pull_refresh.dart",
        "p": "lib/pages/pull_refresh.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PullRefreshPage extends StatefulWidget {\n  const PullRefreshPage({super.key});\n  @override\n  State<PullRefreshPage> createState() => _PullRefreshPageState();\n}\n\nclass _PullRefreshPageState extends State<PullRefreshPage> {\n  int _count = 0;\n  Future<void> _refresh() async {\n    await Future.delayed(const Duration(seconds: 2));\n    setState(() => _count++);\n  }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Pull to Refresh')),\n      body: RefreshIndicator(onRefresh: _refresh,\n        child: ListView.builder(itemCount: 20,\n          itemBuilder: (_, i) => ListTile(title: Text('Element ' + (i + 1).toString() + ' (v' + _count.toString() + ')')))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Pull to Refresh\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** refresh, pull, reload\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/pull_refresh.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add refresh\nflutter pub add pull\nflutter pub add reload\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "lottie",
    "n": "Lottie Player",
    "i": "🎬",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "lottie",
      "json",
      "animation"
    ],
    "d": "Lottie animatsiya.",
    "f": [
      {
        "n": "lottie_page.dart",
        "p": "lib/pages/lottie_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:lottie/lottie.dart';\n\nclass LottiePage extends StatelessWidget {\n  const LottiePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Lottie')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Lottie.asset('assets/animations/loading.json', width: 200, height: 200),\n        const SizedBox(height: 24),\n        const Text('Lottie animatsiya', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () {}, child: const Text('Yuklash')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Lottie Player\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** lottie, json, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/lottie_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add lottie\nflutter pub add json\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "staggered",
    "n": "Staggered Grid",
    "i": "📐",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "staggered",
      "grid",
      "mosaic"
    ],
    "d": "Sakkizburchak grid.",
    "f": [
      {
        "n": "staggered_page.dart",
        "p": "lib/pages/staggered_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_staggered_grid_view/flutter_staggered_grid_view.dart';\n\nclass StaggeredPage extends StatelessWidget {\n  const StaggeredPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Staggered')),\n      body: MasonryGridView.count(crossAxisCount: 2, mainAxisSpacing: 8, crossAxisSpacing: 8, padding: const EdgeInsets.all(8),\n        itemCount: 20,\n        itemBuilder: (_, i) => Container(height: (i % 3 == 0) ? 200 : 120,\n          decoration: BoxDecoration(color: Colors.primaries[i % Colors.primaries.length].withOpacity(0.3), borderRadius: BorderRadius.circular(12)),\n          child: Center(child: Text((i + 1).toString(), style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold))))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Staggered Grid\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** staggered, grid, mosaic\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/staggered_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add staggered\nflutter pub add grid\nflutter pub add mosaic\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "parallax",
    "n": "Parallax Scroll",
    "i": "📜",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "parallax",
      "scroll",
      "effect"
    ],
    "d": "Parallax skrol effekti.",
    "f": [
      {
        "n": "parallax_page.dart",
        "p": "lib/pages/parallax_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ParallaxPage extends StatelessWidget {\n  const ParallaxPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: CustomScrollView(slivers: [\n      SliverAppBar(expandedHeight: 250, pinned: true, flexibleSpace: FlexibleSpaceBar(\n        title: const Text('Parallax'),\n        background: Container(decoration: const BoxDecoration(\n          gradient: LinearGradient(colors: [Colors.blue, Colors.purple]))))),\n      SliverList(delegate: SliverChildBuilderDelegate((_, i) => ListTile(\n        leading: CircleAvatar(child: Text((i + 1).toString())),\n        title: Text('Element ' + (i + 1).toString()), subtitle: const Text('Tavsif'),\n      ), childCount: 20)),\n    ]));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Parallax Scroll\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** parallax, scroll, effect\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/parallax_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add parallax\nflutter pub add scroll\nflutter pub add effect\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "transition",
    "n": "Page Transition",
    "i": "🚪",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "transition",
      "route",
      "animation"
    ],
    "d": "Sahifalararo o'tish animatsiyasi.",
    "f": [
      {
        "n": "transition_page.dart",
        "p": "lib/pages/transition_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TransitionPage extends StatelessWidget {\n  const TransitionPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Transitions')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        FilledButton(onPressed: () => Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => FadeTransition(opacity: anim, child: child))),\n          child: const Text('Fade')),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () => Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => SlideTransition(\n            position: Tween(begin: const Offset(1, 0), end: Offset.zero).animate(anim), child: child))),\n          child: const Text('Slide')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Page Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** transition, route, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/transition_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add transition\nflutter pub add route\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "video",
    "n": "Video Player",
    "i": "🎬",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "video",
      "player",
      "media"
    ],
    "d": "Video pleer.",
    "f": [
      {
        "n": "video_page.dart",
        "p": "lib/pages/video_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:video_player/video_player.dart';\n\nclass VideoPage extends StatefulWidget {\n  const VideoPage({super.key});\n  @override\n  State<VideoPage> createState() => _VideoPageState();\n}\n\nclass _VideoPageState extends State<VideoPage> {\n  late VideoPlayerController _ctrl;\n  bool _init = false;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = VideoPlayerController.networkUrl(Uri.parse('https://example.com/video.mp4'))\n      ..initialize().then((_) => setState(() => _init = true));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Video')),\n      body: Center(child: _init ? AspectRatio(aspectRatio: _ctrl.value.aspectRatio, child: VideoPlayer(_ctrl)) : const CircularProgressIndicator()),\n      floatingActionButton: FloatingActionButton(onPressed: () => setState(() => _ctrl.value.isPlaying ? _ctrl.pause() : _ctrl.play()),\n        child: Icon(_ctrl.value.isPlaying ? Icons.pause : Icons.play_arrow)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Video Player\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** video, player, media\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/video_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add video\nflutter pub add player\nflutter pub add media\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "audio",
    "n": "Audio Player",
    "i": "🎵",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "audio",
      "player",
      "music"
    ],
    "d": "Audio pleer.",
    "f": [
      {
        "n": "audio_player.dart",
        "p": "lib/widgets/audio_player.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AudioPlayerWidget extends StatelessWidget {\n  const AudioPlayerWidget({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Card(margin: const EdgeInsets.all(16), child: Padding(padding: const EdgeInsets.all(16), child: Column(children: [\n      Row(children: const [\n        CircleAvatar(radius: 24, child: Icon(Icons.music_note)),\n        SizedBox(width: 12),\n        Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text('Qo'shiq nomi', style: TextStyle(fontWeight: FontWeight.bold)),\n          Text('Artist', style: TextStyle(color: Colors.grey, fontSize: 12)),\n        ]),\n      ]),\n      const SizedBox(height: 16),\n      const LinearProgressIndicator(value: 0.4),\n      const SizedBox(height: 12),\n      Row(mainAxisAlignment: MainAxisAlignment.center, children: const [\n        IconButton(icon: Icon(Icons.skip_previous), onPressed: null),\n        IconButton(icon: Icon(Icons.play_circle_filled, size: 48), onPressed: null),\n        IconButton(icon: Icon(Icons.skip_next), onPressed: null),\n      ]),\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Audio Player\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** audio, player, music\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/audio_player.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add audio\nflutter pub add player\nflutter pub add music\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "timeline-page",
    "n": "Timeline Page",
    "i": "📅",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "timeline",
      "steps",
      "history"
    ],
    "d": "Timeline sahifasi.",
    "f": [
      {
        "n": "timeline_page.dart",
        "p": "lib/pages/timeline_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TimelinePage extends StatelessWidget {\n  const TimelinePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Timeline')),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        _item(Icons.check_circle, 'Buyurtma qabul qilindi', 'Bugun 10:00', Colors.green),\n        _item(Icons.check_circle, 'Tayyorlanmoqda', 'Bugun 11:30', Colors.green),\n        _item(Icons.access_time, 'Yetkazishda', 'Bugun 14:00', Colors.orange),\n        _item(Icons.circle, 'Yetkazildi', 'Bugun 16:00', Colors.grey),\n      ]));\n  }\n  static Widget _item(IconData icon, String title, String time, Color color) {\n    return Row(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Column(children: [\n        CircleAvatar(radius: 16, backgroundColor: color.withOpacity(0.2), child: Icon(icon, color: color, size: 16)),\n        Container(width: 2, height: 50, color: color.withOpacity(0.3)),\n      ]),\n      const SizedBox(width: 12),\n      Expanded(child: Padding(padding: const EdgeInsets.only(bottom: 16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(title, style: const TextStyle(fontWeight: FontWeight.bold)),\n        Text(time, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n      ]))),\n    ]);\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Timeline Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** timeline, steps, history\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/timeline_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add timeline\nflutter pub add steps\nflutter pub add history\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "about",
    "n": "About Page",
    "i": "ℹ️",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "about",
      "info",
      "app"
    ],
    "d": "Biz haqimizda sahifasi.",
    "f": [
      {
        "n": "about_page.dart",
        "p": "lib/pages/about_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AboutPage extends StatelessWidget {\n  const AboutPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Biz haqimizda')),\n      body: ListView(children: const [\n        SizedBox(height: 32),\n        CircleAvatar(radius: 50, child: Icon(Icons.flutter_dash, size: 60)),\n        SizedBox(height: 16),\n        Center(child: Text('APP_NAME', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold))),\n        Center(child: Text('Versiya 1.0.0', style: TextStyle(color: Colors.grey))),\n        SizedBox(height: 32),\n        Padding(padding: EdgeInsets.symmetric(horizontal: 24), child: Text(\n          'Bu ilova Flutter yordamida yaratilgan.', textAlign: TextAlign.center, style: TextStyle(color: Colors.grey))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## About Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** about, info, app\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/about_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add about\nflutter pub add info\nflutter pub add app\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "contact",
    "n": "Contact Page",
    "i": "📞",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "contact",
      "form",
      "message"
    ],
    "d": "Bog'lanish sahifasi.",
    "f": [
      {
        "n": "contact_page.dart",
        "p": "lib/pages/contact_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ContactPage extends StatelessWidget {\n  const ContactPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Bog'lanish')),\n      body: Padding(padding: const EdgeInsets.all(24), child: Column(children: const [\n        TextField(decoration: InputDecoration(labelText: 'Ism', border: OutlineInputBorder())),\n        SizedBox(height: 16),\n        TextField(decoration: InputDecoration(labelText: 'Email', border: OutlineInputBorder())),\n        SizedBox(height: 16),\n        TextField(maxLines: 4, decoration: InputDecoration(labelText: 'Xabar', border: OutlineInputBorder(), alignLabelWithHint: true)),\n        SizedBox(height: 24),\n      ])),\n    );\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Contact Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** contact, form, message\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/contact_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add contact\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "file-manager",
    "n": "File Manager",
    "i": "📁",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "file",
      "folder",
      "storage"
    ],
    "d": "Fayl menejeri.",
    "f": [
      {
        "n": "file_manager.dart",
        "p": "lib/pages/file_manager.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FileManagerPage extends StatelessWidget {\n  const FileManagerPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Fayllar')),\n      body: ListView(children: [\n        _folder('Documents', Icons.folder, Colors.amber),\n        _folder('Images', Icons.folder, Colors.blue),\n        _folder('Downloads', Icons.folder, Colors.green),\n        const Divider(),\n        _file('report.pdf', Icons.picture_as_pdf, Colors.red, '2.5 MB'),\n        _file('photo.jpg', Icons.image, Colors.green, '1.2 MB'),\n      ]));\n  }\n  static Widget _folder(String n, IconData i, Color c) => ListTile(leading: Icon(i, color: c, size: 32), title: Text(n), subtitle: const Text('12 ta fayl'), trailing: const Icon(Icons.chevron_right));\n  static Widget _file(String n, IconData i, Color c, String s) => ListTile(leading: Icon(i, color: c), title: Text(n), subtitle: Text(s));\n}"
      }
    ],
    "icon": "document",
    "instructions": "## File Manager\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** file, folder, storage\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/file_manager.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add file\nflutter pub add folder\nflutter pub add storage\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "map",
    "n": "Map Page",
    "i": "🗺️",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "map",
      "location",
      "geo"
    ],
    "d": "Xarita sahifasi.",
    "f": [
      {
        "n": "map_page.dart",
        "p": "lib/pages/map_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass MapPage extends StatelessWidget {\n  const MapPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Xarita')),\n      body: Stack(children: [\n        Container(color: Colors.green[100], child: const Center(child: Text('Xarita joylashuvi'))),\n        Positioned(bottom: 20, left: 20, right: 20, child: Card(child: ListTile(\n          leading: const Icon(Icons.location_on, color: Colors.red),\n          title: const Text('Toshkent'),\n          subtitle: const Text('39.6542° N, 66.9597° E'),\n          trailing: FilledButton(onPressed: () {}, child: const Text('Borish')),\n        ))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Map Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** map, location, geo\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/map_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add map\nflutter pub add location\nflutter pub add geo\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "calendar",
    "n": "Calendar Page",
    "i": "📅",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "calendar",
      "date",
      "event"
    ],
    "d": "Kalendar sahifasi.",
    "f": [
      {
        "n": "calendar_page.dart",
        "p": "lib/pages/calendar_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CalendarPage extends StatefulWidget {\n  const CalendarPage({super.key});\n  @override\n  State<CalendarPage> createState() => _CalendarPageState();\n}\n\nclass _CalendarPageState extends State<CalendarPage> {\n  DateTime _selected = DateTime.now();\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Kalendar')),\n      body: Column(children: [\n        CalendarDatePicker(initialDate: _selected, firstDate: DateTime(2020), lastDate: DateTime(2030), onDateChanged: (d) => setState(() => _selected = d)),\n        const Divider(),\n        const Expanded(child: ListView(children: [\n          ListTile(leading: Icon(Icons.event, color: Colors.blue), title: Text('Yig'ilish'), subtitle: Text('10:00 - 12:00')),\n          ListTile(leading: Icon(Icons.flight, color: Colors.green), title: Text('Parvoz'), subtitle: Text('14:00 - 17:00')),\n        ])),\n      ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Calendar Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** calendar, date, event\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/calendar_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add calendar\nflutter pub add date\nflutter pub add event\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "list",
    "n": "List View",
    "i": "📋",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "list",
      "scroll",
      "item"
    ],
    "d": "Oddiy ro'yxat sahifasi.",
    "f": [
      {
        "n": "list_page.dart",
        "p": "lib/pages/list_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ListPage extends StatelessWidget {\n  const ListPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Ro'yxat')),\n      body: ListView.builder(itemCount: 50,\n        itemBuilder: (context, i) => ListTile(\n          leading: CircleAvatar(child: Text((i + 1).toString())),\n          title: Text('Element ' + (i + 1).toString()),\n          subtitle: Text('Tavsif ' + (i + 1).toString()),\n          trailing: const Icon(Icons.chevron_right),\n          onTap: () {},\n        )));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## List View\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** list, scroll, item\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/list_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add scroll\nflutter pub add item\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "detail",
    "n": "Detail View",
    "i": "🔎",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "detail",
      "view",
      "info"
    ],
    "d": "Element tafsilotlari.",
    "f": [
      {
        "n": "detail_page.dart",
        "p": "lib/pages/detail_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DetailPage extends StatelessWidget {\n  final String title;\n  const DetailPage({super.key, required this.title});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: Text(title)),\n      body: ListView(children: [\n        Container(height: 200, color: Colors.blue[100], child: const Center(child: Icon(Icons.image, size: 64, color: Colors.blue))),\n        Padding(padding: const EdgeInsets.all(16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(title, style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n          const SizedBox(height: 8),\n          const Text('Bu element haqida batafsil ma'lumot.', style: TextStyle(color: Colors.grey)),\n          const SizedBox(height: 16),\n          FilledButton(onPressed: () {}, child: const Text('Harakat')),\n        ])),\n      ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Detail View\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** detail, view, info\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/detail_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add detail\nflutter pub add view\nflutter pub add info\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "create",
    "n": "Create Page",
    "i": "➕",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "create",
      "add",
      "form"
    ],
    "d": "Yangi element yaratish.",
    "f": [
      {
        "n": "create_page.dart",
        "p": "lib/pages/create_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CreatePage extends StatelessWidget {\n  const CreatePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Yaratish'), actions: [TextButton(onPressed: () {}, child: const Text('Saqlash'))]),\n      body: Padding(padding: const EdgeInsets.all(24), child: Column(children: const [\n        TextFormField(decoration: InputDecoration(labelText: 'Sarlavha', border: OutlineInputBorder())),\n        SizedBox(height: 16),\n        TextFormField(maxLines: 4, decoration: InputDecoration(labelText: 'Tavsif', border: OutlineInputBorder(), alignLabelWithHint: true)),\n      ])));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Create Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** create, add, form\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/create_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add create\nflutter pub add add\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "edit",
    "n": "Edit Page",
    "i": "✏️",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "edit",
      "update",
      "form"
    ],
    "d": "Elementni tahrirlash.",
    "f": [
      {
        "n": "edit_page.dart",
        "p": "lib/pages/edit_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass EditPage extends StatelessWidget {\n  const EditPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Tahrirlash')),\n      body: Padding(padding: const EdgeInsets.all(24), child: Column(children: const [\n        TextField(decoration: InputDecoration(labelText: 'Sarlavha', border: OutlineInputBorder())),\n        SizedBox(height: 16),\n        TextField(maxLines: 3, decoration: InputDecoration(labelText: 'Tavsif', border: OutlineInputBorder())),\n        SizedBox(height: 24),\n      ])),);\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Edit Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** edit, update, form\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/edit_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add edit\nflutter pub add update\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "biometric",
    "n": "Biometric Auth",
    "i": "👆",
    "diff": "easy",
    "c": "auth",
    "t": [
      "flutter",
      "biometric",
      "fingerprint",
      "face"
    ],
    "d": "Barmoq izi bilan kirish.",
    "f": [
      {
        "n": "biometric_page.dart",
        "p": "lib/pages/auth/biometric_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass BiometricPage extends StatelessWidget {\n  const BiometricPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n      const Icon(Icons.fingerprint, size: 100, color: Colors.blue),\n      const SizedBox(height: 20),\n      const Text('Barmoq izi bilan kirish', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n      const SizedBox(height: 32),\n      FilledButton.icon(onPressed: () {}, icon: const Icon(Icons.fingerprint), label: const Text('Tekshirish')),\n      const SizedBox(height: 16),\n      TextButton(onPressed: () {}, child: const Text('Boshqa usul bilan kirish')),\n    ])));\n  }\n}"
      }
    ],
    "icon": "lock",
    "instructions": "## Biometric Auth\n\n**Kategoriya:** Auth\n**Qiyinlik:** Easy\n**Teglar:** biometric, fingerprint, face\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/biometric_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add biometric\nflutter pub add fingerprint\nflutter pub add face\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "responsive",
    "n": "Responsive Layout",
    "i": "📱",
    "diff": "medium",
    "c": "starter",
    "t": [
      "flutter",
      "responsive",
      "adaptive",
      "layout"
    ],
    "d": "Responsive layout.",
    "f": [
      {
        "n": "responsive.dart",
        "p": "lib/widgets/responsive.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ResponsiveLayout extends StatelessWidget {\n  final Widget mobile, tablet, desktop;\n  const ResponsiveLayout({super.key, required this.mobile, required this.tablet, required this.desktop});\n  static bool isMobile(BuildContext c) => MediaQuery.of(c).size.width < 600;\n  static bool isTablet(BuildContext c) => MediaQuery.of(c).size.width >= 600 && MediaQuery.of(c).size.width < 1200;\n  static bool isDesktop(BuildContext c) => MediaQuery.of(c).size.width >= 1200;\n  @override\n  Widget build(BuildContext context) {\n    return LayoutBuilder(builder: (context, constraints) {\n      if (constraints.maxWidth >= 1200) return desktop;\n      if (constraints.maxWidth >= 600) return tablet;\n      return mobile;\n    });\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Responsive Layout\n\n**Kategoriya:** Starter\n**Qiyinlik:** Medium\n**Teglar:** responsive, adaptive, layout\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/responsive.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add adaptive\nflutter pub add layout\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "value-notifier",
    "n": "ValueNotifier",
    "i": "📦",
    "diff": "easy",
    "c": "state",
    "t": [
      "flutter",
      "valuenotifier",
      "simple"
    ],
    "d": "ValueNotifier state.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\n\nfinal counter = ValueNotifier<int>(0);\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true),\n      home: Scaffold(appBar: AppBar(title: const Text('ValueNotifier')),\n        body: Center(child: ValueListenableBuilder<int>(valueListenable: counter,\n          builder: (_, count, __) => Text(count.toString(), style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold)))),\n        floatingActionButton: Row(mainAxisAlignment: MainAxisAlignment.end, children: [\n          FloatingActionButton(heroTag: 'dec', onPressed: () => counter.value--, child: const Icon(Icons.remove)),\n          const SizedBox(width: 16),\n          FloatingActionButton(heroTag: 'inc', onPressed: () => counter.value++, child: const Icon(Icons.add)),\n        ])));\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## ValueNotifier\n\n**Kategoriya:** State\n**Qiyinlik:** Easy\n**Teglar:** valuenotifier, simple\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add valuenotifier\nflutter pub add simple\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "firebase-firestore",
    "n": "Firestore CRUD",
    "i": "🔥",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "firestore",
      "crud",
      "cloud"
    ],
    "d": "Firestore bilan CRUD operatsiyalari.",
    "f": [
      {
        "n": "firestore_service.dart",
        "p": "lib/services/firestore_service.dart",
        "c": "import 'package:cloud_firestore/cloud_firestore.dart';\n\nclass FirestoreService {\n  final _db = FirebaseFirestore.instance;\n\n  Future<void> add(String col, Map<String, dynamic> data) async {\n    await _db.collection(col).add(data);\n  }\n\n  Future<void> set(String col, String id, Map<String, dynamic> data) async {\n    await _db.collection(col).doc(id).set(data);\n  }\n\n  Future<DocumentSnapshot> get(String col, String id) async {\n    return await _db.collection(col).doc(id).get();\n  }\n\n  Stream<QuerySnapshot> stream(String col) {\n    return _db.collection(col).snapshots();\n  }\n\n  Future<void> update(String col, String id, Map<String, dynamic> data) async {\n    await _db.collection(col).doc(id).update(data);\n  }\n\n  Future<void> delete(String col, String id) async {\n    await _db.collection(col).doc(id).delete();\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## Firestore CRUD\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** firestore, crud, cloud\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/firestore_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add firestore\nflutter pub add crud\nflutter pub add cloud\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "graphql",
    "n": "GraphQL Client",
    "i": "📡",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "graphql",
      "query",
      "mutation"
    ],
    "d": "GraphQL API mijoz.",
    "f": [
      {
        "n": "graphql_service.dart",
        "p": "lib/services/graphql_service.dart",
        "c": "import 'package:graphql_flutter/graphql_flutter.dart';\n\nclass GraphQLService {\n  static final GraphQLService _i = GraphQLService._();\n  factory GraphQLService() => _i;\n  late final GraphQLClient _client;\n\n  GraphQLService._() {\n    final httpLink = HttpLink('https://api.example.com/graphql');\n    _client = GraphQLClient(link: httpLink, cache: GraphQLCache(store: InMemoryStore()));\n  }\n\n  Future<QueryResult> query(String q, {Map<String, dynamic>? vars}) async {\n    return await _client.query(QueryOptions(document: gql(q), variables: vars));\n  }\n\n  Future<QueryResult> mutate(String m, {Map<String, dynamic>? vars}) async {\n    return await _client.mutate(MutationOptions(document: gql(m), variables: vars));\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## GraphQL Client\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** graphql, query, mutation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/graphql_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add graphql\nflutter pub add query\nflutter pub add mutation\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "websocket",
    "n": "WebSocket Service",
    "i": "🔌",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "websocket",
      "realtime",
      "socket"
    ],
    "d": "WebSocket ulanish.",
    "f": [
      {
        "n": "websocket_service.dart",
        "p": "lib/services/websocket_service.dart",
        "c": "import 'dart:async';\nimport 'dart:convert';\nimport 'package:web_socket_channel/web_socket_channel.dart';\n\nclass WebSocketService {\n  WebSocketChannel? _channel;\n  final _ctrl = StreamController<dynamic>.broadcast();\n  Stream<dynamic> get stream => _ctrl.stream;\n\n  void connect(String url) {\n    _channel = WebSocketChannel.connect(Uri.parse(url));\n    _channel!.stream.listen(\n      (data) => _ctrl.add(jsonDecode(data)),\n      onError: (e) => _ctrl.addError(e),\n      onDone: () => _ctrl.close(),\n    );\n  }\n\n  void send(dynamic data) => _channel?.sink.add(jsonEncode(data));\n  void disconnect() { _channel?.sink.close(); _ctrl.close(); }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## WebSocket Service\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** websocket, realtime, socket\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/websocket_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add websocket\nflutter pub add realtime\nflutter pub add socket\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "cubit-pattern",
    "n": "Cubit Pattern",
    "i": "🎯",
    "diff": "medium",
    "c": "state",
    "t": [
      "flutter",
      "cubit",
      "bloc",
      "simple"
    ],
    "d": "Oddiy Cubit pattern.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_bloc/flutter_bloc.dart';\n\nclass ThemeCubit extends Cubit<bool> {\n  ThemeCubit() : super(false);\n  void toggle() => emit(!state);\n}\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return BlocProvider(create: (_) => ThemeCubit(), child: BlocBuilder<ThemeCubit, bool>(\n      builder: (_, dark) => MaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n        themeMode: dark ? ThemeMode.dark : ThemeMode.light,\n        theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true, brightness: Brightness.light),\n        darkTheme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true, brightness: Brightness.dark),\n        home: const HomePage())));\n  }\n}\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Cubit'), actions: [\n      IconButton(icon: Icon(context.read<ThemeCubit>().state ? Icons.light_mode : Icons.dark_mode),\n        onPressed: () => context.read<ThemeCubit>().toggle()),\n    ]), body: const Center(child: Text('Mavzu almashtiring')));\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## Cubit Pattern\n\n**Kategoriya:** State\n**Qiyinlik:** Medium\n**Teglar:** cubit, bloc, simple\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add cubit\nflutter pub add bloc\nflutter pub add simple\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "inherited-widget",
    "n": "InheritedWidget",
    "i": "🧩",
    "diff": "medium",
    "c": "state",
    "t": [
      "flutter",
      "inherited",
      "widget",
      "tree"
    ],
    "d": "InheritedWidget pattern.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AppState extends InheritedWidget {\n  final int counter;\n  final VoidCallback increment;\n  const AppState({super.key, required this.counter, required this.increment, required super.child});\n  static AppState? of(BuildContext c) => c.dependOnInheritedWidgetOfExactType<AppState>();\n  @override\n  bool updateShouldNotify(AppState old) => counter != old.counter;\n}\n\nclass App extends StatefulWidget {\n  const App({super.key});\n  @override\n  State<App> createState() => _AppState();\n}\n\nclass _AppState extends State<App> {\n  int _c = 0;\n  @override\n  Widget build(BuildContext context) {\n    return AppState(counter: _c, increment: () => setState(() => _c++),\n      child: MaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n        theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true),\n        home: Scaffold(appBar: AppBar(title: const Text('Inherited')),\n          body: Center(child: Builder(builder: (c) => Text(\n            AppState.of(c)!.counter.toString(), style: const TextStyle(fontSize: 48)))),\n          floatingActionButton: FloatingActionButton(\n            onPressed: () => AppState.of(context)!.increment(), child: const Icon(Icons.add)),\n        )));\n  }\n}\nvoid main() => runApp(const App());"
      }
    ],
    "icon": "lightning",
    "instructions": "## InheritedWidget\n\n**Kategoriya:** State\n**Qiyinlik:** Medium\n**Teglar:** inherited, widget, tree\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add inherited\nflutter pub add widget\nflutter pub add tree\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "responsive",
    "n": "Responsive Layout",
    "i": "📱",
    "diff": "medium",
    "c": "starter",
    "t": [
      "flutter",
      "responsive",
      "adaptive",
      "layout"
    ],
    "d": "Responsive layout.",
    "f": [
      {
        "n": "responsive.dart",
        "p": "lib/widgets/responsive.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ResponsiveLayout extends StatelessWidget {\n  final Widget mobile, tablet, desktop;\n  const ResponsiveLayout({super.key, required this.mobile, required this.tablet, required this.desktop});\n  static bool isMobile(BuildContext c) => MediaQuery.of(c).size.width < 600;\n  static bool isTablet(BuildContext c) => MediaQuery.of(c).size.width >= 600 && MediaQuery.of(c).size.width < 1200;\n  static bool isDesktop(BuildContext c) => MediaQuery.of(c).size.width >= 1200;\n  @override\n  Widget build(BuildContext context) {\n    return LayoutBuilder(builder: (context, constraints) {\n      if (constraints.maxWidth >= 1200) return desktop;\n      if (constraints.maxWidth >= 600) return tablet;\n      return mobile;\n    });\n  }\n}\n\nclass ResponsivePage extends StatelessWidget {\n  const ResponsivePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: ResponsiveLayout(\n      mobile: const Center(child: Text('Mobil', style: TextStyle(fontSize: 24))),\n      tablet: const Center(child: Text('Planshet', style: TextStyle(fontSize: 32))),\n      desktop: const Center(child: Text('Kompyuter', style: TextStyle(fontSize: 40))),\n    ));\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Responsive Layout\n\n**Kategoriya:** Starter\n**Qiyinlik:** Medium\n**Teglar:** responsive, adaptive, layout\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/responsive.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add adaptive\nflutter pub add layout\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "multi-screen",
    "n": "Multi Screen App",
    "i": "🖥️",
    "diff": "medium",
    "c": "starter",
    "t": [
      "flutter",
      "multi",
      "screen",
      "route"
    ],
    "d": "Ko'p sahifali ilova.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/home_page.dart';\nimport 'pages/profile_page.dart';\nimport 'pages/settings_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(title: 'APP_NAME', debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true),\n      initialRoute: '/',\n      routes: {\n        '/': (_) => const HomePage(),\n        '/profile': (_) => const ProfilePage(),\n        '/settings': (_) => const SettingsPage(),\n      });\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Multi Screen App\n\n**Kategoriya:** Starter\n**Qiyinlik:** Medium\n**Teglar:** multi, screen, route\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add multi\nflutter pub add screen\nflutter pub add route\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "custom-paint",
    "n": "Custom Paint",
    "i": "🖌️",
    "diff": "hard",
    "c": "animations",
    "t": [
      "flutter",
      "custompaint",
      "canvas",
      "draw"
    ],
    "d": "CustomPainter bilan chizish.",
    "f": [
      {
        "n": "custom_paint_page.dart",
        "p": "lib/pages/custom_paint_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CustomPaintPage extends StatelessWidget {\n  const CustomPaintPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Custom Paint')),\n      body: Center(child: CustomPaint(size: const Size(300, 300), painter: MyPainter())));\n  }\n}\n\nclass MyPainter extends CustomPainter {\n  @override\n  void paint(Canvas canvas, Size size) {\n    final paint = Paint()..color = Colors.blue..strokeWidth = 4..style = PaintingStyle.stroke;\n    canvas.drawCircle(Offset(size.width / 2, size.height / 2), 100, paint);\n    paint.color = Colors.red;\n    canvas.drawRect(Rect.fromLTWH(50, 50, 200, 200), paint);\n    paint.color = Colors.green;\n    final path = Path()..moveTo(150, 50)..lineTo(250, 250)..lineTo(50, 250)..close();\n    canvas.drawPath(path, paint);\n  }\n  @override\n  bool shouldRepaint(covariant CustomPainter oldDelegate) => false;\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Custom Paint\n\n**Kategoriya:** Animations\n**Qiyinlik:** Hard\n**Teglar:** custompaint, canvas, draw\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/custom_paint_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add custompaint\nflutter pub add canvas\nflutter pub add draw\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "rotation-anim",
    "n": "Rotation Animation",
    "i": "🔄",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "rotation",
      "spin",
      "rotate"
    ],
    "d": "Aylanish animatsiyasi.",
    "f": [
      {
        "n": "rotation_page.dart",
        "p": "lib/pages/rotation_page.dart",
        "c": "import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass RotationPage extends StatefulWidget {\n  const RotationPage({super.key});\n  @override\n  State<RotationPage> createState() => _RotationPageState();\n}\n\nclass _RotationPageState extends State<RotationPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 3))..repeat();\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Rotation')),\n      body: Center(child: AnimatedBuilder(animation: _ctrl, builder: (_, child) => Transform.rotate(\n        angle: _ctrl.value * 2 * pi, child: child),\n        child: Container(width: 120, height: 120, decoration: BoxDecoration(\n          gradient: const LinearGradient(colors: [Colors.blue, Colors.purple]),\n          borderRadius: BorderRadius.circular(20)),\n          child: const Icon(Icons.refresh, color: Colors.white, size: 48)))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Rotation Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** rotation, spin, rotate\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/rotation_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add rotation\nflutter pub add spin\nflutter pub add rotate\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "explicit-anim",
    "n": "Explicit Animation",
    "i": "🎭",
    "diff": "hard",
    "c": "animations",
    "t": [
      "flutter",
      "explicit",
      "animationcontroller",
      "tween"
    ],
    "d": "Boshqariladigan animatsiya.",
    "f": [
      {
        "n": "explicit_page.dart",
        "p": "lib/pages/explicit_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ExplicitPage extends StatefulWidget {\n  const ExplicitPage({super.key});\n  @override\n  State<ExplicitPage> createState() => _ExplicitPageState();\n}\n\nclass _ExplicitPageState extends State<ExplicitPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<Color?> _colorAnim;\n  late Animation<double> _sizeAnim;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 2));\n    _colorAnim = ColorTween(begin: Colors.blue, end: Colors.red).animate(_ctrl);\n    _sizeAnim = Tween<double>(begin: 100, end: 200).animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeInOut));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Explicit')),\n      body: Center(child: AnimatedBuilder(animation: _ctrl, builder: (_, child) => Container(\n        width: _sizeAnim.value, height: _sizeAnim.value,\n        decoration: BoxDecoration(color: _colorAnim.value, borderRadius: BorderRadius.circular(20)),\n        child: const Icon(Icons.star, color: Colors.white, size: 48))),\n      ), floatingActionButton: FloatingActionButton(onPressed: () =>\n        _ctrl.isCompleted ? _ctrl.reverse() : _ctrl.forward(), child: const Icon(Icons.play_arrow)));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Explicit Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Hard\n**Teglar:** explicit, animationcontroller, tween\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/explicit_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add explicit\n# (kerakli paketni o'rnating)controller\nflutter pub add tween\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "staggered",
    "n": "Staggered Grid",
    "i": "📐",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "staggered",
      "grid",
      "mosaic"
    ],
    "d": "Sakkizburchak grid.",
    "f": [
      {
        "n": "staggered_page.dart",
        "p": "lib/pages/staggered_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_staggered_grid_view/flutter_staggered_grid_view.dart';\n\nclass StaggeredPage extends StatelessWidget {\n  const StaggeredPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Staggered')),\n      body: MasonryGridView.count(crossAxisCount: 2, mainAxisSpacing: 8, crossAxisSpacing: 8, padding: const EdgeInsets.all(8),\n        itemCount: 20,\n        itemBuilder: (_, i) => Container(height: (i % 3 == 0) ? 200 : 120,\n          decoration: BoxDecoration(color: Colors.primaries[i % Colors.primaries.length].withOpacity(0.3), borderRadius: BorderRadius.circular(12)),\n          child: Center(child: Text((i + 1).toString(), style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold))))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Staggered Grid\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** staggered, grid, mosaic\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/staggered_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add staggered\nflutter pub add grid\nflutter pub add mosaic\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "parallax",
    "n": "Parallax Scroll",
    "i": "📜",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "parallax",
      "scroll",
      "effect"
    ],
    "d": "Parallax skrol effekti.",
    "f": [
      {
        "n": "parallax_page.dart",
        "p": "lib/pages/parallax_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ParallaxPage extends StatelessWidget {\n  const ParallaxPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: CustomScrollView(slivers: [\n      SliverAppBar(expandedHeight: 250, pinned: true, flexibleSpace: FlexibleSpaceBar(\n        title: const Text('Parallax'),\n        background: Container(decoration: const BoxDecoration(\n          gradient: LinearGradient(colors: [Colors.blue, Colors.purple]))))),\n      SliverList(delegate: SliverChildBuilderDelegate((_, i) => ListTile(\n        leading: CircleAvatar(child: Text((i + 1).toString())),\n        title: Text('Element ' + (i + 1).toString()), subtitle: const Text('Tavsif'),\n      ), childCount: 20)),\n    ]));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Parallax Scroll\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** parallax, scroll, effect\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/parallax_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add parallax\nflutter pub add scroll\nflutter pub add effect\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "transition",
    "n": "Page Transition",
    "i": "🚪",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "transition",
      "route",
      "animation"
    ],
    "d": "Sahifalararo o'tish animatsiyasi.",
    "f": [
      {
        "n": "transition_page.dart",
        "p": "lib/pages/transition_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TransitionPage extends StatelessWidget {\n  const TransitionPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Transitions')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        FilledButton(onPressed: () => Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => FadeTransition(opacity: anim, child: child))),\n          child: const Text('Fade')),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () => Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => SlideTransition(\n            position: Tween(begin: const Offset(1, 0), end: Offset.zero).animate(anim), child: child))),\n          child: const Text('Slide')),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () => Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => ScaleTransition(scale: anim, child: child))),\n          child: const Text('Scale')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Page Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** transition, route, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/transition_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add transition\nflutter pub add route\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "video-player",
    "n": "Video Player",
    "i": "🎬",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "video",
      "player",
      "media"
    ],
    "d": "Video pleer.",
    "f": [
      {
        "n": "video_page.dart",
        "p": "lib/pages/video_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:video_player/video_player.dart';\n\nclass VideoPage extends StatefulWidget {\n  const VideoPage({super.key});\n  @override\n  State<VideoPage> createState() => _VideoPageState();\n}\n\nclass _VideoPageState extends State<VideoPage> {\n  late VideoPlayerController _ctrl;\n  bool _init = false;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = VideoPlayerController.networkUrl(Uri.parse('https://example.com/video.mp4'))\n      ..initialize().then((_) => setState(() => _init = true));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Video')),\n      body: Center(child: _init ? AspectRatio(aspectRatio: _ctrl.value.aspectRatio, child: VideoPlayer(_ctrl)) : const CircularProgressIndicator()),\n      floatingActionButton: FloatingActionButton(onPressed: () => setState(() => _ctrl.value.isPlaying ? _ctrl.pause() : _ctrl.play()),\n        child: Icon(_ctrl.value.isPlaying ? Icons.pause : Icons.play_arrow)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Video Player\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** video, player, media\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/video_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add video\nflutter pub add player\nflutter pub add media\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "audio-player",
    "n": "Audio Player",
    "i": "🎵",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "audio",
      "player",
      "music"
    ],
    "d": "Audio pleer.",
    "f": [
      {
        "n": "audio_player.dart",
        "p": "lib/widgets/audio_player.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AudioPlayerWidget extends StatelessWidget {\n  const AudioPlayerWidget({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Card(margin: const EdgeInsets.all(16), child: Padding(padding: const EdgeInsets.all(16), child: Column(children: [\n      Row(children: const [\n        CircleAvatar(radius: 24, child: Icon(Icons.music_note)),\n        SizedBox(width: 12),\n        Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text('Qo'shiq nomi', style: TextStyle(fontWeight: FontWeight.bold)),\n          Text('Artist', style: TextStyle(color: Colors.grey, fontSize: 12)),\n        ]),\n      ]),\n      const SizedBox(height: 16),\n      const LinearProgressIndicator(value: 0.4),\n      const SizedBox(height: 12),\n      Row(mainAxisAlignment: MainAxisAlignment.center, children: const [\n        IconButton(icon: Icon(Icons.skip_previous), onPressed: null),\n        IconButton(icon: Icon(Icons.play_circle_filled, size: 48), onPressed: null),\n        IconButton(icon: Icon(Icons.skip_next), onPressed: null),\n      ]),\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Audio Player\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** audio, player, music\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/audio_player.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add audio\nflutter pub add player\nflutter pub add music\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "image-carousel",
    "n": "Image Carousel",
    "i": "🖼️",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "carousel",
      "slider",
      "swipe"
    ],
    "d": "Rasm karuseli.",
    "f": [
      {
        "n": "carousel.dart",
        "p": "lib/widgets/carousel.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ImageCarousel extends StatefulWidget {\n  final List<String> images;\n  const ImageCarousel({super.key, required this.images});\n  @override\n  State<ImageCarousel> createState() => _ImageCarouselState();\n}\n\nclass _ImageCarouselState extends State<ImageCarousel> {\n  final _ctrl = PageController();\n  int _current = 0;\n  @override\n  Widget build(BuildContext context) {\n    return Column(children: [\n      AspectRatio(aspectRatio: 16/9, child: PageView.builder(\n        controller: _ctrl, itemCount: widget.images.length,\n        onPageChanged: (i) => setState(() => _current = i),\n        itemBuilder: (_, i) => Container(color: Colors.primaries[i % Colors.primaries.length].withOpacity(0.3),\n          child: const Center(child: Icon(Icons.image, size: 48))),\n      )),\n      Row(mainAxisAlignment: MainAxisAlignment.center, children: List.generate(widget.images.length, (i) =>\n        Container(margin: const EdgeInsets.all(4), width: _current == i ? 24 : 8, height: 8,\n          decoration: BoxDecoration(color: _current == i ? Colors.blue : Colors.grey, borderRadius: BorderRadius.circular(4))))),\n    ]);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Image Carousel\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** carousel, slider, swipe\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/carousel.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add carousel\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "feature-grid",
    "n": "Feature Grid",
    "i": "⭐",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "feature",
      "grid",
      "showcase"
    ],
    "d": "Xususiyatlar grid.",
    "f": [
      {
        "n": "feature_grid.dart",
        "p": "lib/widgets/feature_grid.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FeatureGrid extends StatelessWidget {\n  final List<Map<String, dynamic>> features;\n  const FeatureGrid({super.key, required this.features});\n  @override\n  Widget build(BuildContext context) {\n    return GridView.builder(shrinkWrap: true, physics: const NeverScrollableScrollPhysics(),\n      gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3, childAspectRatio: 0.9),\n      itemCount: features.length,\n      itemBuilder: (_, i) => Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        CircleAvatar(radius: 24, backgroundColor: (features[i]['color'] as Color).withOpacity(0.1),\n          child: Icon(features[i]['icon'] as IconData, color: features[i]['color'] as Color)),\n        const SizedBox(height: 8),\n        Text(features[i]['title'] as String, style: const TextStyle(fontSize: 12, fontWeight: FontWeight.w500)),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Feature Grid\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** feature, grid, showcase\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/feature_grid.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add feature\nflutter pub add grid\nflutter pub add showcase\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "testimonial",
    "n": "Testimonial",
    "i": "💬",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "testimonial",
      "review",
      "feedback"
    ],
    "d": "Fikr mulohaza widget.",
    "f": [
      {
        "n": "testimonial.dart",
        "p": "lib/widgets/testimonial.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TestimonialWidget extends StatelessWidget {\n  final String name, role, text;\n  const TestimonialWidget({super.key, required this.name, required this.role, required this.text});\n  @override\n  Widget build(BuildContext context) {\n    return Card(margin: const EdgeInsets.all(8), child: Padding(padding: const EdgeInsets.all(20), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      const Icon(Icons.format_quote, color: Colors.blue, size: 32),\n      const SizedBox(height: 8),\n      Text(text, style: const TextStyle(fontSize: 15, height: 1.5)),\n      const SizedBox(height: 16),\n      Row(children: [\n        const CircleAvatar(child: Icon(Icons.person)),\n        const SizedBox(width: 12),\n        Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(name, style: const TextStyle(fontWeight: FontWeight.bold)),\n          Text(role, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n        ]),\n      ]),\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Testimonial\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** testimonial, review, feedback\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/testimonial.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add testimonial\nflutter pub add review\n# (kerakli paketni o'rnating)back\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "accordion",
    "n": "Accordion Widget",
    "i": "🪗",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "accordion",
      "expand",
      "collapse"
    ],
    "d": "Kengaytiriladigan panel.",
    "f": [
      {
        "n": "accordion.dart",
        "p": "lib/widgets/accordion.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AccordionItem {\n  final String title, content;\n  const AccordionItem({required this.title, required this.content});\n}\n\nclass Accordion extends StatefulWidget {\n  final List<AccordionItem> items;\n  const Accordion({super.key, required this.items});\n  @override\n  State<Accordion> createState() => _AccordionState();\n}\n\nclass _AccordionState extends State<Accordion> {\n  int _expandedIndex = -1;\n  @override\n  Widget build(BuildContext context) {\n    return Column(children: List.generate(widget.items.length, (i) {\n      final item = widget.items[i];\n      return Card(child: Column(children: [\n        ListTile(title: Text(item.title), trailing: Icon(_expandedIndex == i ? Icons.expand_less : Icons.expand_more),\n          onTap: () => setState(() => _expandedIndex = _expandedIndex == i ? -1 : i)),\n        if (_expandedIndex == i) Padding(padding: const EdgeInsets.all(16), child: Text(item.content)),\n      ]));\n    }));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Accordion Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** accordion, expand, collapse\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/accordion.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add accordion\nflutter pub add expand\nflutter pub add collapse\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "progress",
    "n": "Progress Indicators",
    "i": "📈",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "progress",
      "loading",
      "indicator"
    ],
    "d": "Jarayon ko'rsatkichlari.",
    "f": [
      {
        "n": "progress_page.dart",
        "p": "lib/pages/progress_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ProgressPage extends StatelessWidget {\n  const ProgressPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Progress')),\n      body: ListView(padding: const EdgeInsets.all(24), children: const [\n        Text('Linear', style: TextStyle(fontWeight: FontWeight.bold)),\n        SizedBox(height: 8), LinearProgressIndicator(value: 0.7),\n        SizedBox(height: 24),\n        Text('Circular', style: TextStyle(fontWeight: FontWeight.bold)),\n        SizedBox(height: 16), Center(child: CircularProgressIndicator()),\n        SizedBox(height: 24),\n        Text('Custom', style: TextStyle(fontWeight: FontWeight.bold)),\n        SizedBox(height: 16), Center(child: SizedBox(width: 100, height: 100, child: CircularProgressIndicator(strokeWidth: 8))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Progress Indicators\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** progress, loading, indicator\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/progress_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add progress\n# (kerakli paketni o'rnating)\nflutter pub add indicator\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "timeline",
    "n": "Timeline Widget",
    "i": "📅",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "timeline",
      "history",
      "steps"
    ],
    "d": "Timeline komponenti.",
    "f": [
      {
        "n": "timeline.dart",
        "p": "lib/widgets/timeline.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TimelineItem {\n  final String title, subtitle;\n  final IconData icon;\n  const TimelineItem({required this.title, required this.subtitle, required this.icon});\n}\n\nclass TimelineWidget extends StatelessWidget {\n  final List<TimelineItem> items;\n  const TimelineWidget({super.key, required this.items});\n  @override\n  Widget build(BuildContext context) {\n    return Column(children: items.map((item) => Row(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Column(children: [\n        CircleAvatar(radius: 16, child: Icon(item.icon, size: 16)),\n        Container(width: 2, height: 40, color: Colors.grey[300]),\n      ]),\n      const SizedBox(width: 12),\n      Expanded(child: Padding(padding: const EdgeInsets.only(bottom: 16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(item.title, style: const TextStyle(fontWeight: FontWeight.bold)),\n        Text(item.subtitle, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n      ]))),\n    ])).toList());\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Timeline Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** timeline, history, steps\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/timeline.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add timeline\nflutter pub add history\nflutter pub add steps\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "notification-item",
    "n": "Notification Item",
    "i": "🔔",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "notification",
      "item",
      "list"
    ],
    "d": "Bildirishnoma elementi.",
    "f": [
      {
        "n": "notification_item.dart",
        "p": "lib/widgets/notification_item.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass NotificationItem extends StatelessWidget {\n  final String title, subtitle, time;\n  final IconData icon;\n  final Color color;\n  const NotificationItem({super.key, required this.title, required this.subtitle, required this.time, required this.icon, required this.color});\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(\n      leading: CircleAvatar(backgroundColor: color.withOpacity(0.1), child: Icon(icon, color: color, size: 20)),\n      title: Text(title, style: const TextStyle(fontWeight: FontWeight.w500)),\n      subtitle: Text(subtitle, style: const TextStyle(fontSize: 12)),\n      trailing: Text(time, style: const TextStyle(fontSize: 11, color: Colors.grey)),\n    );\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Notification Item\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** notification, item, list\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/notification_item.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add notification\nflutter pub add item\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "article-card",
    "n": "Article Card",
    "i": "📰",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "article",
      "blog",
      "post"
    ],
    "d": "Maqola kartochkasi.",
    "f": [
      {
        "n": "article_card.dart",
        "p": "lib/widgets/article_card.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ArticleCard extends StatelessWidget {\n  final String title, subtitle, date;\n  const ArticleCard({super.key, required this.title, required this.subtitle, required this.date});\n  @override\n  Widget build(BuildContext context) {\n    return Card(clipBehavior: Clip.antiAlias, child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Container(height: 160, color: Colors.blue[100], child: const Center(child: Icon(Icons.article, size: 48, color: Colors.blue))),\n      Padding(padding: const EdgeInsets.all(16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(title, style: const TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 8),\n        Text(subtitle, style: const TextStyle(color: Colors.grey, fontSize: 13)),\n        const SizedBox(height: 8),\n        Text(date, style: const TextStyle(color: Colors.grey, fontSize: 11)),\n      ])),\n    ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Article Card\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** article, blog, post\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/article_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add article\nflutter pub add blog\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "skeleton-loading",
    "n": "Skeleton Loading",
    "i": "💀",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "skeleton",
      "loading",
      "placeholder"
    ],
    "d": "Skelet yuklanish effekti.",
    "f": [
      {
        "n": "skeleton.dart",
        "p": "lib/widgets/skeleton.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass Skeleton extends StatelessWidget {\n  final double width, height, borderRadius;\n  const Skeleton({super.key, this.width = double.infinity, this.height = 20, this.borderRadius = 8});\n  @override\n  Widget build(BuildContext context) {\n    return Container(width: width, height: height,\n      decoration: BoxDecoration(color: Colors.grey[300], borderRadius: BorderRadius.circular(borderRadius)));\n  }\n}\n\nclass SkeletonListTile extends StatelessWidget {\n  const SkeletonListTile({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(leading: const CircleAvatar(backgroundColor: Colors.grey),\n      title: Skeleton(width: 150), subtitle: Skeleton(width: 100, height: 14));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Skeleton Loading\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** skeleton, loading, placeholder\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/skeleton.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add skeleton\n# (kerakli paketni o'rnating)\nflutter pub add placeholder\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "shimmer-effect",
    "n": "Shimmer Effect",
    "i": "✨",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "shimmer",
      "glow",
      "loading"
    ],
    "d": "Yaltirash effekti.",
    "f": [
      {
        "n": "shimmer_widget.dart",
        "p": "lib/widgets/shimmer_widget.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:shimmer/shimmer.dart';\n\nclass ShimmerWidget extends StatelessWidget {\n  final Widget child;\n  const ShimmerWidget({super.key, required this.child});\n  @override\n  Widget build(BuildContext context) {\n    return Shimmer.fromColors(baseColor: Colors.grey[300]!, highlightColor: Colors.grey[100]!, child: child);\n  }\n}\n\nclass ShimmerListTile extends StatelessWidget {\n  const ShimmerListTile({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return ShimmerWidget(child: ListTile(\n      leading: const CircleAvatar(backgroundColor: Colors.white),\n      title: Container(height: 14, width: 150, color: Colors.white),\n      subtitle: Container(height: 12, width: 100, color: Colors.white)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Shimmer Effect\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** shimmer, glow, loading\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/shimmer_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add shimmer\nflutter pub add glow\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "swipe-dismiss",
    "n": "Swipe Dismiss",
    "i": "👆",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "swipe",
      "dismiss",
      "dismissible"
    ],
    "d": "Surgan bilan o'chirish.",
    "f": [
      {
        "n": "swipe_page.dart",
        "p": "lib/pages/swipe_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SwipePage extends StatefulWidget {\n  const SwipePage({super.key});\n  @override\n  State<SwipePage> createState() => _SwipePageState();\n}\n\nclass _SwipePageState extends State<SwipePage> {\n  final List<String> _items = List.generate(20, (i) => 'Element ' + (i + 1).toString());\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Swipe')),\n      body: ListView.builder(itemCount: _items.length,\n        itemBuilder: (_, i) => Dismissible(key: Key(_items[i]),\n          background: Container(color: Colors.red, alignment: Alignment.centerRight, padding: const EdgeInsets.only(right: 20),\n            child: const Icon(Icons.delete, color: Colors.white)),\n          onDismissed: (_) => setState(() => _items.removeAt(i)),\n          child: ListTile(title: Text(_items[i]), leading: const Icon(Icons.drag_handle)))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Swipe Dismiss\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** swipe, dismiss, dismissible\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/swipe_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add dismiss\nflutter pub add dismissible\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "pull-refresh",
    "n": "Pull to Refresh",
    "i": "🔄",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "refresh",
      "pull",
      "reload"
    ],
    "d": "Surgan bilan yangilash.",
    "f": [
      {
        "n": "pull_refresh.dart",
        "p": "lib/pages/pull_refresh.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PullRefreshPage extends StatefulWidget {\n  const PullRefreshPage({super.key});\n  @override\n  State<PullRefreshPage> createState() => _PullRefreshPageState();\n}\n\nclass _PullRefreshPageState extends State<PullRefreshPage> {\n  int _count = 0;\n  Future<void> _refresh() async {\n    await Future.delayed(const Duration(seconds: 2));\n    setState(() => _count++);\n  }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Pull to Refresh')),\n      body: RefreshIndicator(onRefresh: _refresh,\n        child: ListView.builder(itemCount: 20,\n          itemBuilder: (_, i) => ListTile(title: Text('Element ' + (i + 1).toString() + ' (v' + _count.toString() + ')')))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Pull to Refresh\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** refresh, pull, reload\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/pull_refresh.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add refresh\nflutter pub add pull\nflutter pub add reload\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "lottie-player",
    "n": "Lottie Player",
    "i": "🎬",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "lottie",
      "json",
      "animation"
    ],
    "d": "Lottie animatsiya.",
    "f": [
      {
        "n": "lottie_page.dart",
        "p": "lib/pages/lottie_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:lottie/lottie.dart';\n\nclass LottiePage extends StatelessWidget {\n  const LottiePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Lottie')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Lottie.asset('assets/animations/loading.json', width: 200, height: 200),\n        const SizedBox(height: 24),\n        const Text('Lottie animatsiya', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () {}, child: const Text('Yuklash')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Lottie Player\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** lottie, json, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/lottie_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add lottie\nflutter pub add json\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "implicit-anim",
    "n": "Implicit Animations",
    "i": "🔄",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "implicit",
      "animatedcontainer"
    ],
    "d": "Avtomatik animatsiyalar.",
    "f": [
      {
        "n": "implicit_page.dart",
        "p": "lib/pages/implicit_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ImplicitPage extends StatefulWidget {\n  const ImplicitPage({super.key});\n  @override\n  State<ImplicitPage> createState() => _ImplicitPageState();\n}\n\nclass _ImplicitPageState extends State<ImplicitPage> {\n  bool _big = false;\n  Color _color = Colors.blue;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Implicit')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        AnimatedContainer(duration: const Duration(milliseconds: 500), curve: Curves.easeInOut,\n          width: _big ? 200 : 100, height: _big ? 200 : 100,\n          decoration: BoxDecoration(color: _color, borderRadius: BorderRadius.circular(_big ? 100 : 16))),\n        const SizedBox(height: 32),\n        Row(mainAxisAlignment: MainAxisAlignment.center, children: [\n          FilledButton(onPressed: () => setState(() => _big = !_big), child: const Text('O'lcham')),\n          const SizedBox(width: 16),\n          FilledButton(onPressed: () => setState(() => _color = _color == Colors.blue ? Colors.red : Colors.blue), child: const Text('Rang')),\n        ]),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Implicit Animations\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** implicit, animatedcontainer\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/implicit_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add implicit\nflutter pub add animatedcontainer\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "scale-anim",
    "n": "Scale Animation",
    "i": "📏",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "scale",
      "zoom",
      "transform"
    ],
    "d": "Masshtab animatsiyasi.",
    "f": [
      {
        "n": "scale_page.dart",
        "p": "lib/pages/scale_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ScalePage extends StatefulWidget {\n  const ScalePage({super.key});\n  @override\n  State<ScalePage> createState() => _ScalePageState();\n}\n\nclass _ScalePageState extends State<ScalePage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<double> _scale;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 600));\n    _scale = Tween<double>(begin: 1, end: 1.5).animate(CurvedAnimation(parent: _ctrl, curve: Curves.elasticOut));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Scale')),\n      body: Center(child: ScaleTransition(scale: _scale, child: GestureDetector(\n        onTap: () { _ctrl.isCompleted ? _ctrl.reverse() : _ctrl.forward(); },\n        child: Container(width: 100, height: 100, decoration: BoxDecoration(color: Colors.blue, borderRadius: BorderRadius.circular(16)),\n          child: const Icon(Icons.touch_app, color: Colors.white, size: 40)),\n      ))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Scale Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** scale, zoom, transform\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/scale_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add scale\nflutter pub add zoom\nflutter pub add transform\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "hero-anim",
    "n": "Hero Animation",
    "i": "🦸",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "hero",
      "animation",
      "transition"
    ],
    "d": "Hero transition animatsiyasi.",
    "f": [
      {
        "n": "hero_page.dart",
        "p": "lib/pages/hero_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass HeroPage extends StatelessWidget {\n  const HeroPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Hero')),\n      body: ListView.builder(padding: const EdgeInsets.all(8), itemCount: 10,\n        itemBuilder: (_, i) => Card(child: ListTile(\n          leading: Hero(tag: 'hero-' + i.toString(), child: CircleAvatar(backgroundColor: Colors.primaries[i],\n            child: const Icon(Icons.star, color: Colors.white))),\n          title: Text('Element ' + (i + 1).toString()),\n          onTap: () => Navigator.push(context, MaterialPageRoute(\n            builder: (_) => Scaffold(appBar: AppBar(title: Text('Element ' + (i + 1).toString())),\n              body: Center(child: Hero(tag: 'hero-' + i.toString(),\n                child: Container(width: 200, height: 200,\n                  decoration: BoxDecoration(color: Colors.primaries[i], shape: BoxShape.circle),\n                  child: const Icon(Icons.star, size: 80, color: Colors.white))))))),\n        )));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Hero Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** hero, animation, transition\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/hero_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add hero\n# (kerakli paketni o'rnating)\nflutter pub add transition\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "timeline-page",
    "n": "Timeline Page",
    "i": "📅",
    "diff": "medium",
    "c": "pages",
    "t": [
      "flutter",
      "timeline",
      "steps",
      "history"
    ],
    "d": "Timeline sahifasi.",
    "f": [
      {
        "n": "timeline_page.dart",
        "p": "lib/pages/timeline_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TimelinePage extends StatelessWidget {\n  const TimelinePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Timeline')),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        _item(Icons.check_circle, 'Buyurtma qabul qilindi', 'Bugun 10:00', Colors.green),\n        _item(Icons.check_circle, 'Tayyorlanmoqda', 'Bugun 11:30', Colors.green),\n        _item(Icons.access_time, 'Yetkazishda', 'Bugun 14:00', Colors.orange),\n        _item(Icons.circle, 'Yetkazildi', 'Bugun 16:00', Colors.grey),\n      ]));\n  }\n  static Widget _item(IconData icon, String title, String time, Color color) {\n    return Row(crossAxisAlignment: CrossAxisAlignment.start, children: [\n      Column(children: [\n        CircleAvatar(radius: 16, backgroundColor: color.withOpacity(0.2), child: Icon(icon, color: color, size: 16)),\n        Container(width: 2, height: 50, color: color.withOpacity(0.3)),\n      ]),\n      const SizedBox(width: 12),\n      Expanded(child: Padding(padding: const EdgeInsets.only(bottom: 16), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n        Text(title, style: const TextStyle(fontWeight: FontWeight.bold)),\n        Text(time, style: const TextStyle(color: Colors.grey, fontSize: 12)),\n      ]))),\n    ]);\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Timeline Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Medium\n**Teglar:** timeline, steps, history\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/timeline_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add timeline\nflutter pub add steps\nflutter pub add history\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "pricing-card",
    "n": "Pricing Card",
    "i": "💳",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "pricing",
      "plan",
      "subscription"
    ],
    "d": "Narx kartochkasi.",
    "f": [
      {
        "n": "pricing_card.dart",
        "p": "lib/widgets/pricing_card.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PricingCard extends StatelessWidget {\n  final String title, price, period;\n  final List<String> features;\n  final bool isPopular;\n  const PricingCard({super.key, required this.title, required this.price, required this.period, required this.features, this.isPopular = false});\n  @override\n  Widget build(BuildContext context) {\n    return Card(elevation: isPopular ? 8 : 2, child: Padding(padding: const EdgeInsets.all(24), child: Column(children: [\n      if (isPopular) Container(padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4), decoration: BoxDecoration(color: Colors.blue, borderRadius: BorderRadius.circular(20)), child: const Text('Mashhur', style: TextStyle(color: Colors.white, fontSize: 12))),\n      const SizedBox(height: 12),\n      Text(title, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n      Text(price + '/' + period, style: const TextStyle(fontSize: 32, fontWeight: FontWeight.bold)),\n      const Divider(),\n      ...features.map((f) => Padding(padding: const EdgeInsets.symmetric(vertical: 4), child: Row(children: [const Icon(Icons.check_circle, color: Colors.green, size: 18), const SizedBox(width: 8), Text(f)]))),\n      const SizedBox(height: 16),\n      SizedBox(width: double.infinity, child: FilledButton(onPressed: () {}, child: const Text('Tanlash'))),\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Pricing Card\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** pricing, plan, subscription\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/pricing_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add pricing\nflutter pub add plan\nflutter pub add subscription\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "tooltip-widget",
    "n": "Tooltip Widget",
    "i": "🏷️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "tooltip",
      "hint",
      "info"
    ],
    "d": "Maslahat tooltip.",
    "f": [
      {
        "n": "tooltip_page.dart",
        "p": "lib/pages/tooltip_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TooltipPage extends StatelessWidget {\n  const TooltipPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Tooltip')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Tooltip(message: 'Bu Home tugmasi', child: IconButton(icon: const Icon(Icons.home, size: 48), onPressed: () {})),\n        const SizedBox(height: 16),\n        Tooltip(message: 'Qidiruv', triggerMode: TooltipTriggerMode.tap, child: ElevatedButton.icon(onPressed: () {}, icon: const Icon(Icons.search), label: const Text('Qidirish'))),\n      ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Tooltip Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** tooltip, hint, info\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/tooltip_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add tooltip\nflutter pub add hint\nflutter pub add info\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "chip-widget",
    "n": "Chip Widget",
    "i": "🏷️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "chip",
      "tag",
      "filter"
    ],
    "d": "Chip widget.",
    "f": [
      {
        "n": "chip_page.dart",
        "p": "lib/pages/chip_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ChipPage extends StatefulWidget {\n  const ChipPage({super.key});\n  @override\n  State<ChipPage> createState() => _ChipPageState();\n}\n\nclass _ChipPageState extends State<ChipPage> {\n  bool _selected = false;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Chips')),\n      body: ListView(padding: const EdgeInsets.all(16), children: [\n        Wrap(spacing: 8, children: [\n          const Chip(label: Text('Flutter')),\n          const Chip(label: Text('Dart'), avatar: Icon(Icons.code)),\n          FilterChip(label: const Text('Filter'), selected: _selected, onSelected: (v) => setState(() => _selected = v)),\n          ChoiceChip(label: const Text('Tanlangan'), selected: true, onSelected: (_) {}),\n        ]),\n        const SizedBox(height: 16),\n        Wrap(spacing: 8, children: List.generate(10, (i) => ActionChip(label: Text('Tag ' + (i + 1).toString()), onPressed: () {}))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Chip Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** chip, tag, filter\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/chip_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add chip\nflutter pub add tag\nflutter pub add filter\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "lottie-page",
    "n": "Lottie Page",
    "i": "🎬",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "lottie",
      "json",
      "animation"
    ],
    "d": "Lottie animatsiya sahifasi.",
    "f": [
      {
        "n": "lottie_page.dart",
        "p": "lib/pages/lottie_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:lottie/lottie.dart';\n\nclass LottiePage extends StatelessWidget {\n  const LottiePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Lottie')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Lottie.asset('assets/animations/loading.json', width: 200, height: 200),\n        const SizedBox(height: 24),\n        const Text('Lottie animatsiya', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () {}, child: const Text('Yuklash')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Lottie Page\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** lottie, json, animation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/lottie_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add lottie\nflutter pub add json\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "splash-screen2",
    "n": "Animated Splash",
    "i": "💫",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "splash",
      "animation",
      "launch"
    ],
    "d": "Animatsiyali splash.",
    "f": [
      {
        "n": "splash.dart",
        "p": "lib/pages/splash.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SplashPage extends StatefulWidget {\n  const SplashPage({super.key});\n  @override\n  State<SplashPage> createState() => _SplashPageState();\n}\n\nclass _SplashPageState extends State<SplashPage> with TickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<double> _fade;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 2));\n    _fade = Tween<double>(begin: 0, end: 1).animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeIn));\n    _ctrl.forward();\n    Future.delayed(const Duration(seconds: 3), () {\n      if (mounted) Navigator.pushReplacementNamed(context, '/home');\n    });\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: Center(child: FadeTransition(opacity: _fade,\n      child: const Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        Icon(Icons.flutter_dash, size: 100, color: Colors.blue),\n        SizedBox(height: 20),\n        Text('APP_NAME', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold)),\n      ]))));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Animated Splash\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** splash, animation, launch\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/splash.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add launch\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "error-page",
    "n": "Error Page",
    "i": "⚠️",
    "diff": "easy",
    "c": "pages",
    "t": [
      "flutter",
      "error",
      "404",
      "notfound"
    ],
    "d": "Xatolik sahifasi.",
    "f": [
      {
        "n": "error_page.dart",
        "p": "lib/pages/error_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ErrorPage extends StatelessWidget {\n  final String? message;\n  const ErrorPage({super.key, this.message});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n      const Icon(Icons.error_outline, size: 100, color: Colors.red),\n      const SizedBox(height: 20),\n      Text(message ?? 'Xatolik yuz berdi', style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n      const SizedBox(height: 16),\n      FilledButton(onPressed: () => Navigator.pop(context), child: const Text('Orqaga qaytish')),\n    ])));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Error Page\n\n**Kategoriya:** Pages\n**Qiyinlik:** Easy\n**Teglar:** error, 404, notfound\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/error_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add error\nflutter pub add 404\nflutter pub add notfound\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "empty-state",
    "n": "Empty State",
    "i": "📭",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "empty",
      "placeholder",
      "nostate"
    ],
    "d": "Bo'sh holat widget.",
    "f": [
      {
        "n": "empty_state.dart",
        "p": "lib/widgets/empty_state.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass EmptyState extends StatelessWidget {\n  final IconData icon;\n  final String title, subtitle;\n  final String? actionText;\n  final VoidCallback? onAction;\n  const EmptyState({super.key, required this.icon, required this.title, required this.subtitle, this.actionText, this.onAction});\n  @override\n  Widget build(BuildContext context) {\n    return Center(child: Padding(padding: const EdgeInsets.all(32), child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n      Icon(icon, size: 80, color: Colors.grey[400]),\n      const SizedBox(height: 20),\n      Text(title, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n      const SizedBox(height: 8),\n      Text(subtitle, style: const TextStyle(color: Colors.grey), textAlign: TextAlign.center),\n      if (actionText != null) ...[\n        const SizedBox(height: 24),\n        FilledButton(onPressed: onAction, child: Text(actionText!)),\n      ],\n    ])));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Empty State\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** empty, placeholder, nostate\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/empty_state.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add empty\nflutter pub add placeholder\nflutter pub add nostate\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "search-widget",
    "n": "Search Widget",
    "i": "🔍",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "search",
      "bar",
      "input"
    ],
    "d": "Qidiruv widget.",
    "f": [
      {
        "n": "search_widget.dart",
        "p": "lib/widgets/search_widget.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SearchWidget extends StatelessWidget {\n  final String hint;\n  final ValueChanged<String>? onChanged;\n  final VoidCallback? onFilter;\n  const SearchWidget({super.key, this.hint = 'Qidirish...', this.onChanged, this.onFilter});\n  @override\n  Widget build(BuildContext context) {\n    return Container(padding: const EdgeInsets.symmetric(horizontal: 16),\n      decoration: BoxDecoration(color: Colors.grey[100], borderRadius: BorderRadius.circular(12)),\n      child: TextField(onChanged: onChanged, decoration: InputDecoration(\n        icon: const Icon(Icons.search), hintText: hint, border: InputBorder.none,\n        suffixIcon: onFilter != null ? IconButton(icon: const Icon(Icons.tune), onPressed: onFilter) : null)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Search Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** search, bar, input\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/search_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add search\nflutter pub add bar\nflutter pub add input\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "badge-widget",
    "n": "Badge Widget",
    "i": "🔴",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "badge",
      "notification",
      "count"
    ],
    "d": "Badge widget.",
    "f": [
      {
        "n": "badge_widget.dart",
        "p": "lib/widgets/badge_widget.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass BadgeWidget extends StatelessWidget {\n  final Widget child;\n  final int count;\n  const BadgeWidget({super.key, required this.child, this.count = 0});\n  @override\n  Widget build(BuildContext context) {\n    return Stack(children: [\n      child,\n      if (count > 0) Positioned(right: 0, top: 0,\n        child: Container(padding: const EdgeInsets.all(4),\n          decoration: const BoxDecoration(color: Colors.red, shape: BoxShape.circle),\n          child: Text(count > 99 ? '99+' : count.toString(),\n            style: const TextStyle(color: Colors.white, fontSize: 10, fontWeight: FontWeight.bold)))),\n    ]);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Badge Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** badge, notification, count\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/badge_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add badge\nflutter pub add notification\nflutter pub add count\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "avatar-group",
    "n": "Avatar Group",
    "i": "👥",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "avatar",
      "group",
      "stack"
    ],
    "d": "Avatar guruhi.",
    "f": [
      {
        "n": "avatar_group.dart",
        "p": "lib/widgets/avatar_group.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AvatarGroup extends StatelessWidget {\n  final int count;\n  final double size;\n  const AvatarGroup({super.key, this.count = 5, this.size = 36});\n  @override\n  Widget build(BuildContext context) {\n    return SizedBox(height: size, width: size * 2.5,\n      child: Stack(children: List.generate(count > 4 ? 4 : count, (i) => Positioned(\n        left: i * (size * 0.6), child: CircleAvatar(radius: size / 2,\n          backgroundColor: Colors.primaries[i],\n          child: i == 3 && count > 4 ? Text('+' + (count - 3).toString(), style: const TextStyle(fontSize: 10)) : null),\n      ))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Avatar Group\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** avatar, group, stack\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/avatar_group.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add group\nflutter pub add stack\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "expandable-card",
    "n": "Expandable Card",
    "i": "📂",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "expandable",
      "card",
      "toggle"
    ],
    "d": "Kengaytiriladigan karta.",
    "f": [
      {
        "n": "expandable_card.dart",
        "p": "lib/widgets/expandable_card.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ExpandableCard extends StatefulWidget {\n  final String title;\n  final Widget child;\n  const ExpandableCard({super.key, required this.title, required this.child});\n  @override\n  State<ExpandableCard> createState() => _ExpandableCardState();\n}\n\nclass _ExpandableCardState extends State<ExpandableCard> {\n  bool _expanded = false;\n  @override\n  Widget build(BuildContext context) {\n    return Card(child: Column(children: [\n      ListTile(title: Text(widget.title, style: const TextStyle(fontWeight: FontWeight.bold)),\n        trailing: Icon(_expanded ? Icons.expand_less : Icons.expand_more),\n        onTap: () => setState(() => _expanded = !_expanded)),\n      if (_expanded) Padding(padding: const EdgeInsets.all(16), child: widget.child),\n    ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Expandable Card\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** expandable, card, toggle\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/expandable_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add expandable\nflutter pub add card\nflutter pub add toggle\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "countdown",
    "n": "Countdown Timer",
    "i": "⏱️",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "countdown",
      "timer",
      "clock"
    ],
    "d": "Teskari sanash.",
    "f": [
      {
        "n": "countdown.dart",
        "p": "lib/widgets/countdown.dart",
        "c": "import 'dart:async';\nimport 'package:flutter/material.dart';\n\nclass CountdownTimer extends StatefulWidget {\n  final int seconds;\n  final VoidCallback? onComplete;\n  const CountdownTimer({super.key, required this.seconds, this.onComplete});\n  @override\n  State<CountdownTimer> createState() => _CountdownTimerState();\n}\n\nclass _CountdownTimerState extends State<CountdownTimer> {\n  late int _seconds;\n  Timer? _timer;\n  @override\n  void initState() {\n    super.initState();\n    _seconds = widget.seconds;\n    _timer = Timer.periodic(const Duration(seconds: 1), (t) {\n      if (_seconds > 0) setState(() => _seconds--);\n      else { t.cancel(); widget.onComplete?.call(); }\n    });\n  }\n  @override\n  void dispose() { _timer?.cancel(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    final m = (_seconds ~/ 60);\n    final s = (_seconds % 60);\n    return Text(m + ':' + s, style: const TextStyle(fontSize: 48, fontWeight: FontWeight.bold));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Countdown Timer\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** countdown, timer, clock\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/countdown.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add countdown\nflutter pub add timer\nflutter pub add clock\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "qr-scanner",
    "n": "QR Scanner",
    "i": "📷",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "qr",
      "scanner",
      "barcode"
    ],
    "d": "QR kod skaneri.",
    "f": [
      {
        "n": "qr_page.dart",
        "p": "lib/pages/qr_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:mobile_scanner/mobile_scanner.dart';\n\nclass QRScannerPage extends StatelessWidget {\n  const QRScannerPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('QR Scanner')),\n      body: MobileScanner(\n        onDetect: (barcode) {\n          if (barcode.rawValue != null) {\n            ScaffoldMessenger.of(context).showSnackBar(\n              SnackBar(content: Text(barcode.rawValue!)));\n          }\n        },\n      ));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## QR Scanner\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** qr, scanner, barcode\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/qr_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add qr\nflutter pub add scanner\nflutter pub add barcode\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "color-picker",
    "n": "Color Picker",
    "i": "🎨",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "color",
      "picker",
      "select"
    ],
    "d": "Rang tanlash.",
    "f": [
      {
        "n": "color_picker.dart",
        "p": "lib/widgets/color_picker.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ColorPickerWidget extends StatelessWidget {\n  final Color selected;\n  final ValueChanged<Color> onChanged;\n  final List<Color> colors;\n  const ColorPickerWidget({super.key, required this.selected, required this.onChanged,\n    this.colors = const [Colors.red, Colors.blue, Colors.green, Colors.purple, Colors.orange, Colors.pink]});\n  @override\n  Widget build(BuildContext context) {\n    return Wrap(spacing: 8, runSpacing: 8, children: colors.map((c) =>\n      GestureDetector(onTap: () => onChanged(c),\n        child: Container(width: 40, height: 40, decoration: BoxDecoration(\n          color: c, shape: BoxShape.circle,\n          border: selected == c ? Border.all(color: Colors.white, width: 3) : null,\n          boxShadow: selected == c ? [BoxShadow(color: c.withOpacity(0.5), blurRadius: 8)] : null))).\n    ).toList());\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Color Picker\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** color, picker, select\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/color_picker.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add color\nflutter pub add picker\nflutter pub add select\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "rating-dialog",
    "n": "Rating Dialog",
    "i": "⭐",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "rating",
      "dialog",
      "feedback"
    ],
    "d": "Baholash dialog.",
    "f": [
      {
        "n": "rating_dialog.dart",
        "p": "lib/widgets/rating_dialog.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass RatingDialog extends StatefulWidget {\n  final String title;\n  const RatingDialog({super.key, this.title = 'Baholang'});\n  @override\n  State<RatingDialog> createState() => _RatingDialogState();\n}\n\nclass _RatingDialogState extends State<RatingDialog> {\n  int _rating = 0;\n  @override\n  Widget build(BuildContext context) {\n    return AlertDialog(title: Text(widget.title),\n      content: Row(mainAxisAlignment: MainAxisAlignment.center, children: List.generate(5, (i) =>\n        GestureDetector(onTap: () => setState(() => _rating = i + 1),\n          child: Icon(i < _rating ? Icons.star : Icons.star_border,\n            color: Colors.amber, size: 40)))),\n      actions: [\n        TextButton(onPressed: () => Navigator.pop(context), child: const Text('Bekor qilish')),\n        FilledButton(onPressed: () { Navigator.pop(context, _rating); }, child: const Text('Yuborish')),\n      ]);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Rating Dialog\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** rating, dialog, feedback\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/rating_dialog.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add rating\nflutter pub add dialog\n# (kerakli paketni o'rnating)back\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "date-range",
    "n": "Date Range Picker",
    "i": "📅",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "date",
      "range",
      "picker"
    ],
    "d": "Sana oralig'i.",
    "f": [
      {
        "n": "date_range.dart",
        "p": "lib/widgets/date_range.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DateRangePickerWidget extends StatefulWidget {\n  const DateRangePickerWidget({super.key});\n  @override\n  State<DateRangePickerWidget> createState() => _DateRangePickerWidgetState();\n}\n\nclass _DateRangePickerWidgetState extends State<DateRangePickerWidget> {\n  DateTimeRange? _range;\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(title: const Text('Sana oralig'i'),\n      subtitle: Text(_range != null ? _range!.start.toString().split(' ')[0] + ' - ' + _range!.end.toString().split(' ')[0] : 'Tanlang'),\n      trailing: const Icon(Icons.date_range),\n      onTap: () async {\n        final r = await showDateRangePicker(context: context, firstDate: DateTime(2020), lastDate: DateTime(2030));\n        if (r != null) setState(() => _range = r);\n      });\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Date Range Picker\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** date, range, picker\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/date_range.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add date\nflutter pub add range\nflutter pub add picker\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "time-picker",
    "n": "Time Picker",
    "i": "🕐",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "time",
      "picker",
      "clock"
    ],
    "d": "Vaqt tanlash.",
    "f": [
      {
        "n": "time_picker.dart",
        "p": "lib/widgets/time_picker.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TimePickerWidget extends StatefulWidget {\n  const TimePickerWidget({super.key});\n  @override\n  State<TimePickerWidget> createState() => _TimePickerWidgetState();\n}\n\nclass _TimePickerWidgetState extends State<TimePickerWidget> {\n  TimeOfDay _time = TimeOfDay.now();\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(title: const Text('Vaqt'),\n      subtitle: Text(_time.format(context)),\n      trailing: const Icon(Icons.access_time),\n      onTap: () async {\n        final t = await showTimePicker(context: context, initialTime: _time);\n        if (t != null) setState(() => _time = t);\n      });\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Time Picker\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** time, picker, clock\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/time_picker.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add time\nflutter pub add picker\nflutter pub add clock\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "bottom-sheet",
    "n": "Bottom Sheet",
    "i": "📋",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "bottomsheet",
      "modal",
      "sheet"
    ],
    "d": "Bottom sheet.",
    "f": [
      {
        "n": "bottom_sheet.dart",
        "p": "lib/widgets/bottom_sheet.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CustomBottomSheet extends StatelessWidget {\n  final Widget child;\n  const CustomBottomSheet({super.key, required this.child});\n  @override\n  Widget build(BuildContext context) {\n    return Container(padding: const EdgeInsets.all(24), decoration: const BoxDecoration(\n      borderRadius: BorderRadius.vertical(top: Radius.circular(20))), child: child);\n  }\n}\n\nvoid showCustomBottomSheet(BuildContext context, Widget child) {\n  showModalBottomSheet(context: context, isScrollControlled: true,\n    shape: const RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(20))),\n    builder: (_) => DraggableScrollableSheet(initialChildSize: 0.5, minChildSize: 0.3, maxChildSize: 0.9,\n      expand: false, builder: (_, ctrl) => ListView(controller: ctrl, children: [\n        const SizedBox(height: 12),\n        Center(child: Container(width: 40, height: 4, decoration: BoxDecoration(color: Colors.grey[300], borderRadius: BorderRadius.circular(2)))),\n        const SizedBox(height: 16),\n        child,\n      ])));\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Bottom Sheet\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** bottomsheet, modal, sheet\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/bottom_sheet.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add bottomsheet\nflutter pub add modal\nflutter pub add sheet\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "alert-dialog",
    "n": "Alert Dialog",
    "i": "⚠️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "alert",
      "dialog",
      "confirm"
    ],
    "d": "Ogohlantirish dialog.",
    "f": [
      {
        "n": "alert_dialog.dart",
        "p": "lib/widgets/alert_dialog.dart",
        "c": "import 'package:flutter/material.dart';\n\nFuture<bool?> showConfirmDialog(BuildContext context, String title, String message) {\n  return showDialog<bool>(context: context, builder: (_) => AlertDialog(\n    title: Text(title), content: Text(message),\n    actions: [\n      TextButton(onPressed: () => Navigator.pop(context, false), child: const Text('Yo'q')),\n      FilledButton(onPressed: () => Navigator.pop(context, true), child: const Text('Ha')),\n    ]));\n}\n\nvoid showInfoDialog(BuildContext context, String title, String message) {\n  showDialog(context: context, builder: (_) => AlertDialog(\n    title: Text(title), content: Text(message),\n    actions: [FilledButton(onPressed: () => Navigator.pop(context), child: const Text('OK'))]));\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Alert Dialog\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** alert, dialog, confirm\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/alert_dialog.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add alert\nflutter pub add dialog\nflutter pub add confirm\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "snackbar",
    "n": "Custom Snackbar",
    "i": "📢",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "snackbar",
      "toast",
      "message"
    ],
    "d": "Snackbar xabar.",
    "f": [
      {
        "n": "snackbar.dart",
        "p": "lib/widgets/snackbar.dart",
        "c": "import 'package:flutter/material.dart';\n\nvoid showAppSnackBar(BuildContext context, String message, {bool isError = false}) {\n  ScaffoldMessenger.of(context).hideCurrentSnackBar();\n  ScaffoldMessenger.of(context).showSnackBar(SnackBar(\n    content: Row(children: [\n      Icon(isError ? Icons.error : Icons.check_circle, color: Colors.white, size: 20),\n      const SizedBox(width: 12),\n      Expanded(child: Text(message)),\n    ]),\n    backgroundColor: isError ? Colors.red : Colors.green,\n    behavior: SnackBarBehavior.floating,\n    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),\n    margin: const EdgeInsets.all(16),\n    duration: const Duration(seconds: 3),\n  ));\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Custom Snackbar\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** snackbar, toast, message\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/snackbar.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add snackbar\nflutter pub add toast\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-opacity",
    "n": "Animated Opacity",
    "i": "👁️",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "opacity",
      "fade",
      "animated"
    ],
    "d": "Shaffoflik animatsiyasi.",
    "f": [
      {
        "n": "opacity_page.dart",
        "p": "lib/pages/opacity_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass OpacityPage extends StatefulWidget {\n  const OpacityPage({super.key});\n  @override\n  State<OpacityPage> createState() => _OpacityPageState();\n}\n\nclass _OpacityPageState extends State<OpacityPage> {\n  bool _visible = true;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Opacity')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        AnimatedOpacity(opacity: _visible ? 1.0 : 0.0, duration: const Duration(milliseconds: 500),\n          child: const Icon(Icons.flutter_dash, size: 100, color: Colors.blue)),\n        const SizedBox(height: 32),\n        FilledButton(onPressed: () => setState(() => _visible = !_visible),\n          child: Text(_visible ? 'Yashirish' : 'Ko'rsatish')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animated Opacity\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** opacity, fade, animated\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/opacity_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add opacity\nflutter pub add fade\nflutter pub add animated\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "slide-transition",
    "n": "Slide Transition",
    "i": "➡️",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "slide",
      "transition",
      "page"
    ],
    "d": "Surgan o'tish.",
    "f": [
      {
        "n": "slide_page.dart",
        "p": "lib/pages/slide_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SlideTransitionPage extends StatelessWidget {\n  const SlideTransitionPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Slide')),\n      body: Center(child: FilledButton(onPressed: () {\n        Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) => SlideTransition(\n            position: Tween(begin: const Offset(1, 0), end: Offset.zero).animate(anim), child: child)));\n      }, child: const Text('Sahifaga o'tish'))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Slide Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** slide, transition, page\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/slide_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add slide\nflutter pub add transition\nflutter pub add page\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "fade-transition",
    "n": "Fade Transition",
    "i": "🫥",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "fade",
      "transition",
      "page"
    ],
    "d": "Yo'qolish o'tishi.",
    "f": [
      {
        "n": "fade_page.dart",
        "p": "lib/pages/fade_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FadeTransitionPage extends StatelessWidget {\n  const FadeTransitionPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Fade')),\n      body: Center(child: FilledButton(onPressed: () {\n        Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) =>\n            FadeTransition(opacity: anim, child: child)));\n      }, child: const Text('Sahifaga o'tish'))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Fade Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** fade, transition, page\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/fade_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add fade\nflutter pub add transition\nflutter pub add page\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "scale-transition",
    "n": "Scale Transition",
    "i": "🔎",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "scale",
      "transition",
      "page"
    ],
    "d": "Masshtab o'tishi.",
    "f": [
      {
        "n": "scale_page.dart",
        "p": "lib/pages/scale_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ScaleTransitionPage extends StatelessWidget {\n  const ScaleTransitionPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Scale')),\n      body: Center(child: FilledButton(onPressed: () {\n        Navigator.push(context, PageRouteBuilder(\n          pageBuilder: (_, __, ___) => const Scaffold(body: Center(child: Text('Yangi sahifa!'))),\n          transitionsBuilder: (_, anim, __, child) =>\n            ScaleTransition(scale: anim, child: child)));\n      }, child: const Text('Sahifaga o'tish'))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Scale Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** scale, transition, page\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/scale_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add scale\nflutter pub add transition\nflutter pub add page\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "size-transition",
    "n": "Size Transition",
    "i": "📐",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "size",
      "transition",
      "expand"
    ],
    "d": "O'lcham o'tishi.",
    "f": [
      {
        "n": "size_page.dart",
        "p": "lib/pages/size_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SizeTransitionPage extends StatefulWidget {\n  const SizeTransitionPage({super.key});\n  @override\n  State<SizeTransitionPage> createState() => _SizeTransitionPageState();\n}\n\nclass _SizeTransitionPageState extends State<SizeTransitionPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 500));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Size')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        SizeTransition(sizeFactor: _ctrl, child: Container(height: 200, color: Colors.blue.withOpacity(0.3),\n          child: const Center(child: Text('Kengaytirilgan kontent')))),\n        const SizedBox(height: 16),\n        FilledButton(onPressed: () => _ctrl.isCompleted ? _ctrl.reverse() : _ctrl.forward(),\n          child: const Text('Toggle')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Size Transition\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** size, transition, expand\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/size_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add size\nflutter pub add transition\nflutter pub add expand\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "positioned-animation",
    "n": "Positioned Animation",
    "i": "🎯",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "positioned",
      "animated",
      "move"
    ],
    "d": "Joylashuv animatsiyasi.",
    "f": [
      {
        "n": "positioned_page.dart",
        "p": "lib/pages/positioned_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PositionedPage extends StatefulWidget {\n  const PositionedPage({super.key});\n  @override\n  State<PositionedPage> createState() => _PositionedPageState();\n}\n\nclass _PositionedPageState extends State<PositionedPage> {\n  bool _top = true;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Positioned')),\n      body: Stack(children: [\n        AnimatedPositioned(duration: const Duration(milliseconds: 500), curve: Curves.easeInOut,\n          top: _top ? 50 : 300, left: _top ? 50 : 200,\n          child: GestureDetector(onTap: () => setState(() => _top = !_top),\n            child: Container(width: 80, height: 80, decoration: BoxDecoration(\n              color: Colors.blue, borderRadius: BorderRadius.circular(16)))),\n        ),\n      ]));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Positioned Animation\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** positioned, animated, move\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/positioned_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add positioned\nflutter pub add animated\nflutter pub add move\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-switcher",
    "n": "Animated Switcher",
    "i": "🔀",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "switcher",
      "animated",
      "change"
    ],
    "d": "Almashtirish animatsiyasi.",
    "f": [
      {
        "n": "switcher_page.dart",
        "p": "lib/pages/switcher_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SwitcherPage extends StatefulWidget {\n  const SwitcherPage({super.key});\n  @override\n  State<SwitcherPage> createState() => _SwitcherPageState();\n}\n\nclass _SwitcherPageState extends State<SwitcherPage> {\n  int _index = 0;\n  final _icons = [Icons.home, Icons.person, Icons.settings, Icons.star];\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Switcher')),\n      body: Center(child: AnimatedSwitcher(duration: const Duration(milliseconds: 300),\n        transitionBuilder: (child, anim) => ScaleTransition(scale: anim, child: child),\n        child: Icon(_icons[_index], key: ValueKey(_index), size: 100, color: Colors.blue))),\n      floatingActionButton: FloatingActionButton(onPressed: () =>\n        setState(() => _index = (_index + 1) % _icons.length), child: const Icon(Icons.swap_horiz)));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animated Switcher\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** switcher, animated, change\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/switcher_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)er\nflutter pub add animated\nflutter pub add change\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animation-controller",
    "n": "Animation Controller",
    "i": "🎮",
    "diff": "hard",
    "c": "animations",
    "t": [
      "flutter",
      "controller",
      "advanced",
      "custom"
    ],
    "d": "AnimationController misol.",
    "f": [
      {
        "n": "controller_page.dart",
        "p": "lib/pages/controller_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ControllerPage extends StatefulWidget {\n  const ControllerPage({super.key});\n  @override\n  State<ControllerPage> createState() => _ControllerPageState();\n}\n\nclass _ControllerPageState extends State<ControllerPage> with TickerProviderStateMixin {\n  late AnimationController _colorCtrl, _sizeCtrl, _rotateCtrl;\n  @override\n  void initState() {\n    super.initState();\n    _colorCtrl = AnimationController(vsync: this, duration: const Duration(seconds: 2))..repeat(reverse: true);\n    _sizeCtrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 800));\n    _rotateCtrl = AnimationController(vsync: this, duration: const Duration(seconds: 3))..repeat();\n  }\n  @override\n  void dispose() { _colorCtrl.dispose(); _sizeCtrl.dispose(); _rotateCtrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Controllers')),\n      body: Center(child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [\n        AnimatedBuilder(animation: _colorCtrl, builder: (_, __) => Container(\n          width: 100, height: 100, decoration: BoxDecoration(\n            color: Color.lerp(Colors.blue, Colors.red, _colorCtrl.value),\n            borderRadius: BorderRadius.circular(20)))),\n        const SizedBox(height: 32),\n        FilledButton(onPressed: () => _sizeCtrl.forward(from: 0), child: const Text('Size')),\n      ])));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animation Controller\n\n**Kategoriya:** Animations\n**Qiyinlik:** Hard\n**Teglar:** controller, advanced, custom\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/controller_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add controller\nflutter pub add advanced\nflutter pub add custom\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "form-multi-step",
    "n": "Multi-Step Form",
    "i": "📝",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "form",
      "multistep",
      "wizard"
    ],
    "d": "Ko'p qadamli forma.",
    "f": [
      {
        "n": "multi_step_form.dart",
        "p": "lib/pages/multi_step_form.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass MultiStepForm extends StatefulWidget {\n  const MultiStepForm({super.key});\n  @override\n  State<MultiStepForm> createState() => _MultiStepFormState();\n}\n\nclass _MultiStepFormState extends State<MultiStepForm> {\n  int _step = 0;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Form')),\n      body: Stepper(currentStep: _step, onStepContinue: () {\n        if (_step < 2) setState(() => _step++);\n      }, onStepCancel: () {\n        if (_step > 0) setState(() => _step--);\n      }, steps: const [\n        Step(title: Text('Ism'), content: TextField(decoration: InputDecoration(labelText: 'Ismingiz'))),\n        Step(title: Text('Email'), content: TextField(decoration: InputDecoration(labelText: 'Email'))),\n        Step(title: Text('Tasdiqlash'), content: Text('Yuborishga tayyor')),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Multi-Step Form\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** form, multistep, wizard\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/multi_step_form.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add multistep\nflutter pub add wizard\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "form-wizard",
    "n": "Form Wizard",
    "i": "🧙",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "wizard",
      "step",
      "form"
    ],
    "d": "Forma sehrli qiz.",
    "f": [
      {
        "n": "form_wizard.dart",
        "p": "lib/pages/form_wizard.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass FormWizard extends StatefulWidget {\n  const FormWizard({super.key});\n  @override\n  State<FormWizard> createState() => _FormWizardState();\n}\n\nclass _FormWizardState extends State<FormWizard> {\n  final _pageCtrl = PageController();\n  int _current = 0;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Wizard')),\n      body: Column(children: [\n        LinearProgressIndicator(value: (_current + 1) / 3),\n        Expanded(child: PageView(controller: _pageCtrl, physics: const NeverScrollableScrollPhysics(),\n          onPageChanged: (i) => setState(() => _current = i),\n          children: const [\n            Padding(padding: EdgeInsets.all(24), child: TextField(decoration: InputDecoration(labelText: 'Ism'))),\n            Padding(padding: EdgeInsets.all(24), child: TextField(decoration: InputDecoration(labelText: 'Email'))),\n            Padding(padding: EdgeInsets.all(24), child: Center(child: Text('Tayyor!'))),\n          ])),\n        Padding(padding: const EdgeInsets.all(16), child: Row(children: [\n          if (_current > 0) Expanded(child: OutlinedButton(onPressed: () => _pageCtrl.previousPage(duration: const Duration(milliseconds: 300), curve: Curves.ease), child: const Text('Orqaga'))),\n          if (_current > 0) const SizedBox(width: 16),\n          Expanded(child: FilledButton(onPressed: () {\n            if (_current < 2) _pageCtrl.nextPage(duration: const Duration(milliseconds: 300), curve: Curves.ease);\n          }, child: Text(_current < 2 ? 'Keyingi' : 'Yuborish'))),\n        ])),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Form Wizard\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** wizard, step, form\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/form_wizard.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add wizard\nflutter pub add step\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-list-tile",
    "n": "Animated ListTile",
    "i": "📱",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "animated",
      "listtile",
      "item"
    ],
    "d": "Animatsiyali satr.",
    "f": [
      {
        "n": "animated_list_tile.dart",
        "p": "lib/widgets/animated_list_tile.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AnimatedListTile extends StatefulWidget {\n  final IconData icon;\n  final String title, subtitle;\n  final VoidCallback? onTap;\n  const AnimatedListTile({super.key, required this.icon, required this.title, required this.subtitle, this.onTap});\n  @override\n  State<AnimatedListTile> createState() => _AnimatedListTileState();\n}\n\nclass _AnimatedListTileState extends State<AnimatedListTile> {\n  bool _hovered = false;\n  @override\n  Widget build(BuildContext context) {\n    return MouseRegion(onEnter: (_) => setState(() => _hovered = true),\n      onExit: (_) => setState(() => _hovered = false),\n      child: AnimatedContainer(duration: const Duration(milliseconds: 200),\n        decoration: BoxDecoration(color: _hovered ? Colors.blue.withOpacity(0.05) : Colors.transparent,\n          borderRadius: BorderRadius.circular(12)),\n        child: ListTile(leading: Icon(widget.icon), title: Text(widget.title),\n          subtitle: Text(widget.subtitle), trailing: const Icon(Icons.chevron_right),\n          onTap: widget.onTap)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Animated ListTile\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** animated, listtile, item\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/animated_list_tile.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add animated\n# (kerakli paketni o'rnating)tile\nflutter pub add item\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "flip-card",
    "n": "Flip Card",
    "i": "🃏",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "flip",
      "card",
      "rotate"
    ],
    "d": "Aylanuvchi karta.",
    "f": [
      {
        "n": "flip_card.dart",
        "p": "lib/widgets/flip_card.dart",
        "c": "import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass FlipCard extends StatefulWidget {\n  final Widget front, back;\n  const FlipCard({super.key, required this.front, required this.back});\n  @override\n  State<FlipCard> createState() => _FlipCardState();\n}\n\nclass _FlipCardState extends State<FlipCard> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  late Animation<double> _anim;\n  bool _showFront = true;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(milliseconds: 600));\n    _anim = Tween<double>(begin: 0, end: 1).animate(CurvedAnimation(parent: _ctrl, curve: Curves.easeInOut));\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return GestureDetector(onTap: () {\n      if (_showFront) _ctrl.forward(); else _ctrl.reverse();\n      _showFront = !_showFront;\n    },\n      child: AnimatedBuilder(animation: _anim, builder: (_, __) {\n        final angle = _anim.value * pi;\n        return Transform(transform: Matrix4.identity()..setEntry(3, 1, 0.001)..rotateY(angle),\n          alignment: Alignment.center,\n          child: angle > pi / 2 ? Transform(transform: Matrix4.identity()..rotateY(pi), child: widget.back) : widget.front);\n      }));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Flip Card\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** flip, card, rotate\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/flip_card.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add flip\nflutter pub add card\nflutter pub add rotate\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "marquee-text",
    "n": "Marquee Text",
    "i": "📰",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "marquee",
      "scroll",
      "text"
    ],
    "d": "Yuruvchi matn.",
    "f": [
      {
        "n": "marquee.dart",
        "p": "lib/widgets/marquee.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass MarqueeText extends StatefulWidget {\n  final String text;\n  final TextStyle? style;\n  const MarqueeText({super.key, required this.text, this.style});\n  @override\n  State<MarqueeText> createState() => _MarqueeTextState();\n}\n\nclass _MarqueeTextState extends State<MarqueeText> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 5))..repeat();\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return ClipRect(child: AnimatedBuilder(animation: _ctrl, builder: (_, child) => Transform.translate(\n      offset: Offset(-_ctrl.value * 200, 0), child: child),\n      child: Text(widget.text, style: widget.style ?? const TextStyle(fontSize: 16))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Marquee Text\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** marquee, scroll, text\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/marquee.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add marquee\nflutter pub add scroll\nflutter pub add text\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-counter",
    "n": "Animated Counter",
    "i": "🔢",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "counter",
      "number",
      "animated"
    ],
    "d": "Animatsiyali raqam.",
    "f": [
      {
        "n": "animated_counter.dart",
        "p": "lib/widgets/animated_counter.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AnimatedCounter extends StatelessWidget {\n  final int value;\n  final TextStyle? style;\n  const AnimatedCounter({super.key, required this.value, this.style});\n  @override\n  Widget build(BuildContext context) {\n    return AnimatedSwitcher(duration: const Duration(milliseconds: 500),\n      transitionBuilder: (child, anim) => ScaleTransition(scale: anim, child: child),\n      child: Text(value.toString(), key: ValueKey(value), style: style ?? const TextStyle(fontSize: 48, fontWeight: FontWeight.bold)));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animated Counter\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** counter, number, animated\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/animated_counter.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add counter\nflutter pub add number\nflutter pub add animated\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "loading-overlay",
    "n": "Loading Overlay",
    "i": "⏳",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "loading",
      "overlay",
      "spinner"
    ],
    "d": "Yuklanish qatlami.",
    "f": [
      {
        "n": "loading_overlay.dart",
        "p": "lib/widgets/loading_overlay.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass LoadingOverlay extends StatelessWidget {\n  final bool isLoading;\n  final Widget child;\n  const LoadingOverlay({super.key, required this.isLoading, required this.child});\n  @override\n  Widget build(BuildContext context) {\n    return Stack(children: [\n      child,\n      if (isLoading) Container(color: Colors.black38, child: const Center(child: CircularProgressIndicator())),\n    ]);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Loading Overlay\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** loading, overlay, spinner\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/loading_overlay.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add overlay\nflutter pub add spinner\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "network-image",
    "n": "Network Image Widget",
    "i": "🖼️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "network",
      "image",
      "cache"
    ],
    "d": "Tarmoq rasmi.",
    "f": [
      {
        "n": "network_image.dart",
        "p": "lib/widgets/network_image.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass NetworkImageWidget extends StatelessWidget {\n  final String url;\n  final double? width, height;\n  final BoxFit fit;\n  const NetworkImageWidget({super.key, required this.url, this.width, this.height, this.fit = BoxFit.cover});\n  @override\n  Widget build(BuildContext context) {\n    return ClipRRect(borderRadius: BorderRadius.circular(8),\n      child: Image.network(url, width: width, height: height, fit: fit,\n        loadingBuilder: (_, child, progress) => progress == null ? child :\n          const Center(child: CircularProgressIndicator()),\n        errorBuilder: (_, __, ___) => Container(color: Colors.grey[200],\n          child: const Center(child: Icon(Icons.broken_image, color: Colors.grey)))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Network Image Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** network, image, cache\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/network_image.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add network\n# (kerakli paketni o'rnating)\nflutter pub add cache\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "cached-network",
    "n": "Cached Network Image",
    "i": "💾",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "cached",
      "image",
      "offline"
    ],
    "d": "Keshlangan rasm.",
    "f": [
      {
        "n": "cached_image.dart",
        "p": "lib/widgets/cached_image.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:cached_network_image/cached_network_image.dart';\n\nclass CachedImageWidget extends StatelessWidget {\n  final String url;\n  final double? width, height;\n  const CachedImageWidget({super.key, required this.url, this.width, this.height});\n  @override\n  Widget build(BuildContext context) {\n    return ClipRRect(borderRadius: BorderRadius.circular(8),\n      child: CachedNetworkImage(\n        imageUrl: url, width: width, height: height, fit: BoxFit.cover,\n        placeholder: (_, __) => const Center(child: CircularProgressIndicator()),\n        errorWidget: (_, __, ___) => const Icon(Icons.error)));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Cached Network Image\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** cached, image, offline\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/cached_image.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add cached\n# (kerakli paketni o'rnating)\nflutter pub add offline\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "lifecycle-widget",
    "n": "Lifecycle Widget",
    "i": "🔄",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "lifecycle",
      "init",
      "dispose"
    ],
    "d": "Lifecycle boshqaruv.",
    "f": [
      {
        "n": "lifecycle_widget.dart",
        "p": "lib/widgets/lifecycle_widget.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass LifecycleWidget extends StatefulWidget {\n  final Widget child;\n  final VoidCallback? onInit, onDispose;\n  final VoidCallback? onResume, onPause;\n  const LifecycleWidget({super.key, required this.child, this.onInit, this.onDispose, this.onResume, this.onPause});\n  @override\n  State<LifecycleWidget> createState() => _LifecycleWidgetState();\n}\n\nclass _LifecycleWidgetState extends State<LifecycleWidget> with WidgetsBindingObserver {\n  @override\n  void initState() {\n    super.initState();\n    WidgetsBinding.instance.addObserver(this);\n    widget.onInit?.call();\n  }\n  @override\n  void dispose() {\n    WidgetsBinding.instance.removeObserver(this);\n    widget.onDispose?.call();\n    super.dispose();\n  }\n  @override\n  void didChangeAppLifecycleState(AppLifecycleState state) {\n    if (state == AppLifecycleState.resumed) widget.onResume?.call();\n    if (state == AppLifecycleState.paused) widget.onPause?.call();\n  }\n  @override\n  Widget build(BuildContext context) => widget.child;\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Lifecycle Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** lifecycle, init, dispose\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/lifecycle_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add lifecycle\nflutter pub add init\nflutter pub add dispose\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "orientation-builder",
    "n": "Orientation Builder",
    "i": "📱",
    "diff": "easy",
    "c": "starter",
    "t": [
      "flutter",
      "orientation",
      "builder",
      "responsive"
    ],
    "d": "Orientatsiya builder.",
    "f": [
      {
        "n": "orientation_builder.dart",
        "p": "lib/widgets/orientation_builder.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass OrientationBuilderWidget extends StatelessWidget {\n  final Widget Function(BuildContext, bool isPortrait) builder;\n  const OrientationBuilderWidget({super.key, required this.builder});\n  @override\n  Widget build(BuildContext context) {\n    return OrientationBuilder(builder: (context, orientation) {\n      final isPortrait = orientation == Orientation.portrait;\n      return builder(context, isPortrait);\n    });\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Orientation Builder\n\n**Kategoriya:** Starter\n**Qiyinlik:** Easy\n**Teglar:** orientation, builder, responsive\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/orientation_builder.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add orientation\nflutter pub add builder\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "keyboard-dismiss",
    "n": "Keyboard Dismiss",
    "i": "⌨️",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "keyboard",
      "dismiss",
      "tap"
    ],
    "d": "Klaviaturani yashirish.",
    "f": [
      {
        "n": "keyboard_dismiss.dart",
        "p": "lib/widgets/keyboard_dismiss.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass KeyboardDismiss extends StatelessWidget {\n  final Widget child;\n  const KeyboardDismiss({super.key, required this.child});\n  @override\n  Widget build(BuildContext context) {\n    return GestureDetector(onTap: () => FocusScope.of(context).unfocus(), child: child);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Keyboard Dismiss\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** keyboard, dismiss, tap\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/keyboard_dismiss.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add keyboard\nflutter pub add dismiss\nflutter pub add tap\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "scroll-to-hide",
    "n": "Scroll to Hide",
    "i": "↕️",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "scroll",
      "hide",
      "fab"
    ],
    "d": "Skrol bilan yashirish.",
    "f": [
      {
        "n": "scroll_hide.dart",
        "p": "lib/widgets/scroll_hide.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ScrollHideWidget extends StatefulWidget {\n  final Widget child;\n  final ScrollController controller;\n  const ScrollHideWidget({super.key, required this.child, required this.controller});\n  @override\n  State<ScrollHideWidget> createState() => _ScrollHideWidgetState();\n}\n\nclass _ScrollHideWidgetState extends State<ScrollHideWidget> {\n  bool _visible = true;\n  double _lastOffset = 0;\n  @override\n  void initState() {\n    super.initState();\n    widget.controller.addListener(() {\n      final offset = widget.controller.offset;\n      setState(() => _visible = offset <= _lastOffset || offset < 100);\n      _lastOffset = offset;\n    });\n  }\n  @override\n  Widget build(BuildContext context) {\n    return AnimatedSlide(offset: Offset(0, _visible ? 0 : 1), duration: const Duration(milliseconds: 300), child: widget.child);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Scroll to Hide\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** scroll, hide, fab\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/scroll_hide.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add scroll\nflutter pub add hide\nflutter pub add fab\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "slidable",
    "n": "Slidable Action",
    "i": "👈",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "slidable",
      "swipe",
      "action"
    ],
    "d": "Surish amali.",
    "f": [
      {
        "n": "slidable_page.dart",
        "p": "lib/pages/slidable_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_slidable/flutter_slidable.dart';\n\nclass SlidablePage extends StatelessWidget {\n  const SlidablePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Slidable')),\n      body: ListView.builder(itemCount: 10,\n        itemBuilder: (_, i) => Slidable(\n          endActionPane: ActionPane(motion: const BehindMotion(), children: [\n            SlidableAction(onPressed: (_) {}, backgroundColor: Colors.green, icon: Icons.edit, label: 'Tahrirlash'),\n            SlidableAction(onPressed: (_) {}, backgroundColor: Colors.red, icon: Icons.delete, label: 'O'chirish'),\n          ]),\n          child: ListTile(leading: CircleAvatar(child: Text((i + 1).toString())),\n            title: Text('Element ' + (i + 1).toString()), trailing: const Icon(Icons.chevron_right)))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Slidable Action\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** slidable, swipe, action\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/slidable_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add slidable\n# (kerakli paketni o'rnating)\nflutter pub add action\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "draggable",
    "n": "Draggable Widget",
    "i": "✋",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "draggable",
      "drag",
      "move"
    ],
    "d": "Suraladigan widget.",
    "f": [
      {
        "n": "draggable_page.dart",
        "p": "lib/pages/draggable_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DraggablePage extends StatelessWidget {\n  const DraggablePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Draggable')),\n      body: Stack(children: [\n        Center(child: DragTarget<String>(\n          onAcceptWithDetails: (d) => ScaffoldMessenger.of(context).showSnackBar(\n            SnackBar(content: Text('Qabul qilindi: ' + d.data))),\n          builder: (_, __, ___) => Container(width: 200, height: 200,\n            decoration: BoxDecoration(border: Border.all(color: Colors.blue, width: 2), borderRadius: BorderRadius.circular(16)),\n            child: const Center(child: Text('Shu yerga tashlang', style: TextStyle(color: Colors.blue))))),\n        ),\n        Positioned(top: 50, left: 50, child: Draggable<String>(data: 'Flutter',\n          feedback: Material(color: Colors.blue.withOpacity(0.8), borderRadius: BorderRadius.circular(12),\n            child: const Padding(padding: EdgeInsets.all(16), child: Text('Flutter', style: TextStyle(color: Colors.white, fontSize: 18)))),\n          childWhenDragging: Opacity(opacity: 0.3, child: Container(width: 80, height: 80, color: Colors.blue)),\n          child: Container(width: 80, height: 80, decoration: BoxDecoration(\n            color: Colors.blue, borderRadius: BorderRadius.circular(12)),\n            child: const Icon(Icons.drag_handle, color: Colors.white, size: 32)))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Draggable Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** draggable, drag, move\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/draggable_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add draggable\nflutter pub add drag\nflutter pub add move\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "reorderable-list",
    "n": "Reorderable List",
    "i": "🔃",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "reorder",
      "drag",
      "sort"
    ],
    "d": "Qayta tartiblash.",
    "f": [
      {
        "n": "reorder_page.dart",
        "p": "lib/pages/reorder_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ReorderPage extends StatefulWidget {\n  const ReorderPage({super.key});\n  @override\n  State<ReorderPage> createState() => _ReorderPageState();\n}\n\nclass _ReorderPageState extends State<ReorderPage> {\n  final List<String> _items = List.generate(10, (i) => 'Element ' + (i + 1).toString());\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Reorder')),\n      body: ReorderableListView.builder(itemCount: _items.length,\n        onReorder: (oldIndex, newIndex) {\n          if (newIndex > oldIndex) newIndex--;\n          final item = _items.removeAt(oldIndex);\n          _items.insert(newIndex, item);\n          setState(() {});\n        },\n        itemBuilder: (_, i) => ListTile(key: ValueKey(_items[i]),\n          leading: const Icon(Icons.drag_handle), title: Text(_items[i]))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Reorderable List\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** reorder, drag, sort\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/reorder_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add reorder\nflutter pub add drag\nflutter pub add sort\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "popup-menu",
    "n": "Popup Menu",
    "i": "📌",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "popup",
      "menu",
      "action"
    ],
    "d": "Popup menyu.",
    "f": [
      {
        "n": "popup_menu.dart",
        "p": "lib/widgets/popup_menu.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PopupMenuWidget extends StatelessWidget {\n  final List<Map<String, dynamic>> items;\n  final ValueChanged<String> onSelected;\n  const PopupMenuWidget({super.key, required this.items, required this.onSelected});\n  @override\n  Widget build(BuildContext context) {\n    return PopupMenuButton<String>(\n      onSelected: onSelected,\n      itemBuilder: (_) => items.map((item) => PopupMenuItem(\n        value: item['id'] as String,\n        child: Row(children: [Icon(item['icon'] as IconData, size: 20), const SizedBox(width: 8), Text(item['label'] as String)]))).toList());\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Popup Menu\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** popup, menu, action\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/popup_menu.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add popup\n# (kerakli paketni o'rnating)\nflutter pub add action\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "stepper",
    "n": "Stepper Widget",
    "i": "📊",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "stepper",
      "step",
      "wizard"
    ],
    "d": "Qadam ko'rsatkichi.",
    "f": [
      {
        "n": "stepper_page.dart",
        "p": "lib/pages/stepper_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass StepperPage extends StatefulWidget {\n  const StepperPage({super.key});\n  @override\n  State<StepperPage> createState() => _StepperPageState();\n}\n\nclass _StepperPageState extends State<StepperPage> {\n  int _current = 0;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Stepper')),\n      body: Stepper(currentStep: _current, onStepContinue: () {\n        if (_current < 3) setState(() => _current++);\n      }, onStepCancel: () {\n        if (_current > 0) setState(() => _current--);\n      }, steps: const [\n        Step(title: Text('Ro'yxatdan o'tish'), content: Text('Ma'lumotlarni kiriting')),\n        Step(title: Text('Tasdiqlash'), content: Text('Emailni tasdiqlang')),\n        Step(title: Text('Profil'), content: Text('Profil rasmini yuklang')),\n        Step(title: Text('Tayyor'), content: Text('Boshlashga tayyor!')),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Stepper Widget\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** stepper, step, wizard\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/stepper_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add stepper\nflutter pub add step\nflutter pub add wizard\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "expansion-tile",
    "n": "Expansion Tile",
    "i": "📂",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "expansion",
      "tile",
      "expand"
    ],
    "d": "Kengaytiriladigan satr.",
    "f": [
      {
        "n": "expansion_tile.dart",
        "p": "lib/pages/expansion_tile.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ExpansionTilePage extends StatelessWidget {\n  const ExpansionTilePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Expansion')),\n      body: ListView(children: [\n        ExpansionTile(title: const Text('Bosh sahifa'), leading: const Icon(Icons.home), children: [\n          ListTile(title: const Text('Dashboard'), onTap: () {}),\n          ListTile(title: const Text('Analytics'), onTap: () {}),\n        ]),\n        ExpansionTile(title: const Text('Sozlamalar'), leading: const Icon(Icons.settings), children: [\n          ListTile(title: const Text('Profil'), onTap: () {}),\n          ListTile(title: const Text('Maxfiylik'), onTap: () {}),\n        ]),\n        ExpansionTile(title: const Text('Yordam'), leading: const Icon(Icons.help), children: [\n          ListTile(title: const Text('FAQ'), onTap: () {}),\n          ListTile(title: const Text('Bog'lanish'), onTap: () {}),\n        ]),\n      ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Expansion Tile\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** expansion, tile, expand\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/expansion_tile.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add expansion\nflutter pub add tile\nflutter pub add expand\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "data-table",
    "n": "Data Table",
    "i": "📊",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "table",
      "data",
      "grid"
    ],
    "d": "Ma'lumotlar jadvali.",
    "f": [
      {
        "n": "data_table.dart",
        "p": "lib/pages/data_table.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass DataTablePage extends StatelessWidget {\n  const DataTablePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Jadval')),\n      body: SingleChildScrollView(scrollDirection: Axis.horizontal,\n        child: DataTable(columns: const [\n          DataColumn(label: Text('ID')),\n          DataColumn(label: Text('Ism')),\n          DataColumn(label: Text('Email'), numeric: true),\n          DataColumn(label: Text('Holat')),\n        ], rows: List.generate(20, (i) => DataRow(cells: [\n          DataCell(Text((i + 1).toString())),\n          DataCell(Text('Foydalanuvchi ' + (i + 1).toString())),\n          DataCell(Text('user' + (i + 1).toString() + '@email.com')),\n          DataCell(Chip(label: Text(i % 2 == 0 ? 'Faol' : 'Nofaol'),\n            backgroundColor: i % 2 == 0 ? Colors.green.withOpacity(0.1) : Colors.red.withOpacity(0.1))),\n        ])))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Data Table\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** table, data, grid\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/data_table.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add table\nflutter pub add data\nflutter pub add grid\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "list-tile-custom",
    "n": "Custom ListTile",
    "i": "📋",
    "diff": "easy",
    "c": "ui",
    "t": [
      "flutter",
      "listtile",
      "custom",
      "item"
    ],
    "d": "Maxsus ListTile.",
    "f": [
      {
        "n": "custom_list_tile.dart",
        "p": "lib/widgets/custom_list_tile.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CustomListTile extends StatelessWidget {\n  final IconData icon;\n  final String title, subtitle;\n  final Widget? trailing;\n  final VoidCallback? onTap;\n  final Color? iconColor;\n  const CustomListTile({super.key, required this.icon, required this.title, required this.subtitle, this.trailing, this.onTap, this.iconColor});\n  @override\n  Widget build(BuildContext context) {\n    return ListTile(\n      leading: CircleAvatar(backgroundColor: (iconColor ?? Colors.blue).withOpacity(0.1),\n        child: Icon(icon, color: iconColor ?? Colors.blue, size: 20)),\n      title: Text(title, style: const TextStyle(fontWeight: FontWeight.w500)),\n      subtitle: Text(subtitle, style: const TextStyle(fontSize: 12)),\n      trailing: trailing ?? const Icon(Icons.chevron_right, size: 20),\n      onTap: onTap);\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Custom ListTile\n\n**Kategoriya:** Ui\n**Qiyinlik:** Easy\n**Teglar:** listtile, custom, item\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/custom_list_tile.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)tile\nflutter pub add custom\nflutter pub add item\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "tab-indicator",
    "n": "Tab Indicator",
    "i": "📊",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "tab",
      "indicator",
      "custom"
    ],
    "d": "Maxsus tab ko'rsatkichi.",
    "f": [
      {
        "n": "tab_indicator.dart",
        "p": "lib/widgets/tab_indicator.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TabIndicatorWidget extends StatelessWidget {\n  final int selectedIndex;\n  final List<String> tabs;\n  final ValueChanged<int> onTap;\n  const TabIndicatorWidget({super.key, required this.selectedIndex, required this.tabs, required this.onTap});\n  @override\n  Widget build(BuildContext context) {\n    return Container(padding: const EdgeInsets.all(4), decoration: BoxDecoration(\n      color: Colors.grey[100], borderRadius: BorderRadius.circular(12)),\n      child: Row(children: List.generate(tabs.length, (i) => Expanded(\n        child: GestureDetector(onTap: () => onTap(i),\n          child: AnimatedContainer(duration: const Duration(milliseconds: 200), padding: const EdgeInsets.symmetric(vertical: 10),\n            decoration: BoxDecoration(color: selectedIndex == i ? Colors.white : Colors.transparent,\n              borderRadius: BorderRadius.circular(8),\n              boxShadow: selectedIndex == i ? [BoxShadow(color: Colors.black.withOpacity(0.1), blurRadius: 4)] : []),\n            child: Center(child: Text(tabs[i], style: TextStyle(\n              fontWeight: selectedIndex == i ? FontWeight.bold : FontWeight.normal,\n              color: selectedIndex == i ? Colors.blue : Colors.grey))))))));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Tab Indicator\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** tab, indicator, custom\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/tab_indicator.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add tab\nflutter pub add indicator\nflutter pub add custom\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "drag-to-reorder-grid",
    "n": "Drag Reorder Grid",
    "i": "🔲",
    "diff": "hard",
    "c": "ui",
    "t": [
      "flutter",
      "drag",
      "reorder",
      "grid"
    ],
    "d": "Grid qayta tartiblash.",
    "f": [
      {
        "n": "reorder_grid.dart",
        "p": "lib/pages/reorder_grid.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ReorderGridPage extends StatefulWidget {\n  const ReorderGridPage({super.key});\n  @override\n  State<ReorderGridPage> createState() => _ReorderGridPageState();\n}\n\nclass _ReorderGridPageState extends State<ReorderGridPage> {\n  final List<String> _items = List.generate(12, (i) => 'Item ' + (i + 1).toString());\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Reorder Grid')),\n      body: ReorderableGridView.count(crossAxisCount: 3, mainAxisSpacing: 8, crossAxisSpacing: 8, padding: const EdgeInsets.all(8),\n        onReorder: (oldIndex, newIndex) {\n          final item = _items.removeAt(oldIndex);\n          _items.insert(newIndex, item);\n          setState(() {});\n        },\n        children: _items.map((item) => Card(key: ValueKey(item),\n          child: Center(child: Text(item, style: const TextStyle(fontWeight: FontWeight.bold))))).toList()));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Drag Reorder Grid\n\n**Kategoriya:** Ui\n**Qiyinlik:** Hard\n**Teglar:** drag, reorder, grid\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/reorder_grid.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add drag\nflutter pub add reorder\nflutter pub add grid\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "sliver-header",
    "n": "Sliver Header",
    "i": "📌",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "sliver",
      "header",
      "sticky"
    ],
    "d": "Sliver sarlavha.",
    "f": [
      {
        "n": "sliver_header.dart",
        "p": "lib/pages/sliver_header.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SliverHeaderPage extends StatelessWidget {\n  const SliverHeaderPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(body: CustomScrollView(slivers: [\n      const SliverAppBar(expandedHeight: 200, pinned: true, flexibleSpace: FlexibleSpaceBar(\n        title: Text('Sliver Header'),\n        background: FlutterLogo())),\n      const SliverToBoxAdapter(child: SizedBox(height: 16)),\n      SliverList(delegate: SliverChildBuilderDelegate((_, i) => ListTile(\n        leading: CircleAvatar(child: Text((i + 1).toString())),\n        title: Text('Element ' + (i + 1).toString())),\n        childCount: 50)),\n    ]));\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Sliver Header\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** sliver, header, sticky\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/sliver_header.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add sliver\nflutter pub add header\nflutter pub add sticky\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "persistent-header",
    "n": "Persistent Header",
    "i": "📌",
    "diff": "hard",
    "c": "ui",
    "t": [
      "flutter",
      "persistent",
      "header",
      "collapse"
    ],
    "d": "Doimiy sarlavha.",
    "f": [
      {
        "n": "persistent_header.dart",
        "p": "lib/widgets/persistent_header.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PersistentHeader extends SliverPersistentHeaderDelegate {\n  final String title;\n  PersistentHeader({required this.title});\n  @override\n  double get minExtent => 48;\n  @override\n  double get maxExtent => 48;\n  @override\n  Widget build(BuildContext context, double shrinkOffset, bool overlapsContent) {\n    return Container(color: Theme.of(context).scaffoldBackgroundColor,\n      padding: const EdgeInsets.symmetric(horizontal: 16),\n      alignment: Alignment.centerLeft,\n      child: Text(title, style: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold)));\n  }\n  @override\n  bool shouldRebuild(covariant SliverPersistentHeaderDelegate oldDelegate) => false;\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Persistent Header\n\n**Kategoriya:** Ui\n**Qiyinlik:** Hard\n**Teglar:** persistent, header, collapse\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/widgets/persistent_header.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add persistent\nflutter pub add header\nflutter pub add collapse\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "sticky-header-list",
    "n": "Sticky Header List",
    "i": "📋",
    "diff": "medium",
    "c": "ui",
    "t": [
      "flutter",
      "sticky",
      "header",
      "list"
    ],
    "d": "Doimiy sarlavhali ro'yxat.",
    "f": [
      {
        "n": "sticky_list.dart",
        "p": "lib/pages/sticky_list.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass StickyHeaderList extends StatelessWidget {\n  const StickyHeaderList({super.key});\n  @override\n  Widget build(BuildContext context) {\n    final letters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');\n    return CustomScrollView(slivers: letters.map((letter) => SliverMainAxisGroup(slivers: [\n      SliverPersistentHeader(delegate: _HeaderDelegate(letter), pinned: true),\n      SliverList(delegate: SliverChildBuilderDelegate((_, i) =>\n        ListTile(title: Text(letter + (i + 1).toString())),\n        childCount: 5)),\n    ])).toList());\n  }\n}\n\nclass _HeaderDelegate extends SliverPersistentHeaderDelegate {\n  final String title;\n  _HeaderDelegate(this.title);\n  @override double get minExtent => 40;\n  @override double get maxExtent => 40;\n  @override\n  Widget build(BuildContext context, double s, bool o) => Container(\n    color: Colors.blue, alignment: Alignment.centerLeft,\n    padding: const EdgeInsets.only(left: 16),\n    child: Text(title, style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold)));\n  @override bool shouldRebuild(covariant SliverPersistentHeaderDelegate o) => false;\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Sticky Header List\n\n**Kategoriya:** Ui\n**Qiyinlik:** Medium\n**Teglar:** sticky, header, list\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/sticky_list.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add sticky\nflutter pub add header\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-container-page",
    "n": "Animated Container",
    "i": "📦",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "animated",
      "container",
      "size"
    ],
    "d": "Animatsiyali konteyner.",
    "f": [
      {
        "n": "animated_container.dart",
        "p": "lib/pages/animated_container.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AnimatedContainerPage extends StatefulWidget {\n  const AnimatedContainerPage({super.key});\n  @override\n  State<AnimatedContainerPage> createState() => _AnimatedContainerPageState();\n}\n\nclass _AnimatedContainerPageState extends State<AnimatedContainerPage> {\n  bool _expanded = false;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('AnimatedContainer')),\n      body: Center(child: GestureDetector(onTap: () => setState(() => _expanded = !_expanded),\n        child: AnimatedContainer(duration: const Duration(milliseconds: 400), curve: Curves.easeInOut,\n          width: _expanded ? 300 : 100, height: _expanded ? 300 : 100,\n          decoration: BoxDecoration(color: _expanded ? Colors.blue : Colors.red,\n            borderRadius: BorderRadius.circular(_expanded ? 100 : 16),\n            boxShadow: [BoxShadow(color: (_expanded ? Colors.blue : Colors.red).withOpacity(0.3), blurRadius: 20, spreadRadius: 5)]),\n          child: const Icon(Icons.touch_app, color: Colors.white, size: 40)))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Animated Container\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** animated, container, size\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/animated_container.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add animated\nflutter pub add container\nflutter pub add size\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-physical-model",
    "n": "Physical Model",
    "i": "🧊",
    "diff": "easy",
    "c": "animations",
    "t": [
      "flutter",
      "physical",
      "model",
      "elevation"
    ],
    "d": "Fizik model animatsiyasi.",
    "f": [
      {
        "n": "physical_model.dart",
        "p": "lib/pages/physical_model.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass PhysicalModelPage extends StatefulWidget {\n  const PhysicalModelPage({super.key});\n  @override\n  State<PhysicalModelPage> createState() => _PhysicalModelPageState();\n}\n\nclass _PhysicalModelPageState extends State<PhysicalModelPage> {\n  bool _raised = false;\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('Physical Model')),\n      body: Center(child: GestureDetector(onTap: () => setState(() => _raised = !_raised),\n        child: AnimatedPhysicalModel(duration: const Duration(milliseconds: 400),\n          elevation: _raised ? 20 : 0, shape: BoxShape.circle,\n          color: _raised ? Colors.blue : Colors.grey[300]!, shadowColor: Colors.blue,\n          child: const SizedBox(width: 120, height: 120,\n            child: Center(child: Icon(Icons.touch_app, size: 40, color: Colors.white))))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Physical Model\n\n**Kategoriya:** Animations\n**Qiyinlik:** Easy\n**Teglar:** physical, model, elevation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/physical_model.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add physical\nflutter pub add model\nflutter pub add elevation\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "hero-detail",
    "n": "Hero Detail Page",
    "i": "🦸",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "hero",
      "detail",
      "page"
    ],
    "d": "Hero tafsil sahifasi.",
    "f": [
      {
        "n": "hero_detail.dart",
        "p": "lib/pages/hero_detail.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass HeroDetailPage extends StatelessWidget {\n  final String tag, title;\n  final Color color;\n  const HeroDetailPage({super.key, required this.tag, required this.title, required this.color});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: CustomScrollView(slivers: [\n        SliverAppBar(expandedHeight: 250, pinned: true, flexibleSpace: FlexibleSpaceBar(\n          title: Text(title),\n          background: Hero(tag: tag,\n            child: Container(decoration: BoxDecoration(gradient: LinearGradient(\n              colors: [color, color.withOpacity(0.7)])))),)),\n        SliverToBoxAdapter(child: Padding(padding: const EdgeInsets.all(16), child: Column(\n          crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(title, style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n          const SizedBox(height: 16),\n          const Text('Bu yerda to'liq ma'lumot bor. Flutter yordamida yaratilgan sahifa.'),\n        ]))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Hero Detail Page\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** hero, detail, page\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/hero_detail.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add hero\nflutter pub add detail\nflutter pub add page\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animated-builder",
    "n": "AnimatedBuilder",
    "i": "🎮",
    "diff": "medium",
    "c": "animations",
    "t": [
      "flutter",
      "builder",
      "animated",
      "rebuild"
    ],
    "d": "AnimatedBuilder misol.",
    "f": [
      {
        "n": "animated_builder.dart",
        "p": "lib/pages/animated_builder.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AnimatedBuilderPage extends StatefulWidget {\n  const AnimatedBuilderPage({super.key});\n  @override\n  State<AnimatedBuilderPage> createState() => _AnimatedBuilderPageState();\n}\n\nclass _AnimatedBuilderPageState extends State<AnimatedBuilderPage> with SingleTickerProviderStateMixin {\n  late AnimationController _ctrl;\n  @override\n  void initState() {\n    super.initState();\n    _ctrl = AnimationController(vsync: this, duration: const Duration(seconds: 2))..repeat();\n  }\n  @override\n  void dispose() { _ctrl.dispose(); super.dispose(); }\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(appBar: AppBar(title: const Text('AnimatedBuilder')),\n      body: Center(child: AnimatedBuilder(animation: _ctrl, builder: (_, child) {\n        return Transform.rotate(angle: _ctrl.value * 2 * 3.14159,\n          child: Container(width: 100, height: 100,\n            decoration: BoxDecoration(color: Colors.blue,\n              borderRadius: BorderRadius.circular(20)),\n            child: child));\n      }, child: const Icon(Icons.refresh, color: Colors.white, size: 40))));\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## AnimatedBuilder\n\n**Kategoriya:** Animations\n**Qiyinlik:** Medium\n**Teglar:** builder, animated, rebuild\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/animations\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/animated_builder.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add builder\nflutter pub add animated\nflutter pub add rebuild\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "clean-architecture",
    "n": "Clean Architecture",
    "i": "🏗️",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "clean-architecture",
      "domain",
      "data",
      "presentation"
    ],
    "d": "Clean Architecture pattern with domain, data, and presentation layers. Scalable and testable.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:provider/provider.dart';\nimport 'injection.dart' as di;\nimport 'core/theme/app_theme.dart';\nimport 'features/auth/presentation/providers/auth_provider.dart';\nimport 'features/auth/presentation/pages/login_page.dart';\n\nvoid main() async {\n  WidgetsFlutterBinding.ensureInitialized();\n  await di.init();\n  runApp(const MyApp());\n}\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MultiProvider(\n      providers: [\n        ChangeNotifierProvider(create: (_) => di.sl<AuthProvider>()),\n      ],\n      child: MaterialApp(\n        title: 'Clean Architecture App',\n        debugShowCheckedModeBanner: false,\n        theme: AppTheme.light,\n        darkTheme: AppTheme.dark,\n        themeMode: ThemeMode.system,\n        home: const LoginPage(),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "injection.dart",
        "p": "lib/injection.dart",
        "c": "import 'package:get_it/get_it.dart';\nimport 'package:dio/dio.dart';\nimport 'features/auth/data/repositories/auth_repository_impl.dart';\nimport 'features/auth/domain/repositories/auth_repository.dart';\nimport 'features/auth/domain/usecases/login_usecase.dart';\nimport 'features/auth/presentation/providers/auth_provider.dart';\nimport 'core/network/dio_client.dart';\n\nfinal sl = GetIt.instance;\n\nFuture<void> init() async {\n  sl.registerLazySingleton(() => DioClient());\n  sl.registerLazySingleton<AuthRepository>(\n    () => AuthRepositoryImpl(dioClient: sl()),\n  );\n  sl.registerLazySingleton(() => LoginUseCase(repository: sl()));\n  sl.registerFactory(() => AuthProvider(loginUseCase: sl()));\n}"
      },
      {
        "n": "dio_client.dart",
        "p": "lib/core/network/dio_client.dart",
        "c": "import 'package:dio/dio.dart';\n\nclass DioClient {\n  late final Dio _dio;\n\n  DioClient() {\n    _dio = Dio(BaseOptions(\n      baseUrl: 'https://api.example.com/v1',\n      connectTimeout: const Duration(seconds: 30),\n      receiveTimeout: const Duration(seconds: 30),\n      headers: {\n        'Content-Type': 'application/json',\n        'Accept': 'application/json',\n      },\n    ));\n\n    _dio.interceptors.addAll([\n      LogInterceptor(requestBody: true, responseBody: true),\n      InterceptorsWrapper(\n        onRequest: (options, handler) {\n          handler.next(options);\n        },\n        onError: (error, handler) {\n          handler.next(error);\n        },\n      ),\n    ]);\n  }\n\n  Dio get client => _dio;\n}"
      },
      {
        "n": "auth_repository.dart",
        "p": "lib/features/auth/domain/repositories/auth_repository.dart",
        "c": "import 'package:dartz/dartz.dart';\nimport '../entities/user_entity.dart';\nimport '../../../../core/error/failures.dart';\n\nabstract class AuthRepository {\n  Future<Either<Failure, UserEntity>> login(String email, String password);\n  Future<Either<Failure, UserEntity>> register(String name, String email, String password);\n  Future<Either<Failure, void>> logout();\n  Future<Either<Failure, UserEntity>> getCurrentUser();\n}"
      },
      {
        "n": "user_entity.dart",
        "p": "lib/features/auth/domain/entities/user_entity.dart",
        "c": "class UserEntity {\n  final String id;\n  final String name;\n  final String email;\n  final String? avatarUrl;\n  final DateTime createdAt;\n\n  const UserEntity({\n    required this.id,\n    required this.name,\n    required this.email,\n    this.avatarUrl,\n    required this.createdAt,\n  });\n\n  @override\n  bool operator ==(Object other) =>\n      identical(this, other) ||\n      other is UserEntity &&\n          runtimeType == other.runtimeType &&\n          id == other.id;\n\n  @override\n  int get hashCode => id.hashCode;\n}"
      },
      {
        "n": "login_usecase.dart",
        "p": "lib/features/auth/domain/usecases/login_usecase.dart",
        "c": "import 'package:dartz/dartz.dart';\nimport '../repositories/auth_repository.dart';\nimport '../entities/user_entity.dart';\nimport '../../../../core/error/failures.dart';\nimport '../../../../core/usecases/usecase.dart';\n\nclass LoginUseCase extends UseCase<UserEntity, LoginParams> {\n  final AuthRepository repository;\n\n  LoginUseCase({required this.repository});\n\n  @override\n  Future<Either<Failure, UserEntity>> call(LoginParams params) {\n    return repository.login(params.email, params.password);\n  }\n}\n\nclass LoginParams {\n  final String email;\n  final String password;\n\n  const LoginParams({required this.email, required this.password});\n}"
      },
      {
        "n": "auth_repository_impl.dart",
        "p": "lib/features/auth/data/repositories/auth_repository_impl.dart",
        "c": "import 'package:dartz/dartz.dart';\nimport 'package:dio/dio.dart';\nimport '../../domain/repositories/auth_repository.dart';\nimport '../../domain/entities/user_entity.dart';\nimport '../../../../core/error/failures.dart';\nimport '../../../../core/network/dio_client.dart';\n\nclass AuthRepositoryImpl implements AuthRepository {\n  final DioClient dioClient;\n\n  AuthRepositoryImpl({required this.dioClient});\n\n  @override\n  Future<Either<Failure, UserEntity>> login(String email, String password) async {\n    try {\n      final response = await dioClient.client.post(\n        '/auth/login',\n        data: {'email': email, 'password': password},\n      );\n      return Right(UserEntity.fromJson(response.data['user']));\n    } on DioException catch (e) {\n      return Left(ServerFailure(message: e.message ?? 'Server error'));\n    }\n  }\n\n  @override\n  Future<Either<Failure, UserEntity>> register(String name, String email, String password) async {\n    try {\n      final response = await dioClient.client.post(\n        '/auth/register',\n        data: {'name': name, 'email': email, 'password': password},\n      );\n      return Right(UserEntity.fromJson(response.data['user']));\n    } on DioException catch (e) {\n      return Left(ServerFailure(message: e.message ?? 'Server error'));\n    }\n  }\n\n  @override\n  Future<Either<Failure, void>> logout() async {\n    try {\n      await dioClient.client.post('/auth/logout');\n      return const Right(null);\n    } on DioException catch (e) {\n      return Left(ServerFailure(message: e.message ?? 'Server error'));\n    }\n  }\n\n  @override\n  Future<Either<Failure, UserEntity>> getCurrentUser() async {\n    try {\n      final response = await dioClient.client.get('/auth/me');\n      return Right(UserEntity.fromJson(response.data['user']));\n    } on DioException catch (e) {\n      return Left(ServerFailure(message: e.message ?? 'Server error'));\n    }\n  }\n}"
      },
      {
        "n": "usecase.dart",
        "p": "lib/core/usecases/usecase.dart",
        "c": "import 'package:dartz/dartz.dart';\nimport '../error/failures.dart';\n\nabstract class UseCase<Type, Params> {\n  Future<Either<Failure, Type>> call(Params params);\n}\n\nclass NoParams {\n  const NoParams();\n}"
      },
      {
        "n": "failures.dart",
        "p": "lib/core/error/failures.dart",
        "c": "abstract class Failure {\n  final String message;\n  const Failure({required this.message});\n}\n\nclass ServerFailure extends Failure {\n  const ServerFailure({required super.message});\n}\n\nclass CacheFailure extends Failure {\n  const CacheFailure({required super.message});\n}\n\nclass NetworkFailure extends Failure {\n  const NetworkFailure({required super.message});\n}"
      },
      {
        "n": "app_theme.dart",
        "p": "lib/core/theme/app_theme.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AppTheme {\n  static final light = ThemeData(\n    useMaterial3: true,\n    brightness: Brightness.light,\n    colorSchemeSeed: Colors.blue,\n    fontFamily: 'Inter',\n  );\n\n  static final dark = ThemeData(\n    useMaterial3: true,\n    brightness: Brightness.dark,\n    colorSchemeSeed: Colors.blue,\n    fontFamily: 'Inter',\n  );\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Clean Architecture\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** clean-architecture, domain, data, presentation\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/injection.dart`\n- `lib/core/network/dio_client.dart`\n- `lib/features/auth/domain/repositories/auth_repository.dart`\n- `lib/features/auth/domain/entities/user_entity.dart`\n- `lib/features/auth/domain/usecases/login_usecase.dart`\n- `lib/features/auth/data/repositories/auth_repository_impl.dart`\n- `lib/core/usecases/usecase.dart`\n- `lib/core/error/failures.dart`\n- `lib/core/theme/app_theme.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add clean-architecture\nflutter pub add domain\nflutter pub add data\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "bloc-advanced",
    "n": "Advanced BLoC Pattern",
    "i": "⚡",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "bloc",
      "cubit",
      "state-management",
      "events"
    ],
    "d": "Advanced BLoC pattern with events, states, cubits, and complex business logic.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_bloc/flutter_bloc.dart';\nimport 'features/counter/presentation/bloc/counter_bloc.dart';\nimport 'features/counter/presentation/pages/counter_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Advanced BLoC',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        colorSchemeSeed: Colors.indigo,\n        useMaterial3: true,\n      ),\n      home: BlocProvider(\n        create: (_) => CounterBloc()..add(CounterInitialized()),\n        child: const CounterPage(),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "counter_bloc.dart",
        "p": "lib/features/counter/presentation/bloc/counter_bloc.dart",
        "c": "import 'package:flutter_bloc/flutter_bloc.dart';\nimport 'counter_event.dart';\nimport 'counter_state.dart';\n\nclass CounterBloc extends Bloc<CounterEvent, CounterState> {\n  CounterBloc() : super(const CounterState()) {\n    on<CounterInitialized>(_onInitialized);\n    on<CounterIncremented>(_onIncremented);\n    on<CounterDecremented>(_onDecremented);\n    on<CounterMultiplied>(_onMultiplied);\n    on<CounterReset>(_onReset);\n    on<CounterAsyncOperation>(_onAsyncOperation);\n  }\n\n  Future<void> _onInitialized(CounterInitialized event, Emitter<CounterState> emit) async {\n    emit(state.copyWith(status: CounterStatus.loading));\n    await Future.delayed(const Duration(milliseconds: 500));\n    emit(state.copyWith(status: CounterStatus.success, value: 0));\n  }\n\n  void _onIncremented(CounterIncremented event, Emitter<CounterState> emit) {\n    emit(state.copyWith(value: state.value + 1, status: CounterStatus.success));\n  }\n\n  void _onDecremented(CounterDecremented event, Emitter<CounterState> emit) {\n    emit(state.copyWith(value: state.value - 1, status: CounterStatus.success));\n  }\n\n  void _onMultiplied(CounterMultiplied event, Emitter<CounterState> emit) {\n    emit(state.copyWith(value: state.value * event.factor, status: CounterStatus.success));\n  }\n\n  void _onReset(CounterReset event, Emitter<CounterState> emit) {\n    emit(const CounterState(status: CounterStatus.success));\n  }\n\n  Future<void> _onAsyncOperation(CounterAsyncOperation event, Emitter<CounterState> emit) async {\n    emit(state.copyWith(status: CounterStatus.loading));\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      final result = state.value * state.value;\n      emit(state.copyWith(value: result, status: CounterStatus.success));\n    } catch (e) {\n      emit(state.copyWith(status: CounterStatus.failure, errorMessage: e.toString()));\n    }\n  }\n}"
      },
      {
        "n": "counter_event.dart",
        "p": "lib/features/counter/presentation/bloc/counter_event.dart",
        "c": "sealed class CounterEvent {\n  const CounterEvent();\n}\n\nclass CounterInitialized extends CounterEvent {\n  const CounterInitialized();\n}\n\nclass CounterIncremented extends CounterEvent {\n  const CounterIncremented();\n}\n\nclass CounterDecremented extends CounterEvent {\n  const CounterDecremented();\n}\n\nclass CounterMultiplied extends CounterEvent {\n  final int factor;\n  const CounterMultiplied({required this.factor});\n}\n\nclass CounterReset extends CounterEvent {\n  const CounterReset();\n}\n\nclass CounterAsyncOperation extends CounterEvent {\n  const CounterAsyncOperation();\n}"
      },
      {
        "n": "counter_state.dart",
        "p": "lib/features/counter/presentation/bloc/counter_state.dart",
        "c": "enum CounterStatus { initial, loading, success, failure }\n\nclass CounterState {\n  final int value;\n  final CounterStatus status;\n  final String? errorMessage;\n\n  const CounterState({\n    this.value = 0,\n    this.status = CounterStatus.initial,\n    this.errorMessage,\n  });\n\n  CounterState copyWith({\n    int? value,\n    CounterStatus? status,\n    String? errorMessage,\n  }) {\n    return CounterState(\n      value: value ?? this.value,\n      status: status ?? this.status,\n      errorMessage: errorMessage ?? this.errorMessage,\n    );\n  }\n\n  bool get isLoading => status == CounterStatus.loading;\n  bool get isSuccess => status == CounterStatus.success;\n  bool get isFailure => status == CounterStatus.failure;\n}"
      },
      {
        "n": "counter_page.dart",
        "p": "lib/features/counter/presentation/pages/counter_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_bloc/flutter_bloc.dart';\nimport '../bloc/counter_bloc.dart';\nimport '../bloc/counter_event.dart';\nimport '../bloc/counter_state.dart';\n\nclass CounterPage extends StatelessWidget {\n  const CounterPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('Advanced BLoC Counter'),\n        centerTitle: true,\n      ),\n      body: BlocBuilder<CounterBloc, CounterState>(\n        builder: (context, state) {\n          if (state.isLoading) {\n            return const Center(child: CircularProgressIndicator());\n          }\n          if (state.isFailure) {\n            return Center(child: Text('Error: \\${state.errorMessage}'));\n          }\n          return Center(\n            child: Column(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                Text(\n                  '\\${state.value}',\n                  style: Theme.of(context).textTheme.displayLarge,\n                ),\n                const SizedBox(height: 32),\n                Row(\n                  mainAxisAlignment: MainAxisAlignment.center,\n                  children: [\n                    FilledButton.tonal(\n                      onPressed: () => context.read<CounterBloc>().add(const CounterDecremented()),\n                      child: const Icon(Icons.remove),\n                    ),\n                    const SizedBox(width: 16),\n                    FilledButton(\n                      onPressed: () => context.read<CounterBloc>().add(const CounterIncremented()),\n                      child: const Icon(Icons.add),\n                    ),\n                  ],\n                ),\n                const SizedBox(height: 16),\n                Row(\n                  mainAxisAlignment: MainAxisAlignment.center,\n                  children: [\n                    OutlinedButton(\n                      onPressed: () => context.read<CounterBloc>().add(const CounterMultiplied(factor: 2)),\n                      child: const Text('×2'),\n                    ),\n                    const SizedBox(width: 8),\n                    OutlinedButton(\n                      onPressed: () => context.read<CounterBloc>().add(const CounterAsyncOperation()),\n                      child: const Text('x² async'),\n                    ),\n                    const SizedBox(width: 8),\n                    OutlinedButton(\n                      onPressed: () => context.read<CounterBloc>().add(const CounterReset()),\n                      child: const Text('Reset'),\n                    ),\n                  ],\n                ),\n              ],\n            ),\n          );\n        },\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Advanced BLoC Pattern\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** bloc, cubit, state-management, events\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/features/counter/presentation/bloc/counter_bloc.dart`\n- `lib/features/counter/presentation/bloc/counter_event.dart`\n- `lib/features/counter/presentation/bloc/counter_state.dart`\n- `lib/features/counter/presentation/pages/counter_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add bloc\nflutter pub add cubit\nflutter pub add state-management\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "riverpod-advanced",
    "n": "Advanced Riverpod",
    "i": "🦞",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "riverpod",
      "state-management",
      "providers",
      "generators"
    ],
    "d": "Advanced Riverpod with async providers, families, and code generation.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\nimport 'app.dart';\n\nvoid main() {\n  runApp(const ProviderScope(child: MyApp()));\n}"
      },
      {
        "n": "app.dart",
        "p": "lib/app.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\nimport 'features/posts/presentation/pages/posts_page.dart';\n\nclass MyApp extends ConsumerWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    return MaterialApp(\n      title: 'Advanced Riverpod',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        colorSchemeSeed: Colors.teal,\n        useMaterial3: true,\n      ),\n      home: const PostsPage(),\n    );\n  }\n}"
      },
      {
        "n": "posts_provider.dart",
        "p": "lib/features/posts/presentation/providers/posts_provider.dart",
        "c": "import 'package:flutter_riverpod/flutter_riverpod.dart';\nimport '../../data/repositories/posts_repository.dart';\nimport '../../data/models/post_model.dart';\n\nfinal postsRepositoryProvider = Provider<PostsRepository>((ref) {\n  return PostsRepository();\n});\n\nfinal postsProvider = FutureProvider.autoDispose<List<PostModel>>((ref) async {\n  final repository = ref.watch(postsRepositoryProvider);\n  ref.keepAlive();\n  return repository.getPosts();\n});\n\nfinal postDetailProvider = FutureProvider.family.autoDispose<PostModel, int>((ref, id) async {\n  final repository = ref.watch(postsRepositoryProvider);\n  return repository.getPostById(id);\n});\n\nfinal searchQueryProvider = StateProvider<String>((ref) => '');\n\nfinal filteredPostsProvider = Provider<AsyncValue<List<PostModel>>>((ref) {\n  final postsAsync = ref.watch(postsProvider);\n  final query = ref.watch(searchQueryProvider);\n\n  return postsAsync.whenData((posts) {\n    if (query.isEmpty) return posts;\n    return posts.where((p) =>\n      p.title.toLowerCase().contains(query.toLowerCase()) ||\n      p.body.toLowerCase().contains(query.toLowerCase())\n    ).toList();\n  });\n});\n\nfinal postsNotifierProvider = StateNotifierProvider<PostsNotifier, AsyncValue<List<PostModel>>>((ref) {\n  return PostsNotifier(ref);\n});\n\nclass PostsNotifier extends StateNotifier<AsyncValue<List<PostModel>>> {\n  final Ref ref;\n\n  PostsNotifier(this.ref) : super(const AsyncValue.loading()) {\n    loadPosts();\n  }\n\n  Future<void> loadPosts() async {\n    state = const AsyncValue.loading();\n    try {\n      final posts = await ref.read(postsRepositoryProvider).getPosts();\n      state = AsyncValue.data(posts);\n    } catch (e, st) {\n      state = AsyncValue.error(e, st);\n    }\n  }\n\n  Future<void> addPost(PostModel post) async {\n    state = const AsyncValue.loading();\n    try {\n      final newPost = await ref.read(postsRepositoryProvider).createPost(post);\n      state = AsyncValue.data([...state.value ?? [], newPost]);\n    } catch (e, st) {\n      state = AsyncValue.error(e, st);\n    }\n  }\n\n  Future<void> deletePost(int id) async {\n    try {\n      await ref.read(postsRepositoryProvider).deletePost(id);\n      state = AsyncValue.data(\n        (state.value ?? []).where((p) => p.id != id).toList(),\n      );\n    } catch (e, st) {\n      state = AsyncValue.error(e, st);\n    }\n  }\n}"
      },
      {
        "n": "post_model.dart",
        "p": "lib/features/posts/data/models/post_model.dart",
        "c": "class PostModel {\n  final int id;\n  final int userId;\n  final String title;\n  final String body;\n  final DateTime createdAt;\n\n  const PostModel({\n    required this.id,\n    required this.userId,\n    required this.title,\n    required this.body,\n    required this.createdAt,\n  });\n\n  factory PostModel.fromJson(Map<String, dynamic> json) {\n    return PostModel(\n      id: json['id'] ?? 0,\n      userId: json['userId'] ?? 0,\n      title: json['title'] ?? '',\n      body: json['body'] ?? '',\n      createdAt: DateTime.now(),\n    );\n  }\n\n  Map<String, dynamic> toJson() {\n    return {\n      'id': id,\n      'userId': userId,\n      'title': title,\n      'body': body,\n    };\n  }\n\n  PostModel copyWith({\n    int? id,\n    int? userId,\n    String? title,\n    String? body,\n  }) {\n    return PostModel(\n      id: id ?? this.id,\n      userId: userId ?? this.userId,\n      title: title ?? this.title,\n      body: body ?? this.body,\n      createdAt: createdAt,\n    );\n  }\n}"
      },
      {
        "n": "posts_repository.dart",
        "p": "lib/features/posts/data/repositories/posts_repository.dart",
        "c": "import 'package:dio/dio.dart';\nimport '../models/post_model.dart';\n\nclass PostsRepository {\n  final Dio _dio = Dio(BaseOptions(baseUrl: 'https://jsonplaceholder.typicode.com'));\n\n  Future<List<PostModel>> getPosts() async {\n    final response = await _dio.get('/posts');\n    return (response.data as List).map((json) => PostModel.fromJson(json)).toList();\n  }\n\n  Future<PostModel> getPostById(int id) async {\n    final response = await _dio.get('/posts/$id');\n    return PostModel.fromJson(response.data);\n  }\n\n  Future<PostModel> createPost(PostModel post) async {\n    final response = await _dio.post('/posts', data: post.toJson());\n    return PostModel.fromJson(response.data);\n  }\n\n  Future<void> deletePost(int id) async {\n    await _dio.delete('/posts/$id');\n  }\n}"
      },
      {
        "n": "posts_page.dart",
        "p": "lib/features/posts/presentation/pages/posts_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\nimport '../providers/posts_provider.dart';\n\nclass PostsPage extends ConsumerWidget {\n  const PostsPage({super.key});\n\n  @override\n  Widget build(BuildContext context, WidgetRef ref) {\n    final postsAsync = ref.watch(filteredPostsProvider);\n\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('Posts'),\n        centerTitle: true,\n      ),\n      body: Column(\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: TextField(\n              decoration: const InputDecoration(\n                hintText: 'Search posts...',\n                prefixIcon: Icon(Icons.search),\n                border: OutlineInputBorder(),\n              ),\n              onChanged: (value) {\n                ref.read(searchQueryProvider.notifier).state = value;\n              },\n            ),\n          ),\n          Expanded(\n            child: postsAsync.when(\n              data: (posts) => ListView.builder(\n                itemCount: posts.length,\n                itemBuilder: (context, index) {\n                  final post = posts[index];\n                  return ListTile(\n                    title: Text(post.title),\n                    subtitle: Text(post.body, maxLines: 2),\n                    trailing: IconButton(\n                      icon: const Icon(Icons.delete),\n                      onPressed: () {\n                        ref.read(postsNotifierProvider.notifier).deletePost(post.id);\n                      },\n                    ),\n                  );\n                },\n              ),\n              loading: () => const Center(child: CircularProgressIndicator()),\n              error: (e, _) => Center(child: Text('Error: $e')),\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Advanced Riverpod\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** riverpod, state-management, providers, generators\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/app.dart`\n- `lib/features/posts/presentation/providers/posts_provider.dart`\n- `lib/features/posts/data/models/post_model.dart`\n- `lib/features/posts/data/repositories/posts_repository.dart`\n- `lib/features/posts/presentation/pages/posts_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add riverpod\nflutter pub add state-management\nflutter pub add providers\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "gorouter-setup",
    "n": "GoRouter Advanced",
    "i": "🧭",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "gorouter",
      "navigation",
      "routing",
      "deep-linking"
    ],
    "d": "Advanced GoRouter setup with nested routes, redirects, and deep linking.",
    "f": [
      {
        "n": "app_router.dart",
        "p": "lib/core/router/app_router.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:go_router/go_router.dart';\nimport '../../features/auth/presentation/pages/login_page.dart';\nimport '../../features/home/presentation/pages/home_page.dart';\nimport '../../features/profile/presentation/pages/profile_page.dart';\nimport '../../features/settings/presentation/pages/settings_page.dart';\nimport '../../features/detail/presentation/pages/detail_page.dart';\nimport '../guards/auth_guard.dart';\n\nfinal _rootNavigatorKey = GlobalKey<NavigatorState>();\nfinal _shellNavigatorKey = GlobalKey<NavigatorState>();\n\nfinal goRouter = GoRouter(\n  navigatorKey: _rootNavigatorKey,\n  initialLocation: '/login',\n  debugLogDiagnostics: true,\n  redirect: (context, state) {\n    final isLoggedIn = AuthGuard.isAuthenticated;\n    final isLoginRoute = state.matchedLocation == '/login';\n\n    if (!isLoggedIn && !isLoginRoute) return '/login';\n    if (isLoggedIn && isLoginRoute) return '/home';\n    return null;\n  },\n  routes: [\n    GoRoute(\n      path: '/login',\n      name: 'login',\n      builder: (context, state) => const LoginPage(),\n    ),\n    ShellRoute(\n      navigatorKey: _shellNavigatorKey,\n      builder: (context, state, child) => ScaffoldWithNav(child: child),\n      routes: [\n        GoRoute(\n          path: '/home',\n          name: 'home',\n          pageBuilder: (context, state) => const NoTransitionPage(\n            child: HomePage(),\n          ),\n          routes: [\n            GoRoute(\n              path: 'detail/:id',\n              name: 'detail',\n              builder: (context, state) => DetailPage(\n                id: state.pathParameters['id']!,\n              ),\n            ),\n          ],\n        ),\n        GoRoute(\n          path: '/profile',\n          name: 'profile',\n          pageBuilder: (context, state) => const NoTransitionPage(\n            child: ProfilePage(),\n          ),\n        ),\n        GoRoute(\n          path: '/settings',\n          name: 'settings',\n          pageBuilder: (context, state) => const NoTransitionPage(\n            child: SettingsPage(),\n          ),\n        ),\n      ],\n    ),\n  ],\n);\n\nclass ScaffoldWithNav extends StatelessWidget {\n  final Widget child;\n  const ScaffoldWithNav({super.key, required this.child});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: child,\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _calculateSelectedIndex(context),\n        onDestinationSelected: (i) => _onItemTapped(i, context),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home), label: 'Home'),\n          NavigationDestination(icon: Icon(Icons.person_outlined), selectedIcon: Icon(Icons.person), label: 'Profile'),\n          NavigationDestination(icon: Icon(Icons.settings_outlined), selectedIcon: Icon(Icons.settings), label: 'Settings'),\n        ],\n      ),\n    );\n  }\n\n  int _calculateSelectedIndex(BuildContext context) {\n    final location = GoRouterState.of(context).matchedLocation;\n    if (location.startsWith('/home')) return 0;\n    if (location.startsWith('/profile')) return 1;\n    if (location.startsWith('/settings')) return 2;\n    return 0;\n  }\n\n  void _onItemTapped(int index, BuildContext context) {\n    switch (index) {\n      case 0: context.go('/home');\n      case 1: context.go('/profile');\n      case 2: context.go('/settings');\n    }\n  }\n}"
      },
      {
        "n": "auth_guard.dart",
        "p": "lib/core/guards/auth_guard.dart",
        "c": "class AuthGuard {\n  static bool _isAuthenticated = false;\n\n  static bool get isAuthenticated => _isAuthenticated;\n\n  static Future<bool> login(String email, String password) async {\n    await Future.delayed(const Duration(seconds: 1));\n    _isAuthenticated = true;\n    return true;\n  }\n\n  static Future<void> logout() async {\n    _isAuthenticated = false;\n  }\n}"
      },
      {
        "n": "home_page.dart",
        "p": "lib/features/home/presentation/pages/home_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:go_router/go_router.dart';\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Home')),\n      body: ListView.builder(\n        itemCount: 20,\n        itemBuilder: (context, index) {\n          return ListTile(\n            title: Text('Item \\${index + 1}'),\n            subtitle: Text('Tap to view details'),\n            trailing: const Icon(Icons.chevron_right),\n            onTap: () => context.go('/home/detail/\\${index + 1}'),\n          );\n        },\n      ),\n    );\n  }\n}"
      },
      {
        "n": "detail_page.dart",
        "p": "lib/features/detail/presentation/pages/detail_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:go_router/go_router.dart';\n\nclass DetailPage extends StatelessWidget {\n  final String id;\n  const DetailPage({super.key, required this.id});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: Text('Detail #$id'),\n        leading: IconButton(\n          icon: const Icon(Icons.arrow_back),\n          onPressed: () => context.go('/home'),\n        ),\n      ),\n      body: Center(\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            Text('Item ID: $id', style: Theme.of(context).textTheme.headlineMedium),\n            const SizedBox(height: 16),\n            FilledButton(onPressed: () => context.go('/home'), child: const Text('Back to Home')),\n          ],\n        ),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "profile_page.dart",
        "p": "lib/features/profile/presentation/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Profile')),\n      body: const Center(child: Text('Profile Page')),\n    );\n  }\n}"
      },
      {
        "n": "settings_page.dart",
        "p": "lib/features/settings/presentation/pages/settings_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SettingsPage extends StatelessWidget {\n  const SettingsPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Settings')),\n      body: const Center(child: Text('Settings Page')),\n    );\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## GoRouter Advanced\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** gorouter, navigation, routing, deep-linking\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/core/router/app_router.dart`\n- `lib/core/guards/auth_guard.dart`\n- `lib/features/home/presentation/pages/home_page.dart`\n- `lib/features/detail/presentation/pages/detail_page.dart`\n- `lib/features/profile/presentation/pages/profile_page.dart`\n- `lib/features/settings/presentation/pages/settings_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add gorouter\n# (kerakli paketni o'rnating)\nflutter pub add routing\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "realtime-chat",
    "n": "Realtime Chat (WebSocket)",
    "i": "💬",
    "diff": "expert",
    "c": "api",
    "t": [
      "flutter",
      "websocket",
      "realtime",
      "chat",
      "stream"
    ],
    "d": "Real-time chat application using WebSocket with message streaming and presence.",
    "f": [
      {
        "n": "chat_service.dart",
        "p": "lib/services/chat_service.dart",
        "c": "import 'dart:async';\nimport 'dart:convert';\nimport 'package:web_socket_channel/web_socket_channel.dart';\n\nclass ChatService {\n  WebSocketChannel? _channel;\n  final _messageController = StreamController<Map<String, dynamic>>.broadcast();\n  final _presenceController = StreamController<List<String>>.broadcast();\n  final _typingController = StreamController<String>.broadcast();\n  bool _isConnected = false;\n\n  Stream<Map<String, dynamic>> get messages => _messageController.stream;\n  Stream<List<String>> get presence => _presenceController.stream;\n  Stream<String> get typing => _typingController.stream;\n  bool get isConnected => _isConnected;\n\n  void connect(String url, String token) {\n    _channel = WebSocketChannel.connect(Uri.parse('$url?token=$token'));\n    _isConnected = true;\n\n    _channel!.stream.listen(\n      (data) {\n        final json = jsonDecode(data);\n        switch (json['type']) {\n          case 'message':\n            _messageController.add(json);\n            break;\n          case 'presence':\n            _presenceController.add(List<String>.from(json['users']));\n            break;\n          case 'typing':\n            _typingController.add(json['userId']);\n            break;\n        }\n      },\n      onDone: () {\n        _isConnected = false;\n        _reconnect(url, token);\n      },\n      onError: (error) {\n        _isConnected = false;\n      },\n    );\n  }\n\n  void _reconnect(String url, String token) {\n    Future.delayed(const Duration(seconds: 3), () {\n      if (!_isConnected) connect(url, token);\n    });\n  }\n\n  void sendMessage(String roomId, String content) {\n    if (_channel == null) return;\n    _channel!.sink.add(jsonEncode({\n      'type': 'message',\n      'roomId': roomId,\n      'content': content,\n      'timestamp': DateTime.now().toIso8601String(),\n    }));\n  }\n\n  void sendTyping(String roomId) {\n    if (_channel == null) return;\n    _channel!.sink.add(jsonEncode({\n      'type': 'typing',\n      'roomId': roomId,\n    }));\n  }\n\n  void joinRoom(String roomId) {\n    if (_channel == null) return;\n    _channel!.sink.add(jsonEncode({\n      'type': 'join',\n      'roomId': roomId,\n    }));\n  }\n\n  void disconnect() {\n    _channel?.sink.close();\n    _isConnected = false;\n  }\n\n  void dispose() {\n    disconnect();\n    _messageController.close();\n    _presenceController.close();\n    _typingController.close();\n  }\n}"
      },
      {
        "n": "chat_page.dart",
        "p": "lib/pages/chat_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../services/chat_service.dart';\n\nclass ChatPage extends StatefulWidget {\n  final String roomId;\n  const ChatPage({super.key, required this.roomId});\n\n  @override\n  State<ChatPage> createState() => _ChatPageState();\n}\n\nclass _ChatPageState extends State<ChatPage> {\n  final ChatService _chatService = ChatService();\n  final TextEditingController _controller = TextEditingController();\n  final ScrollController _scrollController = ScrollController();\n  final List<Map<String, dynamic>> _messages = [];\n  List<String> _onlineUsers = [];\n  String? _typingUser;\n\n  @override\n  void initState() {\n    super.initState();\n    _chatService.connect('wss://api.example.com/ws', 'token');\n    _chatService.joinRoom(widget.roomId);\n    _chatService.messages.listen((msg) {\n      setState(() => _messages.add(msg));\n      _scrollToBottom();\n    });\n    _chatService.presence.listen((users) {\n      setState(() => _onlineUsers = users);\n    });\n    _chatService.typing.listen((userId) {\n      setState(() => _typingUser = userId);\n      Future.delayed(const Duration(seconds: 3), () {\n        if (mounted) setState(() => _typingUser = null);\n      });\n    });\n  }\n\n  void _scrollToBottom() {\n    Future.delayed(const Duration(milliseconds: 100), () {\n      if (_scrollController.hasClients) {\n        _scrollController.animateTo(\n          _scrollController.position.maxScrollExtent,\n          duration: const Duration(milliseconds: 300),\n          curve: Curves.easeOut,\n        );\n      }\n    });\n  }\n\n  void _sendMessage() {\n    final text = _controller.text.trim();\n    if (text.isEmpty) return;\n    _chatService.sendMessage(widget.roomId, text);\n    _controller.clear();\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: Text('Room: \\${widget.roomId}'),\n        actions: [\n          Center(\n            child: Padding(\n              padding: const EdgeInsets.only(right: 16),\n              child: Text(\n                '\\${_onlineUsers.length} online',\n                style: const TextStyle(fontSize: 12),\n              ),\n            ),\n          ),\n        ],\n      ),\n      body: Column(\n        children: [\n          Expanded(\n            child: ListView.builder(\n              controller: _scrollController,\n              padding: const EdgeInsets.all(16),\n              itemCount: _messages.length,\n              itemBuilder: (context, index) {\n                final msg = _messages[index];\n                return ChatBubble(\n                  message: msg['content'] ?? '',\n                  isMe: msg['isMe'] ?? false,\n                  timestamp: msg['timestamp'] ?? '',\n                );\n              },\n            ),\n          ),\n          if (_typingUser != null)\n            Padding(\n              padding: const EdgeInsets.symmetric(horizontal: 16),\n              child: Text(\n                '\\$_typingUser is typing...',\n                style: TextStyle(fontSize: 12, color: Colors.grey[600]),\n              ),\n            ),\n          Container(\n            padding: const EdgeInsets.all(8),\n            decoration: BoxDecoration(\n              color: Theme.of(context).colorScheme.surface,\n              border: Border(top: BorderSide(color: Colors.grey[300]!)),\n            ),\n            child: Row(\n              children: [\n                Expanded(\n                  child: TextField(\n                    controller: _controller,\n                    decoration: const InputDecoration(\n                      hintText: 'Type a message...',\n                      border: OutlineInputBorder(),\n                      contentPadding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),\n                    ),\n                    onChanged: (_) => _chatService.sendTyping(widget.roomId),\n                    onSubmitted: (_) => _sendMessage(),\n                  ),\n                ),\n                const SizedBox(width: 8),\n                IconButton.filled(\n                  onPressed: _sendMessage,\n                  icon: const Icon(Icons.send),\n                ),\n              ],\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  @override\n  void dispose() {\n    _chatService.dispose();\n    _controller.dispose();\n    _scrollController.dispose();\n    super.dispose();\n  }\n}\n\nclass ChatBubble extends StatelessWidget {\n  final String message;\n  final bool isMe;\n  final String timestamp;\n\n  const ChatBubble({\n    super.key,\n    required this.message,\n    required this.isMe,\n    required this.timestamp,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    return Align(\n      alignment: isMe ? Alignment.centerRight : Alignment.centerLeft,\n      child: Container(\n        margin: const EdgeInsets.only(bottom: 8),\n        padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),\n        decoration: BoxDecoration(\n          color: isMe ? Theme.of(context).colorScheme.primary : Colors.grey[200],\n          borderRadius: BorderRadius.circular(12),\n        ),\n        child: Column(\n          crossAxisAlignment: CrossAxisAlignment.start,\n          children: [\n            Text(\n              message,\n              style: TextStyle(color: isMe ? Colors.white : Colors.black87),\n            ),\n            Text(\n              timestamp,\n              style: TextStyle(\n                fontSize: 10,\n                color: isMe ? Colors.white70 : Colors.grey[600],\n              ),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## Realtime Chat (WebSocket)\n\n**Kategoriya:** Api\n**Qiyinlik:** Expert\n**Teglar:** websocket, realtime, chat, stream\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/chat_service.dart`\n- `lib/pages/chat_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add websocket\nflutter pub add realtime\n# (kerakli paketni o'rnating)\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "firebase-complete",
    "n": "Firebase Complete Setup",
    "i": "🔥",
    "diff": "expert",
    "c": "api",
    "t": [
      "flutter",
      "firebase",
      "auth",
      "firestore",
      "storage"
    ],
    "d": "Complete Firebase setup with Auth, Firestore, Storage, and Cloud Messaging.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:firebase_core/firebase_core.dart';\nimport 'package:firebase_messaging/firebase_messaging.dart';\nimport 'firebase_options.dart';\nimport 'app.dart';\n\n@pragma('vm:entry-point')\nFuture<void> _firebaseMessagingBackgroundHandler(RemoteMessage message) async {\n  await Firebase.initializeApp();\n  print('Background message: \\${message.messageId}');\n}\n\nvoid main() async {\n  WidgetsFlutterBinding.ensureInitialized();\n  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);\n  FirebaseMessaging.onBackgroundMessage(_firebaseMessagingBackgroundHandler);\n  runApp(const MyApp());\n}"
      },
      {
        "n": "firebase_service.dart",
        "p": "lib/services/firebase_service.dart",
        "c": "import 'package:firebase_auth/firebase_auth.dart';\nimport 'package:cloud_firestore/cloud_firestore.dart';\nimport 'package:firebase_storage/firebase_storage.dart';\nimport 'dart:io';\n\nclass FirebaseService {\n  final FirebaseAuth _auth = FirebaseAuth.instance;\n  final FirebaseFirestore _firestore = FirebaseFirestore.instance;\n  final FirebaseStorage _storage = FirebaseStorage.instance;\n\n  User? get currentUser => _auth.currentUser;\n  Stream<User?> get authStateChanges => _auth.authStateChanges();\n\n  Future<UserCredential> signIn(String email, String password) async {\n    return await _auth.signInWithEmailAndPassword(\n      email: email,\n      password: password,\n    );\n  }\n\n  Future<UserCredential> signUp(String email, String password, String name) async {\n    final credential = await _auth.createUserWithEmailAndPassword(\n      email: email,\n      password: password,\n    );\n    await credential.user?.updateDisplayName(name);\n    await _firestore.collection('users').doc(credential.user!.uid).set({\n      'name': name,\n      'email': email,\n      'createdAt': FieldValue.serverTimestamp(),\n    });\n    return credential;\n  }\n\n  Future<void> signOut() async {\n    await _auth.signOut();\n  }\n\n  Future<void> addDocument(String collection, Map<String, dynamic> data) async {\n    await _firestore.collection(collection).add({\n      ...data,\n      'createdAt': FieldValue.serverTimestamp(),\n      'updatedAt': FieldValue.serverTimestamp(),\n    });\n  }\n\n  Future<void> updateDocument(String collection, String id, Map<String, dynamic> data) async {\n    await _firestore.collection(collection).doc(id).update({\n      ...data,\n      'updatedAt': FieldValue.serverTimestamp(),\n    });\n  }\n\n  Future<void> deleteDocument(String collection, String id) async {\n    await _firestore.collection(collection).doc(id).delete();\n  }\n\n  Stream<QuerySnapshot> getDocuments(String collection) {\n    return _firestore.collection(collection)\n      .orderBy('createdAt', descending: true)\n      .snapshots();\n  }\n\n  Future<String> uploadFile(String path, File file) async {\n    final ref = _storage.ref().child(path);\n    await ref.putFile(file);\n    return await ref.getDownloadURL();\n  }\n\n  Future<void> deleteFile(String path) async {\n    await _storage.ref().child(path).delete();\n  }\n\n  Future<String> getFcmToken() async {\n    final token = await FirebaseMessaging.instance.getToken();\n    return token ?? '';\n  }\n}"
      },
      {
        "n": "app.dart",
        "p": "lib/app.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'services/firebase_service.dart';\nimport 'pages/home_page.dart';\nimport 'pages/login_page.dart';\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Firebase App',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        colorSchemeSeed: Colors.amber,\n        useMaterial3: true,\n      ),\n      home: StreamBuilder(\n        stream: FirebaseService().authStateChanges,\n        builder: (context, snapshot) {\n          if (snapshot.connectionState == ConnectionState.waiting) {\n            return const Scaffold(body: Center(child: CircularProgressIndicator()));\n          }\n          if (snapshot.hasData) {\n            return const HomePage();\n          }\n          return const LoginPage();\n        },\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## Firebase Complete Setup\n\n**Kategoriya:** Api\n**Qiyinlik:** Expert\n**Teglar:** firebase, auth, firestore, storage\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/services/firebase_service.dart`\n- `lib/app.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add firebase\nflutter pub add auth\nflutter pub add firestore\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "animation-controller-advanced",
    "n": "Advanced Animation Controller",
    "i": "✨",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "animation",
      "controller",
      "tween",
      "custom-paint"
    ],
    "d": "Advanced animation controller with multiple tweens, staggered animations, and custom paint.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/animation_showcase.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Advanced Animations',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.purple, useMaterial3: true),\n      home: const AnimationShowcase(),\n    );\n  }\n}"
      },
      {
        "n": "animation_showcase.dart",
        "p": "lib/pages/animation_showcase.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../widgets/staggered_animation.dart';\nimport '../widgets/animated_clock.dart';\nimport '../widgets/morphing_shape.dart';\nimport '../widgets/particle_effect.dart';\n\nclass AnimationShowcase extends StatelessWidget {\n  const AnimationShowcase({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Animation Showcase')),\n      body: ListView(\n        padding: const EdgeInsets.all(16),\n        children: [\n          _buildCard('Staggered Animation', const StaggeredAnimation()),\n          _buildCard('Animated Clock', const AnimatedClock()),\n          _buildCard('Morphing Shape', const MorphingShape()),\n          _buildCard('Particle Effect', const ParticleEffect()),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildCard(String title, Widget child) {\n    return Card(\n      margin: const EdgeInsets.only(bottom: 16),\n      child: Column(\n        crossAxisAlignment: CrossAxisAlignment.start,\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: Text(title, style: const TextStyle(fontWeight: FontWeight.bold)),\n          ),\n          SizedBox(height: 200, child: child),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "staggered_animation.dart",
        "p": "lib/widgets/staggered_animation.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass StaggeredAnimation extends StatefulWidget {\n  const StaggeredAnimation({super.key});\n\n  @override\n  State<StaggeredAnimation> createState() => _StaggeredAnimationState();\n}\n\nclass _StaggeredAnimationState extends State<StaggeredAnimation>\n    with TickerProviderStateMixin {\n  late final AnimationController _controller;\n  late final Animation<double> _fadeAnim;\n  late final Animation<Offset> _slideAnim;\n  late final Animation<double> _scaleAnim;\n  late final Animation<double> _rotateAnim;\n\n  @override\n  void initState() {\n    super.initState();\n    _controller = AnimationController(\n      duration: const Duration(milliseconds: 2000),\n      vsync: this,\n    );\n\n    _fadeAnim = Tween<double>(begin: 0, end: 1).animate(\n      CurvedAnimation(parent: _controller, curve: const Interval(0.0, 0.4)),\n    );\n    _slideAnim = Tween<Offset>(begin: const Offset(0, 0.5), end: Offset.zero).animate(\n      CurvedAnimation(parent: _controller, curve: const Interval(0.2, 0.6)),\n    );\n    _scaleAnim = Tween<double>(begin: 0.5, end: 1).animate(\n      CurvedAnimation(parent: _controller, curve: const Interval(0.4, 0.8)),\n    );\n    _rotateAnim = Tween<double>(begin: 0, end: 1).animate(\n      CurvedAnimation(parent: _controller, curve: const Interval(0.6, 1.0)),\n    );\n\n    _controller.forward();\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return AnimatedBuilder(\n      animation: _controller,\n      builder: (context, child) {\n        return Center(\n          child: Transform.rotate(\n            angle: _rotateAnim.value * 3.14159,\n            child: Transform.scale(\n              scale: _scaleAnim.value,\n              child: FadeTransition(\n                opacity: _fadeAnim,\n                child: SlideTransition(\n                  position: _slideAnim,\n                  child: Container(\n                    width: 120,\n                    height: 120,\n                    decoration: BoxDecoration(\n                      gradient: const LinearGradient(\n                        colors: [Colors.purple, Colors.blue],\n                      ),\n                      borderRadius: BorderRadius.circular(20),\n                      boxShadow: [\n                        BoxShadow(\n                          color: Colors.purple.withOpacity(0.3),\n                          blurRadius: 20,\n                          offset: const Offset(0, 10),\n                        ),\n                      ],\n                    ),\n                    child: const Center(\n                      child: Icon(Icons.star, color: Colors.white, size: 48),\n                    ),\n                  ),\n                ),\n              ),\n            ),\n          ),\n        );\n      },\n    );\n  }\n\n  @override\n  void dispose() {\n    _controller.dispose();\n    super.dispose();\n  }\n}"
      },
      {
        "n": "animated_clock.dart",
        "p": "lib/widgets/animated_clock.dart",
        "c": "import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass AnimatedClock extends StatefulWidget {\n  const AnimatedClock({super.key});\n\n  @override\n  State<AnimatedClock> createState() => _AnimatedClockState();\n}\n\nclass _AnimatedClockState extends State<AnimatedClock>\n    with SingleTickerProviderStateMixin {\n  late final AnimationController _controller;\n\n  @override\n  void initState() {\n    super.initState();\n    _controller = AnimationController(\n      duration: const Duration(seconds: 10),\n      vsync: this,\n    )..repeat();\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Center(\n      child: AnimatedBuilder(\n        animation: _controller,\n        builder: (context, child) {\n          final now = DateTime.now();\n          return CustomPaint(\n            size: const Size(150, 150),\n            painter: ClockPainter(\n              seconds: _controller.value * 60,\n              minutes: now.minute.toDouble(),\n              hours: now.hour.toDouble(),\n            ),\n          );\n        },\n      ),\n    );\n  }\n\n  @override\n  void dispose() {\n    _controller.dispose();\n    super.dispose();\n  }\n}\n\nclass ClockPainter extends CustomPainter {\n  final double seconds;\n  final double minutes;\n  final double hours;\n\n  ClockPainter({\n    required this.seconds,\n    required this.minutes,\n    required this.hours,\n  });\n\n  @override\n  void paint(Canvas canvas, Size size) {\n    final center = Offset(size.width / 2, size.height / 2);\n    final radius = min(size.width, size.height) / 2;\n\n    final bgPaint = Paint()..color = Colors.grey[200]!;\n    canvas.drawCircle(center, radius, bgPaint);\n\n    final borderPaint = Paint()\n      ..color = Colors.purple\n      ..style = PaintingStyle.stroke\n      ..strokeWidth = 3;\n    canvas.drawCircle(center, radius, borderPaint);\n\n    _drawHand(canvas, center, radius * 0.6, seconds * 6, Colors.red, 2);\n    _drawHand(canvas, center, radius * 0.45, minutes * 6, Colors.blue, 3);\n    _drawHand(canvas, center, radius * 0.3, hours * 30, Colors.black, 4);\n\n    final centerDot = Paint()..color = Colors.black;\n    canvas.drawCircle(center, 5, centerDot);\n  }\n\n  void _drawHand(Canvas canvas, Offset center, double length, double angle, Color color, double width) {\n    final paint = Paint()\n      ..color = color\n      ..strokeWidth = width\n      ..strokeCap = StrokeCap.round;\n    final radians = angle * pi / 180 - pi / 2;\n    final end = Offset(\n      center.dx + length * cos(radians),\n      center.dy + length * sin(radians),\n    );\n    canvas.drawLine(center, end, paint);\n  }\n\n  @override\n  bool shouldRepaint(covariant ClockPainter oldDelegate) => true;\n}"
      },
      {
        "n": "morphing_shape.dart",
        "p": "lib/widgets/morphing_shape.dart",
        "c": "import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass MorphingShape extends StatefulWidget {\n  const MorphingShape({super.key});\n\n  @override\n  State<MorphingShape> createState() => _MorphingShapeState();\n}\n\nclass _MorphingShapeState extends State<MorphingShape>\n    with SingleTickerProviderStateMixin {\n  late final AnimationController _controller;\n  late final Animation<double> _animation;\n\n  @override\n  void initState() {\n    super.initState();\n    _controller = AnimationController(\n      duration: const Duration(milliseconds: 1500),\n      vsync: this,\n    );\n    _animation = Tween<double>(begin: 0, end: 1).animate(\n      CurvedAnimation(parent: _controller, curve: Curves.easeInOut),\n    );\n    _controller.repeat(reverse: true);\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Center(\n      child: AnimatedBuilder(\n        animation: _animation,\n        builder: (context, child) {\n          return CustomPaint(\n            size: const Size(150, 150),\n            painter: ShapePainter(progress: _animation.value),\n          );\n        },\n      ),\n    );\n  }\n\n  @override\n  void dispose() {\n    _controller.dispose();\n    super.dispose();\n  }\n}\n\nclass ShapePainter extends CustomPainter {\n  final double progress;\n\n  ShapePainter({required this.progress});\n\n  @override\n  void paint(Canvas canvas, Size size) {\n    final center = Offset(size.width / 2, size.height / 2);\n    final radius = min(size.width, size.height) / 3;\n\n    final paint = Paint()\n      ..shader = LinearGradient(\n        colors: [Colors.purple, Colors.blue, Colors.cyan],\n      ).createShader(Rect.fromCircle(center: center, radius: radius));\n\n    final path = Path();\n    final sides = 3 + (progress * 5).floor();\n    final angle = 2 * pi / sides;\n\n    for (int i = 0; i < sides; i++) {\n      final x = center.dx + radius * cos(angle * i - pi / 2);\n      final y = center.dy + radius * sin(angle * i - pi / 2);\n      if (i == 0) {\n        path.moveTo(x, y);\n      } else {\n        path.lineTo(x, y);\n      }\n    }\n    path.close();\n\n    canvas.drawPath(path, paint);\n  }\n\n  @override\n  bool shouldRepaint(covariant ShapePainter oldDelegate) => true;\n}"
      },
      {
        "n": "particle_effect.dart",
        "p": "lib/widgets/particle_effect.dart",
        "c": "import 'dart:math';\nimport 'package:flutter/material.dart';\n\nclass ParticleEffect extends StatefulWidget {\n  const ParticleEffect({super.key});\n\n  @override\n  State<ParticleEffect> createState() => _ParticleEffectState();\n}\n\nclass _ParticleEffectState extends State<ParticleEffect>\n    with SingleTickerProviderStateMixin {\n  late final AnimationController _controller;\n  final List<Particle> _particles = [];\n  final Random _random = Random();\n\n  @override\n  void initState() {\n    super.initState();\n    _controller = AnimationController(\n      duration: const Duration(milliseconds: 16),\n      vsync: this,\n    )..addListener(_updateParticles);\n\n    for (int i = 0; i < 50; i++) {\n      _particles.add(Particle(\n        x: _random.nextDouble() * 300,\n        y: _random.nextDouble() * 200,\n        vx: (_random.nextDouble() - 0.5) * 2,\n        vy: (_random.nextDouble() - 0.5) * 2,\n        radius: _random.nextDouble() * 4 + 2,\n        color: Colors.primaries[_random.nextInt(Colors.primaries.length)],\n      ));\n    }\n    _controller.repeat();\n  }\n\n  void _updateParticles() {\n    for (var p in _particles) {\n      p.x += p.vx;\n      p.y += p.vy;\n      if (p.x < 0 || p.x > 300) p.vx *= -1;\n      if (p.y < 0 || p.y > 200) p.vy *= -1;\n    }\n    setState(() {});\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return CustomPaint(\n      size: const Size(300, 200),\n      painter: ParticlePainter(particles: _particles),\n    );\n  }\n\n  @override\n  void dispose() {\n    _controller.dispose();\n    super.dispose();\n  }\n}\n\nclass Particle {\n  double x, y, vx, vy, radius;\n  Color color;\n  Particle({\n    required this.x,\n    required this.y,\n    required this.vx,\n    required this.vy,\n    required this.radius,\n    required this.color,\n  });\n}\n\nclass ParticlePainter extends CustomPainter {\n  final List<Particle> particles;\n\n  ParticlePainter({required this.particles});\n\n  @override\n  void paint(Canvas canvas, Size size) {\n    for (var p in particles) {\n      final paint = Paint()..color = p.color.withOpacity(0.7);\n      canvas.drawCircle(Offset(p.x, p.y), p.radius, paint);\n    }\n  }\n\n  @override\n  bool shouldRepaint(covariant ParticlePainter oldDelegate) => true;\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Advanced Animation Controller\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** animation, controller, tween, custom-paint\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/pages/animation_showcase.dart`\n- `lib/widgets/staggered_animation.dart`\n- `lib/widgets/animated_clock.dart`\n- `lib/widgets/morphing_shape.dart`\n- `lib/widgets/particle_effect.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add controller\nflutter pub add tween\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "platform-channels",
    "n": "Platform Channels (Native)",
    "i": "🔌",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "platform-channels",
      "native",
      "ios",
      "android"
    ],
    "d": "Platform channels for native iOS/Android communication with Flutter.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'services/platform_service.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Platform Channels',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.deepOrange, useMaterial3: true),\n      home: const PlatformPage(),\n    );\n  }\n}\n\nclass PlatformPage extends StatefulWidget {\n  const PlatformPage({super.key});\n\n  @override\n  State<PlatformPage> createState() => _PlatformPageState();\n}\n\nclass _PlatformPageState extends State<PlatformPage> {\n  String _batteryLevel = 'Unknown';\n  String _deviceInfo = 'Unknown';\n  String _nativeResult = 'Unknown';\n\n  @override\n  void initState() {\n    super.initState();\n    _getBatteryLevel();\n    _getDeviceInfo();\n  }\n\n  Future<void> _getBatteryLevel() async {\n    final level = await PlatformService.getBatteryLevel();\n    setState(() => _batteryLevel = level.toString());\n  }\n\n  Future<void> _getDeviceInfo() async {\n    final info = await PlatformService.getDeviceInfo();\n    setState(() => _deviceInfo = info);\n  }\n\n  Future<void> _callNativeMethod() async {\n    final result = await PlatformService.callNativeMethod({\n      'action': 'process',\n      'data': {'key': 'value'},\n    });\n    setState(() => _nativeResult = result.toString());\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Platform Channels')),\n      body: ListView(\n        padding: const EdgeInsets.all(16),\n        children: [\n          _buildInfoCard('Battery Level', '$_batteryLevel%', Icons.battery_full),\n          _buildInfoCard('Device Info', _deviceInfo, Icons.phone_android),\n          _buildInfoCard('Native Result', _nativeResult, Icons.code),\n          const SizedBox(height: 16),\n          FilledButton.icon(\n            onPressed: _getBatteryLevel,\n            icon: const Icon(Icons.refresh),\n            label: const Text('Refresh Battery'),\n          ),\n          const SizedBox(height: 8),\n          FilledButton.tonalIcon(\n            onPressed: _callNativeMethod,\n            icon: const Icon(Icons.play_arrow),\n            label: const Text('Call Native Method'),\n          ),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildInfoCard(String title, String value, IconData icon) {\n    return Card(\n      margin: const EdgeInsets.only(bottom: 8),\n      child: ListTile(\n        leading: Icon(icon),\n        title: Text(title),\n        subtitle: Text(value),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "platform_service.dart",
        "p": "lib/services/platform_service.dart",
        "c": "import 'package:flutter/services.dart';\n\nclass PlatformService {\n  static const _channel = MethodChannel('com.example/app');\n  static const _eventChannel = EventChannel('com.example/events');\n\n  static Future<int> getBatteryLevel() async {\n    try {\n      final result = await _channel.invokeMethod<int>('getBatteryLevel');\n      return result ?? -1;\n    } on PlatformException catch (e) {\n      print('Error: \\${e.message}');\n      return -1;\n    }\n  }\n\n  static Future<String> getDeviceInfo() async {\n    try {\n      final result = await _channel.invokeMethod<String>('getDeviceInfo');\n      return result ?? 'Unknown';\n    } on PlatformException catch (e) {\n      print('Error: \\${e.message}');\n      return 'Error';\n    }\n  }\n\n  static Future<dynamic> callNativeMethod(Map<String, dynamic> args) async {\n    try {\n      final result = await _channel.invokeMethod('callNativeMethod', args);\n      return result;\n    } on PlatformException catch (e) {\n      print('Error: \\${e.message}');\n      return null;\n    }\n  }\n\n  static Stream<dynamic> get nativeEvents {\n    return _eventChannel.receiveBroadcastStream();\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Platform Channels (Native)\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** platform-channels, native, ios, android\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/services/platform_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add platform-channels\nflutter pub add native\nflutter pub add ios\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "custom-render-object",
    "n": "Custom RenderObject",
    "i": "🎨",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "render-object",
      "custom-layout",
      "layout",
      "paint"
    ],
    "d": "Custom RenderObject for advanced layout and painting control.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'widgets/circular_layout.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Custom RenderObject',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.indigo, useMaterial3: true),\n      home: const Scaffold(\n        body: Center(\n          child: CircularLayout(\n            children: [\n              CircleItem(color: Colors.red, label: 'A'),\n              CircleItem(color: Colors.blue, label: 'B'),\n              CircleItem(color: Colors.green, label: 'C'),\n              CircleItem(color: Colors.orange, label: 'D'),\n              CircleItem(color: Colors.purple, label: 'E'),\n              CircleItem(color: Colors.teal, label: 'F'),\n            ],\n          ),\n        ),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "circular_layout.dart",
        "p": "lib/widgets/circular_layout.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter/rendering.dart';\n\nclass CircularLayout extends StatelessWidget {\n  final List<Widget> children;\n  const CircularLayout({super.key, required this.children});\n\n  @override\n  Widget build(BuildContext context) {\n    return CustomMultiChildLayout(\n      delegate: CircularLayoutDelegate(count: children.length),\n      children: children.asMap().entries.map((entry) {\n        return LayoutId(\n          id: entry.key,\n          child: entry.value,\n        );\n      }).toList(),\n    );\n  }\n}\n\nclass CircleItem extends StatelessWidget {\n  final Color color;\n  final String label;\n  const CircleItem({super.key, required this.color, required this.label});\n\n  @override\n  Widget build(BuildContext context) {\n    return Container(\n      width: 60,\n      height: 60,\n      decoration: BoxDecoration(\n        color: color,\n        shape: BoxShape.circle,\n        boxShadow: [\n          BoxShadow(color: color.withOpacity(0.3), blurRadius: 8, offset: const Offset(0, 4)),\n        ],\n      ),\n      child: Center(\n        child: Text(label, style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),\n      ),\n    );\n  }\n}\n\nclass CircularLayoutDelegate extends MultiChildLayoutDelegate {\n  final int count;\n\n  CircularLayoutDelegate({required this.count});\n\n  @override\n  void performLayout(Size size) {\n    final center = Offset(size.width / 2, size.height / 2);\n    final radius = size.width / 3;\n\n    for (int i = 0; i < count; i++) {\n      final angle = (2 * 3.14159 * i / count) - 3.14159 / 2;\n      final childSize = const Size(60, 60);\n      final offset = Offset(\n        center.dx + radius * cos(angle) - childSize.width / 2,\n        center.dy + radius * sin(angle) - childSize.height / 2,\n      );\n      layoutChild(i, BoxConstraints.tight(childSize));\n      positionChild(i, offset);\n    }\n  }\n\n  @override\n  bool shouldRelayout(covariant CircularLayoutDelegate oldDelegate) {\n    return count != oldDelegate.count;\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Custom RenderObject\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** render-object, custom-layout, layout, paint\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/widgets/circular_layout.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add render-object\nflutter pub add custom-layout\nflutter pub add layout\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "advanced-form-validation",
    "n": "Advanced Form Validation",
    "i": "📝",
    "diff": "hard",
    "c": "ui",
    "t": [
      "flutter",
      "form",
      "validation",
      "text-field",
      "reactive"
    ],
    "d": "Advanced form with reactive validation, multi-step, and custom validators.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/advanced_form_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Advanced Form',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.blue, useMaterial3: true),\n      home: const AdvancedFormPage(),\n    );\n  }\n}"
      },
      {
        "n": "advanced_form_page.dart",
        "p": "lib/pages/advanced_form_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../widgets/validated_field.dart';\n\nclass AdvancedFormPage extends StatefulWidget {\n  const AdvancedFormPage({super.key});\n\n  @override\n  State<advancedFormPage> createState() => _AdvancedFormPageState();\n}\n\nclass _AdvancedFormPageState extends State<AdvancedFormPage> {\n  final _formKey = GlobalKey<FormState>();\n  final _nameController = TextEditingController();\n  final _emailController = TextEditingController();\n  final _passwordController = TextEditingController();\n  final _confirmPasswordController = TextEditingController();\n  final _phoneController = TextEditingController();\n  bool _obscurePassword = true;\n  int _currentStep = 0;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Advanced Form')),\n      body: Form(\n        key: _formKey,\n        child: Stepper(\n          currentStep: _currentStep,\n          onStepContinue: () {\n            if (_currentStep < 2) {\n              setState(() => _currentStep++);\n            } else {\n              _submitForm();\n            }\n          },\n          onStepCancel: () {\n            if (_currentStep > 0) {\n              setState(() => _currentStep--);\n            }\n          },\n          steps: [\n            Step(\n              title: const Text('Personal Info'),\n              isActive: _currentStep >= 0,\n              content: Column(\n                children: [\n                  ValidatedField(\n                    controller: _nameController,\n                    label: 'Full Name',\n                    icon: Icons.person,\n                    validator: (value) {\n                      if (value == null || value.isEmpty) return 'Name is required';\n                      if (value.length < 3) return 'Name must be at least 3 characters';\n                      return null;\n                    },\n                  ),\n                  const SizedBox(height: 16),\n                  ValidatedField(\n                    controller: _emailController,\n                    label: 'Email',\n                    icon: Icons.email,\n                    keyboardType: TextInputType.emailAddress,\n                    validator: (value) {\n                      if (value == null || value.isEmpty) return 'Email is required';\n                      if (!RegExp(r'^[^@]+@[^@]+\\.[^@]+').hasMatch(value)) return 'Invalid email';\n                      return null;\n                    },\n                  ),\n                  const SizedBox(height: 16),\n                  ValidatedField(\n                    controller: _phoneController,\n                    label: 'Phone',\n                    icon: Icons.phone,\n                    keyboardType: TextInputType.phone,\n                    validator: (value) {\n                      if (value == null || value.isEmpty) return 'Phone is required';\n                      if (value.length < 10) return 'Invalid phone number';\n                      return null;\n                    },\n                  ),\n                ],\n              ),\n            ),\n            Step(\n              title: const Text('Security'),\n              isActive: _currentStep >= 1,\n              content: Column(\n                children: [\n                  ValidatedField(\n                    controller: _passwordController,\n                    label: 'Password',\n                    icon: Icons.lock,\n                    obscureText: _obscurePassword,\n                    suffixIcon: IconButton(\n                      icon: Icon(_obscurePassword ? Icons.visibility : Icons.visibility_off),\n                      onPressed: () => setState(() => _obscurePassword = !_obscurePassword),\n                    ),\n                    validator: (value) {\n                      if (value == null || value.isEmpty) return 'Password is required';\n                      if (value.length < 8) return 'Password must be at least 8 characters';\n                      if (!RegExp(r'(?=.*[A-Z])').hasMatch(value)) return 'Must contain uppercase';\n                      if (!RegExp(r'(?=.*[0-9])').hasMatch(value)) return 'Must contain number';\n                      return null;\n                    },\n                  ),\n                  const SizedBox(height: 16),\n                  ValidatedField(\n                    controller: _confirmPasswordController,\n                    label: 'Confirm Password',\n                    icon: Icons.lock_outline,\n                    obscureText: true,\n                    validator: (value) {\n                      if (value != _passwordController.text) return 'Passwords do not match';\n                      return null;\n                    },\n                  ),\n                ],\n              ),\n            ),\n            Step(\n              title: const Text('Confirm'),\n              isActive: _currentStep >= 2,\n              content: const Text('Review your information and submit.'),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n\n  void _submitForm() {\n    if (_formKey.currentState!.validate()) {\n      ScaffoldMessenger.of(context).showSnackBar(\n        const SnackBar(content: Text('Form submitted successfully!')),\n      );\n    }\n  }\n\n  @override\n  void dispose() {\n    _nameController.dispose();\n    _emailController.dispose();\n    _passwordController.dispose();\n    _confirmPasswordController.dispose();\n    _phoneController.dispose();\n    super.dispose();\n  }\n}"
      },
      {
        "n": "validated_field.dart",
        "p": "lib/widgets/validated_field.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ValidatedField extends StatelessWidget {\n  final TextEditingController controller;\n  final String label;\n  final IconData icon;\n  final String? Function(String?)? validator;\n  final bool obscureText;\n  final Widget? suffixIcon;\n  final TextInputType? keyboardType;\n\n  const ValidatedField({\n    super.key,\n    required this.controller,\n    required this.label,\n    required this.icon,\n    this.validator,\n    this.obscureText = false,\n    this.suffixIcon,\n    this.keyboardType,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    return TextFormField(\n      controller: controller,\n      obscureText: obscureText,\n      keyboardType: keyboardType,\n      validator: validator,\n      decoration: InputDecoration(\n        labelText: label,\n        prefixIcon: Icon(icon),\n        suffixIcon: suffixIcon,\n        border: const OutlineInputBorder(),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Advanced Form Validation\n\n**Kategoriya:** Ui\n**Qiyinlik:** Hard\n**Teglar:** form, validation, text-field, reactive\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/pages/advanced_form_page.dart`\n- `lib/widgets/validated_field.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add validation\nflutter pub add text-field\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "in-app-purchase",
    "n": "In-App Purchase",
    "i": "💰",
    "diff": "expert",
    "c": "api",
    "t": [
      "flutter",
      "in-app-purchase",
      "billing",
      "subscription",
      "payment"
    ],
    "d": "In-app purchase integration with subscriptions and one-time purchases.",
    "f": [
      {
        "n": "purchase_service.dart",
        "p": "lib/services/purchase_service.dart",
        "c": "import 'dart:async';\nimport 'package:in_app_purchase/in_app_purchase.dart';\n\nclass PurchaseService {\n  final InAppPurchase _inAppPurchase = InAppPurchase.instance;\n  StreamSubscription<List<PurchaseDetails>>? _subscription;\n  final _purchaseController = StreamController<PurchaseDetails>.broadcast();\n\n  Stream<PurchaseDetails> get purchaseStream => _purchaseController.stream;\n\n  Future<bool> initialize() async {\n    final available = await _inAppPurchase.isAvailable();\n    if (!available) return false;\n\n    _subscription = _inAppPurchase.purchaseStream.listen(\n      (purchases) {\n        for (final purchase in purchases) {\n          _handlePurchase(purchase);\n        }\n      },\n      onDone: () => _subscription?.cancel(),\n      onError: (error) => _subscription?.cancel(),\n    );\n\n    return true;\n  }\n\n  Future<List<ProductDetails>> getProducts(Set<String> ids) async {\n    final response = await _inAppPurchase.queryProductDetails(ids);\n    return response.productDetails;\n  }\n\n  Future<bool> buyProduct(ProductDetails product) async {\n    final params = PurchaseParam(productDetails: product);\n    return await _inAppPurchase.buyNonConsumable(purchaseParam: params);\n  }\n\n  Future<bool> buySubscription(ProductDetails product) async {\n    final params = PurchaseParam(productDetails: product);\n    return await _inAppPurchase.buyNonConsumable(purchaseParam: params);\n  }\n\n  void _handlePurchase(PurchaseDetails purchase) {\n    if (purchase.status == PurchaseStatus.purchased) {\n      _verifyPurchase(purchase);\n    }\n    if (purchase.pendingCompletePurchase) {\n      _inAppPurchase.completePurchase(purchase);\n    }\n    _purchaseController.add(purchase);\n  }\n\n  Future<void> _verifyPurchase(PurchaseDetails purchase) async {\n    // Send to server for verification\n  }\n\n  void dispose() {\n    _subscription?.cancel();\n    _purchaseController.close();\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## In-App Purchase\n\n**Kategoriya:** Api\n**Qiyinlik:** Expert\n**Teglar:** in-app-purchase, billing, subscription, payment\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/purchase_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add in-app-purchase\nflutter pub add billing\nflutter pub add subscription\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "background-isolate",
    "n": "Background Processing",
    "i": "⚙️",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "isolate",
      "background",
      "compute",
      "processing"
    ],
    "d": "Background processing with Isolates and compute for heavy tasks.",
    "f": [
      {
        "n": "isolate_service.dart",
        "p": "lib/services/isolate_service.dart",
        "c": "import 'dart:async';\nimport 'dart:isolate';\nimport 'package:flutter/foundation.dart';\n\nclass IsolateService {\n  static Future<T> compute<T, A>(Future<T> Function(A) computation, A argument) {\n    return compute(computation, argument);\n  }\n\n  static Future<List<Map<String, dynamic>>> processDataInIsolate(List<Map<String, dynamic>> data) async {\n    final receivePort = ReceivePort();\n    await Isolate.spawn(\n      _processData,\n      _IsolateMessage(data: data, sendPort: receivePort.sendPort),\n    );\n    return await receivePort.first;\n  }\n\n  static void _processData(_IsolateMessage message) {\n    final result = message.data.map((item) {\n      return {\n        ...item,\n        'processed': true,\n        'timestamp': DateTime.now().toIso8601String(),\n        'hash': item.toString().hashCode,\n      };\n    }).toList();\n    message.sendPort.send(result);\n  }\n\n  static Stream<double> processLargeFile(String path) {\n    final controller = StreamController<double>();\n\n    Isolate.spawn((String filePath) {\n      // Simulate large file processing\n      for (int i = 0; i <= 100; i++) {\n        Future.delayed(Duration(milliseconds: 50));\n      }\n    }, path);\n\n    return controller.stream;\n  }\n}\n\nclass _IsolateMessage {\n  final List<Map<String, dynamic>> data;\n  final SendPort sendPort;\n\n  _IsolateMessage({required this.data, required this.sendPort});\n}"
      },
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'services/isolate_service.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Background Processing',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.cyan, useMaterial3: true),\n      home: const IsolatePage(),\n    );\n  }\n}\n\nclass IsolatePage extends StatefulWidget {\n  const IsolatePage({super.key});\n\n  @override\n  State<IsolatePage> createState() => _IsolatePageState();\n}\n\nclass _IsolatePageState extends State<IsolatePage> {\n  String _status = 'Idle';\n  List<Map<String, dynamic>> _results = [];\n\n  Future<void> _processData() async {\n    setState(() => _status = 'Processing...');\n\n    final data = List.generate(100, (i) => {'id': i, 'value': 'Item $i'});\n\n    final results = await IsolateService.processDataInIsolate(data);\n\n    setState(() {\n      _status = 'Done! Processed ${results.length} items';\n      _results = results;\n    });\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Background Processing')),\n      body: Column(\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: Row(\n              children: [\n                Expanded(child: Text(_status)),\n                FilledButton(\n                  onPressed: _processData,\n                  child: const Text('Process'),\n                ),\n              ],\n            ),\n          ),\n          Expanded(\n            child: ListView.builder(\n              itemCount: _results.length,\n              itemBuilder: (context, index) {\n                final item = _results[index];\n                return ListTile(\n                  title: Text('Item ${item['id']}'),\n                  subtitle: Text('Hash: ${item['hash']}'),\n                  trailing: Icon(item['processed'] == true ? Icons.check_circle : Icons.circle),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Background Processing\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** isolate, background, compute, processing\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/isolate_service.dart`\n- `lib/main.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add isolate\nflutter pub add background\nflutter pub add compute\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "shader-effects",
    "n": "Shader Effects",
    "i": "🌈",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "shader",
      "fragment",
      "custom-paint",
      "gpu"
    ],
    "d": "Custom shader effects with fragment shaders and GPU acceleration.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'widgets/shader_widget.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Shader Effects',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData.dark(),\n      home: const Scaffold(\n        body: Center(\n          child: ShaderWidget(),\n        ),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "shader_widget.dart",
        "p": "lib/widgets/shader_widget.dart",
        "c": "import 'dart:ui' as ui;\nimport 'package:flutter/material.dart';\nimport 'package:flutter_shaders/flutter_shaders.dart';\n\nclass ShaderWidget extends StatefulWidget {\n  const ShaderWidget({super.key});\n\n  @override\n  State<ShaderWidget> createState() => _ShaderWidgetState();\n}\n\nclass _ShaderWidgetState extends State<ShaderWidget>\n    with SingleTickerProviderStateMixin {\n  late final AnimationController _controller;\n\n  @override\n  void initState() {\n    super.initState();\n    _controller = AnimationController(\n      duration: const Duration(seconds: 3),\n      vsync: this,\n    )..repeat();\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return AnimatedBuilder(\n      animation: _controller,\n      builder: (context, child) {\n        return CustomPaint(\n          size: const Size(300, 300),\n          painter: ShaderPainter(progress: _controller.value),\n        );\n      },\n    );\n  }\n\n  @override\n  void dispose() {\n    _controller.dispose();\n    super.dispose();\n  }\n}\n\nclass ShaderPainter extends CustomPainter {\n  final double progress;\n\n  ShaderPainter({required this.progress});\n\n  @override\n  void paint(Canvas canvas, Size size) {\n    final center = Offset(size.width / 2, size.height / 2);\n    final radius = size.width / 3;\n\n    for (int i = 0; i < 5; i++) {\n      final angle = (progress * 2 * 3.14159) + (i * 2 * 3.14159 / 5);\n      final offset = Offset(\n        center.dx + radius * 0.3 * cos(angle),\n        center.dy + radius * 0.3 * sin(angle),\n      );\n\n      final paint = Paint()\n        ..shader = ui.Gradient.radial(\n          offset,\n          radius * 0.5,\n          [\n            HSLColor.fromAHSL(1.0, (progress * 360 + i * 72) % 360, 0.8, 0.6).toColor(),\n            HSLColor.fromAHSL(0.0, (progress * 360 + i * 72) % 360, 0.8, 0.6).toColor(),\n          ],\n        );\n\n      canvas.drawCircle(offset, radius * 0.4, paint);\n    }\n  }\n\n  @override\n  bool shouldRepaint(covariant ShaderPainter oldDelegate) => true;\n}\n\ndouble cos(double x) => x - (x * x * x) / 6 + (x * x * x * x * x) / 120;\ndouble sin(double x) => 1 - (x * x) / 2 + (x * x * x * x) / 24 - (x * x * x * x * x * x) / 720;"
      }
    ],
    "icon": "building",
    "instructions": "## Shader Effects\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** shader, fragment, custom-paint, gpu\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/widgets/shader_widget.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add shader\nflutter pub add fragment\nflutter pub add custom-paint\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "advanced-slivers",
    "n": "Advanced Slivers",
    "i": "📜",
    "diff": "hard",
    "c": "ui",
    "t": [
      "flutter",
      "slivers",
      "scroll",
      "custom-scroll",
      "nested"
    ],
    "d": "Advanced sliver-based scrolling with nested scroll views and persistent headers.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/sliver_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Advanced Slivers',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.orange, useMaterial3: true),\n      home: const SliverPage(),\n    );\n  }\n}"
      },
      {
        "n": "sliver_page.dart",
        "p": "lib/pages/sliver_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass SliverPage extends StatelessWidget {\n  const SliverPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: CustomScrollView(\n        slivers: [\n          SliverAppBar(\n            expandedHeight: 200,\n            floating: true,\n            pinned: true,\n            snap: true,\n            flexibleSpace: FlexibleSpaceBar(\n              title: const Text('Advanced Slivers'),\n              background: Container(\n                decoration: const BoxDecoration(\n                  gradient: LinearGradient(\n                    colors: [Colors.orange, Colors.deepOrange],\n                    begin: Alignment.topLeft,\n                    end: Alignment.bottomRight,\n                  ),\n                ),\n              ),\n            ),\n          ),\n          SliverPersistentHeader(\n            pinned: true,\n            delegate: _SliverHeaderDelegate(\n              minHeight: 40,\n              maxHeight: 40,\n              child: Container(\n                color: Theme.of(context).colorScheme.surface,\n                padding: const EdgeInsets.symmetric(horizontal: 16),\n                alignment: Alignment.centerLeft,\n                child: const Text('Section Header', style: TextStyle(fontWeight: FontWeight.bold)),\n              ),\n            ),\n          ),\n          SliverList(\n            delegate: SliverChildBuilderDelegate(\n              (context, index) => ListTile(\n                leading: CircleAvatar(child: Text('\\${index + 1}')),\n                title: Text('Item \\${index + 1}'),\n                subtitle: const Text('Subtitle text'),\n              ),\n              childCount: 50,\n            ),\n          ),\n          SliverPersistentHeader(\n            pinned: true,\n            delegate: _SliverHeaderDelegate(\n              minHeight: 40,\n              maxHeight: 40,\n              child: Container(\n                color: Theme.of(context).colorScheme.surface,\n                padding: const EdgeInsets.symmetric(horizontal: 16),\n                alignment: Alignment.centerLeft,\n                child: const Text('Grid Section', style: TextStyle(fontWeight: FontWeight.bold)),\n              ),\n            ),\n          ),\n          SliverGrid(\n            delegate: SliverChildBuilderDelegate(\n              (context, index) => Card(\n                child: Center(child: Text('Grid \\${index + 1}')),\n              ),\n              childCount: 20,\n            ),\n            gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(\n              crossAxisCount: 2,\n              childAspectRatio: 2,\n              mainAxisSpacing: 8,\n              crossAxisSpacing: 8,\n            ),\n          ),\n          const SliverToBoxAdapter(child: SizedBox(height: 80)),\n        ],\n      ),\n    );\n  }\n}\n\nclass _SliverHeaderDelegate extends SliverPersistentHeaderDelegate {\n  final double minHeight;\n  final double maxHeight;\n  final Widget child;\n\n  _SliverHeaderDelegate({\n    required this.minHeight,\n    required this.maxHeight,\n    required this.child,\n  });\n\n  @override\n  double get minExtent => minHeight;\n  @override\n  double get maxExtent => maxHeight;\n\n  @override\n  Widget build(BuildContext context, double shrinkOffset, bool overlapsContent) {\n    return child;\n  }\n\n  @override\n  bool shouldRebuild(covariant _SliverHeaderDelegate oldDelegate) {\n    return child != oldDelegate.child;\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Advanced Slivers\n\n**Kategoriya:** Ui\n**Qiyinlik:** Hard\n**Teglar:** slivers, scroll, custom-scroll, nested\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/pages/sliver_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add slivers\nflutter pub add scroll\nflutter pub add custom-scroll\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "state-machine",
    "n": "State Machine Pattern",
    "i": "🔄",
    "diff": "expert",
    "c": "advanced",
    "t": [
      "flutter",
      "state-machine",
      "pattern",
      "fsm",
      "architecture"
    ],
    "d": "Finite State Machine pattern for complex state transitions and business logic.",
    "f": [
      {
        "n": "state_machine.dart",
        "p": "lib/core/state_machine.dart",
        "c": "abstract class State<S, E> {\n  final S state;\n  final Map<E, Transition<S, E>> transitions;\n\n  State({required this.state, this.transitions = const {}});\n\n  Transition<S, E>? transition(E event) => transitions[event];\n}\n\nclass Transition<S, E> {\n  final S nextState;\n  final void Function()? action;\n\n  Transition({required this.nextState, this.action});\n}\n\nclass StateMachine<S, E> {\n  State<S, E> currentState;\n  final Map<S, State<S, E>> states;\n  final List<Function(S, S)> listeners = [];\n\n  StateMachine({required this.states, required this currentState});\n\n  void addListener(Function(S, S) listener) => listeners.add(listener);\n\n  bool fire(E event) {\n    final transition = currentState.transition(event);\n    if (transition == null) return false;\n\n    final previousState = currentState.state;\n    transition.action?.call();\n    currentState = states[transition.nextState]!;\n\n    for (final listener in listeners) {\n      listener(previousState, currentState.state);\n    }\n    return true;\n  }\n\n  S get state => currentState.state;\n}"
      },
      {
        "n": "order_states.dart",
        "p": "lib/features/order/order_states.dart",
        "c": "import '../../core/state_machine.dart';\n\nenum OrderEvent { submit, confirm, ship, deliver, cancel }\nenum OrderStatus { draft, pending, confirmed, shipped, delivered, cancelled }\n\nclass OrderStateMachine {\n  late final StateMachine<OrderStatus, OrderEvent> _machine;\n\n  OrderStateMachine() {\n    _machine = StateMachine(\n      states: {\n        OrderStatus.draft: State(\n          state: OrderStatus.draft,\n          transitions: {\n            OrderEvent.submit: Transition(nextState: OrderStatus.pending),\n          },\n        ),\n        OrderStatus.pending: State(\n          state: OrderStatus.pending,\n          transitions: {\n            OrderEvent.confirm: Transition(nextState: OrderStatus.confirmed),\n            OrderEvent.cancel: Transition(nextState: OrderStatus.cancelled),\n          },\n        ),\n        OrderStatus.confirmed: State(\n          state: OrderStatus.confirmed,\n          transitions: {\n            OrderEvent.ship: Transition(nextState: OrderStatus.shipped),\n            OrderEvent.cancel: Transition(nextState: OrderStatus.cancelled),\n          },\n        ),\n        OrderStatus.shipped: State(\n          state: OrderStatus.shipped,\n          transitions: {\n            OrderEvent.deliver: Transition(nextState: OrderStatus.delivered),\n          },\n        ),\n        OrderStatus.delivered: State(state: OrderStatus.delivered),\n        OrderStatus.cancelled: State(state: OrderStatus.cancelled),\n      },\n      currentState: State(state: OrderStatus.draft),\n    );\n  }\n\n  OrderStatus get currentStatus => _machine.state;\n\n  bool fireEvent(OrderEvent event) => _machine.fire(event);\n\n  void addListener(Function(OrderStatus, OrderStatus) listener) {\n    _machine.addListener(listener);\n  }\n}"
      }
    ],
    "icon": "building",
    "instructions": "## State Machine Pattern\n\n**Kategoriya:** Advanced\n**Qiyinlik:** Expert\n**Teglar:** state-machine, pattern, fsm, architecture\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/advanced\n```\n\n2. Fayllarni joylashtiring:\n- `lib/core/state_machine.dart`\n- `lib/features/order/order_states.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add state-machine\nflutter pub add pattern\nflutter pub add fsm\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "responsive-layout-advanced",
    "n": "Responsive Layout System",
    "i": "📱",
    "diff": "hard",
    "c": "starter",
    "t": [
      "flutter",
      "responsive",
      "layout",
      "adaptive",
      "breakpoints"
    ],
    "d": "Advanced responsive layout system with breakpoints, adaptive widgets, and fluid typography.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'core/responsive/responsive.dart';\nimport 'pages/dashboard_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Responsive Layout',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.green, useMaterial3: true),\n      home: const DashboardPage(),\n    );\n  }\n}"
      },
      {
        "n": "responsive.dart",
        "p": "lib/core/responsive/responsive.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass Responsive {\n  static double width(BuildContext context) => MediaQuery.of(context).size.width;\n  static double height(BuildContext context) => MediaQuery.of(context).size.height;\n\n  static bool isMobile(BuildContext context) => width(context) < 600;\n  static bool isTablet(BuildContext context) => width(context) >= 600 && width(context) < 1024;\n  static bool isDesktop(BuildContext context) => width(context) >= 1024;\n\n  static T responsive<T>(BuildContext context, {required T mobile, T? tablet, T? desktop}) {\n    if (isDesktop(context)) return desktop ?? tablet ?? mobile;\n    if (isTablet(context)) return tablet ?? mobile;\n    return mobile;\n  }\n}\n\nclass ResponsiveLayout extends StatelessWidget {\n  final Widget mobile;\n  final Widget? tablet;\n  final Widget? desktop;\n\n  const ResponsiveLayout({\n    super.key,\n    required this.mobile,\n    this.tablet,\n    this.desktop,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    return Responsive.responsive(\n      context,\n      mobile: mobile,\n      tablet: tablet,\n      desktop: desktop,\n    );\n  }\n}\n\nclass FluidText extends StatelessWidget {\n  final String text;\n  final TextStyle? style;\n  final double minScale;\n  final double maxScale;\n\n  const FluidText({\n    super.key,\n    required this.text,\n    this.style,\n    this.minScale = 0.8,\n    this.maxScale = 1.5,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    final width = Responsive.width(context);\n    final scale = (width / 1200).clamp(minScale, maxScale);\n\n    return Text(\n      text,\n      style: (style ?? const TextStyle()).copyWith(\n        fontSize: (style?.fontSize ?? 16) * scale,\n      ),\n    );\n  }\n}"
      },
      {
        "n": "dashboard_page.dart",
        "p": "lib/pages/dashboard_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../core/responsive/responsive.dart';\n\nclass DashboardPage extends StatelessWidget {\n  const DashboardPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Dashboard')),\n      body: ResponsiveLayout(\n        mobile: _buildMobileLayout(context),\n        tablet: _buildTabletLayout(context),\n        desktop: _buildDesktopLayout(context),\n      ),\n    );\n  }\n\n  Widget _buildMobileLayout(BuildContext context) {\n    return ListView(\n      padding: const EdgeInsets.all(16),\n      children: [\n        _buildStatCard(context, 'Users', '1,234', Icons.people),\n        _buildStatCard(context, 'Revenue', '\\$12,345', Icons.attach_money),\n        _buildStatCard(context, 'Orders', '567', Icons.shopping_cart),\n        _buildStatCard(context, 'Growth', '+23%', Icons.trending_up),\n      ],\n    );\n  }\n\n  Widget _buildTabletLayout(BuildContext context) {\n    return GridView.count(\n      crossAxisCount: 2,\n      padding: const EdgeInsets.all(16),\n      childAspectRatio: 2,\n      children: [\n        _buildStatCard(context, 'Users', '1,234', Icons.people),\n        _buildStatCard(context, 'Revenue', '\\$12,345', Icons.attach_money),\n        _buildStatCard(context, 'Orders', '567', Icons.shopping_cart),\n        _buildStatCard(context, 'Growth', '+23%', Icons.trending_up),\n      ],\n    );\n  }\n\n  Widget _buildDesktopLayout(BuildContext context) {\n    return Row(\n      children: [\n        NavigationRail(\n          selectedIndex: 0,\n          destinations: const [\n            NavigationRailDestination(icon: Icon(Icons.dashboard), label: Text('Dashboard')),\n            NavigationRailDestination(icon: Icon(Icons.people), label: Text('Users')),\n            NavigationRailDestination(icon: Icon(Icons.settings), label: Text('Settings')),\n          ],\n        ),\n        Expanded(\n          child: GridView.count(\n            crossAxisCount: 4,\n            padding: const EdgeInsets.all(16),\n            childAspectRatio: 1.5,\n            children: [\n              _buildStatCard(context, 'Users', '1,234', Icons.people),\n              _buildStatCard(context, 'Revenue', '\\$12,345', Icons.attach_money),\n              _buildStatCard(context, 'Orders', '567', Icons.shopping_cart),\n              _buildStatCard(context, 'Growth', '+23%', Icons.trending_up),\n            ],\n          ),\n        ),\n      ],\n    );\n  }\n\n  Widget _buildStatCard(BuildContext context, String title, String value, IconData icon) {\n    return Card(\n      margin: const EdgeInsets.all(8),\n      child: Padding(\n        padding: const EdgeInsets.all(16),\n        child: Column(\n          crossAxisAlignment: CrossAxisAlignment.start,\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            Icon(icon, size: 24, color: Theme.of(context).colorScheme.primary),\n            const SizedBox(height: 8),\n            Text(value, style: Theme.of(context).textTheme.headlineSmall),\n            Text(title, style: Theme.of(context).textTheme.bodySmall),\n          ],\n        ),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "rocket",
    "instructions": "## Responsive Layout System\n\n**Kategoriya:** Starter\n**Qiyinlik:** Hard\n**Teglar:** responsive, layout, adaptive, breakpoints\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/starter\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/core/responsive/responsive.dart`\n- `lib/pages/dashboard_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)\nflutter pub add layout\nflutter pub add adaptive\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "n": "Advanced Data Table",
    "id": "data-table-advanced",
    "i": "📊",
    "diff": "hard",
    "c": "ui",
    "t": [
      "flutter",
      "data-table",
      "sorting",
      "pagination",
      "filtering"
    ],
    "d": "Advanced data table with sorting, pagination, filtering, and selection.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/data_table_page.dart';\n\nvoid main() => runApp(const MyApp());\n\nclass MyApp extends StatelessWidget {\n  const MyApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Advanced Data Table',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(colorSchemeSeed: Colors.indigo, useMaterial3: true),\n      home: const DataTablePage(),\n    );\n  }\n}"
      },
      {
        "n": "data_table_page.dart",
        "p": "lib/pages/data_table_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../widgets/advanced_data_table.dart';\n\nclass DataTablePage extends StatefulWidget {\n  const DataTablePage({super.key});\n\n  @override\n  State<DataTablePage> createState() => _DataTablePageState();\n}\n\nclass _DataTablePageState extends State<DataTablePage> {\n  int _currentPage = 0;\n  int _rowsPerPage = 10;\n  String _sortColumn = 'name';\n  bool _sortAscending = true;\n  String _filterQuery = '';\n\n  final List<Map<String, dynamic>> _data = List.generate(\n    100,\n    (i) => {\n      'id': i + 1,\n      'name': 'User \\${i + 1}',\n      'email': 'user\\${i + 1}@example.com',\n      'role': ['Admin', 'User', 'Editor'][i % 3],\n      'status': ['Active', 'Inactive', 'Pending'][i % 3],\n    },\n  );\n\n  List<Map<String, dynamic>> get _filteredData {\n    var data = _data.where((row) {\n      return row['name'].toString().toLowerCase().contains(_filterQuery.toLowerCase()) ||\n          row['email'].toString().toLowerCase().contains(_filterQuery.toLowerCase());\n    }).toList();\n\n    data.sort((a, b) {\n      final aVal = a[_sortColumn] ?? '';\n      final bVal = b[_sortColumn] ?? '';\n      return _sortAscending\n          ? aVal.toString().compareTo(bVal.toString())\n          : bVal.toString().compareTo(aVal.toString());\n    });\n\n    return data;\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    final pageData = _filteredData\n        .skip(_currentPage * _rowsPerPage)\n        .take(_rowsPerPage)\n        .toList();\n\n    return Scaffold(\n      appBar: AppBar(title: const Text('Advanced Data Table')),\n      body: Column(\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: TextField(\n              decoration: const InputDecoration(\n                hintText: 'Filter...',\n                prefixIcon: Icon(Icons.search),\n                border: OutlineInputBorder(),\n              ),\n              onChanged: (v) => setState(() {\n                _filterQuery = v;\n                _currentPage = 0;\n              }),\n            ),\n          ),\n          Expanded(\n            child: SingleChildScrollView(\n              scrollDirection: Axis.horizontal,\n              child: DataTable(\n                sortColumnIndex: ['id', 'name', 'email', 'role', 'status'].indexOf(_sortColumn),\n                sortAscending: _sortAscending,\n                columns: [\n                  DataColumn(label: const Text('ID'), onSort: (i, _) => _sort('id')),\n                  DataColumn(label: const Text('Name'), onSort: (i, _) => _sort('name')),\n                  DataColumn(label: const Text('Email'), onSort: (i, _) => _sort('email')),\n                  DataColumn(label: const Text('Role'), onSort: (i, _) => _sort('role')),\n                  DataColumn(label: const Text('Status'), onSort: (i, _) => _sort('status')),\n                ],\n                rows: pageData.map((row) {\n                  return DataRow(cells: [\n                    DataCell(Text('${row['id']}')),\n                    DataCell(Text('${row['name']}')),\n                    DataCell(Text('${row['email']}')),\n                    DataCell(Chip(label: Text('${row['role']}'))),\n                    DataCell(_buildStatusChip('${row['status']}')),\n                  ]);\n                }).toList(),\n              ),\n            ),\n          ),\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: Row(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                IconButton(\n                  onPressed: _currentPage > 0\n                      ? () => setState(() => _currentPage--)\n                      : null,\n                  icon: const Icon(Icons.chevron_left),\n                ),\n                Text('Page \\${_currentPage + 1} of \\${(_filteredData.length / _rowsPerPage).ceil()}'),\n                IconButton(\n                  onPressed: (_currentPage + 1) * _rowsPerPage < _filteredData.length\n                      ? () => setState(() => _currentPage++)\n                      : null,\n                  icon: const Icon(Icons.chevron_right),\n                ),\n              ],\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  void _sort(String column) {\n    setState(() {\n      if (_sortColumn == column) {\n        _sortAscending = !_sortAscending;\n      } else {\n        _sortColumn = column;\n        _sortAscending = true;\n      }\n    });\n  }\n\n  Widget _buildStatusChip(String status) {\n    final color = status == 'Active'\n        ? Colors.green\n        : status == 'Inactive'\n            ? Colors.red\n            : Colors.orange;\n    return Chip(\n      label: Text(status, style: const TextStyle(fontSize: 12)),\n      backgroundColor: color.withOpacity(0.1),\n      labelStyle: TextStyle(color: color),\n    );\n  }\n}"
      },
      {
        "n": "advanced_data_table.dart",
        "p": "lib/widgets/advanced_data_table.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AdvancedDataTable extends StatelessWidget {\n  final List<Map<String, dynamic>> data;\n  final List<String> columns;\n  final String sortColumn;\n  final bool sortAscending;\n  final Function(String) onSort;\n\n  const AdvancedDataTable({\n    super.key,\n    required this.data,\n    required this.columns,\n    required this.sortColumn,\n    required this.sortAscending,\n    required this.onSort,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    return DataTable(\n      sortColumnIndex: columns.indexOf(sortColumn),\n      sortAscending: sortAscending,\n      columns: columns.map((col) {\n        return DataColumn(\n          label: Text(col.toUpperCase()),\n          onSort: (_, __) => onSort(col),\n        );\n      }).toList(),\n      rows: data.map((row) {\n        return DataRow(\n          cells: columns.map((col) {\n            return DataCell(Text('${row[col] ?? ''}'));\n          }).toList(),\n        );\n      }).toList(),\n    );\n  }\n}"
      }
    ],
    "icon": "palette",
    "instructions": "## Advanced Data Table\n\n**Kategoriya:** Ui\n**Qiyinlik:** Hard\n**Teglar:** data-table, sorting, pagination, filtering\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/ui\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/pages/data_table_page.dart`\n- `lib/widgets/advanced_data_table.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add data-table\nflutter pub add sorting\nflutter pub add pagination\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "push-notifications",
    "n": "Push Notifications (FCM)",
    "i": "🔔",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "fcm",
      "push",
      "notifications",
      "firebase"
    ],
    "d": "Firebase Cloud Messaging push notifications with local notifications.",
    "f": [
      {
        "n": "notification_service.dart",
        "p": "lib/services/notification_service.dart",
        "c": "import 'package:firebase_messaging/firebase_messaging.dart';\nimport 'package:flutter_local_notifications/flutter_local_notifications.dart';\n\nclass NotificationService {\n  final FirebaseMessaging _fcm = FirebaseMessaging.instance;\n  final FlutterLocalNotificationsPlugin _local = FlutterLocalNotificationsPlugin();\n\n  Future<void> initialize() async {\n    await _requestPermission();\n    await _initializeLocalNotifications();\n    _setupFCMListeners();\n    final token = await _fcm.getToken();\n    print('FCM Token: \\$token');\n  }\n\n  Future<void> _requestPermission() async {\n    final settings = await _fcm.requestPermission(\n      alert: true,\n      badge: true,\n      sound: true,\n      provisional: false,\n      criticalAlert: true,\n    );\n    print('Permission: \\${settings.authorizationStatus}');\n  }\n\n  Future<void> _initializeLocalNotifications() async {\n    const androidSettings = AndroidInitializationSettings('@mipmap/ic_launcher');\n    const iosSettings = DarwinInitializationSettings();\n    const settings = InitializationSettings(android: androidSettings, iOS: iosSettings);\n    await _local.initialize(settings);\n  }\n\n  void _setupFCMListeners() {\n    FirebaseMessaging.onMessage.listen((RemoteMessage message) {\n      _showLocalNotification(message);\n    });\n\n    FirebaseMessaging.onMessageOpenedApp.listen((RemoteMessage message) {\n      _handleNotificationTap(message);\n    });\n\n    FirebaseMessaging.onBackgroundMessage(_backgroundHandler);\n  }\n\n  static Future<void> _backgroundHandler(RemoteMessage message) async {\n    print('Background: \\${message.messageId}');\n  }\n\n  void _showLocalNotification(RemoteMessage message) async {\n    final notification = message.notification;\n    if (notification == null) return;\n\n    await _local.show(\n      notification.hashCode,\n      notification.title,\n      notification.body,\n      const NotificationDetails(\n        android: AndroidNotificationDetails(\n          'main_channel',\n          'Main Channel',\n          importance: Importance.high,\n          priority: Priority.high,\n        ),\n      ),\n    );\n  }\n\n  void _handleNotificationTap(RemoteMessage message) {\n    final data = message.data;\n    print('Navigate to: \\${data['route']}');\n  }\n\n  Future<void> subscribeToTopic(String topic) async {\n    await _fcm.subscribeToTopic(topic);\n  }\n\n  Future<void> unsubscribeFromTopic(String topic) async {\n    await _fcm.unsubscribeFromTopic(topic);\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## Push Notifications (FCM)\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** fcm, push, notifications, firebase\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/notification_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add fcm\nflutter pub add push\nflutter pub add notifications\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "cubit-pattern-advanced",
    "n": "Advanced Cubit Pattern",
    "i": "🧊",
    "diff": "hard",
    "c": "state",
    "t": [
      "flutter",
      "cubit",
      "bloc",
      "state-management",
      "flutter_bloc"
    ],
    "d": "Advanced Cubit pattern with complex state, side effects, and state transitions.",
    "f": [
      {
        "n": "counter_cubit.dart",
        "p": "lib/cubits/counter_cubit.dart",
        "c": "import 'package:flutter_bloc/flutter_bloc.dart';\n\nclass CounterState {\n  final int value;\n  final bool isLoading;\n  final String? error;\n  final List<int> history;\n\n  const CounterState({\n    this.value = 0,\n    this.isLoading = false,\n    this.error,\n    this.history = const [],\n  });\n\n  CounterState copyWith({\n    int? value,\n    bool? isLoading,\n    String? error,\n    List<int>? history,\n  }) {\n    return CounterState(\n      value: value ?? this.value,\n      isLoading: isLoading ?? this.isLoading,\n      error: error,\n      history: history ?? this.history,\n    );\n  }\n}\n\nclass CounterCubit extends Cubit<CounterState> {\n  CounterCubit() : super(const CounterState());\n\n  void increment() {\n    emit(state.copyWith(\n      value: state.value + 1,\n      history: [...state.history, state.value + 1],\n    ));\n  }\n\n  void decrement() {\n    emit(state.copyWith(\n      value: state.value - 1,\n      history: [...state.history, state.value - 1],\n    ));\n  }\n\n  void reset() {\n    emit(const CounterState());\n  }\n\n  Future<void> asyncIncrement() async {\n    emit(state.copyWith(isLoading: true));\n    try {\n      await Future.delayed(const Duration(seconds: 1));\n      emit(state.copyWith(\n        value: state.value + 10,\n        isLoading: false,\n        history: [...state.history, state.value + 10],\n      ));\n    } catch (e) {\n      emit(state.copyWith(isLoading: false, error: e.toString()));\n    }\n  }\n}"
      },
      {
        "n": "counter_page.dart",
        "p": "lib/pages/counter_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_bloc/flutter_bloc.dart';\nimport '../cubits/counter_cubit.dart';\n\nclass CounterPage extends StatelessWidget {\n  const CounterPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return BlocProvider(\n      create: (_) => CounterCubit(),\n      child: Scaffold(\n        appBar: AppBar(title: const Text('Advanced Cubit')),\n        body: BlocBuilder<CounterCubit, CounterState>(\n          builder: (context, state) {\n            if (state.isLoading) {\n              return const Center(child: CircularProgressIndicator());\n            }\n            return Column(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                Text('${state.value}', style: Theme.of(context).textTheme.displayLarge),\n                const SizedBox(height: 16),\n                if (state.error != null)\n                  Text(state.error!, style: const TextStyle(color: Colors.red)),\n                const SizedBox(height: 24),\n                Row(\n                  mainAxisAlignment: MainAxisAlignment.center,\n                  children: [\n                    FilledButton.tonal(\n                      onPressed: () => context.read<CounterCubit>().decrement(),\n                      child: const Icon(Icons.remove),\n                    ),\n                    const SizedBox(width: 16),\n                    FilledButton(\n                      onPressed: () => context.read<CounterCubit>().increment(),\n                      child: const Icon(Icons.add),\n                    ),\n                    const SizedBox(width: 16),\n                    OutlinedButton(\n                      onPressed: () => context.read<CounterCubit>().asyncIncrement(),\n                      child: const Text('+10 async'),\n                    ),\n                  ],\n                ),\n                const SizedBox(height: 32),\n                Text('History: ${state.history.join(' → ')}'),\n              ],\n            );\n          },\n        ),\n      ),\n    );\n  }\n}"
      }
    ],
    "icon": "lightning",
    "instructions": "## Advanced Cubit Pattern\n\n**Kategoriya:** State\n**Qiyinlik:** Hard\n**Teglar:** cubit, bloc, state-management, flutter_bloc\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/state\n```\n\n2. Fayllarni joylashtiring:\n- `lib/cubits/counter_cubit.dart`\n- `lib/pages/counter_page.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add cubit\nflutter pub add bloc\nflutter pub add state-management\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "websockets-realtime",
    "n": "WebSocket Realtime",
    "i": "🔌",
    "diff": "hard",
    "c": "api",
    "t": [
      "flutter",
      "websocket",
      "realtime",
      "stream",
      "live"
    ],
    "d": "WebSocket service with auto-reconnection, heartbeat, and message queuing.",
    "f": [
      {
        "n": "websocket_service.dart",
        "p": "lib/services/websocket_service.dart",
        "c": "import 'dart:async';\nimport 'dart:convert';\nimport 'package:web_socket_channel/web_socket_channel.dart';\n\nclass WebSocketService {\n  WebSocketChannel? _channel;\n  final _messageController = StreamController<dynamic>.broadcast();\n  final _connectionController = StreamController<ConnectionState>.broadcast();\n  final _messageQueue = <String>[];\n  Timer? _heartbeatTimer;\n  Timer? _reconnectTimer;\n  bool _isConnected = false;\n  int _reconnectAttempts = 0;\n  static const int _maxReconnectAttempts = 5;\n\n  Stream<dynamic> get messages => _messageController.stream;\n  Stream<ConnectionState> get connectionState => _connectionController.stream;\n  bool get isConnected => _isConnected;\n\n  void connect(String url, {Map<String, String>? headers}) {\n    _connectionController.add(ConnectionState.connecting);\n\n    _channel = WebSocketChannel.connect(\n      Uri.parse(url),\n      headers: headers,\n    );\n\n    _channel!.stream.listen(\n      (data) {\n        _onMessage(data);\n      },\n      onDone: () {\n        _isConnected = false;\n        _connectionController.add(ConnectionState.disconnected);\n        _attemptReconnect(url, headers: headers);\n      },\n      onError: (error) {\n        _isConnected = false;\n        _connectionController.add(ConnectionState.error);\n      },\n    );\n\n    _isConnected = true;\n    _connectionController.add(ConnectionState.connected);\n    _startHeartbeat();\n    _flushQueue();\n  }\n\n  void _onMessage(dynamic data) {\n    final json = jsonDecode(data);\n\n    if (json['type'] == 'pong') {\n      _reconnectAttempts = 0;\n      return;\n    }\n\n    _messageController.add(json);\n  }\n\n  void send(String type, Map<String, dynamic> payload) {\n    final message = jsonEncode({\n      'type': type,\n      'payload': payload,\n      'timestamp': DateTime.now().toIso8601String(),\n    });\n\n    if (_isConnected) {\n      _channel!.sink.add(message);\n    } else {\n      _messageQueue.add(message);\n    }\n  }\n\n  void _startHeartbeat() {\n    _heartbeatTimer?.cancel();\n    _heartbeatTimer = Timer.periodic(const Duration(seconds: 30), (_) {\n      if (_isConnected) {\n        send('ping', {});\n      }\n    });\n  }\n\n  void _flushQueue() {\n    while (_messageQueue.isNotEmpty && _isConnected) {\n      _channel!.sink.add(_messageQueue.removeAt(0));\n    }\n  }\n\n  void _attemptReconnect(String url, {Map<String, String>? headers}) {\n    if (_reconnectAttempts >= _maxReconnectAttempts) return;\n\n    _reconnectTimer?.cancel();\n    final delay = Duration(seconds: (2 * (_reconnectAttempts + 1)));\n    _reconnectAttempts++;\n\n    _reconnectTimer = Timer(delay, () {\n      connect(url, headers: headers);\n    });\n  }\n\n  void disconnect() {\n    _heartbeatTimer?.cancel();\n    _reconnectTimer?.cancel();\n    _channel?.sink.close();\n    _isConnected = false;\n    _connectionController.add(ConnectionState.disconnected);\n  }\n\n  void dispose() {\n    disconnect();\n    _messageController.close();\n    _connectionController.close();\n  }\n}\n\nenum ConnectionState { connecting, connected, disconnected, error }"
      }
    ],
    "icon": "globe",
    "instructions": "## WebSocket Realtime\n\n**Kategoriya:** Api\n**Qiyinlik:** Hard\n**Teglar:** websocket, realtime, stream, live\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/api\n```\n\n2. Fayllarni joylashtiring:\n- `lib/services/websocket_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\nflutter pub add websocket\nflutter pub add realtime\nflutter pub add stream\n\n```\n\n4. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  },
  {
    "id": "google-login",
    "n": "Google-Style Login",
    "i": "google-login",
    "diff": "medium",
    "c": "auth",
    "t": [
      "flutter",
      "google",
      "login",
      "material3",
      "authentication"
    ],
    "d": "Google-Style login sahifasi. Material 3, clean design, social logins.",
    "icon": "lock",
    "instructions": "## Google-Style Login\n\n**Kategoriya:** Auth\n**Qiyinlik:** Medium\n**Teglar:** google, login, material3, authentication\n\n### Qanday ishlatish kerak:\n\n1. Auth papkasini yarating:\n```bash\nmkdir -p lib/pages/auth\n```\n\n2. Fayllarni joylashtiring:\n- `lib/pages/auth/google_login_page.dart`\n- `lib/pages/auth/google_register_page.dart`\n- `lib/services/auth_service.dart`\n\n3. Kerakli paketlarni o'rnating:\n```bash\n# (kerakli paketni o'rnating)_sign_in\nflutter pub add firebase_auth\nflutter pub add flutter_svg\n```\n\n4. Firebase sozlamasini qiling\n\n5. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Xususiyatlar:\n- Google, Apple, GitHub bilan kirish\n- Email/parol bilan kirish\n- Clean Material 3 dizayn\n- Form validation\n- Loading holatlari\n",
    "f": [
      {
        "n": "google_login_page.dart",
        "p": "lib/pages/auth/google_login_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter_svg/flutter_svg.dart';\nimport 'google_register_page.dart';\n\n/// Google-Style Login sahifasi\n/// Material 3 dizayn tizimida yaratilgan\n/// Social login va email/parol bilan kirish imkoniyati\nclass GoogleLoginPage extends StatefulWidget {\n  const GoogleLoginPage({super.key});\n\n  @override\n  State<GoogleLoginPage> createState() => _GoogleLoginPageState();\n}\n\nclass _GoogleLoginPageState extends State<GoogleLoginPage> {\n  // Form kaliti — form validation uchun\n  final _formKey = GlobalKey<FormState>();\n  \n  // Email va parol controllerlari\n  final _emailController = TextEditingController();\n  final _passwordController = TextEditingController();\n  \n  // Parolni ko'rsatish/yashirish uchun\n  bool _obscurePassword = true;\n  \n  // Yuklanmoqda holati\n  bool _isLoading = false;\n\n  @override\n  Widget build(BuildContext context) {\n    final theme = Theme.of(context);\n    \n    return Scaffold(\n      body: SafeArea(\n        child: SingleChildScrollView(\n          padding: const EdgeInsets.symmetric(horizontal: 24),\n          child: Column(\n            crossAxisAlignment: CrossAxisAlignment.stretch,\n            children: [\n              const SizedBox(height: 60),\n              \n              // Logo va sarlavha\n              _buildHeader(theme),\n              \n              const SizedBox(height: 48),\n              \n              // Email/Parol formasi\n              _buildLoginForm(theme),\n              \n              const SizedBox(height: 24),\n              \n              // \"Yoki\" ajratuvchisi\n              _buildDivider(),\n              \n              const SizedBox(height: 24),\n              \n              // Social login tugmalari\n              _buildSocialLogins(theme),\n              \n              const SizedBox(height: 24),\n              \n              // Ro'yxatdan o'tish havolasi\n              _buildRegisterLink(theme),\n            ],\n          ),\n        ),\n      ),\n    );\n  }\n\n  /// Sarlavha — logo va matn\n  Widget _buildHeader(ThemeData theme) {\n    return Column(\n      children: [\n        // Google-Style logotip\n        Container(\n          width: 72,\n          height: 72,\n          decoration: BoxDecoration(\n            color: theme.colorScheme.primaryContainer,\n            shape: BoxShape.circle,\n          ),\n          child: Icon(\n            Icons.flutter_dash,\n            size: 40,\n            color: theme.colorScheme.primary,\n          ),\n        ),\n        const SizedBox(height: 16),\n        Text(\n          'Xush kelibsiz',\n          style: theme.textTheme.headlineMedium?.copyWith(\n            fontWeight: FontWeight.bold,\n          ),\n        ),\n        const SizedBox(height: 8),\n        Text(\n          'Davom etish uchun tizimga kiring',\n          style: theme.textTheme.bodyMedium?.copyWith(\n            color: theme.colorScheme.onSurfaceVariant,\n          ),\n        ),\n      ],\n    );\n  }\n\n  /// Email/Parol formasi\n  Widget _buildLoginForm(ThemeData theme) {\n    return Form(\n      key: _formKey,\n      child: Column(\n        children: [\n          // Email maydoni\n          TextFormField(\n            controller: _emailController,\n            keyboardType: TextInputType.emailAddress,\n            decoration: InputDecoration(\n              labelText: 'Email',\n              prefixIcon: const Icon(Icons.email_outlined),\n              border: OutlineInputBorder(\n                borderRadius: BorderRadius.circular(12),\n              ),\n            ),\n            validator: (value) {\n              if (value == null || value.isEmpty) {\n                return 'Email kiriting';\n              }\n              if (!value.contains('@')) {\n                return \"To'g'ri email kiriting\";\n              }\n              return null;\n            },\n          ),\n          \n          const SizedBox(height: 16),\n          \n          // Parol maydoni\n          TextFormField(\n            controller: _passwordController,\n            obscureText: _obscurePassword,\n            decoration: InputDecoration(\n              labelText: 'Parol',\n              prefixIcon: const Icon(Icons.lock_outlined),\n              suffixIcon: IconButton(\n                icon: Icon(\n                  _obscurePassword\n                      ? Icons.visibility_outlined\n                      : Icons.visibility_off_outlined,\n                ),\n                onPressed: () {\n                  setState(() => _obscurePassword = !_obscurePassword);\n                },\n              ),\n              border: OutlineInputBorder(\n                borderRadius: BorderRadius.circular(12),\n              ),\n            ),\n            validator: (value) {\n              if (value == null || value.isEmpty) {\n                return 'Parol kiriting';\n              }\n              if (value.length < 6) {\n                return 'Kamida 6 ta belgi kiriting';\n              }\n              return null;\n            },\n          ),\n          \n          const SizedBox(height: 12),\n          \n          // Unutdingizmi? havolasi\n          Align(\n            alignment: Alignment.centerRight,\n            child: TextButton(\n              onPressed: () {\n                // Parolni tiklash logikasi\n              },\n              child: const Text('Parolni unutdingizmi?'),\n            ),\n          ),\n          \n          const SizedBox(height: 16),\n          \n          // Kirish tugmasi\n          FilledButton(\n            onPressed: _isLoading ? null : _handleLogin,\n            style: FilledButton.styleFrom(\n              minimumSize: const Size(double.infinity, 48),\n              shape: RoundedRectangleBorder(\n                borderRadius: BorderRadius.circular(12),\n              ),\n            ),\n            child: _isLoading\n                ? const SizedBox(\n                    width: 20,\n                    height: 20,\n                    child: CircularProgressIndicator(strokeWidth: 2),\n                  )\n                : const Text('Kirish'),\n          ),\n        ],\n      ),\n    );\n  }\n\n  /// \"Yoki\" ajratuvchisi\n  Widget _buildDivider() {\n    return Row(\n      children: [\n        const Expanded(child: Divider()),\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 16),\n          child: Text(\n            'YOKI',\n            style: TextStyle(\n              color: Colors.grey[500],\n              fontSize: 12,\n              fontWeight: FontWeight.w500,\n            ),\n          ),\n        ),\n        const Expanded(child: Divider()),\n      ],\n    );\n  }\n\n  /// Social login tugmalari\n  Widget _buildSocialLogins(ThemeData theme) {\n    return Column(\n      children: [\n        // Google bilan kirish\n        _SocialLoginButton(\n          label: 'Google bilan kirish',\n          icon: Icons.g_mobiledata,\n          color: Colors.red,\n          onPressed: _handleGoogleLogin,\n        ),\n        \n        const SizedBox(height: 12),\n        \n        // Apple bilan kirish\n        _SocialLoginButton(\n          label: 'Apple bilan kirish',\n          icon: Icons.apple,\n          color: Colors.black,\n          onPressed: _handleAppleLogin,\n        ),\n        \n        const SizedBox(height: 12),\n        \n        // GitHub bilan kirish\n        _SocialLoginButton(\n          label: 'GitHub bilan kirish',\n          icon: Icons.code,\n          color: Colors.grey[800]!,\n          onPressed: _handleGitHubLogin,\n        ),\n      ],\n    );\n  }\n\n  /// Ro'yxatdan o'tish havolasi\n  Widget _buildRegisterLink(ThemeData theme) {\n    return Row(\n      mainAxisAlignment: MainAxisAlignment.center,\n      children: [\n        Text(\n          'Hisobingiz yo'qmi? ',\n          style: theme.textTheme.bodyMedium,\n        ),\n        TextButton(\n          onPressed: () {\n            Navigator.push(\n              context,\n              MaterialPageRoute(\n                builder: (_) => const GoogleRegisterPage(),\n              ),\n            );\n          },\n          child: const Text('Ro'yxatdan o'ting'),\n        ),\n      ],\n    );\n  }\n\n  // --- Logik metodlar ---\n\n  /// Email/parol bilan kirish\n  Future<void> _handleLogin() async {\n    if (!_formKey.currentState!.validate()) return;\n    \n    setState(() => _isLoading = true);\n    \n    try {\n      // API chaqirish\n      await Future.delayed(const Duration(seconds: 2));\n      \n      if (mounted) {\n        // Muvaffaqiyatli kirish — asosiy sahifaga o'tish\n        Navigator.pushReplacementNamed(context, '/home');\n      }\n    } catch (e) {\n      if (mounted) {\n        ScaffoldMessenger.of(context).showSnackBar(\n          SnackBar(content: Text('Xatolik: \\$e')),\n        );\n      }\n    } finally {\n      if (mounted) setState(() => _isLoading = false);\n    }\n  }\n\n  /// Google bilan kirish\n  Future<void> _handleGoogleLogin() async {\n    setState(() => _isLoading = true);\n    try {\n      // Google Sign-In logikasi\n      await Future.delayed(const Duration(seconds: 2));\n      if (mounted) Navigator.pushReplacementNamed(context, '/home');\n    } finally {\n      if (mounted) setState(() => _isLoading = false);\n    }\n  }\n\n  /// Apple bilan kirish\n  Future<void> _handleAppleLogin() async {\n    setState(() => _isLoading = true);\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      if (mounted) Navigator.pushReplacementNamed(context, '/home');\n    } finally {\n      if (mounted) setState(() => _isLoading = false);\n    }\n  }\n\n  /// GitHub bilan kirish\n  Future<void> _handleGitHubLogin() async {\n    setState(() => _isLoading = true);\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      if (mounted) Navigator.pushReplacementNamed(context, '/home');\n    } finally {\n      if (mounted) setState(() => _isLoading = false);\n    }\n  }\n\n  @override\n  void dispose() {\n    _emailController.dispose();\n    _passwordController.dispose();\n    super.dispose();\n  }\n}\n\n/// Social login tugmasi — qayta ishlatiladigan widget\nclass _SocialLoginButton extends StatelessWidget {\n  final String label;\n  final IconData icon;\n  final Color color;\n  final VoidCallback onPressed;\n\n  const _SocialLoginButton({\n    required this.label,\n    required this.icon,\n    required this.color,\n    required this.onPressed,\n  });\n\n  @override\n  Widget build(BuildContext context) {\n    return OutlinedButton.icon(\n      onPressed: onPressed,\n      style: OutlinedButton.styleFrom(\n        minimumSize: const Size(double.infinity, 48),\n        shape: RoundedRectangleBorder(\n          borderRadius: BorderRadius.circular(12),\n        ),\n        side: BorderSide(color: Colors.grey[300]!),\n      ),\n      icon: Icon(icon, color: color),\n      label: Text(\n        label,\n        style: TextStyle(color: Colors.grey[800]),\n      ),\n    );\n  }\n}"
      },
      {
        "n": "google_register_page.dart",
        "p": "lib/pages/auth/google_register_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'google_login_page.dart';\n\n/// Google-Style Ro'yxatdan o'tish sahifasi\n/// To'liq form validation bilan\nclass GoogleRegisterPage extends StatefulWidget {\n  const GoogleRegisterPage({super.key});\n\n  @override\n  State<GoogleRegisterPage> createState() => _GoogleRegisterPageState();\n}\n\nclass _GoogleRegisterPageState extends State<GoogleRegisterPage> {\n  final _formKey = GlobalKey<FormState>();\n  final _nameController = TextEditingController();\n  final _emailController = TextEditingController();\n  final _passwordController = TextEditingController();\n  final _confirmPasswordController = TextEditingController();\n  \n  bool _obscurePassword = true;\n  bool _obscureConfirm = true;\n  bool _isLoading = false;\n  bool _agreeToTerms = false;\n\n  @override\n  Widget build(BuildContext context) {\n    final theme = Theme.of(context);\n    \n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('Ro'yxatdan o'tish'),\n        centerTitle: true,\n      ),\n      body: SafeArea(\n        child: SingleChildScrollView(\n          padding: const EdgeInsets.symmetric(horizontal: 24),\n          child: Form(\n            key: _formKey,\n            child: Column(\n              crossAxisAlignment: CrossAxisAlignment.stretch,\n              children: [\n                const SizedBox(height: 32),\n                \n                // Sarlavha\n                Text(\n                  'Yangi hisob yarating',\n                  style: theme.textTheme.headlineSmall?.copyWith(\n                    fontWeight: FontWeight.bold,\n                  ),\n                ),\n                const SizedBox(height: 8),\n                Text(\n                  'Barcha xususiyatlardan foydalanish uchun ro'yxatdan o'ting',\n                  style: theme.textTheme.bodyMedium?.copyWith(\n                    color: theme.colorScheme.onSurfaceVariant,\n                  ),\n                ),\n                \n                const SizedBox(height: 32),\n                \n                // Ism maydoni\n                TextFormField(\n                  controller: _nameController,\n                  textCapitalization: TextCapitalization.words,\n                  decoration: InputDecoration(\n                    labelText: 'To'liq ism',\n                    prefixIcon: const Icon(Icons.person_outlined),\n                    border: OutlineInputBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                  ),\n                  validator: (value) {\n                    if (value == null || value.isEmpty) {\n                      return 'Ismingizni kiriting';\n                    }\n                    if (value.length < 2) {\n                      return 'Kamida 2 ta belgi kiriting';\n                    }\n                    return null;\n                  },\n                ),\n                \n                const SizedBox(height: 16),\n                \n                // Email maydoni\n                TextFormField(\n                  controller: _emailController,\n                  keyboardType: TextInputType.emailAddress,\n                  decoration: InputDecoration(\n                    labelText: 'Email',\n                    prefixIcon: const Icon(Icons.email_outlined),\n                    border: OutlineInputBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                  ),\n                  validator: (value) {\n                    if (value == null || value.isEmpty) {\n                      return 'Email kiriting';\n                    }\n                    if (!value.contains('@') || !value.contains('.')) {\n                      return \"To'g'ri email kiriting\";\n                    }\n                    return null;\n                  },\n                ),\n                \n                const SizedBox(height: 16),\n                \n                // Parol maydoni\n                TextFormField(\n                  controller: _passwordController,\n                  obscureText: _obscurePassword,\n                  decoration: InputDecoration(\n                    labelText: 'Parol',\n                    prefixIcon: const Icon(Icons.lock_outlined),\n                    suffixIcon: IconButton(\n                      icon: Icon(\n                        _obscurePassword\n                            ? Icons.visibility_outlined\n                            : Icons.visibility_off_outlined,\n                      ),\n                      onPressed: () {\n                        setState(() => _obscurePassword = !_obscurePassword);\n                      },\n                    ),\n                    border: OutlineInputBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                  ),\n                  validator: (value) {\n                    if (value == null || value.isEmpty) {\n                      return 'Parol kiriting';\n                    }\n                    if (value.length < 8) {\n                      return 'Kamida 8 ta belgi kiriting';\n                    }\n                    if (!RegExp(r'(?=.*[A-Z])').hasMatch(value)) {\n                      return 'Kamida 1 ta katta harf kiriting';\n                    }\n                    if (!RegExp(r'(?=.*[0-9])').hasMatch(value)) {\n                      return 'Kamida 1 ta raqam kiriting';\n                    }\n                    return null;\n                  },\n                ),\n                \n                const SizedBox(height: 16),\n                \n                // Parolni tasdiqlash\n                TextFormField(\n                  controller: _confirmPasswordController,\n                  obscureText: _obscureConfirm,\n                  decoration: InputDecoration(\n                    labelText: 'Parolni tasdiqlang',\n                    prefixIcon: const Icon(Icons.lock_outlined),\n                    suffixIcon: IconButton(\n                      icon: Icon(\n                        _obscureConfirm\n                            ? Icons.visibility_outlined\n                            : Icons.visibility_off_outlined,\n                      ),\n                      onPressed: () {\n                        setState(() => _obscureConfirm = !_obscureConfirm);\n                      },\n                    ),\n                    border: OutlineInputBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                  ),\n                  validator: (value) {\n                    if (value != _passwordController.text) {\n                      return 'Parollar mos emas';\n                    }\n                    return null;\n                  },\n                ),\n                \n                const SizedBox(height: 16),\n                \n                // Shartlar va qoidalar\n                Row(\n                  children: [\n                    Checkbox(\n                      value: _agreeToTerms,\n                      onChanged: (value) {\n                        setState(() => _agreeToTerms = value ?? false);\n                      },\n                    ),\n                    Expanded(\n                      child: Text.rich(\n                        TextSpan(\n                          text: 'Men ',\n                          style: theme.textTheme.bodySmall,\n                          children: [\n                            TextSpan(\n                              text: 'Foydalanish shartlari',\n                              style: TextStyle(\n                                color: theme.colorScheme.primary,\n                                fontWeight: FontWeight.w500,\n                              ),\n                            ),\n                            const TextSpan(text: ' va '),\n                            TextSpan(\n                              text: 'Maxfiylik siyosati',\n                              style: TextStyle(\n                                color: theme.colorScheme.primary,\n                                fontWeight: FontWeight.w500,\n                              ),\n                            ),\n                            const TextSpan(text: ' ga roziman'),\n                          ],\n                        ),\n                      ),\n                    ),\n                  ],\n                ),\n                \n                const SizedBox(height: 24),\n                \n                // Ro'yxatdan o'tish tugmasi\n                FilledButton(\n                  onPressed: (_isLoading || !_agreeToTerms)\n                      ? null\n                      : _handleRegister,\n                  style: FilledButton.styleFrom(\n                    minimumSize: const Size(double.infinity, 48),\n                    shape: RoundedRectangleBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                  ),\n                  child: _isLoading\n                      ? const SizedBox(\n                          width: 20,\n                          height: 20,\n                          child: CircularProgressIndicator(strokeWidth: 2),\n                        )\n                      : const Text('Ro'yxatdan o'tish'),\n                ),\n                \n                const SizedBox(height: 24),\n                \n                // Tizimga kirish havolasi\n                Row(\n                  mainAxisAlignment: MainAxisAlignment.center,\n                  children: [\n                    const Text('Hisobingiz bormi? '),\n                    TextButton(\n                      onPressed: () => Navigator.pop(context),\n                      child: const Text('Tizimga kiring'),\n                    ),\n                  ],\n                ),\n                \n                const SizedBox(height: 32),\n              ],\n            ),\n          ),\n        ),\n      ),\n    );\n  }\n\n  /// Ro'yxatdan o'tish logikasi\n  Future<void> _handleRegister() async {\n    if (!_formKey.currentState!.validate()) return;\n    \n    setState(() => _isLoading = true);\n    \n    try {\n      // API chaqirish\n      await Future.delayed(const Duration(seconds: 2));\n      \n      if (mounted) {\n        // Muvaffaqiyatli ro'yxatdan o'tish\n        ScaffoldMessenger.of(context).showSnackBar(\n          const SnackBar(\n            content: Text('Muvaffaqiyatli ro'yxatdan o'tingiz!'),\n          ),\n        );\n        \n        // Tizimga kirish sahifasiga qaytish\n        Navigator.pushReplacement(\n          context,\n          MaterialPageRoute(builder: (_) => const GoogleLoginPage()),\n        );\n      }\n    } catch (e) {\n      if (mounted) {\n        ScaffoldMessenger.of(context).showSnackBar(\n          SnackBar(content: Text('Xatolik: \\$e')),\n        );\n      }\n    } finally {\n      if (mounted) setState(() => _isLoading = false);\n    }\n  }\n\n  @override\n  void dispose() {\n    _nameController.dispose();\n    _emailController.dispose();\n    _passwordController.dispose();\n    _confirmPasswordController.dispose();\n    super.dispose();\n  }\n}"
      },
      {
        "n": "auth_service.dart",
        "p": "lib/services/auth_service.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Autentifikatsiya xizmati\n/// Barcha kirish/chiqish logikasini boshqaradi\nclass AuthService extends ChangeNotifier {\n  bool _isLoading = false;\n  bool _isAuthenticated = false;\n  String? _errorMessage;\n  \n  // Getterlar\n  bool get isLoading => _isLoading;\n  bool get isAuthenticated => _isAuthenticated;\n  String? get errorMessage => _errorMessage;\n\n  /// Email/parol bilan kirish\n  Future<bool> signInWithEmail(String email, String password) async {\n    _isLoading = true;\n    _errorMessage = null;\n    notifyListeners();\n\n    try {\n      // API chaqirish\n      await Future.delayed(const Duration(seconds: 2));\n      \n      // Simulyatsiya — haqiqiy loyihada API chaqiring\n      if (email.isEmpty || password.isEmpty) {\n        throw Exception('Email va parol kiritish shart');\n      }\n\n      _isAuthenticated = true;\n      _isLoading = false;\n      notifyListeners();\n      return true;\n    } catch (e) {\n      _errorMessage = e.toString();\n      _isLoading = false;\n      notifyListeners();\n      return false;\n    }\n  }\n\n  /// Email/parol bilan ro'yxatdan o'tish\n  Future<bool> signUpWithEmail({\n    required String name,\n    required String email,\n    required String password,\n  }) async {\n    _isLoading = true;\n    _errorMessage = null;\n    notifyListeners();\n\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      \n      _isAuthenticated = true;\n      _isLoading = false;\n      notifyListeners();\n      return true;\n    } catch (e) {\n      _errorMessage = e.toString();\n      _isLoading = false;\n      notifyListeners();\n      return false;\n    }\n  }\n\n  /// Google bilan kirish\n  Future<bool> signInWithGoogle() async {\n    _isLoading = true;\n    notifyListeners();\n\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      _isAuthenticated = true;\n      _isLoading = false;\n      notifyListeners();\n      return true;\n    } catch (e) {\n      _errorMessage = e.toString();\n      _isLoading = false;\n      notifyListeners();\n      return false;\n    }\n  }\n\n  /// Tizimdan chiqish\n  Future<void> signOut() async {\n    _isLoading = true;\n    notifyListeners();\n\n    await Future.delayed(const Duration(seconds: 1));\n    \n    _isAuthenticated = false;\n    _isLoading = false;\n    notifyListeners();\n  }\n\n  /// Parolni tiklash\n  Future<bool> resetPassword(String email) async {\n    _isLoading = true;\n    notifyListeners();\n\n    try {\n      await Future.delayed(const Duration(seconds: 2));\n      _isLoading = false;\n      notifyListeners();\n      return true;\n    } catch (e) {\n      _errorMessage = e.toString();\n      _isLoading = false;\n      notifyListeners();\n      return false;\n    }\n  }\n}"
      }
    ]
  },
  {
    "id": "instagram-clone",
    "n": "Instagram Clone",
    "i": "instagram",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "instagram",
      "feed",
      "stories",
      "profile",
      "social"
    ],
    "d": "Instagram ilovasining to'liq kloni. Feed, Stories, Profile, Search sahifalari.",
    "icon": "globe",
    "instructions": "## Instagram Clone — Qanday ishlatish\n\n### Bu nima?\nInstagram ilovasining Flutter versiyasi. Feed (rasmlar lentasi), Hikoyalar (Stories), Profil va Qidiruv sahifalari bor.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create instagram_clone\ncd instagram_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models lib/services\n```\n\n**3-qadam: Fayllarni joylashtiring**\nQuyidagi fayllarni ko'chirib boring:\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/feed_page.dart` — rasmlar lentali\n- `lib/pages/stories_page.dart` — hikoyalar\n- `lib/pages/profile_page.dart` — profil sahifasi\n- `lib/pages/search_page.dart` — qidiruv\n\n**4-qadam: Paketlarni o'rnating**\n```\nflutter pub add cached_network_image\nflutter pub add shimmer\n```\n\n**5-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Rasm manbasini o'zgartirish uchun `NetworkImage` dagi URL ni almashtiring\n- ranglarni `ThemeData` da o'zgartiring\n- Foydalanuvchi nomlarini ro'yxatdan o'zgartiring\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/feed_page.dart';\nimport 'pages/search_page.dart';\nimport 'pages/reels_page.dart';\nimport 'pages/profile_page.dart';\n\n/// Instagram Clone — Asosiy fayl\n/// Barcha sahifalar bottom navigation orqali boshqariladi\nvoid main() => runApp(const InstagramClone());\n\nclass InstagramClone extends StatelessWidget {\n  const InstagramClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Instagram',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        brightness: Brightness.dark,\n        colorSchemeSeed: Colors.pink,\n      ),\n      home: const MainScreen(),\n    );\n  }\n}\n\n/// Asosiy ekran — bottom navigation bilan\nclass MainScreen extends StatefulWidget {\n  const MainScreen({super.key});\n\n  @override\n  State<MainScreen> createState() => _MainScreenState();\n}\n\nclass _MainScreenState extends State<MainScreen> {\n  int _currentIndex = 0;\n\n  // Sahifalar ro'yxati\n  final List<Widget> _pages = [\n    const FeedPage(),\n    const SearchPage(),\n    const ReelsPage(),\n    const ProfilePage(),\n  ];\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      // Sahifalar\n      body: _pages[_currentIndex],\n\n      // Pastki navigatsiya paneli\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (i) => setState(() => _currentIndex = i),\n        destinations: const [\n          NavigationDestination(\n            icon: Icon(Icons.home_outlined),\n            selectedIcon: Icon(Icons.home),\n            label: 'Home',\n          ),\n          NavigationDestination(\n            icon: Icon(Icons.search),\n            selectedIcon: Icon(Icons.search),\n            label: 'Search',\n          ),\n          NavigationDestination(\n            icon: Icon(Icons.video_collection_outlined),\n            selectedIcon: Icon(Icons.video_collection),\n            label: 'Reels',\n          ),\n          NavigationDestination(\n            icon: Icon(Icons.person_outline),\n            selectedIcon: Icon(Icons.person),\n            label: 'Profile',\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "feed_page.dart",
        "p": "lib/pages/feed_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Feed sahifasi — Instagramdagi rasmlar lentali\n/// Stories tepada, postlar pastda joylashgan\nclass FeedPage extends StatelessWidget {\n  const FeedPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('Instagram'),\n        centerTitle: false,\n        actions: [\n          IconButton(onPressed: () {}, icon: const Icon(Icons.favorite_border)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.send_outlined)),\n        ],\n      ),\n      body: Column(\n        children: [\n          // Stories qismi — tepada horizontal ro'yxat\n          SizedBox(\n            height: 100,\n            child: ListView.builder(\n              scrollDirection: Axis.horizontal,\n              padding: const EdgeInsets.symmetric(horizontal: 12),\n              itemCount: 10,\n              itemBuilder: (context, index) {\n                return _buildStoryAvatar(index);\n              },\n            ),\n          ),\n\n          const Divider(height: 1),\n\n          // Postlar lentali\n          Expanded(\n            child: ListView.builder(\n              itemCount: 5,\n              itemBuilder: (context, index) {\n                return _buildPost(index);\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  /// Bitta story avatarini yasaydi\n  Widget _buildStoryAvatar(int index) {\n    return Padding(\n      padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 8),\n      child: Column(\n        children: [\n          // Doira rasm — chegarasi rangli\n          Container(\n            width: 64,\n            height: 64,\n            decoration: BoxDecoration(\n              shape: BoxShape.circle,\n              gradient: LinearGradient(\n                colors: [Colors.pink, Colors.orange, Colors.purple],\n              ),\n            ),\n            padding: const EdgeInsets.all(3),\n            child: Container(\n              decoration: BoxDecoration(\n                shape: BoxShape.circle,\n                color: Colors.grey[800],\n              ),\n              padding: const EdgeInsets.all(2),\n              child: CircleAvatar(\n                backgroundColor: Colors.grey[600],\n                child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n              ),\n            ),\n          ),\n          const SizedBox(height: 4),\n          Text(\n            'User \\${index + 1}',\n            style: const TextStyle(fontSize: 11),\n            overflow: TextOverflow.ellipsis,\n          ),\n        ],\n      ),\n    );\n  }\n\n  /// Bitta postni yasaydi\n  Widget _buildPost(int index) {\n    return Column(\n      crossAxisAlignment: CrossAxisAlignment.start,\n      children: [\n        // Post sarlavhasi — avatar, nom, menyu\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),\n          child: Row(\n            children: [\n              CircleAvatar(\n                radius: 16,\n                backgroundColor: Colors.grey[600],\n                child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n              ),\n              const SizedBox(width: 10),\n              Expanded(\n                child: Column(\n                  crossAxisAlignment: CrossAxisAlignment.start,\n                  children: [\n                    Text('user_\\${index + 1}', style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 13)),\n                    const Text('Toshkent, Uzbekistan', style: TextStyle(fontSize: 11)),\n                  ],\n                ),\n              ),\n              IconButton(onPressed: () {}, icon: const Icon(Icons.more_vert, size: 20)),\n            ],\n          ),\n        ),\n\n        // Post rasmi — placeholder\n        Container(\n          height: 300,\n          width: double.infinity,\n          color: Colors.grey[800],\n          child: Center(\n            child: Icon(Icons.image, size: 64, color: Colors.grey[600]),\n          ),\n        ),\n\n        // Reaksiya tugmalari\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),\n          child: Row(\n            children: [\n              IconButton(onPressed: () {}, icon: const Icon(Icons.favorite_border, size: 26)),\n              IconButton(onPressed: () {}, icon: const Icon(Icons.chat_bubble_outline, size: 24)),\n              IconButton(onPressed: () {}, icon: const Icon(Icons.send_outlined, size: 24)),\n              const Spacer(),\n              IconButton(onPressed: () {}, icon: const Icon(Icons.bookmark_border, size: 24)),\n            ],\n          ),\n        ),\n\n        // Layklar soni\n        const Padding(\n          padding: EdgeInsets.symmetric(horizontal: 16),\n          child: Text('1,234 likes', style: TextStyle(fontWeight: FontWeight.bold, fontSize: 13)),\n        ),\n\n        // Izoh\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 4),\n          child: RichText(\n            text: TextSpan(\n              children: [\n                const TextSpan(text: 'user_1 ', style: TextStyle(fontWeight: FontWeight.bold, fontSize: 13)),\n                TextSpan(text: 'Beautiful day! #flutter #dart #coding', style: TextStyle(fontSize: 13, color: Colors.grey[300])),\n              ],\n            ),\n          ),\n        ),\n\n        const SizedBox(height: 12),\n      ],\n    );\n  }\n}"
      },
      {
        "n": "search_page.dart",
        "p": "lib/pages/search_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Qidiruv sahifasi — Instagramdagi Search/Explore\n/// Qidiruv maydoni va kategoriyali grid\nclass SearchPage extends StatelessWidget {\n  const SearchPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: TextField(\n          decoration: InputDecoration(\n            hintText: 'Qidirish...',\n            prefixIcon: const Icon(Icons.search, size: 20),\n            filled: true,\n            fillColor: Colors.grey[800],\n            contentPadding: const EdgeInsets.symmetric(vertical: 8),\n            border: OutlineInputBorder(\n              borderRadius: BorderRadius.circular(10),\n              borderSide: BorderSide.none,\n            ),\n          ),\n        ),\n      ),\n      body: Column(\n        children: [\n          // Kategoriyalar — yuqorida horizontal chip\n          SizedBox(\n            height: 40,\n            child: ListView(\n              scrollDirection: Axis.horizontal,\n              padding: const EdgeInsets.symmetric(horizontal: 12),\n              children: [\"Barchasi\", \"Trendlar\", \"Musiqa\", \"O'yinlar\", \"Sayohat\"]\n                  .map((c) => Padding(\n                        padding: const EdgeInsets.symmetric(horizontal: 4),\n                        child: Chip(\n                          label: Text(c, style: const TextStyle(fontSize: 12)),\n                          backgroundColor: Colors.grey[800],\n                        ),\n                      ))\n                  .toList(),\n            ),\n          ),\n\n          // Grid — rasmlar to'ridi\n          Expanded(\n            child: GridView.builder(\n              padding: const EdgeInsets.all(4),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(\n                crossAxisCount: 3,\n                mainAxisSpacing: 4,\n                crossAxisSpacing: 4,\n              ),\n              itemCount: 30,\n              itemBuilder: (context, index) {\n                return Container(\n                  color: Colors.grey[800],\n                  child: Center(\n                    child: Icon(Icons.image, color: Colors.grey[600]),\n                  ),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "reels_page.dart",
        "p": "lib/pages/reels_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Reels sahifasi — qisqa videolar\n/// To'liq ekran video ko'rinishi\nclass ReelsPage extends StatelessWidget {\n  const ReelsPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      backgroundColor: Colors.black,\n      body: Stack(\n        children: [\n          // Video — to'liq ekran\n          Container(\n            color: Colors.grey[900],\n            child: const Center(\n              child: Icon(Icons.play_circle_outline, size: 80, color: Colors.white54),\n            ),\n          ),\n\n          // O'ng tomon — reaksiya tugmalari\n          Positioned(\n            right: 12,\n            bottom: 100,\n            child: Column(\n              children: [\n                _buildActionButton(Icons.favorite, '12.5K'),\n                const SizedBox(height: 16),\n                _buildActionButton(Icons.chat_bubble, '342'),\n                const SizedBox(height: 16),\n                _buildActionButton(Icons.send, 'Share'),\n                const SizedBox(height: 16),\n                _buildActionButton(Icons.more_vert, ''),\n              ],\n            ),\n          ),\n\n          // Pastki qism — foydalanuvchi va izoh\n          Positioned(\n            left: 12,\n            bottom: 20,\n            right: 60,\n            child: Column(\n              crossAxisAlignment: CrossAxisAlignment.start,\n              children: [\n                Row(\n                  children: [\n                    const CircleAvatar(radius: 16, backgroundColor: Colors.white24),\n                    const SizedBox(width: 8),\n                    const Text('@username', style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),\n                    const SizedBox(width: 8),\n                    OutlinedButton(\n                      onPressed: () {},\n                      style: OutlinedButton.styleFrom(side: const BorderSide(color: Colors.white)),\n                      child: const Text('Follow', style: TextStyle(color: Colors.white, fontSize: 12)),\n                    ),\n                  ],\n                ),\n                const SizedBox(height: 8),\n                const Text(\n                  'Beautiful video! #flutter #reels #coding',\n                  style: TextStyle(color: Colors.white, fontSize: 13),\n                ),\n              ],\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildActionButton(IconData icon, String label) {\n    return Column(\n      children: [\n        Icon(icon, color: Colors.white, size: 28),\n        if (label.isNotEmpty)\n          Text(label, style: const TextStyle(color: Colors.white, fontSize: 11)),\n      ],\n    );\n  }\n}"
      },
      {
        "n": "profile_page.dart",
        "p": "lib/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Profil sahifasi — foydalanuvchi profili\n/// Avatar, bio, postlar gridi\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('username'),\n        actions: [\n          IconButton(onPressed: () {}, icon: const Icon(Icons.add_box_outlined)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.menu)),\n        ],\n      ),\n      body: SingleChildScrollView(\n        child: Column(\n          children: [\n            // Profil ma'lumotlari\n            Padding(\n              padding: const EdgeInsets.all(16),\n              child: Row(\n                children: [\n                  // Avatar\n                  const CircleAvatar(\n                    radius: 40,\n                    backgroundColor: Colors.pink,\n                    child: Text('U', style: TextStyle(fontSize: 32, color: Colors.white)),\n                  ),\n                  const SizedBox(width: 20),\n\n                  // Statistika — postlar, obunachilar, obuna\n                  Expanded(\n                    child: Row(\n                      mainAxisAlignment: MainAxisAlignment.spaceEvenly,\n                      children: [\n                        _buildStat('128', 'Posts'),\n                        _buildStat('1.2K', 'Followers'),\n                        _buildStat('345', 'Following'),\n                      ],\n                    ),\n                  ),\n                ],\n              ),\n            ),\n\n            // Bio\n            const Padding(\n              padding: EdgeInsets.symmetric(horizontal: 16),\n              child: Align(\n                alignment: Alignment.centerLeft,\n                child: Column(\n                  crossAxisAlignment: CrossAxisAlignment.start,\n                  children: [\n                    Text('Flutter Developer', style: TextStyle(fontWeight: FontWeight.bold)),\n                    Text('Building beautiful apps with Flutter'),\n                    Text('t.me/username', style: TextStyle(color: Colors.blue)),\n                  ],\n                ),\n              ),\n            ),\n\n            const SizedBox(height: 12),\n\n            // Tugmalar\n            Padding(\n              padding: const EdgeInsets.symmetric(horizontal: 16),\n              child: Row(\n                children: [\n                  Expanded(\n                    child: FilledButton(onPressed: () {}, child: const Text('Edit Profile')),\n                  ),\n                  const SizedBox(width: 8),\n                  Expanded(\n                    child: OutlinedButton(onPressed: () {}, child: const Text('Share Profile')),\n                  ),\n                ],\n              ),\n            ),\n\n            const SizedBox(height: 16),\n\n            // Postlar gridi\n            GridView.builder(\n              shrinkWrap: true,\n              physics: const NeverScrollableScrollPhysics(),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(\n                crossAxisCount: 3,\n                mainAxisSpacing: 4,\n                crossAxisSpacing: 4,\n              ),\n              itemCount: 21,\n              itemBuilder: (context, index) {\n                return Container(\n                  color: Colors.grey[800],\n                  child: Center(child: Icon(Icons.image, color: Colors.grey[600])),\n                );\n              },\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n\n  Widget _buildStat(String count, String label) {\n    return Column(\n      children: [\n        Text(count, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 16)),\n        Text(label, style: const TextStyle(fontSize: 12, color: Colors.grey)),\n      ],\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "whatsapp-clone",
    "n": "WhatsApp Clone",
    "i": "whatsapp",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "whatsapp",
      "chat",
      "calls",
      "status",
      "messaging"
    ],
    "d": "WhatsApp ilovasining kloni. Chatlar, Qo'ng'iroqlar, Holat va Kamera sahifalari.",
    "icon": "globe",
    "instructions": "## WhatsApp Clone — Qanday ishlatish\n\n### Bu nima?\nWhatsApp ilovasining Flutter versiyasi. Chatlar ro'yxati, Qo'ng'iroqlar tarixi, Holat (Status) sahifasi va Kamera mavjud.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create whatsapp_clone\ncd whatsapp_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models\n```\n\n**3-qadam: Fayllarni joylashtiring**\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/chats_page.dart` — chatlar ro'yxati\n- `lib/pages/calls_page.dart` — qo'ng'iroqlar\n- `lib/pages/status_page.dart` — holatlar\n\n**4-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Chat nomlarini ro'yxatdan o'zgartiring\n- Ranglarni `ThemeData` da o'zgartiring (yashil rang WhatsApp rangi)\n- Yangi sahifalar qo'shing\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/chats_page.dart';\nimport 'pages/calls_page.dart';\nimport 'pages/status_page.dart';\n\n/// WhatsApp Clone — Asosiy fayl\nvoid main() => runApp(const WhatsAppClone());\n\nclass WhatsAppClone extends StatelessWidget {\n  const WhatsAppClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'WhatsApp',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        colorSchemeSeed: const Color(0xFF075E54),\n        brightness: Brightness.dark,\n      ),\n      home: const WhatsAppHome(),\n    );\n  }\n}\n\nclass WhatsAppHome extends StatefulWidget {\n  const WhatsAppHome({super.key});\n\n  @override\n  State<WhatsAppHome> createState() => _WhatsAppHomeState();\n}\n\nclass _WhatsAppHomeState extends State<WhatsAppHome> {\n  int _currentIndex = 0;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('WhatsApp'),\n        actions: [\n          IconButton(onPressed: () {}, icon: const Icon(Icons.camera_alt_outlined)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.search)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.more_vert)),\n        ],\n      ),\n      body: [\n        const ChatsPage(),\n        const StatusPage(),\n        const CallsPage(),\n      ][_currentIndex],\n      floatingActionButton: FloatingActionButton(\n        onPressed: () {},\n        child: Icon(_currentIndex == 0 ? Icons.message : Icons.add_call),\n      ),\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (i) => setState(() => _currentIndex = i),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.chat), label: 'Chats'),\n          NavigationDestination(icon: Icon(Icons.update), label: 'Status'),\n          NavigationDestination(icon: Icon(Icons.call), label: 'Calls'),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "chats_page.dart",
        "p": "lib/pages/chats_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Chatlar sahifasi — WhatsAppdagi asosiy sahifa\n/// Chatlar ro'yxati ko'rsatiladi\nclass ChatsPage extends StatelessWidget {\n  const ChatsPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return ListView.builder(\n      itemCount: 15,\n      itemBuilder: (context, index) {\n        return ListTile(\n          leading: CircleAvatar(\n            backgroundColor: Colors.grey[600],\n            child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n          ),\n          title: Text('Chat \\${index + 1}', style: const TextStyle(fontWeight: FontWeight.w500)),\n          subtitle: Text('Oxirgi xabar: \\${index + 1} daqiqa oldin', style: TextStyle(color: Colors.grey[500], fontSize: 12)),\n          trailing: Column(\n            mainAxisAlignment: MainAxisAlignment.center,\n            children: [\n              Text('\\${index + 1}:\\${(index * 7) % 60}', style: TextStyle(color: Colors.grey[500], fontSize: 11)),\n              const SizedBox(height: 4),\n              Container(\n                padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),\n                decoration: BoxDecoration(color: Colors.grey[600], borderRadius: BorderRadius.circular(10)),\n                child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white, fontSize: 10)),\n              ),\n            ],\n          ),\n        );\n      },\n    );\n  }\n}"
      },
      {
        "n": "calls_page.dart",
        "p": "lib/pages/calls_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Qo'ng'iroqlar sahifasi — WhatsAppdagi Calls bo'limi\nclass CallsPage extends StatelessWidget {\n  const CallsPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return ListView.builder(\n      itemCount: 10,\n      itemBuilder: (context, index) {\n        final isVideo = index % 3 == 0;\n        return ListTile(\n          leading: CircleAvatar(\n            backgroundColor: Colors.grey[600],\n            child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n          ),\n          title: Text('User \\${index + 1}'),\n          subtitle: Row(\n            children: [\n              Icon(isVideo ? Icons.videocam : Icons.call, size: 14, color: Colors.grey[500]),\n              const SizedBox(width: 4),\n              Text('Bugun, \\${index + 1}:\\${(index * 13) % 60}', style: TextStyle(color: Colors.grey[500], fontSize: 12)),\n            ],\n          ),\n          trailing: Icon(isVideo ? Icons.videocam : Icons.call, color: Colors.green),\n        );\n      },\n    );\n  }\n}"
      },
      {
        "n": "status_page.dart",
        "p": "lib/pages/status_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Holat (Status) sahifasi — WhatsAppdagi Status bo'limi\nclass StatusPage extends StatelessWidget {\n  const StatusPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return ListView(\n      children: [\n        // Mening holatim\n        ListTile(\n          leading: Stack(\n            children: [\n              CircleAvatar(backgroundColor: Colors.grey[600], child: const Icon(Icons.person)),\n              Positioned(\n                right: 0, bottom: 0,\n                child: Container(\n                  padding: const EdgeInsets.all(4),\n                  decoration: BoxDecoration(color: Colors.green, shape: BoxShape.circle),\n                  child: const Icon(Icons.add, size: 12, color: Colors.white),\n                ),\n              ),\n            ],\n          ),\n          title: const Text('Mening holatim'),\n          subtitle: const Text('Bugun, 10:00'),\n        ),\n\n        const Padding(\n          padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),\n          child: Text('Yangilangan holatlar', style: TextStyle(color: Colors.grey, fontSize: 12)),\n        ),\n\n        // Boshqalarning holatlari\n        ...List.generate(8, (index) => ListTile(\n          leading: Container(\n            padding: const EdgeInsets.all(2),\n            decoration: BoxDecoration(\n              shape: BoxShape.circle,\n              border: Border.all(color: Colors.green, width: 2),\n            ),\n            child: CircleAvatar(\n              backgroundColor: Colors.grey[600],\n              child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n            ),\n          ),\n          title: Text('User \\${index + 1}'),\n          subtitle: Text('Bugun, \\${index + 1}:\\${(index * 15) % 60}', style: TextStyle(color: Colors.grey[500], fontSize: 12)),\n        )),\n      ],\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "spotify-clone",
    "n": "Spotify Clone",
    "i": "spotify",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "spotify",
      "music",
      "player",
      "playlist",
      "audio"
    ],
    "d": "Spotify ilovasining kloni. Home, Search, Library va Player sahifalari.",
    "icon": "globe",
    "instructions": "## Spotify Clone — Qanday ishlatish\n\n### Bu nima?\nSpotify ilovasining Flutter versiyasi. Musiqa ixtiyoriy, qidiruv, kutubxona va pleyer sahifalari.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create spotify_clone\ncd spotify_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models\n```\n\n**3-qadam: Fayllarni joylashtiring**\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/home_page.dart` — bosh sahifa\n- `lib/pages/search_page.dart` — qidiruv\n- `lib/pages/library_page.dart` — kutubxona\n- `lib/widgets/player_bar.dart` — pleyer paneli\n\n**4-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Musiqa nomlarini ro'yxatdan o'zgartiring\n- Ranglarni qora/yashilga o'zgartiring\n- Yangi sahifalar qo'shing\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/home_page.dart';\nimport 'pages/search_page.dart';\nimport 'pages/library_page.dart';\nimport 'widgets/player_bar.dart';\n\n/// Spotify Clone — Asosiy fayl\nvoid main() => runApp(const SpotifyClone());\n\nclass SpotifyClone extends StatelessWidget {\n  const SpotifyClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Spotify',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        brightness: Brightness.dark,\n        colorSchemeSeed: Colors.green,\n        scaffoldBackgroundColor: Colors.black,\n      ),\n      home: const SpotifyHome(),\n    );\n  }\n}\n\nclass SpotifyHome extends StatefulWidget {\n  const SpotifyHome({super.key});\n\n  @override\n  State<SpotifyHome> createState() => _SpotifyHomeState();\n}\n\nclass _SpotifyHomeState extends State<SpotifyHome> {\n  int _currentIndex = 0;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: [\n        const HomePage(),\n        const SearchPage(),\n        const LibraryPage(),\n      ][_currentIndex],\n\n      // Pastda pleyer paneli\n      bottomNavigationBar: Column(\n        mainAxisSize: MainAxisSize.min,\n        children: [\n          const PlayerBar(),\n          NavigationBar(\n            selectedIndex: _currentIndex,\n            onDestinationSelected: (i) => setState(() => _currentIndex = i),\n            backgroundColor: Colors.black,\n            destinations: const [\n              NavigationDestination(icon: Icon(Icons.home), label: 'Home'),\n              NavigationDestination(icon: Icon(Icons.search), label: 'Search'),\n              NavigationDestination(icon: Icon(Icons.library_music), label: 'Library'),\n            ],\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "home_page.dart",
        "p": "lib/pages/home_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Spotify Home sahifasi — pleylistlar va tavsiyalar\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: SingleChildScrollView(\n        child: Column(\n          crossAxisAlignment: CrossAxisAlignment.start,\n          children: [\n            const Padding(\n              padding: EdgeInsets.all(16),\n              child: Text('Good evening', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n            ),\n\n            // Tezkor pleylistlar — 2 ustunli grid\n            Padding(\n              padding: const EdgeInsets.symmetric(horizontal: 16),\n              child: GridView.count(\n                shrinkWrap: true,\n                physics: const NeverScrollableScrollPhysics(),\n                crossAxisCount: 2,\n                crossAxisSpacing: 8,\n                mainAxisSpacing: 8,\n                childAspectRatio: 3,\n                children: List.generate(6, (i) => _buildQuickPlaylist('Playlist \\${i + 1}')),\n              ),\n            ),\n\n            const SizedBox(height: 20),\n\n            // Tavsiya etilgan pleylistlar\n            _buildSection('Made for You', List.generate(5, (i) => _buildAlbumCard('Mix \\${i + 1}'))),\n\n            const SizedBox(height: 20),\n\n            // So'nggi tinglanganlar\n            _buildSection('Recently Played', List.generate(5, (i) => _buildAlbumCard('Album \\${i + 1}'))),\n          ],\n        ),\n      ),\n    );\n  }\n\n  Widget _buildQuickPlaylist(String name) {\n    return Container(\n      padding: const EdgeInsets.symmetric(horizontal: 12),\n      decoration: BoxDecoration(color: Colors.grey[900], borderRadius: BorderRadius.circular(4)),\n      child: Row(\n        children: [\n          Container(width: 40, height: 40, color: Colors.grey[700]),\n          const SizedBox(width: 8),\n          Expanded(child: Text(name, style: const TextStyle(fontSize: 12, fontWeight: FontWeight.w500))),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildSection(String title, List<Widget> children) {\n    return Column(\n      crossAxisAlignment: CrossAxisAlignment.start,\n      children: [\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 16),\n          child: Text(title, style: const TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),\n        ),\n        const SizedBox(height: 12),\n        SizedBox(\n          height: 180,\n          child: ListView(scrollDirection: Axis.horizontal, padding: const EdgeInsets.symmetric(horizontal: 12), children: children),\n        ),\n      ],\n    );\n  }\n\n  Widget _buildAlbumCard(String name) {\n    return Container(\n      width: 140,\n      margin: const EdgeInsets.symmetric(horizontal: 4),\n      child: Column(\n        crossAxisAlignment: CrossAxisAlignment.start,\n        children: [\n          Container(height: 140, width: 140, color: Colors.grey[800]),\n          const SizedBox(height: 8),\n          Text(name, style: const TextStyle(fontSize: 12, fontWeight: FontWeight.w500), maxLines: 1),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "player_bar.dart",
        "p": "lib/widgets/player_bar.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Player bar — Spotifydagi pastdagi pleyer paneli\n/// Joriy qo'shiqni ko'rsatadi va boshqaradi\nclass PlayerBar extends StatelessWidget {\n  const PlayerBar({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Container(\n      margin: const EdgeInsets.symmetric(horizontal: 8),\n      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),\n      decoration: BoxDecoration(\n        color: Colors.grey[900],\n        borderRadius: BorderRadius.circular(8),\n      ),\n      child: Row(\n        children: [\n          // Qo'shiq rasmi\n          Container(\n            width: 40,\n            height: 40,\n            decoration: BoxDecoration(color: Colors.green[800], borderRadius: BorderRadius.circular(4)),\n            child: const Icon(Icons.music_note, color: Colors.white),\n          ),\n          const SizedBox(width: 12),\n\n          // Qo'shiq nomi\n          const Expanded(\n            child: Column(\n              crossAxisAlignment: CrossAxisAlignment.start,\n              mainAxisSize: MainAxisSize.min,\n              children: [\n                Text('Song Name', style: TextStyle(fontSize: 13, fontWeight: FontWeight.w500)),\n                Text('Artist Name', style: TextStyle(fontSize: 11, color: Colors.grey)),\n              ],\n            ),\n          ),\n\n          // Boshqaruv tugmalari\n          IconButton(onPressed: () {}, icon: const Icon(Icons.play_arrow, color: Colors.white)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.skip_next, color: Colors.white)),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "search_page.dart",
        "p": "lib/pages/search_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Spotify Qidiruv sahifasi — musiqa va pleylist qidirish\nclass SearchPage extends StatelessWidget {\n  const SearchPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: Column(\n        crossAxisAlignment: CrossAxisAlignment.start,\n        children: [\n          const Padding(\n            padding: EdgeInsets.all(16),\n            child: Text('Search', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n          ),\n\n          // Qidiruv maydoni\n          Padding(\n            padding: const EdgeInsets.symmetric(horizontal: 16),\n            child: TextField(\n              decoration: InputDecoration(\n                hintText: 'What do you want to listen to?',\n                prefixIcon: const Icon(Icons.search),\n                filled: true,\n                fillColor: Colors.white,\n                border: OutlineInputBorder(borderRadius: BorderRadius.circular(8), borderSide: BorderSide.none),\n              ),\n            ),\n          ),\n\n          const SizedBox(height: 20),\n\n          // Kategoriyalar — rangli kartochkalar\n          Expanded(\n            child: GridView.builder(\n              padding: const EdgeInsets.all(16),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 2, mainAxisSpacing: 12, crossAxisSpacing: 12, childAspectRatio: 1.5),\n              itemCount: 12,\n              itemBuilder: (context, index) {\n                final colors = [Colors.red, Colors.blue, Colors.green, Colors.purple, Colors.orange, Colors.teal];\n                return Container(\n                  decoration: BoxDecoration(color: colors[index % colors.length], borderRadius: BorderRadius.circular(8)),\n                  padding: const EdgeInsets.all(12),\n                  child: Text('Category \\${index + 1}', style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 14)),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "library_page.dart",
        "p": "lib/pages/library_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Spotify Kutubxona sahifasi — pleylistlar va saqlanganlar\nclass LibraryPage extends StatelessWidget {\n  const LibraryPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: Column(\n        crossAxisAlignment: CrossAxisAlignment.start,\n        children: [\n          const Padding(\n            padding: EdgeInsets.all(16),\n            child: Text('Your Library', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),\n          ),\n\n          // Filtrlar\n          Padding(\n            padding: const EdgeInsets.symmetric(horizontal: 16),\n            child: Row(\n              children: [\n                _buildFilter('Playlists'),\n                const SizedBox(width: 8),\n                _buildFilter('Artists'),\n                const SizedBox(width: 8),\n                _buildFilter('Albums'),\n              ],\n            ),\n          ),\n\n          const SizedBox(height: 16),\n\n          // Pleylistlar ro'yxati\n          Expanded(\n            child: ListView.builder(\n              itemCount: 10,\n              itemBuilder: (context, index) {\n                return ListTile(\n                  leading: Container(width: 50, height: 50, color: Colors.grey[800]),\n                  title: Text('Playlist \\${index + 1}', style: const TextStyle(fontWeight: FontWeight.w500)),\n                  subtitle: Text('Playlist • \\${index + 5} songs', style: TextStyle(color: Colors.grey[500], fontSize: 12)),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildFilter(String label) {\n    return Container(\n      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),\n      decoration: BoxDecoration(color: Colors.grey[800], borderRadius: BorderRadius.circular(20)),\n      child: Text(label, style: const TextStyle(fontSize: 12)),\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "telegram-clone",
    "n": "Telegram Clone",
    "i": "telegram",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "telegram",
      "chat",
      "messaging",
      "channels",
      "groups"
    ],
    "d": "Telegram ilovasining kloni. Chatlar, Kanallar, Guruhlar va Sozlamalar sahifalari.",
    "icon": "globe",
    "instructions": "## Telegram Clone — Qanday ishlatish\n\n### Bu nima?\nTelegram ilovasining Flutter versiyasi. Chatlar, Kanallar, Guruhlar va Sozlamalar.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create telegram_clone\ncd telegram_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models\n```\n\n**3-qadam: Fayllarni joylashtiring**\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/chats_page.dart` — chatlar\n- `lib/pages/settings_page.dart` — sozlamalar\n\n**4-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Chat nomlarini o'zgartiring\n- Ranglarni ko'k rangga o'zgartiring\n- Yangi sahifalar qo'shing\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/chats_page.dart';\n\n/// Telegram Clone — Asosiy fayl\nvoid main() => runApp(const TelegramClone());\n\nclass TelegramClone extends StatelessWidget {\n  const TelegramClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Telegram',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        colorSchemeSeed: Colors.blue,\n        brightness: Brightness.light,\n      ),\n      home: const ChatsPage(),\n    );\n  }\n}"
      },
      {
        "n": "chats_page.dart",
        "p": "lib/pages/chats_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Telegram Chatlar sahifasi\nclass ChatsPage extends StatelessWidget {\n  const ChatsPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('Telegram'),\n        actions: [\n          IconButton(onPressed: () {}, icon: const Icon(Icons.search)),\n          IconButton(onPressed: () {}, icon: const Icon(Icons.more_vert)),\n        ],\n      ),\n      body: ListView.builder(\n        itemCount: 20,\n        itemBuilder: (context, index) {\n          return ListTile(\n            leading: CircleAvatar(\n              backgroundColor: [Colors.blue, Colors.green, Colors.red, Colors.purple][index % 4],\n              child: Text('\\${index + 1}', style: const TextStyle(color: Colors.white)),\n            ),\n            title: Row(\n              children: [\n                Expanded(child: Text('Chat \\${index + 1}', style: const TextStyle(fontWeight: FontWeight.w500))),\n                Text('\\${index + 1}:\\${(index * 7) % 60}', style: TextStyle(color: Colors.grey[500], fontSize: 11)),\n              ],\n            ),\n            subtitle: Row(\n              children: [\n                Icon(Icons.done_all, size: 16, color: Colors.blue[300]),\n                const SizedBox(width: 4),\n                Expanded(child: Text('Oxirgi xabar matni...', style: TextStyle(color: Colors.grey[500], fontSize: 13), overflow: TextOverflow.ellipsis)),\n              ],\n            ),\n          );\n        },\n      ),\n      floatingActionButton: FloatingActionButton(\n        onPressed: () {},\n        child: const Icon(Icons.edit),\n      ),\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "netflix-clone",
    "n": "Netflix Clone",
    "i": "netflix",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "netflix",
      "streaming",
      "movies",
      "series",
      "video"
    ],
    "d": "Netflix ilovasining kloni. Home, Search, Downloads va Profile sahifalari.",
    "icon": "globe",
    "instructions": "## Netflix Clone — Qanday ishlatish\n\n### Bu nima?\nNetflix ilovasining Flutter versiyasi. Filmlar, Qidiruv, Yuklab olishlar va Profil.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create netflix_clone\ncd netflix_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models\n```\n\n**3-qadam: Fayllarni joylashtiring**\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/home_page.dart` — filmlar ro'yxati\n- `lib/pages/search_page.dart` — qidiruv\n- `lib/pages/profile_page.dart` — profil\n\n**4-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Filmlar nomlarini o'zgartiring\n- Ranglarni qora/qizilga o'zgartiring\n- Yangi sahifalar qo'shing\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/home_page.dart';\nimport 'pages/search_page.dart';\nimport 'pages/profile_page.dart';\n\n/// Netflix Clone — Asosiy fayl\nvoid main() => runApp(const NetflixClone());\n\nclass NetflixClone extends StatelessWidget {\n  const NetflixClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Netflix',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        brightness: Brightness.dark,\n        colorSchemeSeed: Colors.red,\n        scaffoldBackgroundColor: Colors.black,\n      ),\n      home: const NetflixHome(),\n    );\n  }\n}\n\nclass NetflixHome extends StatefulWidget {\n  const NetflixHome({super.key});\n\n  @override\n  State<NetflixHome> createState() => _NetflixHomeState();\n}\n\nclass _NetflixHomeState extends State<NetflixHome> {\n  int _currentIndex = 0;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: [\n        const HomePage(),\n        const SearchPage(),\n        const ProfilePage(),\n      ][_currentIndex],\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (i) => setState(() => _currentIndex = i),\n        backgroundColor: Colors.black,\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.home), label: 'Home'),\n          NavigationDestination(icon: Icon(Icons.search), label: 'Search'),\n          NavigationDestination(icon: Icon(Icons.person), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "home_page.dart",
        "p": "lib/pages/home_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Netflix Home — filmlar va seriallar ro'yxati\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: SingleChildScrollView(\n        child: Column(\n          crossAxisAlignment: CrossAxisAlignment.start,\n          children: [\n            // Banner — katta film rasmi\n            Container(\n              height: 400,\n              width: double.infinity,\n              color: Colors.grey[900],\n              child: Stack(\n                children: [\n                  Center(child: Icon(Icons.movie, size: 80, color: Colors.grey[700])),\n                  Positioned(\n                    bottom: 20,\n                    left: 16,\n                    child: Column(\n                      crossAxisAlignment: CrossAxisAlignment.start,\n                      children: [\n                        const Text('Trending Now', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),\n                        const SizedBox(height: 8),\n                        Row(\n                          children: [\n                            FilledButton.icon(\n                              onPressed: () {},\n                              icon: const Icon(Icons.play_arrow),\n                              label: const Text('Play'),\n                            ),\n                            const SizedBox(width: 8),\n                            OutlinedButton.icon(\n                              onPressed: () {},\n                              icon: const Icon(Icons.add),\n                              label: const Text('My List'),\n                            ),\n                          ],\n                        ),\n                      ],\n                    ),\n                  ),\n                ],\n              ),\n            ),\n\n            _buildSection('Continue Watching'),\n            _buildSection('Trending Now'),\n            _buildSection('Popular on Netflix'),\n            _buildSection('New Releases'),\n          ],\n        ),\n      ),\n    );\n  }\n\n  Widget _buildSection(String title) {\n    return Column(\n      crossAxisAlignment: CrossAxisAlignment.start,\n      children: [\n        Padding(\n          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),\n          child: Text(title, style: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),\n        ),\n        SizedBox(\n          height: 160,\n          child: ListView.builder(\n            scrollDirection: Axis.horizontal,\n            padding: const EdgeInsets.symmetric(horizontal: 12),\n            itemCount: 8,\n            itemBuilder: (context, index) {\n              return Container(\n                width: 110,\n                margin: const EdgeInsets.symmetric(horizontal: 4),\n                decoration: BoxDecoration(color: Colors.grey[800], borderRadius: BorderRadius.circular(4)),\n              );\n            },\n          ),\n        ),\n      ],\n    );\n  }\n}"
      },
      {
        "n": "search_page.dart",
        "p": "lib/pages/search_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Netflix Qidiruv sahifasi\nclass SearchPage extends StatelessWidget {\n  const SearchPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: Column(\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: TextField(\n              decoration: InputDecoration(\n                hintText: 'Search for movies, shows...',\n                prefixIcon: const Icon(Icons.search),\n                filled: true,\n                fillColor: Colors.grey[800],\n                border: OutlineInputBorder(borderRadius: BorderRadius.circular(8), borderSide: BorderSide.none),\n              ),\n            ),\n          ),\n          Expanded(\n            child: GridView.builder(\n              padding: const EdgeInsets.all(8),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3, mainAxisSpacing: 8, crossAxisSpacing: 8, childAspectRatio: 0.7),\n              itemCount: 18,\n              itemBuilder: (context, index) {\n                return Container(\n                  decoration: BoxDecoration(color: Colors.grey[800], borderRadius: BorderRadius.circular(4)),\n                  child: Center(child: Icon(Icons.movie, color: Colors.grey[600])),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "profile_page.dart",
        "p": "lib/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// Netflix Profil sahifasi\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: ListView(\n        padding: const EdgeInsets.all(16),\n        children: [\n          const Center(\n            child: CircleAvatar(\n              radius: 40,\n              backgroundColor: Colors.red,\n              child: Text('U', style: TextStyle(fontSize: 32, color: Colors.white)),\n            ),\n          ),\n          const SizedBox(height: 8),\n          const Center(child: Text('My Profile', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold))),\n          const SizedBox(height: 24),\n          _buildSetting(Icons.notifications, 'Notifications'),\n          _buildSetting(Icons.download, 'Downloads'),\n          _buildSetting(Icons.language, 'Language'),\n          _buildSetting(Icons.help, 'Help'),\n          _buildSetting(Icons.logout, 'Sign Out'),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildSetting(IconData icon, String title) {\n    return ListTile(\n      leading: Icon(icon),\n      title: Text(title),\n      trailing: const Icon(Icons.chevron_right),\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "tiktok-clone",
    "n": "TikTok Clone",
    "i": "tiktok",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "tiktok",
      "video",
      "feed",
      "short-video",
      "social"
    ],
    "d": "TikTok ilovasining kloni. Video Feed, Discover, Create va Profile sahifalari.",
    "icon": "globe",
    "instructions": "## TikTok Clone — Qanday ishlatish\n\n### Bu nima?\nTikTok ilovasining Flutter versiyasi. Qisqa videolar, kashfiyot, yaratish va profil.\n\n### Qadamlar:\n\n**1-qadam: Loyihani yarating**\n```\nflutter create tiktok_clone\ncd tiktok_clone\n```\n\n**2-qadam: Papkalarni yarating**\n```\nmkdir -p lib/pages lib/widgets lib/models\n```\n\n**3-qadam: Fayllarni joylashtiring**\n- `lib/main.dart` — asosiy fayl\n- `lib/pages/feed_page.dart` — video lentasi\n- `lib/pages/discover_page.dart` — kashfiyot\n- `lib/pages/profile_page.dart` — profil\n\n**4-qadam: Ishga tushiring**\n```\nflutter run\n```\n\n### O'zgartirish:\n- Video nomlarini o'zgartiring\n- Ranglarni qora/oqga o'zgartiring\n- Yangi sahifalar qo'shing\n",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'pages/feed_page.dart';\nimport 'pages/discover_page.dart';\nimport 'pages/profile_page.dart';\n\n/// TikTok Clone — Asosiy fayl\nvoid main() => runApp(const TikTokClone());\n\nclass TikTokClone extends StatelessWidget {\n  const TikTokClone({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'TikTok',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        useMaterial3: true,\n        brightness: Brightness.dark,\n        scaffoldBackgroundColor: Colors.black,\n      ),\n      home: const TikTokHome(),\n    );\n  }\n}\n\nclass TikTokHome extends StatefulWidget {\n  const TikTokHome({super.key});\n\n  @override\n  State<TikTokHome> createState() => _TikTokHomeState();\n}\n\nclass _TikTokHomeState extends State<TikTokHome> {\n  int _currentIndex = 0;\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: [\n        const FeedPage(),\n        const DiscoverPage(),\n        const ProfilePage(),\n      ][_currentIndex],\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (i) => setState(() => _currentIndex = i),\n        backgroundColor: Colors.black,\n        indicatorColor: Colors.transparent,\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.home), label: 'Home'),\n          NavigationDestination(icon: Icon(Icons.add_box, size: 32), label: ''),\n          NavigationDestination(icon: Icon(Icons.person), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "feed_page.dart",
        "p": "lib/pages/feed_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// TikTok Video Feed — to'liq ekran video\nclass FeedPage extends StatelessWidget {\n  const FeedPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      backgroundColor: Colors.black,\n      body: Stack(\n        children: [\n          // Video — to'liq ekran\n          Container(color: Colors.grey[900], child: const Center(child: Icon(Icons.play_circle, size: 80, color: Colors.white54))),\n\n          // O'ng tomon — reaksiyalar\n          Positioned(\n            right: 12,\n            bottom: 120,\n            child: Column(\n              children: [\n                _buildAction(Icons.favorite, '12.5K'),\n                const SizedBox(height: 20),\n                _buildAction(Icons.chat_bubble, '342'),\n                const SizedBox(height: 20),\n                _buildAction(Icons.bookmark, '1.2K'),\n                const SizedBox(height: 20),\n                _buildAction(Icons.share, 'Share'),\n              ],\n            ),\n          ),\n\n          // Pastki qism\n          Positioned(\n            left: 12,\n            bottom: 20,\n            right: 60,\n            child: Column(\n              crossAxisAlignment: CrossAxisAlignment.start,\n              children: [\n                const Text('@username', style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 15)),\n                const SizedBox(height: 8),\n                const Text('Beautiful video! #flutter #tiktok', style: TextStyle(color: Colors.white, fontSize: 13)),\n                const SizedBox(height: 12),\n                Row(\n                  children: [\n                    Icon(Icons.music_note, color: Colors.white, size: 16),\n                    const SizedBox(width: 8),\n                    Expanded(child: Text('Original Sound - Artist', style: TextStyle(color: Colors.white, fontSize: 12), overflow: TextOverflow.ellipsis)),\n                  ],\n                ),\n              ],\n            ),\n          ),\n\n          // Yuqori qism — Following | For You\n          Positioned(\n            top: 40,\n            left: 0,\n            right: 0,\n            child: Row(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                Text('Following', style: TextStyle(color: Colors.white.withOpacity(0.6), fontSize: 16)),\n                const SizedBox(width: 20),\n                const Text('For You', style: TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.bold)),\n              ],\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n\n  Widget _buildAction(IconData icon, String label) {\n    return Column(\n      children: [\n        Icon(icon, color: Colors.white, size: 28),\n        const SizedBox(height: 4),\n        Text(label, style: const TextStyle(color: Colors.white, fontSize: 11)),\n      ],\n    );\n  }\n}"
      },
      {
        "n": "discover_page.dart",
        "p": "lib/pages/discover_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// TikTok Discover — trendlar va kategoriyalar\nclass DiscoverPage extends StatelessWidget {\n  const DiscoverPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: Column(\n        children: [\n          Padding(\n            padding: const EdgeInsets.all(16),\n            child: TextField(\n              decoration: InputDecoration(\n                hintText: 'Search',\n                prefixIcon: const Icon(Icons.search),\n                filled: true,\n                fillColor: Colors.grey[800],\n                border: OutlineInputBorder(borderRadius: BorderRadius.circular(8), borderSide: BorderSide.none),\n              ),\n            ),\n          ),\n          Expanded(\n            child: GridView.builder(\n              padding: const EdgeInsets.all(8),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 2, mainAxisSpacing: 8, crossAxisSpacing: 8, childAspectRatio: 1),\n              itemCount: 10,\n              itemBuilder: (context, index) {\n                return Container(\n                  decoration: BoxDecoration(color: Colors.grey[800], borderRadius: BorderRadius.circular(8)),\n                  child: Center(child: Text('Trend \\${index + 1}', style: const TextStyle(color: Colors.white))),\n                );\n              },\n            ),\n          ),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "profile_page.dart",
        "p": "lib/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\n/// TikTok Profil sahifasi\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return SafeArea(\n      child: SingleChildScrollView(\n        child: Column(\n          children: [\n            const SizedBox(height: 20),\n            const CircleAvatar(radius: 40, backgroundColor: Colors.pink, child: Text('U', style: TextStyle(fontSize: 32, color: Colors.white))),\n            const SizedBox(height: 8),\n            const Text('@username', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),\n            const SizedBox(height: 16),\n            Row(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                _buildStat('128', 'Following'),\n                const SizedBox(width: 24),\n                _buildStat('1.2K', 'Followers'),\n                const SizedBox(width: 24),\n                _buildStat('34.5K', 'Likes'),\n              ],\n            ),\n            const SizedBox(height: 16),\n            Row(\n              mainAxisAlignment: MainAxisAlignment.center,\n              children: [\n                FilledButton(onPressed: () {}, child: const Text('Edit profile')),\n                const SizedBox(width: 8),\n                OutlinedButton(onPressed: () {}, child: const Icon(Icons.share)),\n              ],\n            ),\n            const SizedBox(height: 20),\n            GridView.builder(\n              shrinkWrap: true,\n              physics: const NeverScrollableScrollPhysics(),\n              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3, mainAxisSpacing: 2, crossAxisSpacing: 2),\n              itemCount: 15,\n              itemBuilder: (context, index) {\n                return Container(color: Colors.grey[800], child: Icon(Icons.play_circle_outline, color: Colors.grey[600]));\n              },\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n\n  Widget _buildStat(String count, String label) {\n    return Column(\n      children: [\n        Text(count, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 16)),\n        Text(label, style: const TextStyle(fontSize: 12, color: Colors.grey)),\n      ],\n    );\n  }\n}"
      }
    ]
  },
  {
    "id": "arduino-blink",
    "n": "LED Blink",
    "i": "💡",
    "diff": "easy",
    "c": "arduino",
    "t": [
      "arduino",
      "led",
      "blink",
      "basic"
    ],
    "d": "Asosiy LED yonish-o'chish. Arduino uchun birinchi qadam.",
    "f": [
      {
        "n": "blink.ino",
        "p": "blink.ino",
        "c": "const int LED_PIN = 13;\n\nvoid setup() {\n  pinMode(LED_PIN, OUTPUT);\n  Serial.begin(9600);\n  Serial.println('Arduino LED Blink');\n}\n\nvoid loop() {\n  digitalWrite(LED_PIN, HIGH);\n  Serial.println('LED ON');\n  delay(1000);\n  digitalWrite(LED_PIN, LOW);\n  Serial.println('LED OFF');\n  delay(1000);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Arduino LED Blink\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- LED + 220 ohm rezistor\n\n### Ulanma:\n- LED anode (uzun oyoq) -> Pin 13\n- LED katode (qisqa oyoq) -> GND"
  },
  {
    "id": "arduino-fade",
    "n": "LED Fade (PWM)",
    "i": "🔆",
    "diff": "easy",
    "c": "arduino",
    "t": [
      "arduino",
      "led",
      "pwm",
      "fade",
      "analog"
    ],
    "d": "LED ni asta-sekin yoritish va o'chirish. PWM signali.",
    "f": [
      {
        "n": "fade.ino",
        "p": "fade.ino",
        "c": "const int LED_PIN = 9;\nint brightness = 0;\nint fadeAmount = 5;\n\nvoid setup() {\n  pinMode(LED_PIN, OUTPUT);\n  Serial.begin(9600);\n}\n\nvoid loop() {\n  analogWrite(LED_PIN, brightness);\n  brightness += fadeAmount;\n  if (brightness <= 0 || brightness >= 255) {\n    fadeAmount = -fadeAmount;\n  }\n  Serial.print('Brightness: ');\n  Serial.println(brightness);\n  delay(30);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## LED Fade (PWM)\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- LED + 220 ohm rezistor\n\n### Ulanma:\n- LED anode -> Pin 9 (PWM)\n- LED katode -> GND\n\n### PWM pinlari (Arduino UNO):\n- 3, 5, 6, 9, 10, 11"
  },
  {
    "id": "arduino-rgb-led",
    "n": "RGB LED",
    "i": "🌈",
    "diff": "easy",
    "c": "arduino",
    "t": [
      "arduino",
      "rgb",
      "led",
      "color",
      "pwm"
    ],
    "d": "RGB LED bilan turli ranglar yaratish. Har bir kanal uchun PWM.",
    "f": [
      {
        "n": "rgb_led.ino",
        "p": "rgb_led.ino",
        "c": "const int RED_PIN = 9;\nconst int GREEN_PIN = 10;\nconst int BLUE_PIN = 11;\n\nvoid setup() {\n  pinMode(RED_PIN, OUTPUT);\n  pinMode(GREEN_PIN, OUTPUT);\n  pinMode(BLUE_PIN, OUTPUT);\n  Serial.begin(9600);\n}\n\nvoid setColor(int r, int g, int b) {\n  analogWrite(RED_PIN, r);\n  analogWrite(GREEN_PIN, g);\n  analogWrite(BLUE_PIN, b);\n}\n\nvoid loop() {\n  // Qizil\n  setColor(255, 0, 0);\n  Serial.println('Red');\n  delay(1000);\n  // Yashil\n  setColor(0, 255, 0);\n  Serial.println('Green');\n  delay(1000);\n  // Ko'k\n  setColor(0, 0, 255);\n  Serial.println('Blue');\n  delay(1000);\n  // Sariq (Qizil + Yashil)\n  setColor(255, 255, 0);\n  Serial.println('Yellow');\n  delay(1000);\n  // Oq (Hammasi)\n  setColor(255, 255, 255);\n  Serial.println('White');\n  delay(1000);\n  // O'chirish\n  setColor(0, 0, 0);\n  Serial.println('Off');\n  delay(1000);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## RGB LED\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- RGB LED (Common Cathode)\n- 3x 220 ohm rezistor\n\n### Ulanma:\n- Qizil -> Pin 9\n- Yashil -> Pin 10\n- Ko'k -> Pin 11\n- Katode -> GND"
  },
  {
    "id": "arduino-button",
    "n": "Button Input",
    "i": "🔘",
    "diff": "easy",
    "c": "arduino",
    "t": [
      "arduino",
      "button",
      "input",
      "digital",
      "pullup"
    ],
    "d": "Tugma bosishni aniqlash. Internal pullup resistordan foydalanish.",
    "f": [
      {
        "n": "button.ino",
        "p": "button.ino",
        "c": "const int BUTTON_PIN = 2;\nconst int LED_PIN = 13;\nbool ledState = false;\nbool lastButtonState = LOW;\n\nvoid setup() {\n  pinMode(BUTTON_PIN, INPUT_PULLUP);\n  pinMode(LED_PIN, OUTPUT);\n  Serial.begin(9600);\n  Serial.println('Button Control Ready');\n}\n\nvoid loop() {\n  bool buttonState = digitalRead(BUTTON_PIN);\n  \n  if (buttonState == LOW && lastButtonState == HIGH) {\n    ledState = !ledState;\n    digitalWrite(LED_PIN, ledState ? HIGH : LOW);\n    Serial.print('LED: ');\n    Serial.println(ledState ? 'ON' : 'OFF');\n    delay(50); // Debounce\n  }\n  \n  lastButtonState = buttonState;\n  delay(10);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Button Input\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- Tugma (push button)\n- LED (ixtiyoriy)\n\n### Ulanma:\n- Tugma -> Pin 2 (INPUT_PULLUP)\n- Tugmaning boshqa uchi -> GND\n\n### Xususiyatlar:\n- Debounce himoya\n- Toggle rejimi (bosganda yonadi/o'chadi)"
  },
  {
    "id": "arduino-buzzer",
    "n": "Buzzer Melodiya",
    "i": "🎵",
    "diff": "easy",
    "c": "arduino",
    "t": [
      "arduino",
      "buzzer",
      "sound",
      "melody",
      "tone"
    ],
    "d": "Buzzer bilan musiqa ijro etish. Tone() funksiyasi.",
    "f": [
      {
        "n": "buzzer_melody.ino",
        "p": "buzzer_melody.ino",
        "c": "const int BUZZER_PIN = 8;\n\n// Notalar chastotasi (Hz)\n#define NOTE_C4  262\n#define NOTE_D4  294\n#define NOTE_E4  330\n#define NOTE_F4  349\n#define NOTE_G4  392\n#define NOTE_A4  440\n#define NOTE_B4  494\n#define NOTE_C5  523\n\nint melody[] = {\n  NOTE_C4, NOTE_E4, NOTE_G4, NOTE_C5,\n  NOTE_G4, NOTE_E4, NOTE_C4, 0,\n  NOTE_F4, NOTE_A4, NOTE_C5, NOTE_A4,\n  NOTE_F4, 0, 0, 0\n};\n\nint durations[] = {\n  400, 400, 400, 800,\n  400, 400, 400, 200,\n  400, 400, 400, 400,\n  800, 200, 200, 400\n};\n\nvoid setup() {\n  Serial.begin(9600);\n  Serial.println('Playing melody...');\n  \n  for (int i = 0; i < 16; i++) {\n    if (melody[i] > 0) {\n      tone(BUZZER_PIN, melody[i], durations[i]);\n    }\n    delay(durations[i] * 1.1);\n  }\n  noTone(BUZZER_PIN);\n  Serial.println('Done!');\n}\n\nvoid loop() {\n  // Bo'sh\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Buzzer Melodiya\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- Active Buzzer\n\n### Ulanma:\n- Buzzer + -> Pin 8\n- Buzzer - -> GND\n\n### Notalar:\n- C4 = 262 Hz\n- D4 = 294 Hz\n- E4 = 330 Hz\n- F4 = 349 Hz\n- G4 = 392 Hz\n- A4 = 440 Hz\n- B4 = 494 Hz\n- C5 = 523 Hz"
  },
  {
    "id": "arduino-servo",
    "n": "Servo Motor",
    "i": "⚙️",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "servo",
      "motor",
      "pwm"
    ],
    "d": "Servo motor boshqaruvi. 0-180 daraja oralig'ida aylanish.",
    "f": [
      {
        "n": "servo_control.ino",
        "p": "servo_control.ino",
        "c": "#include <Servo.h>\n\nServo myServo;\nconst int SERVO_PIN = 9;\nconst int POT_PIN = A0;\n\nvoid setup() {\n  myServo.attach(SERVO_PIN);\n  Serial.begin(9600);\n  Serial.println('Servo Motor Control');\n  Serial.println('Potentiometr orqali boshqaring');\n}\n\nvoid loop() {\n  int potValue = analogRead(POT_PIN);\n  int angle = map(potValue, 0, 1023, 0, 180);\n  \n  myServo.write(angle);\n  \n  Serial.print('Pot: ');\n  Serial.print(potValue);\n  Serial.print(' | Angle: ');\n  Serial.print(angle);\n  Serial.println(' deg');\n  \n  delay(15);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Servo Motor\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- SG90 Servo Motor\n- Potentiometr (10k)\n\n### Ulanma:\n- Servo qizil -> 5V\n- Servo jigarrang -> GND\n- Servo sariq -> Pin 9\n- Potentiometr markazi -> A0"
  },
  {
    "id": "arduino-dht11",
    "n": "DHT11 Harorat/Namlik",
    "i": "🌡️",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "dht11",
      "temperature",
      "humidity",
      "sensor"
    ],
    "d": "DHT11 sensor bilan harorat va namlik o'qish.",
    "f": [
      {
        "n": "dht11_sensor.ino",
        "p": "dht11_sensor.ino",
        "c": "#include <DHT.h>\n\n#define DHT_PIN 2\n#define DHT_TYPE DHT11\n\nDHT dht(DHT_PIN, DHT_TYPE);\n\nvoid setup() {\n  Serial.begin(9600);\n  dht.begin();\n  Serial.println('DHT11 Sensor Ready');\n  delay(2000);\n}\n\nvoid loop() {\n  float humidity = dht.readHumidity();\n  float temperature = dht.readTemperature();\n  float heatIndex = dht.computeHeatIndex(temperature, humidity, false);\n  \n  if (isnan(humidity) || isnan(temperature)) {\n    Serial.println('Xatolik: Sensor o\\'qilmadi!');\n    return;\n  }\n  \n  Serial.println('--- DHT11 O\\'qish ---');\n  Serial.print('Harorat: ');\n  Serial.print(temperature);\n  Serial.println(' C');\n  Serial.print('Namlik: ');\n  Serial.print(humidity);\n  Serial.println(' %');\n  Serial.print('His qilinadi: ');\n  Serial.print(heatIndex);\n  Serial.println(' C');\n  \n  delay(5000);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## DHT11 Sensor\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- DHT11 Sensor\n\n### Kutubxona:\n- DHT sensor library by Adafruit\n- Adafruit Unified Sensor\n\n### Ulanma:\n- DHT11 VCC -> 5V\n- DHT11 GND -> GND\n- DHT11 DATA -> Pin 2\n\n### Ma'lumotlar:\n- Harorat: 0-50 C (+-2 C)\n- Namlik: 20-80% (+-5%)"
  },
  {
    "id": "arduino-ultrasonic",
    "n": "Ultrasonic Masofa",
    "i": "📏",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "ultrasonic",
      "distance",
      "hc-sr04",
      "sensor"
    ],
    "d": "HC-SR04 ultrasonic sensor bilan masofa o'lchash.",
    "f": [
      {
        "n": "ultrasonic.ino",
        "p": "ultrasonic.ino",
        "c": "const int TRIG_PIN = 9;\nconst int ECHO_PIN = 10;\nconst int LED_PIN = 13;\n\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(TRIG_PIN, OUTPUT);\n  pinMode(ECHO_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  Serial.println('Ultrasonic Distance Sensor');\n}\n\nvoid loop() {\n  // Trigger signali\n  digitalWrite(TRIG_PIN, LOW);\n  delayMicroseconds(2);\n  digitalWrite(TRIG_PIN, HIGH);\n  delayMicroseconds(10);\n  digitalWrite(TRIG_PIN, LOW);\n  \n  // Echo vaqti\n  long duration = pulseIn(ECHO_PIN, HIGH);\n  \n  // Masofa (sm)\n  float distance = duration * 0.034 / 2;\n  \n  // 30 sm dan yaqin bo'lsa LED yonadi\n  if (distance < 30) {\n    digitalWrite(LED_PIN, HIGH);\n  } else {\n    digitalWrite(LED_PIN, LOW);\n  }\n  \n  Serial.print('Masofa: ');\n  Serial.print(distance);\n  Serial.println(' cm');\n  \n  delay(500);\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Ultrasonic Masofa\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- HC-SR04 Ultrasonic Sensor\n- LED (ixtiyoriy)\n\n### Ulanma:\n- VCC -> 5V\n- GND -> GND\n- TRIG -> Pin 9\n- ECHO -> Pin 10\n- LED -> Pin 13\n\n### Ishlash:\n- 30 sm dan yaqin bo'lsa LED yonadi\n- Masofani Serial Monitor da ko'rish mumkin"
  },
  {
    "id": "arduino-lcd",
    "n": "LCD 16x2 Display",
    "i": "📺",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "lcd",
      "display",
      "16x2",
      "i2c"
    ],
    "d": "LCD 16x2 ekraniga matn chiqarish. I2C ulash usuli.",
    "f": [
      {
        "n": "lcd_display.ino",
        "p": "lcd_display.ino",
        "c": "#include <LiquidCrystal_I2C.h>\n\nLiquidCrystal_I2C lcd(0x27, 16, 2);\n\nvoid setup() {\n  lcd.init();\n  lcd.backlight();\n  \n  lcd.setCursor(0, 0);\n  lcd.print('CodeBase Arduino');\n  lcd.setCursor(0, 1);\n  lcd.print('LCD 16x2 Test');\n  delay(3000);\n  lcd.clear();\n}\n\nvoid loop() {\n  // Harorat va namlik\n  lcd.setCursor(0, 0);\n  lcd.print('Harorat: 25.5 C');\n  lcd.setCursor(0, 1);\n  lcd.print('Namlik:   65.2 %');\n  \n  delay(2000);\n  lcd.clear();\n  \n  // Vaqt\n  static int seconds = 0;\n  lcd.setCursor(0, 0);\n  lcd.print('Vaqt:');\n  lcd.setCursor(0, 1);\n  lcd.print(seconds);\n  lcd.print(' soniya');\n  \n  seconds++;\n  delay(1000);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## LCD 16x2 Display\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- LCD 16x2 + I2C modul\n\n### Kutubxona:\n- LiquidCrystal_I2C\n\n### Ulanma (I2C):\n- SDA -> A4\n- SCL -> A5\n- VCC -> 5V\n- GND -> GND\n\n### I2C manzillari:\n- Ko'pincha 0x27 yoki 0x3F\n- I2C scanner bilan tekshirish mumkin"
  },
  {
    "id": "arduino-ir-remote",
    "n": "IR Pult Boshqaruvi",
    "i": "🎮",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "ir",
      "remote",
      "infrared",
      "control"
    ],
    "d": "IR pult orqali LED, servo, buzzer boshqarish.",
    "f": [
      {
        "n": "ir_remote.ino",
        "p": "ir_remote.ino",
        "c": "#include <IRremote.h>\n\nconst int IR_PIN = 11;\nconst int LED_RED = 3;\nconst int LED_GREEN = 5;\nconst int LED_BLUE = 6;\n\nIRrecv irrecv(IR_PIN);\ndecode_results results;\n\nvoid setup() {\n  Serial.begin(9600);\n  irrecv.enableIRIn();\n  pinMode(LED_RED, OUTPUT);\n  pinMode(LED_GREEN, OUTPUT);\n  pinMode(LED_BLUE, OUTPUT);\n  Serial.println('IR Remote Control Ready');\n}\n\nvoid loop() {\n  if (irrecv.decode(&results)) {\n    Serial.print('Kod: 0x');\n    Serial.println(results.value, HEX);\n    \n    switch (results.value) {\n      case 0xFFA25D: // 1\n        digitalWrite(LED_RED, HIGH);\n        digitalWrite(LED_GREEN, LOW);\n        digitalWrite(LED_BLUE, LOW);\n        Serial.println('Red ON');\n        break;\n      case 0xFF629D: // 2\n        digitalWrite(LED_RED, LOW);\n        digitalWrite(LED_GREEN, HIGH);\n        digitalWrite(LED_BLUE, LOW);\n        Serial.println('Green ON');\n        break;\n      case 0xFFE21D: // 3\n        digitalWrite(LED_RED, LOW);\n        digitalWrite(LED_GREEN, LOW);\n        digitalWrite(LED_BLUE, HIGH);\n        Serial.println('Blue ON');\n        break;\n      case 0xFFC23D: // 4\n        digitalWrite(LED_RED, LOW);\n        digitalWrite(LED_GREEN, LOW);\n        digitalWrite(LED_BLUE, LOW);\n        Serial.println('All OFF');\n        break;\n    }\n    irrecv.resume();\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## IR Pult Boshqaruvi\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- IR Qabul qiluvchi (VS1838B)\n- IR Pult\n- 3x LED + 3x 220 ohm rezistor\n\n### Ulanma:\n- IR Qabul qiluvchi -> Pin 11\n- Qizil LED -> Pin 3\n- Yashil LED -> Pin 5\n- Ko'k LED -> Pin 6\n\n### Pult tugmalari:\n- 1 = Qizil LED\n- 2 = Yashil LED\n- 3 = Ko'k LED\n- 4 = Hammasi o'chadi"
  },
  {
    "id": "arduino-motor-l298n",
    "n": "DC Motor L298N",
    "i": "🚗",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "motor",
      "l298n",
      "dc",
      "driver"
    ],
    "d": "L298N driver orqali DC motor boshqaruvi. Oldga, orqaga, tezlik.",
    "f": [
      {
        "n": "motor_l298n.ino",
        "p": "motor_l298n.ino",
        "c": "// L298N Motor Driver\nconst int IN1 = 5;\nconst int IN2 = 6;\nconst int ENA = 9;\n\nconst int IN3 = 10;\nconst int IN4 = 11;\nconst int ENB = 3;\n\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(IN1, OUTPUT);\n  pinMode(IN2, OUTPUT);\n  pinMode(ENA, OUTPUT);\n  pinMode(IN3, OUTPUT);\n  pinMode(IN4, OUTPUT);\n  pinMode(ENB, OUTPUT);\n  Serial.println('L298N Motor Control');\n}\n\nvoid motorA(int speed, bool forward) {\n  digitalWrite(IN1, forward ? HIGH : LOW);\n  digitalWrite(IN2, forward ? LOW : HIGH);\n  analogWrite(ENA, speed);\n}\n\nvoid motorB(int speed, bool forward) {\n  digitalWrite(IN3, forward ? HIGH : LOW);\n  digitalWrite(IN4, forward ? LOW : HIGH);\n  analogWrite(ENB, speed);\n}\n\nvoid stopMotors() {\n  analogWrite(ENA, 0);\n  analogWrite(ENB, 0);\n}\n\nvoid loop() {\n  // Oldga\n  Serial.println('Oldga...');\n  motorA(200, true);\n  motorB(200, true);\n  delay(2000);\n  \n  // Orqaga\n  Serial.println('Orqaga...');\n  motorA(200, false);\n  motorB(200, false);\n  delay(2000);\n  \n  // Chapga burilish\n  Serial.println('Chapga...');\n  motorA(150, true);\n  motorB(150, false);\n  delay(1000);\n  \n  // O'ngga burilish\n  Serial.println('O\\'ngga...');\n  motorA(150, false);\n  motorB(150, true);\n  delay(1000);\n  \n  // To'xtash\n  Serial.println('To\\'xtash...');\n  stopMotors();\n  delay(1000);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## DC Motor L298N\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- L298N Motor Driver\n- 2x DC Motor\n- 12V batareya\n\n### Ulanma:\n- IN1 -> Pin 5\n- IN2 -> Pin 6\n- ENA -> Pin 9\n- IN3 -> Pin 10\n- IN4 -> Pin 11\n- ENB -> Pin 3\n- L298N 12V -> Batareya +\n- L298N GND -> Batareya -\n\n### Boshqaruv:\n- Oldga: IN1=HIGH, IN2=LOW\n- Orqaga: IN1=LOW, IN2=HIGH\n- Tezlik: 0-255 (PWM)"
  },
  {
    "id": "arduino-line-follower",
    "n": "Line Follower Robot",
    "i": "🤖",
    "diff": "hard",
    "c": "arduino",
    "t": [
      "arduino",
      "line",
      "follower",
      "robot",
      "ir",
      "sensor"
    ],
    "d": "Chiziqni kuzatuvchi robot. 3 ta IR sensor va 2 ta motor.",
    "f": [
      {
        "n": "line_follower.ino",
        "p": "line_follower.ino",
        "c": "// Line Follower Robot\nconst int SENSOR_LEFT = A0;\nconst int SENSOR_CENTER = A1;\nconst int SENSOR_RIGHT = A2;\n\nconst int MOTOR_A_EN = 5;\nconst int MOTOR_A_IN1 = 6;\nconst int MOTOR_A_IN2 = 7;\nconst int MOTOR_B_EN = 10;\nconst int MOTOR_B_IN1 = 8;\nconst int MOTOR_B_IN2 = 9;\n\nconst int THRESHOLD = 500;\nconst int SPEED = 150;\nconst int TURN_SPEED = 120;\n\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(MOTOR_A_EN, OUTPUT);\n  pinMode(MOTOR_A_IN1, OUTPUT);\n  pinMode(MOTOR_A_IN2, OUTPUT);\n  pinMode(MOTOR_B_EN, OUTPUT);\n  pinMode(MOTOR_B_IN1, OUTPUT);\n  pinMode(MOTOR_B_IN2, OUTPUT);\n  Serial.println('Line Follower Ready');\n}\n\nvoid moveForward() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, SPEED);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, SPEED);\n}\n\nvoid turnLeft() {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, 0);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, TURN_SPEED);\n}\n\nvoid turnRight() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, TURN_SPEED);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, 0);\n}\n\nvoid stopMotors() {\n  analogWrite(MOTOR_A_EN, 0);\n  analogWrite(MOTOR_B_EN, 0);\n}\n\nvoid loop() {\n  int left = analogRead(SENSOR_LEFT);\n  int center = analogRead(SENSOR_CENTER);\n  int right = analogRead(SENSOR_RIGHT);\n  \n  Serial.print('L:'); Serial.print(left);\n  Serial.print(' C:'); Serial.print(center);\n  Serial.print(' R:'); Serial.println(right);\n  \n  bool onLine = center < THRESHOLD;\n  \n  if (onLine) {\n    moveForward();\n  } else if (left < THRESHOLD) {\n    turnLeft();\n  } else if (right < THRESHOLD) {\n    turnRight();\n  } else {\n    stopMotors();\n  }\n  \n  delay(10);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Line Follower Robot\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- 3x IR sensor (QTR-8A yoki TCRT5000)\n- L298N Motor Driver\n- 2x DC Motor\n- Chiziqli iz\n\n### Ulanma:\n- Chap sensor -> A0\n- Markaz sensor -> A1\n- O'ng sensor -> A2\n- Motor A -> L298N OUT1/OUT2\n- Motor B -> L298N OUT3/OUT4\n\n### Ishlash:\n- Qora chiziq: sensor qiymati past\n- Oq zamin: sensor qiymati yuqori\n- Robot chiziqni kuzatib boradi"
  },
  {
    "id": "arduino-sumo-robot",
    "n": "Mini Sumo Robot",
    "i": "⚔️",
    "diff": "hard",
    "c": "arduino",
    "t": [
      "arduino",
      "sumo",
      "robot",
      "ultrasonic",
      "motor",
      "competition"
    ],
    "d": "Mini sumo robot. Dushmani topish va itarish. Ultrasonic sensor bilan.",
    "f": [
      {
        "n": "sumo_robot.ino",
        "p": "sumo_robot.ino",
        "c": "#include <Servo.h>\n\n// Sensorlar\nconst int ULTRASONIC_TRIG = 2;\nconst int ULTRASONIC_ECHO = 3;\nconst int IR_LEFT = A0;\nconst int IR_RIGHT = A1;\n\n// Motorlar (L298N)\nconst int MOTOR_A_EN = 5;\nconst int MOTOR_A_IN1 = 6;\nconst int MOTOR_A_IN2 = 7;\nconst int MOTOR_B_EN = 10;\nconst int MOTOR_B_IN1 = 8;\nconst int MOTOR_B_IN2 = 9;\n\n// Boshqa\nconst int BUZZER = 11;\nconst int EDGE_THRESHOLD = 300;\nconst int ENEMY_THRESHOLD = 30;\n\nServo weapon;\nbool startPressed = false;\n\nvoid setup() {\n  Serial.begin(9600);\n  weapon.attach(12);\n  weapon.write(0);\n  \n  pinMode(ULTRASONIC_TRIG, OUTPUT);\n  pinMode(ULTRASONIC_ECHO, INPUT);\n  pinMode(MOTOR_A_EN, OUTPUT);\n  pinMode(MOTOR_A_IN1, OUTPUT);\n  pinMode(MOTOR_A_IN2, OUTPUT);\n  pinMode(MOTOR_B_EN, OUTPUT);\n  pinMode(MOTOR_B_IN1, OUTPUT);\n  pinMode(MOTOR_B_IN2, OUTPUT);\n  pinMode(BUZZER, OUTPUT);\n  \n  Serial.println('Mini Sumo Robot Ready');\n  Serial.println('3 soniyadan keyin boshlanadi...');\n  delay(3000);\n}\n\nfloat getDistance() {\n  digitalWrite(ULTRASONIC_TRIG, LOW);\n  delayMicroseconds(2);\n  digitalWrite(ULTRASONIC_TRIG, HIGH);\n  delayMicroseconds(10);\n  digitalWrite(ULTRASONIC_TRIG, LOW);\n  long duration = pulseIn(ULTRASONIC_ECHO, HIGH);\n  return duration * 0.034 / 2;\n}\n\nvoid moveForward(int speed) {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, speed);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, speed);\n}\n\nvoid moveBackward(int speed) {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, speed);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, speed);\n}\n\nvoid turnLeft(int speed) {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, speed);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, speed);\n}\n\nvoid turnRight(int speed) {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, speed);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, speed);\n}\n\nvoid attack() {\n  Serial.println('Hujum!');\n  weapon.write(90);\n  moveForward(255);\n  delay(500);\n  weapon.write(0);\n}\n\nvoid retreat() {\n  Serial.println('Qochish!');\n  moveBackward(200);\n  delay(500);\n  turnRight(200);\n  delay(300);\n}\n\nvoid loop() {\n  float distance = getDistance();\n  int irLeft = analogRead(IR_LEFT);\n  int irRight = analogRead(IR_RIGHT);\n  \n  // Rangga tushib qolishni tekshirish\n  if (irLeft < EDGE_THRESHOLD || irRight < EDGE_THRESHOLD) {\n    Serial.println('Edge detected! Retreating...');\n    retreat();\n    return;\n  }\n  \n  // Dushmani topish\n  if (distance < ENEMY_THRESHOLD) {\n    Serial.print('Enemy at: ');\n    Serial.print(distance);\n    Serial.println(' cm');\n    attack();\n  } else {\n    // Qidirish - aylanish\n    static unsigned long lastTurn = 0;\n    if (millis() - lastTurn > 1000) {\n      turnLeft(150);\n      lastTurn = millis();\n    }\n  }\n  \n  delay(50);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Mini Sumo Robot\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- 2x DC Motor + L298N Driver\n- HC-SR04 Ultrasonic Sensor\n- 2x IR Sensor (dokon aniqlash)\n- Servo (qurol uchun, ixtiyoriy)\n- Buzzer\n\n### Ulanma:\n- Ultrasonic: TRIG->2, ECHO->3\n- IR Chap: A0\n- IR O'ng: A1\n- Motor A: EN->5, IN1->6, IN2->7\n- Motor B: EN->10, IN1->8, IN2->9\n- Servo -> 12\n- Buzzer -> 11\n\n### Strategiya:\n1. Dushmani ultrasonic bilan topish\n2. 30 sm dan yaqin bo'lsa hujum\n3. Doka tufayli qochish\n4. Dushman topilmasa aylanib qidirish"
  },
  {
    "id": "arduino-avoiding-car",
    "n": "Avoiding RoboCar",
    "i": "🚗",
    "diff": "hard",
    "c": "arduino",
    "t": [
      "arduino",
      "avoiding",
      "car",
      "robot",
      "ultrasonic",
      "servo"
    ],
    "d": "To'siqlardan qochuvchi avtomatik avtomobil. Ultrasonic va servo bilan.",
    "f": [
      {
        "n": "avoiding_car.ino",
        "p": "avoiding_car.ino",
        "c": "#include <Servo.h>\n\nconst int TRIG_PIN = 2;\nconst int ECHO_PIN = 3;\nconst int SERVO_PIN = 9;\nconst int MOTOR_A_EN = 5;\nconst int MOTOR_A_IN1 = 6;\n const int MOTOR_A_IN2 = 7;\nconst int MOTOR_B_EN = 10;\nconst int MOTOR_B_IN1 = 8;\nconst int MOTOR_B_IN2 = 11;\n\nServo scanServo;\nconst int SAFE_DISTANCE = 25;\n\nvoid setup() {\n  Serial.begin(9600);\n  scanServo.attach(SERVO_PIN);\n  \n  pinMode(TRIG_PIN, OUTPUT);\n  pinMode(ECHO_PIN, INPUT);\n  pinMode(MOTOR_A_EN, OUTPUT);\n  pinMode(MOTOR_A_IN1, OUTPUT);\n  pinMode(MOTOR_A_IN2, OUTPUT);\n  pinMode(MOTOR_B_EN, OUTPUT);\n  pinMode(MOTOR_B_IN1, OUTPUT);\n  pinMode(MOTOR_B_IN2, OUTPUT);\n  \n  scanServo.write(90);\n  delay(500);\n  Serial.println('Avoiding Car Ready');\n}\n\nfloat getDistance() {\n  digitalWrite(TRIG_PIN, LOW);\n  delayMicroseconds(2);\n  digitalWrite(TRIG_PIN, HIGH);\n  delayMicroseconds(10);\n  digitalWrite(TRIG_PIN, LOW);\n  long duration = pulseIn(ECHO_PIN, HIGH);\n  return duration * 0.034 / 2;\n}\n\nvoid moveForward() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, 150);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, 150);\n}\n\nvoid moveBackward() {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, 150);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, 150);\n}\n\nvoid turnLeft() {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, 180);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, 180);\n}\n\nvoid turnRight() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, 180);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, 180);\n}\n\nvoid stopMotors() {\n  analogWrite(MOTOR_A_EN, 0);\n  analogWrite(MOTOR_B_EN, 0);\n}\n\nfloat scan(int angle) {\n  scanServo.write(angle);\n  delay(500);\n  return getDistance();\n}\n\nvoid loop() {\n  float distance = getDistance();\n  Serial.print('Distance: ');\n  Serial.print(distance);\n  Serial.println(' cm');\n  \n  if (distance < SAFE_DISTANCE) {\n    Serial.println('To\\'siq topildi! Qochish...');\n    stopMotors();\n    delay(200);\n    moveBackward();\n    delay(400);\n    stopMotors();\n    \n    float leftDist = scan(160);\n    float rightDist = scan(20);\n    scanServo.write(90);\n    \n    Serial.print('Left: '); Serial.print(leftDist);\n    Serial.print(' | Right: '); Serial.println(rightDist);\n    \n    if (leftDist > rightDist) {\n      Serial.println('Chapga burilish');\n      turnLeft();\n    } else {\n      Serial.println('O\\'ngga burilish');\n      turnRight();\n    }\n    delay(600);\n  } else {\n    moveForward();\n  }\n  \n  delay(100);\n}"
      }
    ],
    "icon": "building",
    "instructions": "## Avoiding RoboCar\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- 2x DC Motor + L298N Driver\n- HC-SR04 Ultrasonic\n- SG90 Servo (sensor uchun)\n- Chassi + g'ildiraklar\n\n### Ulanma:\n- Ultrasonic: TRIG->2, ECHO->3\n- Servo -> 9\n- Motor A: EN->5, IN1->6, IN2->7\n- Motor B: EN->10, IN1->8, IN2->11\n\n### Ishlash:\n1. Oldinga yurish\n2. To'siqlarni ultrasonic bilan aniqlash\n3. 25 sm dan yaqin bo'lsa to'xtash\n4. Servo bilan chap/o'ng qarash\n5. Ko'proq joy tomonga burilish\n6. Davom etish"
  },
  {
    "id": "arduino-bluetooth",
    "n": "Bluetooth Boshqaruvi",
    "i": "📶",
    "diff": "medium",
    "c": "arduino",
    "t": [
      "arduino",
      "bluetooth",
      "hc-05",
      "remote",
      "control"
    ],
    "d": "HC-05 Bluetooth modul orqali Arduino boshqaruvi. Telefon App.",
    "f": [
      {
        "n": "bluetooth_control.ino",
        "p": "bluetooth_control.ino",
        "c": "#include <SoftwareSerial.h>\n\nSoftwareSerial BT(10, 11); // RX, TX\n\nconst int MOTOR_A_EN = 5;\nconst int MOTOR_A_IN1 = 6;\nconst int MOTOR_A_IN2 = 7;\nconst int MOTOR_B_EN = 9;\nconst int MOTOR_B_IN1 = 8;\nconst int MOTOR_B_IN2 = 12;\n\nvoid setup() {\n  Serial.begin(9600);\n  BT.begin(9600);\n  \n  pinMode(MOTOR_A_EN, OUTPUT);\n  pinMode(MOTOR_A_IN1, OUTPUT);\n  pinMode(MOTOR_A_IN2, OUTPUT);\n  pinMode(MOTOR_B_EN, OUTPUT);\n  pinMode(MOTOR_B_IN1, OUTPUT);\n  pinMode(MOTOR_B_IN2, OUTPUT);\n  \n  Serial.println('Bluetooth Control Ready');\n  BT.println('Bluetooth Control Ready');\n}\n\nvoid moveForward() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, 200);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, 200);\n}\n\nvoid moveBackward() {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, 200);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, 200);\n}\n\nvoid turnLeft() {\n  digitalWrite(MOTOR_A_IN1, LOW);\n  digitalWrite(MOTOR_A_IN2, HIGH);\n  analogWrite(MOTOR_A_EN, 150);\n  digitalWrite(MOTOR_B_IN1, HIGH);\n  digitalWrite(MOTOR_B_IN2, LOW);\n  analogWrite(MOTOR_B_EN, 150);\n}\n\nvoid turnRight() {\n  digitalWrite(MOTOR_A_IN1, HIGH);\n  digitalWrite(MOTOR_A_IN2, LOW);\n  analogWrite(MOTOR_A_EN, 150);\n  digitalWrite(MOTOR_B_IN1, LOW);\n  digitalWrite(MOTOR_B_IN2, HIGH);\n  analogWrite(MOTOR_B_EN, 150);\n}\n\nvoid stopMotors() {\n  analogWrite(MOTOR_A_EN, 0);\n  analogWrite(MOTOR_B_EN, 0);\n}\n\nvoid loop() {\n  if (BT.available()) {\n    char command = BT.read();\n    \n    switch (command) {\n      case 'F': moveForward(); Serial.println('Forward'); break;\n      case 'B': moveBackward(); Serial.println('Backward'); break;\n      case 'L': turnLeft(); Serial.println('Left'); break;\n      case 'R': turnRight(); Serial.println('Right'); break;\n      case 'S': stopMotors(); Serial.println('Stop'); break;\n      case '1': analogWrite(MOTOR_A_EN, 100); analogWrite(MOTOR_B_EN, 100); Serial.println('Speed: Low'); break;\n      case '2': analogWrite(MOTOR_A_EN, 150); analogWrite(MOTOR_B_EN, 150); Serial.println('Speed: Medium'); break;\n      case '3': analogWrite(MOTOR_A_EN, 200); analogWrite(MOTOR_B_EN, 200); Serial.println('Speed: High'); break;\n      case '4': analogWrite(MOTOR_A_EN, 255); analogWrite(MOTOR_B_EN, 255); Serial.println('Speed: Max'); break;\n    }\n  }\n}"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Bluetooth Boshqaruvi\n\n### Kerakli uskunalar:\n- Arduino UNO/Nano/Mega\n- HC-05/HC-06 Bluetooth\n- L298N Motor Driver\n- 2x DC Motor\n- Telefon (Bluetooth App)\n\n### Ulanma:\n- HC-05 TX -> Pin 10 (RX)\n- HC-05 RX -> Pin 11 (TX) + voltage divider\n- Motor A: EN->5, IN1->6, IN2->7\n- Motor B: EN->9, IN1->8, IN2->12\n\n### Buyruqlar:\n- F = Oldga\n- B = Orqaga\n- L = Chapga\n- R = O'ngga\n- S = To'xtash\n- 1-4 = Tezlik darajasi"
  },
  {
    "id": "mqtt-iot",
    "n": "MQTT IoT Sensor",
    "i": "📡",
    "diff": "hard",
    "c": "arduino",
    "t": [
      "arduino",
      "mqtt",
      "iot",
      "wifi",
      "esp8266",
      "sensor"
    ],
    "d": "ESP8266 bilan WiFi va MQTT orqali sensor ma'lumotlarini yuborish.",
    "f": [
      {
        "n": "mqtt_sensor.ino",
        "p": "mqtt_sensor.ino",
        "c": "#include <ESP8266WiFi.h>\n#include <PubSubClient.h>\n\nconst char* ssid = 'WiFi_Nomi';\nconst char* password = 'WiFi_Parol';\nconst char* mqtt_server = 'broker.hivemq.com';\n\nWiFiClient espClient;\nPubSubClient client(espClient);\n\nvoid setup_wifi() {\n  WiFi.begin(ssid, password);\n  while (WiFi.status() != WL_CONNECTED) delay(500);\n}\n\nvoid callback(char* topic, byte* payload, unsigned int length) {\n  String msg;\n  for (int i = 0; i < length; i++) msg += (char)payload[i];\n  Serial.println(msg);\n}\n\nvoid reconnect() {\n  while (!client.connected()) {\n    client.connect('ESP8266Client');\n    client.subscribe('codebase/led');\n  }\n}\n\nvoid setup() {\n  Serial.begin(9600);\n  setup_wifi();\n  client.setServer(mqtt_server, 1883);\n  client.setCallback(callback);\n}\n\nvoid loop() {\n  if (!client.connected()) reconnect();\n  client.loop();\n  static unsigned long last = 0;\n  if (millis() - last > 5000) {\n    float temp = analogRead(A0) * 0.488;\n    char buf[8];\n    dtostrf(temp, 4, 1, buf);\n    client.publish('codebase/temp', buf);\n    last = millis();\n  }\n}"
      }
    ],
    "icon": "globe",
    "instructions": "## MQTT IoT\n\n### Kerakli:\n- ESP8266 (NodeMCU)\n- DHT11/LM35 sensor\n\n### Ulanma:\n- Sensor -> A0\n- WiFi va MQTT broker kerak"
  },
  {
    "id": "python-fibonacci",
    "n": "Python Fibonacci",
    "i": "🐍",
    "diff": "easy",
    "c": "python",
    "t": [
      "python",
      "fibonacci",
      "algorithm"
    ],
    "d": "Fibonacci ketma-ketligini hisoblash.",
    "f": [
      {
        "n": "fibonacci.py",
        "p": "fibonacci.py",
        "c": "def fibonacci(n):\n    if n <= 1: return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    return b\n\nfor i in range(10):\n    print(f'Fib({i}) = {fibonacci(i)}')"
      }
    ],
    "icon": "document",
    "instructions": "## Python Fibonacci\n\n```bash\npython fibonacci.py\n```"
  },
  {
    "id": "python-flask-api",
    "n": "Flask REST API",
    "i": "🌐",
    "diff": "medium",
    "c": "python",
    "t": [
      "python",
      "flask",
      "api",
      "rest"
    ],
    "d": "Flask bilan REST API yaratish.",
    "f": [
      {
        "n": "app.py",
        "p": "app.py",
        "c": "from flask import Flask, jsonify, request\napp = Flask(__name__)\nusers = [{'id':1,'name':'Ali'}]\n\n@app.route('/api/users')\ndef get_users(): return jsonify(users)\n\n@app.route('/api/users',methods=['POST'])\ndef add_user():\n    users.append({'id':len(users)+1,'name':request.json['name']})\n    return jsonify({'ok':True})\n\nif __name__=='__main__': app.run(debug=True)"
      }
    ],
    "icon": "globe",
    "instructions": "## Flask API\n\n```bash\npip install flask\npython app.py\n```"
  },
  {
    "id": "python-web-scraper",
    "n": "Web Scraper",
    "i": "🕷️",
    "diff": "medium",
    "c": "python",
    "t": [
      "python",
      "scraper",
      "beautifulsoup"
    ],
    "d": "BeautifulSoup bilan web skraper.",
    "f": [
      {
        "n": "scraper.py",
        "p": "scraper.py",
        "c": "import requests\nfrom bs4 import BeautifulSoup\n\nurl = 'https://example.com'\nres = requests.get(url)\nsoup = BeautifulSoup(res.text, 'html.parser')\nfor title in soup.find_all('h1'):\n    print(title.text)"
      }
    ],
    "icon": "document",
    "instructions": "## Web Scraper\n\n```bash\npip install requests beautifulsoup4\npython scraper.py\n```"
  },
  {
    "id": "python-tkinter",
    "n": "Tkinter GUI",
    "i": "🖥️",
    "diff": "medium",
    "c": "python",
    "t": [
      "python",
      "tkinter",
      "gui",
      "desktop"
    ],
    "d": "Tkinter bilan desktop ilova.",
    "f": [
      {
        "n": "gui_app.py",
        "p": "gui_app.py",
        "c": "import tkinter as tk\nfrom tkinter import messagebox\n\ndef on_click():\n    name = entry.get()\n    if name: messagebox.showinfo('Salom', f'Xush kelibsiz, {name}!')\n\nroot = tk.Tk()\nroot.title('CodeBase App')\nroot.geometry('300x200')\ntk.Label(root, text='Ismingiz:', font=('Arial',14)).pack(pady=10)\nentry = tk.Entry(root, font=('Arial',12))\nentry.pack(pady=5)\ntk.Button(root, text='Salomlash', command=on_click, bg='#3b82f6', fg='white').pack(pady=20)\nroot.mainloop()"
      }
    ],
    "icon": "building",
    "instructions": "## Tkinter GUI\n\n```bash\npython gui_app.py\n```"
  },
  {
    "id": "python-decorators",
    "n": "Python Decorators",
    "i": "🎭",
    "diff": "medium",
    "c": "python",
    "t": [
      "python",
      "decorator",
      "closure"
    ],
    "d": "Custom decorator yaratish.",
    "f": [
      {
        "n": "decorators.py",
        "p": "decorators.py",
        "c": "import time\nfrom functools import wraps\n\ndef timer(func):\n    @wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        print(f'{func.__name__}: {time.time()-start:.4f}s')\n        return result\n    return wrapper\n\n@timer\ndef slow():\n    time.sleep(1)\n    return 'Done'\n\nprint(slow())"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Decorators\n\n```bash\npython decorators.py\n```"
  },
  {
    "id": "python-oop",
    "n": "Python OOP",
    "i": "📦",
    "diff": "easy",
    "c": "python",
    "t": [
      "python",
      "class",
      "oop"
    ],
    "d": "Python OOP asoslari.",
    "f": [
      {
        "n": "oop.py",
        "p": "oop.py",
        "c": "class Animal:\n    def __init__(self, name, sound):\n        self.name = name\n        self.sound = sound\n    def speak(self): return f'{self.name}: {self.sound}'\n\nclass Dog(Animal):\n    def __init__(self, name): super().__init__(name, 'Vov-vov')\n    def fetch(self): return f'{self.name} koptokni olib keldi!'\n\ndog = Dog('Rex')\nprint(dog.speak())\nprint(dog.fetch())"
      }
    ],
    "icon": "document",
    "instructions": "## Python OOP\n\n```bash\npython oop.py\n```"
  },
  {
    "id": "python-json-csv",
    "n": "JSON/CSV Handler",
    "i": "📊",
    "diff": "easy",
    "c": "python",
    "t": [
      "python",
      "json",
      "csv",
      "file"
    ],
    "d": "JSON va CSV fayllar bilan ishlash.",
    "f": [
      {
        "n": "data_handler.py",
        "p": "data_handler.py",
        "c": "import json, csv\n\nusers = [{'name':'Ali','age':25},{'name':'Vali','age':30}]\n\nwith open('users.json','w') as f: json.dump(users, f, indent=2)\nwith open('users.json','r') as f: print(json.load(f))\n\nwith open('users.csv','w',newline='') as f:\n    w = csv.DictWriter(f, fieldnames=['name','age'])\n    w.writeheader()\n    w.writerows(users)"
      }
    ],
    "icon": "document",
    "instructions": "## JSON/CSV\n\n```bash\npython data_handler.py\n```"
  },
  {
    "id": "python-async",
    "n": "Python Async/Await",
    "i": "⚡",
    "diff": "hard",
    "c": "python",
    "t": [
      "python",
      "async",
      "await",
      "concurrent"
    ],
    "d": "Asinxron dasturlash.",
    "f": [
      {
        "n": "async_example.py",
        "p": "async_example.py",
        "c": "import asyncio\n\nasync def fetch(n):\n    print(f'Start {n}')\n    await asyncio.sleep(n)\n    print(f'End {n}')\n    return n\n\nasync def main():\n    tasks = [fetch(i) for i in [1,2,3]]\n    results = await asyncio.gather(*tasks)\n    print(f'Results: {results}')\n\nasyncio.run(main())"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Async\n\n```bash\npython async_example.py\n```"
  },
  {
    "id": "js-todo",
    "n": "Todo App",
    "i": "📝",
    "diff": "easy",
    "c": "javascript",
    "t": [
      "javascript",
      "todo",
      "dom",
      "localstorage"
    ],
    "d": "Vanilla JS Todo App.",
    "f": [
      {
        "n": "index.html",
        "p": "index.html",
        "c": "<!DOCTYPE html><html><head><title>Todo</title><style>body{font-family:system-ui;max-width:500px;margin:40px auto;padding:20px}input{padding:10px;width:70%;border:1px solid #ddd;border-radius:6px}button{padding:10px 20px;background:#3b82f6;color:#fff;border:none;border-radius:6px;cursor:pointer}li{padding:10px;margin:5px 0;background:#f5f5f5;border-radius:6px;display:flex;justify-content:space-between}</style></head><body><h1>Todo App</h1><input id='inp' placeholder='Yangi vazifa...'><button onclick='add()'>Qoshish</button><ul id='list'></ul><script>let todos=JSON.parse(localStorage.getItem('todos'))||[];function render(){document.getElementById('list').innerHTML=todos.map((t,i)=>`<li>${t.text}<button onclick='del(${i})'>X</button></li>`).join('');localStorage.setItem('todos',JSON.stringify(todos));}function add(){const v=document.getElementById('inp').value.trim();if(v){todos.push({text:v});document.getElementById('inp').value='';render();}}function del(i){todos.splice(i,1);render();}render();<\\/script></body></html>"
      }
    ],
    "icon": "document",
    "instructions": "## JS Todo\n\nBrauzerda index.html ni oching."
  },
  {
    "id": "js-calculator",
    "n": "Calculator",
    "i": "🔢",
    "diff": "easy",
    "c": "javascript",
    "t": [
      "javascript",
      "calculator",
      "math",
      "dom"
    ],
    "d": "Kalkulyator.",
    "f": [
      {
        "n": "index.html",
        "p": "index.html",
        "c": "<!DOCTYPE html><html><head><title>Calc</title><style>body{font-family:system-ui;display:flex;justify-content:center;align-items:center;min-height:100vh;background:#1a1a2e}.calc{background:#16213e;padding:20px;border-radius:12px;width:300px}#d{background:#0f3460;color:#fff;padding:15px;text-align:right;font-size:24px;border-radius:8px;margin-bottom:10px}.b{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}button{padding:15px;font-size:18px;border:none;border-radius:8px;cursor:pointer;background:#1a1a2e;color:#fff}.o{background:#e94560}</style></head><body><div class='calc'><div id='d'>0</div><div class='b'><button onclick='c()'>C</button><button onclick='b()'>⌫</button><button class='o' onclick='o(\"/\")'>/</button><button class='o' onclick='o(\"*\")'>*</button><button onclick='n(\"7\")'>7</button><button onclick='n(\"8\")'>8</button><button onclick='n(\"9\")'>9</button><button class='o' onclick='o(\"-\")'>-</button><button onclick='n(\"4\")'>4</button><button onclick='n(\"5\")'>5</button><button onclick='n(\"6\")'>6</button><button class='o' onclick='o(\"+\")'>+</button><button onclick='n(\"1\")'>1</button><button onclick='n(\"2\")'>2</button><button onclick='n(\"3\")'>3</button><button class='o' onclick='e()'>=</button><button onclick='n(\"0\")' style='grid-column:span 2'>0</button><button onclick='d()'>.</button></div></div><script>let v='0',p='',o='';function n(x){v=v==='0'?x:v+x;u()}function d(){if(!v.includes('.'))v+='.';u()}function o(x){p=v;o=x;v='0'}function e(){v=String(eval(p+o+v));u()}function c(){v='0';p='';o='';u()}function b(){v=v.slice(0,-1)||'0';u()}function u(){document.getElementById('d').textContent=v}<\\/script></body></html>"
      }
    ],
    "icon": "document",
    "instructions": "## Calculator\n\nBrauzerda index.html ni oching."
  },
  {
    "id": "react-counter",
    "n": "React Counter",
    "i": "⚛️",
    "diff": "easy",
    "c": "react",
    "t": [
      "react",
      "counter",
      "hooks",
      "useState"
    ],
    "d": "React useState Counter.",
    "f": [
      {
        "n": "Counter.jsx",
        "p": "src/Counter.jsx",
        "c": "import { useState } from 'react';\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <div style={{textAlign:'center',padding:'40px'}}>\n      <h1 style={{fontSize:'4rem'}}>{count}</h1>\n      <button onClick={()=>setCount(count-1)}>Kamaytirish</button>\n      <button onClick={()=>setCount(0)}>Reset</button>\n      <button onClick={()=>setCount(count+1)}>Oshirish</button>\n    </div>\n  );\n}\nexport default Counter;"
      }
    ],
    "icon": "sparkle",
    "instructions": "## React Counter\n\n```bash\nnpx create-react-app my-app\nnpm start\n```"
  },
  {
    "id": "react-todo",
    "n": "React Todo",
    "i": "✅",
    "diff": "easy",
    "c": "react",
    "t": [
      "react",
      "todo",
      "hooks",
      "state"
    ],
    "d": "React Todo App.",
    "f": [
      {
        "n": "TodoApp.jsx",
        "p": "src/TodoApp.jsx",
        "c": "import { useState } from 'react';\nfunction TodoApp() {\n  const [todos, setTodos] = useState([]);\n  const [input, setInput] = useState('');\n  const add = () => { if(input.trim()){setTodos([...todos,{text:input,done:false}]);setInput('');} };\n  return (\n    <div style={{maxWidth:500,margin:'40px auto'}}>\n      <h1>React Todo</h1>\n      <input value={input} onChange={e=>setInput(e.target.value)} onKeyPress={e=>e.key==='Enter'&&add()}/>\n      <button onClick={add}>Qoshish</button>\n      <ul>{todos.map((t,i)=><li key={i} onClick={()=>{const n=[...todos];n[i].done=!n[i].done;setTodos(n);}} style={{textDecoration:t.done?'line-through':'none'}}>{t.text}</li>)}</ul>\n    </div>\n  );\n}\nexport default TodoApp;"
      }
    ],
    "icon": "sparkle",
    "instructions": "## React Todo\n\n```bash\nnpx create-react-app my-app\nnpm start\n```"
  },
  {
    "id": "vue-counter",
    "n": "Vue Counter",
    "i": "💚",
    "diff": "easy",
    "c": "vue",
    "t": [
      "vue",
      "counter",
      "composition",
      "reactive"
    ],
    "d": "Vue 3 Counter.",
    "f": [
      {
        "n": "Counter.vue",
        "p": "src/Counter.vue",
        "c": "<template>\n  <div style='text-align:center;padding:40px'>\n    <h1 style='font-size:4rem'>{{ count }}</h1>\n    <button @click='count--'>Kamaytirish</button>\n    <button @click='count=0'>Reset</button>\n    <button @click='count++'>Oshirish</button>\n  </div>\n</template>\n<script setup>\nimport { ref } from 'vue'\nconst count = ref(0)\n</script>"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Vue Counter\n\n```bash\nnpm create vue@latest\nnpm run dev\n```"
  },
  {
    "id": "vue-todo",
    "n": "Vue Todo",
    "i": "✅",
    "diff": "easy",
    "c": "vue",
    "t": [
      "vue",
      "todo",
      "composition",
      "reactive"
    ],
    "d": "Vue Todo App.",
    "f": [
      {
        "n": "TodoApp.vue",
        "p": "src/TodoApp.vue",
        "c": "<template>\n  <div style='max-width:500px;margin:40px auto'>\n    <h1>Vue Todo</h1>\n    <input v-model='input' @keyup.enter='add' placeholder='Yangi vazifa...'>\n    <button @click='add'>Qoshish</button>\n    <ul>\n      <li v-for='(t,i) in todos' :key='i' @click='t.done=!t.done' :style=\"{textDecoration:t.done?'line-through':'none'}\">{{ t.text }}</li>\n    </ul>\n  </div>\n</template>\n<script setup>\nimport { ref } from 'vue'\nconst input = ref('')\nconst todos = ref([])\nconst add = () => { if(input.value.trim()){todos.value.push({text:input.value,done:false});input.value='';} }\n</script>"
      }
    ],
    "icon": "sparkle",
    "instructions": "## Vue Todo\n\n```bash\nnpm create vue@latest\nnpm run dev\n```"
  },
  {
    "id": "nodejs-express",
    "n": "Express REST API",
    "i": "🟢",
    "diff": "medium",
    "c": "nodejs",
    "t": [
      "nodejs",
      "express",
      "api",
      "rest"
    ],
    "d": "Express.js REST API.",
    "f": [
      {
        "n": "server.js",
        "p": "server.js",
        "c": "const express = require('express');\nconst app = express();\napp.use(express.json());\nlet books = [{id:1,title:'Dart',author:'Asadbek'}];\napp.get('/api/books', (req,res) => res.json(books));\napp.post('/api/books', (req,res) => { books.push({id:books.length+1,...req.body}); res.status(201).json({ok:true}); });\napp.listen(3000, () => console.log('Server port 3000'));"
      }
    ],
    "icon": "globe",
    "instructions": "## Express API\n\n```bash\nnpm init -y && npm install express\nnode server.js\n```"
  },
  {
    "id": "html-landing",
    "n": "Landing Page",
    "i": "🌐",
    "diff": "easy",
    "c": "html-css",
    "t": [
      "html",
      "css",
      "landing",
      "responsive"
    ],
    "d": "Responsive landing page.",
    "f": [
      {
        "n": "index.html",
        "p": "index.html",
        "c": "<!DOCTYPE html><html><head><meta charset='UTF-8'><meta name='viewport' content='width=device-width,initial-scale=1'><title>Landing</title><style>*{margin:0;padding:0;box-sizing:border-box}body{font-family:system-ui;background:#0f0f1c;color:#fff}.hero{text-align:center;padding:100px 20px}.hero h1{font-size:clamp(36px,5vw,64px);margin-bottom:20px}.hero span{background:linear-gradient(135deg,#3b82f6,#a855f7);-webkit-background-clip:text;-webkit-text-fill-color:transparent}.btn{padding:14px 32px;background:#3b82f6;color:#fff;border:none;border-radius:8px;font-size:16px;cursor:pointer}.features{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:24px;max-width:1000px;margin:60px auto;padding:0 20px}.card{background:rgba(255,255,255,.03);border:1px solid #27272a;border-radius:12px;padding:32px}.card h3{margin-bottom:8px}.card p{color:#a1a1aa}</style></head><body><section class='hero'><h1>Kodingizni <span>Keyingi Darajaga</span> Olib Chiqing</h1><p style='color:#a1a1aa;margin-bottom:30px'>200+ tayyor kod shablonlari</p><button class='btn'>Bepul Boshlash</button></section><div class='features'><div class='card'><h3>Flutter</h3><p>180+ mobile templates</p></div><div class='card'><h3>Arduino</h3><p>16+ IoT templates</p></div><div class='card'><h3>Python</h3><p>8+ scripting templates</p></div></div></body></html>"
      }
    ],
    "icon": "palette",
    "instructions": "## Landing Page\n\nBrauzerda index.html ni oching."
  },
  {
    "id": "featherbank",
    "n": "Featherbank (Fintech App)",
    "i": "🏦",
    "diff": "hard",
    "c": "pages",
    "t": [
      "flutter",
      "fintech",
      "banking",
      "finance",
      "cards",
      "profile"
    ],
    "d": "To'liq fintech ilova: bosh sahifa balans, kartalar, pul qo'shish, profil.",
    "f": [
      {
        "n": "main.dart",
        "p": "lib/main.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'package:flutter/services.dart';\nimport 'pages/navigation.dart';\n\nvoid main() {\n  WidgetsFlutterBinding.ensureInitialized();\n  SystemChrome.setSystemUIOverlayStyle(const SystemUiOverlayStyle(\n    statusBarColor: Colors.transparent,\n    statusBarIconBrightness: Brightness.light,\n  ));\n  runApp(const FeatherbankApp());\n}\n\nclass FeatherbankApp extends StatelessWidget {\n  const FeatherbankApp({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp(\n      title: 'Featherbank',\n      debugShowCheckedModeBanner: false,\n      theme: ThemeData(\n        colorSchemeSeed: const Color(0xFF1A1A2E),\n        useMaterial3: true,\n        brightness: Brightness.light,\n        scaffoldBackgroundColor: const Color(0xFFF8F9FA),\n        appBarTheme: const AppBarTheme(\n          backgroundColor: Color(0xFF1A1A2E),\n          foregroundColor: Colors.white,\n          elevation: 0,\n        ),\n      ),\n      home: const MainNavigation(),\n    );\n  }\n}"
      },
      {
        "n": "navigation.dart",
        "p": "lib/pages/navigation.dart",
        "c": "import 'package:flutter/material.dart';\nimport 'home_page.dart';\nimport 'cards_page.dart';\nimport 'add_money_page.dart';\nimport 'profile_page.dart';\n\nclass MainNavigation extends StatefulWidget {\n  const MainNavigation({super.key});\n  @override\n  State<MainNavigation> createState() => _MainNavigationState();\n}\n\nclass _MainNavigationState extends State<MainNavigation> {\n  int _currentIndex = 0;\n  final List<Widget> _pages = const [\n    HomePage(),\n    CardsPage(),\n    AddMoneyPage(),\n    ProfilePage(),\n  ];\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: IndexedStack(index: _currentIndex, children: _pages),\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: _currentIndex,\n        onDestinationSelected: (index) => setState(() => _currentIndex = index),\n        backgroundColor: Colors.white,\n        indicatorColor: const Color(0xFF2ECC71).withValues(alpha: 0.2),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home, color: Color(0xFF2ECC71)), label: 'Home'),\n          NavigationDestination(icon: Icon(Icons.credit_card_outlined), selectedIcon: Icon(Icons.credit_card, color: Color(0xFF2ECC71)), label: 'Cards'),\n          NavigationDestination(icon: Icon(Icons.add_circle_outline), selectedIcon: Icon(Icons.add_circle, color: Color(0xFF2ECC71)), label: 'Add'),\n          NavigationDestination(icon: Icon(Icons.person_outline), selectedIcon: Icon(Icons.person, color: Color(0xFF2ECC71)), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}"
      },
      {
        "n": "home_page.dart",
        "p": "lib/pages/home_page.dart",
        "c": "import 'package:flutter/material.dart';\nimport '../widgets/card_carousel.dart';\nimport '../widgets/transaction_tile.dart';\n\nclass HomePage extends StatelessWidget {\n  const HomePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      backgroundColor: const Color(0xFFF8F9FA),\n      body: SafeArea(\n        child: ListView(\n          padding: const EdgeInsets.all(0),\n          children: [\n            Container(\n              padding: const EdgeInsets.fromLTRB(24, 20, 24, 30),\n              decoration: const BoxDecoration(\n                color: Color(0xFF1A1A2E),\n                borderRadius: BorderRadius.only(bottomLeft: Radius.circular(24), bottomRight: Radius.circular(24)),\n              ),\n              child: Column(\n                crossAxisAlignment: CrossAxisAlignment.start,\n                children: [\n                  Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n                    Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n                      Text('Good morning, Tony', style: TextStyle(color: Colors.white.withValues(alpha: 0.7), fontSize: 14)),\n                      const SizedBox(height: 4),\n                      const Text('Featherbank', style: TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.bold)),\n                    ]),\n                    Container(padding: const EdgeInsets.all(10), decoration: BoxDecoration(color: Colors.white.withValues(alpha: 0.1), borderRadius: BorderRadius.circular(12)),\n                      child: const Icon(Icons.notifications_outlined, color: Colors.white, size: 22)),\n                  ]),\n                  const SizedBox(height: 30),\n                  const Text('Total Balance', style: TextStyle(color: Colors.white60, fontSize: 14)),\n                  const SizedBox(height: 8),\n                  const Text('\\$3,200.00', style: TextStyle(color: Colors.white, fontSize: 36, fontWeight: FontWeight.bold)),\n                  const SizedBox(height: 20),\n                  SizedBox(width: double.infinity, height: 48, child: ElevatedButton(\n                    onPressed: () {},\n                    style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFF2ECC71), foregroundColor: Colors.white, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)), elevation: 0),\n                    child: const Text('Add money', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600)),\n                  )),\n                ])),\n            const SizedBox(height: 24),\n            Padding(padding: const EdgeInsets.symmetric(horizontal: 24), child: Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n              const Text('Your cards', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n              TextButton(onPressed: () {}, child: const Text('+ New card', style: TextStyle(color: Color(0xFF2ECC71), fontWeight: FontWeight.w600))),\n            ])),\n            const SizedBox(height: 8),\n            const CardCarousel(),\n            const SizedBox(height: 24),\n            Padding(padding: const EdgeInsets.symmetric(horizontal: 24), child: Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n              const Text('Transactions', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n              TextButton(onPressed: () {}, child: const Text('See all', style: TextStyle(color: Color(0xFF2ECC71), fontWeight: FontWeight.w600))),\n            ])),\n            const SizedBox(height: 8),\n            const TransactionTile(icon: Icons.restaurant, iconColor: Color(0xFFFF6B6B), title: 'Restaurant', subtitle: 'Today, 12:30 PM', amount: '-\\$45.20', isNegative: true),\n            const TransactionTile(icon: Icons.flight, iconColor: Color(0xFF4ECDC4), title: 'Flight Booking', subtitle: 'Yesterday, 9:15 AM', amount: '-\\$320.00', isNegative: true),\n            const TransactionTile(icon: Icons.work, iconColor: Color(0xFF2ECC71), title: 'Salary', subtitle: 'Jul 1, 10:00 AM', amount: '+\\$3,500.00', isNegative: false),\n            const TransactionTile(icon: Icons.shopping_bag, iconColor: Color(0xFF9B59B6), title: 'Shopping', subtitle: 'Jun 28, 3:45 PM', amount: '-\\$89.99', isNegative: true),\n            const SizedBox(height: 20),\n          ])),\n    );\n  }\n}"
      },
      {
        "n": "cards_page.dart",
        "p": "lib/pages/cards_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CardsPage extends StatelessWidget {\n  const CardsPage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      backgroundColor: const Color(0xFFF8F9FA),\n      body: SafeArea(child: ListView(padding: const EdgeInsets.all(24), children: [\n        const Text('My Cards', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 8),\n        Text('Manage your cards', style: TextStyle(fontSize: 14, color: Colors.grey[600])),\n        const SizedBox(height: 24),\n        _buildCard(cardNumber: '**** **** **** 4589', cardHolder: 'TONY MENDOZA', expiry: '12/28',\n          gradient: const LinearGradient(colors: [Color(0xFF2ECC71), Color(0xFF27AE60)], begin: Alignment.topLeft, end: Alignment.bottomRight)),\n        const SizedBox(height: 16),\n        _buildCard(cardNumber: '**** **** **** 7823', cardHolder: 'TONY MENDOZA', expiry: '03/27',\n          gradient: const LinearGradient(colors: [Color(0xFF1A1A2E), Color(0xFF2C3E50)], begin: Alignment.topLeft, end: Alignment.bottomRight)),\n        const SizedBox(height: 24),\n        const Text('Card Actions', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 12),\n        _buildAction(Icons.lock_outline, 'Freeze Card', 'Temporarily disable your card'),\n        _buildAction(Icons.key_outlined, 'Change PIN', 'Update your card PIN'),\n        _buildAction(Icons.speed_outlined, 'Spending Limits', 'Set daily/monthly limits'),\n        _buildAction(Icons.delete_outline, 'Remove Card', 'Delete this card', isDestructive: true),\n      ])));\n  }\n  static Widget _buildCard({required String cardNumber, required String cardHolder, required String expiry, required Gradient gradient}) {\n    return Container(height: 200, padding: const EdgeInsets.all(24),\n      decoration: BoxDecoration(gradient: gradient, borderRadius: BorderRadius.circular(20),\n        boxShadow: [BoxShadow(color: const Color(0xFF1A1A2E).withValues(alpha: 0.3), blurRadius: 20, offset: const Offset(0, 10))]),\n      child: Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n        Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n          const Icon(Icons.credit_card, color: Colors.white, size: 30),\n          Text('VISA', style: TextStyle(color: Colors.white.withValues(alpha: 0.9), fontSize: 18, fontWeight: FontWeight.bold, letterSpacing: 2)),\n        ]),\n        Text(cardNumber, style: const TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.w500, letterSpacing: 2)),\n        Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n          Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n            Text('CARD HOLDER', style: TextStyle(color: Colors.white.withValues(alpha: 0.6), fontSize: 10)),\n            Text(cardHolder, style: const TextStyle(color: Colors.white, fontSize: 12, fontWeight: FontWeight.w600)),\n          ]),\n          Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n            Text('EXPIRES', style: TextStyle(color: Colors.white.withValues(alpha: 0.6), fontSize: 10)),\n            Text(expiry, style: const TextStyle(color: Colors.white, fontSize: 12, fontWeight: FontWeight.w600)),\n          ]),\n        ]),\n      ]));\n  }\n  static Widget _buildAction(IconData icon, String title, String subtitle, {bool isDestructive = false}) {\n    return Container(margin: const EdgeInsets.only(bottom: 12), padding: const EdgeInsets.all(16),\n      decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12),\n        boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n      child: Row(children: [\n        Container(padding: const EdgeInsets.all(10), decoration: BoxDecoration(color: isDestructive ? const Color(0xFFFF6B6B).withValues(alpha: 0.1) : const Color(0xFF2ECC71).withValues(alpha: 0.1), borderRadius: BorderRadius.circular(10)),\n          child: Icon(icon, color: isDestructive ? const Color(0xFFFF6B6B) : const Color(0xFF2ECC71), size: 22)),\n        const SizedBox(width: 16),\n        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(title, style: TextStyle(fontSize: 15, fontWeight: FontWeight.w600, color: isDestructive ? const Color(0xFFFF6B6B) : const Color(0xFF1A1A2E))),\n          Text(subtitle, style: TextStyle(fontSize: 12, color: Colors.grey[500])),\n        ])),\n        Icon(Icons.chevron_right, color: Colors.grey[400]),\n      ]));\n  }\n}"
      },
      {
        "n": "add_money_page.dart",
        "p": "lib/pages/add_money_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass AddMoneyPage extends StatefulWidget {\n  const AddMoneyPage({super.key});\n  @override\n  State<AddMoneyPage> createState() => _AddMoneyPageState();\n}\n\nclass _AddMoneyPageState extends State<AddMoneyPage> {\n  int _selectedCardIndex = 0;\n  final _amountController = TextEditingController(text: '500');\n  final _cards = const [\n    {'name': 'Visa ending 4589', 'icon': Icons.credit_card, 'color': Color(0xFF2ECC71)},\n    {'name': 'Visa ending 7823', 'icon': Icons.credit_card, 'color': Color(0xFF1A1A2E)},\n    {'name': 'Mastercard ending 1234', 'icon': Icons.credit_card, 'color': Color(0xFFE74C3C)},\n  ];\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(backgroundColor: const Color(0xFFF8F9FA),\n      body: SafeArea(child: ListView(padding: const EdgeInsets.all(24), children: [\n        const Text('Add money', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 24),\n        const Text('Select card', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 12),\n        SizedBox(height: 100, child: ListView.builder(scrollDirection: Axis.horizontal, itemCount: _cards.length,\n          itemBuilder: (context, index) {\n            final card = _cards[index];\n            final isSelected = _selectedCardIndex == index;\n            return GestureDetector(onTap: () => setState(() => _selectedCardIndex = index),\n              child: Container(width: 160, margin: const EdgeInsets.only(right: 12), padding: const EdgeInsets.all(16),\n                decoration: BoxDecoration(color: isSelected ? card['color'] as Color : Colors.white, borderRadius: BorderRadius.circular(16),\n                  border: Border.all(color: isSelected ? card['color'] as Color : Colors.grey.withValues(alpha: 0.2), width: 2),\n                  boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.05), blurRadius: 10, offset: const Offset(0, 4))]),\n                child: Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisAlignment: MainAxisAlignment.center, children: [\n                  Icon(card['icon'] as IconData, color: isSelected ? Colors.white : card['color'] as Color, size: 28),\n                  const SizedBox(height: 8),\n                  Text(card['name'] as String, style: TextStyle(color: isSelected ? Colors.white : const Color(0xFF1A1A2E), fontSize: 12, fontWeight: FontWeight.w600), maxLines: 1, overflow: TextOverflow.ellipsis),\n                ])));\n          })),\n        const SizedBox(height: 32),\n        const Text('Add money to featherbank', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 12),\n        Container(padding: const EdgeInsets.all(4), decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(16),\n          boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n          child: TextField(controller: _amountController, keyboardType: TextInputType.number,\n            style: const TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E)),\n            decoration: InputDecoration(prefixText: '\\$ ', prefixStyle: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Colors.grey[400]),\n              border: InputBorder.none, contentPadding: const EdgeInsets.all(16), hintText: '0',\n              hintStyle: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Colors.grey[300])))),\n        const SizedBox(height: 24),\n        Row(children: [\n          _quickAmount('\\$100'), const SizedBox(width: 8), _quickAmount('\\$250'), const SizedBox(width: 8), _quickAmount('\\$500'), const SizedBox(width: 8), _quickAmount('\\$1000'),\n        ]),\n        const SizedBox(height: 24),\n        _buildOption(Icons.swap_horiz, 'Move your existing transfer'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.account_balance, 'Transfer from other banks'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.apple, 'Apple Pay'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.credit_card, 'Debit/Credit Card'),\n        const SizedBox(height: 32),\n        SizedBox(width: double.infinity, height: 56, child: ElevatedButton(\n          onPressed: () {},\n          style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFF2ECC71), foregroundColor: Colors.white, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)), elevation: 0),\n          child: const Text('Add Money', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)))),\n      ])));\n  }\n  Widget _quickAmount(String amount) {\n    return Expanded(child: GestureDetector(onTap: () => _amountController.text = amount.replaceAll('\\$', ''),\n      child: Container(padding: const EdgeInsets.symmetric(vertical: 12),\n        decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(10), border: Border.all(color: Colors.grey.withValues(alpha: 0.2))),\n        child: Center(child: Text(amount, style: const TextStyle(fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E)))))));\n  }\n  Widget _buildOption(IconData icon, String title) {\n    return Container(padding: const EdgeInsets.all(16),\n      decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12),\n        boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n      child: Row(children: [\n        Container(padding: const EdgeInsets.all(10), decoration: BoxDecoration(color: const Color(0xFF2ECC71).withValues(alpha: 0.1), borderRadius: BorderRadius.circular(10)),\n          child: Icon(icon, color: const Color(0xFF2ECC71), size: 22)),\n        const SizedBox(width: 16),\n        Expanded(child: Text(title, style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w500, color: Color(0xFF1A1A2E)))),\n        Icon(Icons.chevron_right, color: Colors.grey[400]),\n      ]));\n  }\n}"
      },
      {
        "n": "profile_page.dart",
        "p": "lib/pages/profile_page.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass ProfilePage extends StatelessWidget {\n  const ProfilePage({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(backgroundColor: const Color(0xFFF8F9FA),\n      body: SafeArea(child: ListView(padding: const EdgeInsets.all(24), children: [\n        const Text('Profile', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 24),\n        Center(child: Column(children: [\n          Container(padding: const EdgeInsets.all(4), decoration: BoxDecoration(shape: BoxShape.circle, border: Border.all(color: const Color(0xFF2ECC71), width: 3)),\n            child: const CircleAvatar(radius: 50, backgroundColor: Color(0xFFE8F5E9), child: Icon(Icons.person, size: 60, color: Color(0xFF2ECC71)))),\n          const SizedBox(height: 16),\n          const Text('Tony Mendoza', style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold, color: Color(0xFF1A1A2E))),\n          const SizedBox(height: 4),\n          Text('tony.mendoza@email.com', style: TextStyle(fontSize: 14, color: Colors.grey[500])),\n          const SizedBox(height: 4),\n          Text('+1 (555) 123-4567', style: TextStyle(fontSize: 14, color: Colors.grey[500])),\n        ])),\n        const SizedBox(height: 32),\n        const Text('Personal info', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 12),\n        _buildInfoField('Full name', 'Tony Mendoza'),\n        _buildInfoField('Email address', 'tony.mendoza@email.com'),\n        _buildInfoField('Phone number', '+1 (555) 123-4567'),\n        _buildInfoField('Date of birth', 'January 15, 1990'),\n        const SizedBox(height: 24),\n        const Text('Account info', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n        const SizedBox(height: 12),\n        _buildInfoField('Account type', 'Premium'),\n        _buildInfoField('Account number', '****4589'),\n        _buildInfoField('Member since', 'January 2023'),\n        const SizedBox(height: 24),\n        _buildOption(Icons.edit_outlined, 'Edit Profile', 'Update your personal info'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.lock_outline, 'Security', 'Password & 2FA settings'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.notifications_outlined, 'Notifications', 'Manage alerts'),\n        const SizedBox(height: 8),\n        _buildOption(Icons.help_outline, 'Help & Support', 'Get assistance'),\n        const SizedBox(height: 24),\n        SizedBox(width: double.infinity, height: 52, child: OutlinedButton(\n          onPressed: () {},\n          style: OutlinedButton.styleFrom(foregroundColor: const Color(0xFFFF6B6B), side: const BorderSide(color: Color(0xFFFF6B6B)), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),\n          child: const Text('Log out', style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600)))),\n      ])));\n  }\n  static Widget _buildInfoField(String label, String value) {\n    return Container(margin: const EdgeInsets.only(bottom: 8), padding: const EdgeInsets.all(16),\n      decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12),\n        boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n      child: Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n        Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(label, style: TextStyle(fontSize: 12, color: Colors.grey[500])),\n          const SizedBox(height: 4),\n          Text(value, style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w500, color: Color(0xFF1A1A2E))),\n        ]),\n        Icon(Icons.chevron_right, color: Colors.grey[400], size: 20),\n      ]));\n  }\n  static Widget _buildOption(IconData icon, String title, String subtitle) {\n    return Container(padding: const EdgeInsets.all(16),\n      decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12),\n        boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n      child: Row(children: [\n        Container(padding: const EdgeInsets.all(10), decoration: BoxDecoration(color: const Color(0xFF2ECC71).withValues(alpha: 0.1), borderRadius: BorderRadius.circular(10)),\n          child: Icon(icon, color: const Color(0xFF2ECC71), size: 22)),\n        const SizedBox(width: 16),\n        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(title, style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n          Text(subtitle, style: TextStyle(fontSize: 12, color: Colors.grey[500])),\n        ])),\n        Icon(Icons.chevron_right, color: Colors.grey[400]),\n      ]));\n  }\n}"
      },
      {
        "n": "card_carousel.dart",
        "p": "lib/widgets/card_carousel.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass CardCarousel extends StatelessWidget {\n  const CardCarousel({super.key});\n  @override\n  Widget build(BuildContext context) {\n    return SizedBox(height: 200,\n      child: ListView(scrollDirection: Axis.horizontal, padding: const EdgeInsets.symmetric(horizontal: 24), children: [\n        _buildCard(cardNumber: '**** **** **** 4589', cardHolder: 'TONY MENDOZA', expiry: '12/28',\n          gradient: const LinearGradient(colors: [Color(0xFF2ECC71), Color(0xFF27AE60)], begin: Alignment.topLeft, end: Alignment.bottomRight)),\n        const SizedBox(width: 16),\n        _buildCard(cardNumber: '**** **** **** 7823', cardHolder: 'TONY MENDOZA', expiry: '03/27',\n          gradient: const LinearGradient(colors: [Color(0xFF1A1A2E), Color(0xFF2C3E50)], begin: Alignment.topLeft, end: Alignment.bottomRight)),\n      ]));\n  }\n  Widget _buildCard({required String cardNumber, required String cardHolder, required String expiry, required Gradient gradient}) {\n    return Container(width: 300, padding: const EdgeInsets.all(24),\n      decoration: BoxDecoration(gradient: gradient, borderRadius: BorderRadius.circular(20),\n        boxShadow: [BoxShadow(color: const Color(0xFF1A1A2E).withValues(alpha: 0.3), blurRadius: 20, offset: const Offset(0, 10))]),\n      child: Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n        Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n          const Icon(Icons.credit_card, color: Colors.white, size: 30),\n          Text('VISA', style: TextStyle(color: Colors.white.withValues(alpha: 0.9), fontSize: 18, fontWeight: FontWeight.bold, letterSpacing: 2)),\n        ]),\n        Text(cardNumber, style: const TextStyle(color: Colors.white, fontSize: 18, fontWeight: FontWeight.w500, letterSpacing: 2)),\n        Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [\n          Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n            Text('CARD HOLDER', style: TextStyle(color: Colors.white.withValues(alpha: 0.6), fontSize: 10)),\n            Text(cardHolder, style: const TextStyle(color: Colors.white, fontSize: 12, fontWeight: FontWeight.w600)),\n          ]),\n          Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n            Text('EXPIRES', style: TextStyle(color: Colors.white.withValues(alpha: 0.6), fontSize: 10)),\n            Text(expiry, style: const TextStyle(color: Colors.white, fontSize: 12, fontWeight: FontWeight.w600)),\n          ]),\n        ]),\n      ]));\n  }\n}"
      },
      {
        "n": "transaction_tile.dart",
        "p": "lib/widgets/transaction_tile.dart",
        "c": "import 'package:flutter/material.dart';\n\nclass TransactionTile extends StatelessWidget {\n  final IconData icon;\n  final Color iconColor;\n  final String title;\n  final String subtitle;\n  final String amount;\n  final bool isNegative;\n  const TransactionTile({super.key, required this.icon, required this.iconColor, required this.title, required this.subtitle, required this.amount, required this.isNegative});\n  @override\n  Widget build(BuildContext context) {\n    return Container(margin: const EdgeInsets.symmetric(horizontal: 24, vertical: 4), padding: const EdgeInsets.all(16),\n      decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12),\n        boxShadow: [BoxShadow(color: Colors.black.withValues(alpha: 0.04), blurRadius: 10, offset: const Offset(0, 2))]),\n      child: Row(children: [\n        Container(padding: const EdgeInsets.all(10), decoration: BoxDecoration(color: iconColor.withValues(alpha: 0.1), borderRadius: BorderRadius.circular(10)),\n          child: Icon(icon, color: iconColor, size: 22)),\n        const SizedBox(width: 16),\n        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [\n          Text(title, style: const TextStyle(fontSize: 15, fontWeight: FontWeight.w600, color: Color(0xFF1A1A2E))),\n          Text(subtitle, style: TextStyle(fontSize: 12, color: Colors.grey[500])),\n        ])),\n        Text(amount, style: TextStyle(fontSize: 15, fontWeight: FontWeight.w600, color: isNegative ? const Color(0xFFFF6B6B) : const Color(0xFF2ECC71))),\n      ]));\n  }\n}"
      }
    ],
    "icon": "document",
    "instructions": "## Featherbank (Fintech App)\n\n**Kategoriya:** Pages\n**Qiyinlik:** Hard\n**Teglar:** fintech, banking, finance, cards, profile\n\n### Qanday ishlatish kerak:\n\n1. Kerakli papkalarni yarating:\n```bash\nmkdir -p lib/pages lib/widgets\n```\n\n2. Fayllarni joylashtiring:\n- `lib/main.dart`\n- `lib/pages/navigation.dart`\n- `lib/pages/home_page.dart`\n- `lib/pages/cards_page.dart`\n- `lib/pages/add_money_page.dart`\n- `lib/pages/profile_page.dart`\n- `lib/widgets/card_carousel.dart`\n- `lib/widgets/transaction_tile.dart`\n\n3. Loyihani ishga tushiring:\n```bash\nflutter run\n```\n\n### Xususiyatlari:\n- Bosh sahifa: balans, kartalar carousel, tranzaksiyalar\n- Kartalar: VISA kartalari, boshqaruv\n- Pul qo'shish: karta tanlash, miqdor kiritish, to'lov usullari\n- Profil: shaxsiy ma'lumotlar, hisob ma'lumotlari\n\n### Eslatmalar:\n- Barcha importlarni to'g'ri yo'nalishda yozing\n- API kalitlarini `.env` faylida saqlang\n- Test fayllarini `test/` papkasiga joylashtiring\n"
  }
]